# Входящая задача

Сюда советник кладёт задачу для рабочей сессии. Сессия читает этот файл
и выполняет то, что в нём написано, а отчёт пишет в `docs/outbox.md`.

Порядок: одна задача за раз. Прежде чем взять новую, убедись, что по
предыдущей отчёт уже написан. Файл переписывается целиком, истории в
нём не копится — след остаётся в git.

**Пока задача в работе, советник этот файл не трогает.**

Если задача противоречит правилам из `CLAUDE.md`, выполняется
`CLAUDE.md`, а расхождение описывается в отчёте.

---

## 2026-09-22, тридцатая задача: сверка со свежим ручным листом — две правки, один хвост, Ebitda

Двадцать девятая принята: утро шесть из шести, механизм застрявших ключей
слит по слову (Render live 13:18 UTC), первая ночь с ним — сегодня, твоё
предсказание §5 (4 + 11, из них с деньгами −171 227,00 и −171,50) — утром
09-23 сверить первым делом вместе со сравнением снимка витрины
(`kpi_ozon_snapshot.py --compare … --until 2026-08-24`).

Владелец прислал свежий ручной файл, 1–21 сентября:
`data/manual_report_september_20260922.xlsx` (вне git, как прежние; лист
«Ozon - сентябрь», колонки те же, что раньше, плюс R «Ebitda»). Советник
сверил его с нашей книгой по 09-20 сам: оборот, комиссия, выручка,
эквайринг — 0,00 на 20 днях из 20; «Реклама по образцу» — 0,00 на 19;
Л + П — 0,00 на 18 (09-01 +209,02 — старое; 09-14 — компенсации);
себестоимость — наша ниже на 3 455 202,46 за 20 дней, и это ровно вся
разница фин. реза (решение 4). Из этой сверки — задача.

### 1. «Реклама по образцу» — вся статья подписки

09-20: их «Реклама» 473 415,71, наша справочная 452 932,11, разница
20 483,60 = 24 990,00 / 1,22 — подписка за тот день сверх типа 51 (тип 52
или 74; ты называла её в двадцать пятой). То есть в «Рекламу» владелец
кладёт всю статью `subscription`, а не только Premium; на 1–15 сентября
других типов подписки не было, и это не было видно. Правка: справочная
колонка = (41 + 54 + 96 + вся статья `subscription`) / НДС — как уже
делает основная колонка «Подписка». Поправить и текст в шапке листа, и
`--check`. Приёмка на новом файле: `--check --xlsx
data/manual_report_september_20260922.xlsx --sheet "Ozon - сентябрь"
--date-to 2026-09-21` — оборот, комиссия, выручка, эквайринг, реклама по
образцу: 21 из 21; Л + П с компенсациями: 20 из 21 (09-01).

### 2. Ebitda — колонка с накладными

У владельца R = P − 311 527,00 на каждый день сентября (в WB-листе своя
константа, 74 002). Это дневные накладные по площадке. Заведи параметр
«накладные в день» по площадке с датой действия (как НДС), значение на
2026-09-01 для Ozon — 311 527,00, источник — ручной лист от 22.09; колонки
«Ebitda» = Фин. рез. − накладные и «% Ebitda» от выручки, плюс справочная
«Ebitda с компенсациями». В `--check` — строка «Ebitda: их − наша = разница
СС + 209,02 на 09-01», как у фин. реза. Откуда число и как оно меняется —
владелец скажет; до тех пор в шапке листа: «накладные 311 527,00/день — из
ручного листа за сентябрь».

### 3. 09-01 +209,02 — закрыть наконец

Их Л + П за 09-01 = 72 274,08, наша «по образцу» 72 483,10; с НДС это
255,00. Сырьё by-day за 09-01 на диске: перечисли строки услуг дня с
суммой ±255,00 (и ±209,02, если Ozon отдал без НДС) — тип, SKU,
отправление. Найдётся один тип — назвать и записать в
`docs/reports_model.md` как известное отличие разноски владельца; не
найдётся — так и написать, и снять вопрос с повестки: 209 рублей за
неделю разбора не стоят. Обращений к API — 0.

### 4. `total orders analytics` — запас по 429

Ночью 09-22 шаг прошёл с четвёртой попытки из четырёх после трёх 429
`rate_limit_per_second` подряд (паузы 1 / 2 / 4 с). Подними до 6 попыток с
паузами до 32 с и паузой 1,5 с между страницами, как у загрузчиков
заказов (`loaders/ozon_sku_total_analytics_loader.py`); шаг остаётся
нефатальным. Тест на последовательность пауз. В ту же ветку, что п. 1–2.

### 5. По слову владельца, со снимком — вне механизма

Две ручные чистки, которые механизм не достанет: строка выкупов 08-21 sku
4453045714 (5 930,50 — вне 30-дневного окна) и 40 ключей `daily_sku_kpi`
без единого источника (`scripts/delete_stale_kpi_keys.py` перепроверит их
по живой базе; список ключей собери из сегодняшнего чтения витрины).
Покажи оба списка с суммами, спроси блоком «вопрос, задача не закончена».
**Не раньше, чем WB-сессия закончит запись в `marketplace_orders`** (её
восстановление сегодня; советник скажет, когда) — и не в 00:15 … 04:30 и
07:20 … 07:45 UTC.

### 6. Чего не делать

Загрузчики заказов, кривую дозревания, леджер, механизм застрявших ключей
— не трогать. Воркер, органику, WB — не трогать. В `marketplace_orders` не
писать.

### Отчёт

В `docs/outbox.md`: утро 09-23 (если задача дожила до утра — иначе
первым пунктом следующей); `--check` на новом файле по колонкам, 21 день;
Ebitda; тип строки 255,00; diff, тесты, `git log main..HEAD`, статус
мержа; статус двух ручных чисток; и отдельной строкой — всё, что не
сошлось.
