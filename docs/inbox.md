# Входящая задача

Сюда советник кладёт задачу для рабочей сессии. Сессия читает этот файл
и выполняет то, что в нём написано, а отчёт пишет в `docs/outbox.md`.

Порядок: одна задача за раз. Прежде чем взять новую, убедись, что по
предыдущей отчёт уже написан. Файл переписывается целиком, истории в
нём не копится — след остаётся в git.

**Пока задача в работе, советник этот файл не трогает.**

Если задача противоречит правилам из `CLAUDE.md`, выполняется
`CLAUDE.md`, а расхождение описывается в отчёте.

---

## 2026-09-22, тридцать вторая задача: утро 09-23 (первая ночь механизма и первая книга новым кодом); справочные колонки по множителям владельца и UTC-суткам; индекс себестоимости

Тридцать первая принята: `c417155` слит по слову, `main` = `origin/main` =
`db0f14c`, 827 тестов, Render live 19:55 UTC; реклама по образцу на листе
«Заказы» 21 из 21; множитель владельца 0,53 / 0,90 по UTC-суткам проверен
тобой по сырью независимо от советника — сошлось (0,530 на 16 из 16). Утро
09-23 — первый пункт здесь.

Решение советника по открытому вопросу §4 отчёта: да, справочные колонки
переводим на множители владельца и добавляем сравнение по UTC-суткам (§2).
Модель не трогаем ни в одном числе. Себестоимость: свежей выгрузки из 1С
нет — вводим датированный индекс справочно (§3).

Порядок: §1 утром после 07:45 UTC, только чтение; §2–§3 — код и тесты в
ветке, db_writes = 0; мерж — по слову владельца.

### 1. Утро 09-23

Чек-лист §1 из тридцать первой, без изменений, плюс первая книга новым кодом:

- лог Render, шаг «дневные финоперации»: сбор полон; «Застрявшие ключи
  marketplace_buyouts: … к удалению 11», «✅ удалено 11 из 11», деньги 08-24
  −171,50 и 08-25 −171 227,00; шаг «расходы и комиссии»: «к удалению 4»,
  «✅ удалено 4 из 4»; леджер — «застрявших ключей нет». «Сбор окна НЕ полон»
  — так и написать, механизм по правилу промолчал, ждём следующей ночи;
- арбитр леджера — 30 из 30 (было 26 из 30);
- `kpi_ozon_snapshot.py --compare data/snapshots/kpi_ozon_by_date_20260922T1559Z.json --until 2026-08-24`:
  ожидание — только 08-21 sku 4453045714 (выкупы −5 930,50, qty −1) и строки
  из §5 двадцать девятой; всё прочее 0,00, каждая другая строка — объяснение
  в той же строке;
- книга 07:30: путь `data/reports/ozon_2026-09_to_2026-09-22.xlsx` в логе,
  09-22 с рекламой живым ответом (не 0), дозревших дней 2 (09-01, 09-02) и
  «прогноз = факту» на обоих, Ebitda в подписи; число обращений к by-day за
  молодой день и 429;
- шаг «WB: загрузка заказов» — одной строкой, что написал лог; разбор за
  WB-сессией.

### 2. Справочные колонки — множители владельца и UTC-сутки

2.1. Параметр с датой действия, как `VAT_RATES`: `OWNER_AFTER_COMMISSION` —
с 2026-09-01: Основная 0,53, Дискаунтер 0,53, Селект 0,90, «Без площадки»
0,53; до 09-01 — 0,59 (подпись июльской книги; июль не перепроверялся).
Справочная «Выручка» на всех листах заказов = создано × множитель площадки
/ НДС; «Маржа (созданных)», «Фин. рез. = Маржа × 0,65 − Реклама по образцу −
Выручка × 0,65 × 0,024», «Наш фин. рез. − владельца» — от неё. В шапке:
«множители владельца по его сентябрьскому листу: комиссия 47 % / 47 % /
10 %; измеренная доля — в модели: …». Справочный ДРР — как есть, но в шапке
назвать его константы (0,58 на «Заказы», 0,73 на «Заказы Standard»).

Ожидание после правки в `--check-orders` за 1…21: строка «Выручка B» —
его/наша ≈ 1,0 по итогу (09-17 у него вбит иначе: +848 179 к правилу — печатать
как расхождение, не чинить); в разложении фин. реза по формулам владельца
строка «база» уходит в разницу границы суток и 09-17, остаются СС и прочие.

2.2. Сравнение по UTC-суткам в `--check-orders`. Его источник режет день по
UTC, наша таблица заказов — по МСК, поэтому по дням его/наша шумит
0,86…0,96. Нужны отправления с временем заказа за месяц. Сырьё ночного сбора
сейчас на диск не пишется; вариант, который советник считает правильным:
ночной загрузчик заказов сохраняет ответы `/v3` и `/v4` в
`data/postings_raw/nightly_<дата>_<схема>.json` (gitignored, хранить 7
ночей, старше — удалять своим механизмом, вслух). Тогда `--check-orders
--day-boundary utc` читает свежее сырьё и печатает по площадкам его B × НДС /
наше созданное по UTC-суткам: ожидание 0,530 / 0,900 / 0,530 на каждом дне,
кроме 09-17 (0,640) и 09-11 Дискаунтера (0,127). Если найдёшь путь дешевле и
такой же честный — опиши в отчёте и делай его. Границу суток самой модели не
менять.

### 3. Индекс себестоимости — справочно, пока нет выгрузки из 1С

Наша СС — снимок 1С от 05-20; у владельца за 1…21 сентября 27 401 217,62
против наших 23 824 962,63 — коэффициент 1,150, по дням 1,175 (начало месяца)
→ 1,116 (21-е), то есть у него не снимок, а величина, которая движется внутри
месяца. По SKU из ручного файла восстановить нельзя (там только итоги дня).

3.1. Параметр с датой действия `COST_INDEX` — с 2026-09-01: 1,150 (источник:
лист владельца 22.09, 1…21 сентября). Справочные колонки на выкупном листе:
«СС по индексу», «Маржа по индексу», «Фин. рез. по индексу», «Ebitda по
индексу»; на общем листе «Заказы»: «СС по индексу», «Фин. рез. прогноз по
индексу». Основные колонки — на снимке, в шапке дата снимка и индекс с
источником. В подпись к Telegram — «…, фин. рез. по индексу СС x,x млн».

3.2. `--check`: строка «Себестоимость» дополняется индексом по дням (его / наша)
и по итогу; если индекс по итогу отличается от параметра больше чем на 3 % —
строка «ВНИМАНИЕ: индекс СС протух: параметр 1,150, по листу x,xxx».

3.3. Когда появится выгрузка из 1С — `scripts/load_article_unit_costs.py`
новым снимком по слову владельца, параметр индекса с той даты 1,00. Сейчас
ничего в БД не писать.

### 4. Чего не делать

Модель — прогноз, измеренная комиссия, кривая, граница суток — без изменений.
db_writes = 0. Таблицы WB не трогать (ночью и утром там своя проверка).
Мерж — по слову владельца после `git log main..HEAD` и тестов.

### Отчёт

В `docs/outbox.md`: утро — каждый пункт §1 с числом против предсказания;
`--check-orders` до и после 2.1 (итоги и разложение), таблица по UTC-суткам по
площадкам; как сохраняется сырьё ночного сбора и сколько места; индекс СС по
дням из `--check`; сухой прогон доставки с новой подписью; обращений к API и
429; `git log main..HEAD`; и отдельной строкой — всё, что не сошлось.
