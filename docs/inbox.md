# Входящая задача

Сюда советник кладёт задачу для рабочей сессии. Сессия читает этот файл
и выполняет то, что в нём написано, а отчёт пишет в `docs/outbox.md`.

Порядок: одна задача за раз. Прежде чем взять новую, убедись, что по
предыдущей отчёт уже написан. Файл переписывается целиком, истории в
нём не копится — след остаётся в git.

**Пока задача в работе, советник этот файл не трогает.**

Если задача противоречит правилам из `CLAUDE.md`, выполняется
`CLAUDE.md`, а расхождение описывается в отчёте.

---

## 2026-09-24, тридцать седьмая задача: утро 09-25; книга «Фин рез» в форме второго кабинета — выкупы Ozon и общий лист заказов; категория товара из карточки; застрявшие ключи вне окна

Тридцать шестая принята: миграции, три мержа (Render live 09:51:54 UTC), пересборка
расходов (176 876 строк, контроль суммы дня 0,00; логистика по августу 26 из 31 вместо 0),
оба бэкфилла соинвеста с тождеством 0,00, читатели колонки покупателя перебраны. Слово на
удаление 11 застрявших ключей выкупов 08-19 … 08-23 (126 848,00) дано в окне; если на момент
чтения не сделано — сначала оно, затем результат добора 09-14 в отчёт, потом §1.

Образец формы — файлы владельца, лежат в облаке советника, в проект положены копии:
`data/owner_finrez_ozon_buyouts.xlsx` («Фин рез дроп бокс Озон Выкуп», лист «Вывод данных»),
`data/owner_finrez_orders.xlsx` («Фин рез Заказы v2.0», листы «Свод» и «Коэффициенты»),
`data/owner_finrez_wb_buyouts.xlsx` (форма WB — для WB-сессии). Это значения без сводных
таблиц (кэш сводной убран, файлы по 15–70 КБ). Это книги другого кабинета —
числа не наши, приёмка по ним невозможна; приёмка — форма один в один и тождества с нашими
листами (ниже). В `data/` они вне git (шаблон `data/owner_*.xlsx` в `.gitignore` уже есть — закоммитить с отчётом).

### §1. Утро 09-25 — по логу ночи и по базе

Первая ночь на свёртке владельца, соинвесте и листе WB в книге:
1. «Шаги не выполнены: нет», `pipeline_run:last`, застрявшие ключи по таблицам, 429 по
   `http_retry` по шагам.
2. `marketplace_expenses` за окно 08-25 … 09-24 переписано новой свёрткой: план из отчёта
   (добавить 1 288 / переписать 2 256 / удалить 6) против факта ночи — сошлось или нет, Σ по
   дням та же; `--check` за 1 … 21.09 — логистика 20 из 21, прочее +64 694,52 как в ветке;
   `--check` за август после удаления 11 ключей — оборот, комиссия, выручка **31 из 31**.
3. Соинвест ночью: в логе шага FBO «цены покупателя из отчёта ЛК — строк ~4 800, без цены 0,
   обращений create 1 / info ~3 / download 1»; SQL — `orders_amount_buyer < orders_amount_seller`
   у строк ночи (FBO и FBS), `bonus_amount` не null у выкупов за 09-24; в книге 09-25 колонки
   «Соинвест» на «Ozon - месяц» и «Заказы» — с числами за 09-01 … 09-24, доля Standard за
   09-24 — число.
4. Книга 09-25: `fetch_ozon_month_report.py --date-to 2026-09-24`, sha256, **лист «WB -
   сентябрь» на месте**, итог 1 … 21.09 WB = 16 667 220,00 / 6 994 883,76; 19 листов; подпись
   без «WB: лист не собран».
5. Сторож `analytics/data` за 09-24 — число; 09-22 живым — 1,397 или поправлено.
6. Добор рекламы 09-14 — по отчёту 36-й закрыт не полностью: 340 кампаний ночи 09-15
   собраны, но не записаны (`exception_after_partial_progress`), CPO не собран; в витрине
   45 216,68 против леджера 41 + 54 = 486 566,66. Закрыть днём 09-25 **по слову в окне**:
   `--mode cpc-recovery --date 2026-09-14 --approve-cpc-recovery-write` (~532 выгрузки) плюс
   CPO «Все» за 09-14 (2 выгрузки); перед запуском — план-онли с квотой за 24 ч (ночь 09-25
   добавит ~380–535; запускать, когда `would_fit_daily_limit true`), после — `ad_spend` 09-14
   против леджера, отношение; детектор — 09-14 из suspicious ушёл.

### §2. Книга «Фин рез» — лист выкупов Ozon в форме владельца (ветка `finrez-book`)

Новый скрипт `scripts/report_finrez.py --month-from 2026-04 --month-to 2026-09` — одна книга
`data/reports/finrez_<from>_<to>.xlsx`, в этой задаче Ozon-часть; WB-часть добавит WB-сессия
своим модулем (`scripts/report_finrez_wb.py`, лист «Выкупы WB» и строки WB в «Данные»),
сборку книги держит этот скрипт — импортом, как `book_wb_sheet.py`. Листы:

1. **«Данные Ozon выкупы»** — одна строка на (дата, SKU): Дата · Месяц (`апр`, `май` … как у
   владельца) · Площадка (Ozon) · Бренд (KARATOV / Топаз по букве артикула) · Категория (§3) ·
   Артикул · SKU · Наименование · Товарооборот (выкупы, с НДС) · Комиссия · Логистика ·
   Реклама · Эквайринг · Прочее (свёртка владельца, знак как в его сводной: списания
   отрицательные) · Ст-ть продаж в себестоимости (СС × штуки, снимок 05-20 через индекс — в
   подвале так и сказать) · Соинвест (bonus + coinvestment) · Штуки. Источники —
   `marketplace_buyouts`, `marketplace_expenses` по SKU, реклама по SKU (Performance, решение
   5), леджер без SKU — строкой с пустым SKU и артикулом «(без SKU)», чтобы Σ листа = «Ozon -
   месяц».
2. **«Выкупы Ozon»** — строки: месяцы (апр … сен) и «Итого»; колонки ровно как у владельца:
   по каждой статье (Комиссия, Логистика, Прочее, Реклама, Товарооборот, Эквайринг) две —
   «Начисления» и «Ст-ть продаж в себ-ти»; справа Оборот (с НДС) · Комиссия (с НДС) ·
   Комиссия % · Выручка без НДС · Себестоимость · Маржа · Мар-ть % · Логистика без НДС · % ·
   Реклама без НДС · % ДРР · Эквайринг без НДС · % · Прочее без НДС · Фин. рез. · %. Формулы —
   как на «Ozon - месяц» (НДС 22 %). Тот же лист в разрезах «бренд × месяц» и «категория ×
   месяц» (два листа или блоки ниже — как читаемее).
3. Приёмка (в отчёт числами): Σ «Выкупы Ozon» за сентябрь 1 … 21 = итог «Ozon - сентябрь» по
   каждой колонке (оборот, комиссия, логистика, реклама, эквайринг, прочее, СС, фин. рез.) —
   до копейки, иначе разложить; то же за август против «Ozon - август» (наш лист); Σ по
   брендам = Σ по категориям = общий; строки «Данные» = число ключей выкупов + расходов без SKU.
   Форма — колонки и порядок как в образце (проверить по файлу владельца, не по памяти).

### §3. Категория товара — из карточки Ozon, одной таблицей

Владелец хочет разрез по категории (кольца, серьги, подвески, цепочки, браслеты, пирсинг,
колье, броши). Взять из карточек: `/v3/product/info/list` (или `/v2/product/info/list` —
по спеке, что актуально) по всем SKU базы (≈ 5 300 SKU, пачками по 1 000 → ~6 обращений) →
`description_category_id` / `type_id`; имена — `/v1/description-category/tree` (1 обращение).
Таблица `ozon_products` (sku, offer_id, name, category_id, category_name, type_id,
type_name, brand по букве, metal по названию, `observed_at`) — миграция по слову; загрузчик
— ночной шаг не добавлять (`run_daily_pipeline.py` не трогать): обновление раз в неделю
скриптом по слову или внутри существующего шага, если есть подходящий (сказать какой).
Нормализация имён категорий к списку владельца — словарём в коде, всё, что не легло, —
«прочее» со счётчиком. Приёмка: доля SKU с категорией ≥ 99 %, таблица «категория · SKU ·
оборот за сентябрь»; сравнить с разбором по названию (как в металле) — где расходятся.
До миграции лист §2 строится по названию, с пометкой.

### §4. Общий лист «Заказы» в форме владельца — Ozon-часть и сборка

Лист «Заказы» книги «Фин рез»: строки — месяцы, колонки — по площадкам Ozon | WB: Оборот
(с НДС, созданные) · Заказы, шт · Цена продавца (средняя) · Комиссия % · Выручка ·
Себестоимость · Маржа · М-ть % · Реклама · ДДР % · Соинвест % · Гр. фин. рез · Гр. фин. рез %,
плюс лист **«Коэффициенты»** (как у владельца: % выкупа по площадке, комиссия по площадке;
у него Ozon до 300 ₽ — 23 %, выше — 52 %, WB 43 %, выкуп 0,843 / 0,865) — значения **наши,
измеренные**: выкуп — из кривой дозревания (плато по площадке), комиссия — фактическая
средняя за месяц; его числа рядом справочно с пометкой «образец». «Гр. фин. рез» — по его
формуле через коэффициенты. Лист «Данные заказы» — строка на (дата, площадка, SKU/nmId):
заказы ₽ и шт, цена покупателя (Ozon — `orders_amount_buyer`), реклама, СС реализации
(шт × СС), бренд, категория, наименование. Ozon-часть — из `marketplace_orders`; WB-часть
— из `wb_funnel_products_daily` (с 09-01; апрель–август снимет WB-сессия в WB-8) через
функции модуля WB-сессии, если они есть на момент сборки, иначе — WB-колонки пустые с
пометкой. Приёмка: Σ Ozon по сентябрю = итог листа «Заказы» утренней книги (создано ₽ и шт,
реклама, соинвест Standard).

### §5. Застрявшие ключи вне окна — план недельной глубокой проверки

11 ключей 08-19 … 08-23 показали класс: Ozon убирает начисления задним числом дальше 30
дней, ночной механизм их не видит. Предложить и написать (без запуска в проде): режим
`--deep` для `delete_stale_buyout_keys.py` / застрявших расходов — сверка таблиц с сырьём
by-day на диске за 180 дней (обращений 0, сырьё есть с 03-28), план списком; для дней без
сырья — живой by-day, посчитать цену. В отчёт — план на сегодня по выкупам и расходам
(сколько ключей, ₽), запуск удаления — по слову. Как встроить раз в неделю — предложение,
не код шага.

### §6. Чего не делать

- Записи в базу — по слову: миграция `ozon_products` (§3), удаления (§5).
- `run_daily_pipeline.py`, расписание, флаги — не трогать. WB-файлы — не трогать; модуль
  `report_finrez_wb.py` — WB-сессии, ей же строки WB в «Данные».
- Обращения считать (Seller: карточки ~7; остальное — с диска и из базы); 429 — по логу.
- Ветки: `finrez-book`, `product-catalog`, `stale-deep`; мержи — по слову.

### §7. Дополнение владельца (24.09 вечер): листы «Данные» — колонки ровно как в исходнике, по порядку

Сводные листы («Выкупы Ozon», «Выкупы WB», «Заказы») по колонкам с образцом совпадают —
проверено советником по заголовкам. Расходятся листы **«Данные»**: у владельца это плоские
таблицы, из которых собирается сводная, и он хочет те же поля в том же порядке, чтобы
класть на них свою сводную без правок. Выполнять **после §1 утром**, ветка `finrez-data-cols`.

1. **«Данные Ozon выкупы» — длинный формат, как в кэше его сводной** (одна строка на
   начисление × SKU × статья), поля по порядку:
   `ID начисления` (accrual_id; у строк остатка леджера без SKU — пусто) · `Дата начисления` ·
   `Артикул` · `SKU` · `Количество` · `Статьи Озон.Вид затрат` (Товарооборот / Комиссия /
   Логистика / Реклама / Эквайринг / Прочее — как в его справочнике) · `Бренд (cc)` (бренд по
   артикулу; у него — бренд из справочника СС, у нас то же значение) · `Месяц` (`апр` …) ·
   `Ст-ть продаж в себ-ти` (только у строк Товарооборот: СС × кол-во × индекс, у остальных 0)
   · `Бренд` · `Статус` (**категория**: кольца, серьги … — владелец: статус не задействован,
   в этом поле — категория) · `Начисления` (сумма со знаком его сводной: товарооборот +,
   статьи −) · `Наименование` · `Дни (Дата начисления)` (день числом) · `Месяцы (Дата
   начисления)` (месяц числом). Наши добавки — **справа** после этих 15: `Соинвест (баллы +
   зелёные цены)`, `Площадка`. Реклама Performance по SKU — строкой статьи «Реклама» с ID
   начисления пустым и пометкой в наименовании источника не нужно — просто ID пусто.
   Тождества: Σ «Начисления» по (месяц, статья) = «Выкупы Ozon»; Σ «Ст-ть продаж в себ-ти»
   = колонке «Себестоимость»; число строк — в отчёт.
2. **«Данные заказы» — поля и порядок его кэша** (одна строка на дата × площадка × SKU/nmId):
   `Дата` · `МП` (Ozon / WB) · `Магазин` (KARATOV) · `Артикул поставщика` · `Заказы, ₽` ·
   `Заказы, шт` · `Реклама, ₽` · `Себ-ть Реал` (СС × шт × индекс) · `Наименование` · `Бренд` ·
   `Статус` (категория) · `Заказы, руб без НДС` · `Реклама, руб без НДС` · `СПП,%` (соинвест
   строки: Ozon — 1 − цена покупателя / цена продавца, где измерено, иначе пусто; WB — пусто)
   · `Заказы, руб c СПП` (по цене покупателя, где измерено) · `Комиссия` (доля: Ozon —
   фактическая средняя месяца с листа «Коэффициенты», WB — 0,43 его / наша 0,42 — взять из
   «Коэффициентов») · `Выручка, руб без НДС с учетом комиссии` · `Комиссия, руб` · `Выкуплено`
   (% выкупа с «Коэффициентов» по площадке) · `Неделя года` · `День` · `Месяцы` · `Маржа, руб
   без НДС` · `Маржинальность, %` · `ДДР, %` · `Соинвест, %` · `Комиссия сред` · `Фин.рез` ·
   `Цена` (средняя цена продавца = ₽ / шт) · `Фин.рез %` — формулы строк такие же, как в его
   «Свод» (то есть «Свод» = Σ «Данных заказы» по месяцу и МП — это и есть приёмка: 0,00 по
   всем 13 колонкам обоих блоков). Наши добавки справа: `SKU/nmId`, `Подтверждено сейчас,
   руб.`, `Площадка Ozon`.
3. **«Данные WB выкупы»** — модуль WB-сессии; ей поставлено то же (WB-8 §6): порядок полей
   его кэша (`Дата · Месяц · Магазин · Артикул поставщика · Продажи, ₽ · Реклама, ₽ ·
   Комиссия, ₽ · Логистика, ₽ · Себес-ть, ₽ · Хранение, ₽ · Ост.расходы и компенсации МП, ₽ ·
   Наименование · Бренд · Статус (категория) · Месяцы`), наши добавки справа. Сборка книги —
   через её `DATA_COLS`, ничего не переставлять у себя.
4. Заголовки сводных листов оставить как есть (совпадают); служебные строки-подписи над
   таблицей («форма владельца на наших данных», «Все бренды и категории») — оставить, но
   строкой выше, чтобы строка «Названия столбцов» / «Названия строк» стояла как в образце
   (у него — с 3-й строки после блока фильтров). Лист «Коэффициенты» — сверху две его
   таблички в его виде (МП / % выкупа; МП / Комиссия) с **нашими** числами, ниже — наша
   развёрнутая таблица как сейчас.
5. **Тяжесть книги — починить, а не выкинуть лист.** Владелец: «Данные заказы» нужны (на них
   ляжет его сводная), но 34 минуты сборки вместо 6 — не годится. Порядок: (а) замерить, где
   время — чтение базы или запись xlsx (по логу с отметками времени на шаг); (б) не писать
   пустые строки: строка «Данных заказы», у которой `Заказы, ₽` = 0, `Заказы, шт` = 0 и
   `Реклама, ₽` = 0, для сводной пуста — выбросить (по обеим площадкам; у WB это большая
   часть 490 тыс. строк воронки — WB-модулю то же поставлено в WB-8 §6); (в) писать лист
   потоково (`openpyxl` `write_only=True` или `xlsxwriter` `constant_memory`) — обычный
   `openpyxl` на 616 тыс. × 30 ячеек и есть вероятная причина; (г) в отчёт: строк было /
   стало, время сборки было / стало, размер файла. Тождество «Свод» = Σ «Данных заказы» от
   выброса нулевых строк не меняется — проверить числом. `--no-orders-data` оставить как
   запасной флаг; по умолчанию писать.
6. Книга пересобирается, приёмка §2 повторяется (0,00 по 9 колонкам), «Свод» = Σ «Данных
   заказы» — новая приёмка; готовую книгу — в `data/reports/`, sha256 и размер в отчёт.
