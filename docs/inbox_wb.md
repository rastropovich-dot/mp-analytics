# Входящая задача — WB

Сюда советник кладёт задачу для рабочей сессии по Wildberries. Сессия
читает этот файл и выполняет то, что в нём написано, а отчёт пишет в
`docs/outbox_wb.md`. Правила обмена те же, что у Ozon-потока
(`docs/how-we-work.md`, раздел «Порядок обмена»): одна задача за раз,
файл переписывается целиком, пока задача в работе — советник его не
трогает; вопрос посреди задачи — в `outbox_wb.md` блоком «вопрос, задача
не закончена».

`docs/inbox.md` и `docs/outbox.md` — канал Ozon-сессии. Не читать как
задачу и не писать туда.

---

## 2026-09-25, десятая задача WB: утро 09-26; выкуп WB в ночной витрине и алерте — по когорте, как коэффициент листа; ускорения по слову (вьюха словаря, воронка фильтром); лаг заказ → продажа с августа — где

Девятая закрыта: §2–§4 слиты (`main` `8e3dba3`, Render live 13:20 UTC), 1 088 тестов. Владелец решил: в «Заказах» /
«Своде» WB считается как Ozon — с измеренным коэффициентом выкупа (`buyout_rate_for_finrez`, итог зрелых месяцев);
Ozon-скрипт подхватывает это своей 38-й задачей. Слов на сегодня нет: §2 и §3 — код и планы в ветке `wb-fixes`, запись
и мерж — по слову. **db_writes = 0**, к WB API — 0. Другим сессиям не писать и не отвечать; обмен — только этот файл и
`outbox_wb.md`.

### §1. Утро 09-26 — по будильнику 07:47 UTC, по логу ночи и по базе

Чек-лист восьмой п. 1–6, числами; ночь на `8e3dba3` для пайплайна ничего нового не несёт — ждём то же, что 09-25.
Плюс: утренняя книга 07:30 UTC — лист WB на ярлыках владельца (строчные категории, «Топаз»), подпись доставки.

### §2. Выкуп WB в ночной витрине и алерте — сейчас неверный, привести к когорте

Твой §3 девятой: `reports_daily_sku_kpi.buyout_rate` = `buyouts_qty` / (`orders_qty` + `cancelled_orders_qty`) из
`marketplace_orders` — числитель календарный (продажи по `saleDt`), знаменатель из другого источника
(`supplier/orders`, история восстановлена 09-22); в алерте 0,9562 за 1 … 22.09, тогда как выкуп по когорте месяца
заказа — 0,37 … 0,53, итого апрель–август 0,4981. Как метрика выкупа для WB это не годится, а владелец её видит каждое
утро рядом с Ozon (там 62,98 % по подтверждённым / созданным за дни возраста ≥ 21 суток — сопоставимое определение).

1. Разобрать по коду, где `buyout_rate` WB считается и куда идёт: `reports_daily_sku_kpi.py` (по SKU и по дню),
   утренний алерт (`buyout_rate` в тексте), листы `report_7_days` / `report_60_days`, `reports_sku_*` (там
   `buyout_rate_rolling_14d/30d` — из тех же полей?). Таблица «кто читает — что показывает — чем это плохо».
2. Предложить и сделать в ветке: для WB — числитель по дню заказа (продажи отчёта реализации по `orderDt` МСК, Продажа −
   Возврат, ₽ и штуки), знаменатель — `orderSum` / `orderCount` воронки того дня; «зрелый» день — возраст ≥ 25 суток
   (`BUYOUT_RATE_MATURE_DAYS` из модуля), для незрелых — прогноз по последним зрелым 30 дням, с пометкой, как у Ozon
   (там дни моложе 21 суток — оценка). В витрине `daily_sku_kpi` это те же поля `buyouts_qty` / `created_orders_qty`
   по (день, nmId), но наполненные по когорте, — или новые поля, если старые кто-то читает как календарные (п. 1 решит);
   в алерте и книге — подпись «выкуп по когорте (зрелые дни ≥ 25 сут.)». Ночной шаг — код в ветке, тест на фикстуре;
   витрину не перезаписывать до слова: план «сколько ключей изменится, было / станет по месяцам» (только чтение).
3. Приёмка: по зрелым месяцам ₽ = `buyout_rate_for_finrez` до 0,0001 (одно определение — одно число), штуки =
   способу (б) девятой; сентябрь помечен незрелым. В отчёт — таблица месяц × (было в витрине, стало) и что изменится в
   алерте завтра утром.

### §3. Ускорения, которые ты предложила в §4 девятой — подготовить, по слову

1. **Вьюха «последняя карточка по nmId»** — `sql/2026092x_create_wb_funnel_products_latest.sql` (`select distinct on
   (nm_id) … order by nm_id, day desc` над `wb_funnel_products_daily`; лучше обычная вьюха, не материализованная —
   воронка переписывается каждую ночь), `load_product_dictionary` читает её (5 202 строки, ~6 страниц вместо 493,
   −295 с), откат на старое чтение, если вьюхи нет (проверка `information_schema`). Миграция — по слову через MCP.
2. **Воронка для строк заказов фильтром на сервере** — `or(order_count.gt.0,order_sum.gt.0)` (~23 страницы вместо 493)
   + пары (день, nmId) с рекламой без заказов из словаря; ожидаемо −320 с и +33,07 ₽ рекламы (83 пары, которые сейчас
   теряются). Сделать под флагом / параметром с приёмкой: Σ orderSum по месяцам до = после, строк заказов до = после
   + 83, реклама +33,07 — числами в отчёт; включение по умолчанию — по слову.
3. `load_costs` адресно по артикулам WB (~−20 с) — если останется время, с тем же контролем чисел.

### §4. Лаг заказ → продажа вырос вдвое с августа — где именно (только чтение)

Твоя находка: медиана `saleDt − orderDt` 3 → 7 дней с августа, p90 15 (август) и 24 (сентябрь). Владельцу это важно
(выкуп, деньги в пути). Разложить по месяцам апрель … сентябрь: по складу отгрузки (`office_name` отчёта — это
склад / ПВЗ выдачи? уточнить по спеке поля `officeName`), по бренду, по категории, по цене (до / выше медианы), по
доставке / возврату (`return_amount`, `delivery_amount`), по доле продаж заказов прошлого месяца. Вопрос один: это
изменилось у WB (логистика, склад) или у нас (откуда отгружаемся, что продаём)? Таблицы и вывод в одну строку; догадки
помечать как догадки.

### §5. Чего не делать

Базу не писать (миграция §3.1 и витрина §2 — по слову); к WB API не ходить; `main` и Ozon-файлы не трогать; мерж — по
слову. Отчёт — новым блоком сверху `outbox_wb.md`: §1 числами, §2 — таблица «кто читает» и «было / стало», §3 — план и
замеры, §4 — таблицы. Обращения к WB API и 429 — числами (ожидаемо 0 и 0).
