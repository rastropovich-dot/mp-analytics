# Отчёт рабочей сессии

Сюда рабочая сессия пишет результат по задаче из `docs/inbox.md`.
Советник читает этот файл сам.

Что должно быть в отчёте:

- что сделано, с номерами коммитов
- что НЕ сделано и почему
- числа, а не оценки: сколько строк, сколько запросов, сколько времени
- любое расхождение объясняется в той же строке, где показано, иначе
  отчёт не выпускается
- если утверждаешь, что работа в ветке — приложи `git log main..HEAD`

Не писать «готово» о том, что не проверено. Проверка — это выполненная
команда с выводом, а не намерение.

---

## 2026-09-18, двадцать третья задача (пересборка выполнена: таблица = план ключ в ключ; 479 ключей витрины удалены)

Две записи в боевую БД, обе по слову владельца, обе со снимком до
удаления. К Ozon API — 0 обращений (сырьё от 13:34–14:00 UTC). Загрузчики,
устаревшие ключи сверх пересборки, реклама 09-14, воркер, ветка, WB,
органика — не тронуты.

**Вопрос владельцу посреди задачи — был, в его окне.** «Владелец одобрил
план» стояло в файле задачи, а не в чате; разрешение на необратимое я беру
только из чата. Спросила с числами (57 256 → 60 654 строк, подтверждённые
1 434 985 797 → 1 116 800 429, 479 ключей на 17 031 211), ответ — «Да, обе
записи». По новому правилу (§3) такой вопрос теперь идёт и в `outbox`;
записываю здесь задним числом, потому что ответ пришёл за минуту.

### 1. Контрольные числа: план против факта

Факт — SQL по таблице после записи, не счётчики скрипта.

```
                                 план                факт               Δ
строк Ozon                      60 654              60 654              0      (FBO 39 531, FBS 21 123; было 57 256)
подтверждённые ₽         1 116 800 429       1 116 800 429              0      (FBO 710 200 054, FBS 406 600 375; было 1 434 985 797)
отменённые ₽               640 651 844         640 651 844              0      (FBO 392 261 595, FBS 248 390 249)
08-18                        7 403 969           7 403 969              0      (было 9 910 881)
08-19                        7 655 901           7 655 901              0      (было 7 566 374)
```

Сверх счётчиков — значения: каждая из 60 654 строк плана найдена в таблице
по ключу, сверено 8 полей (`article`, `product_name`, три `orders_*`, три
`cancelled_orders_*`) — расхождений 0. Множества ключей: только в таблице 0,
только в плане 0 по обеим схемам. `observed_at` заполнен у всех строк и
равен моменту сбора сырья (FBO 13:51:25 UTC, FBS 13:59:59 UTC), а не моменту
записи. WB не задет: 23 770 строк, `observed_at` — 0, как утром. Строк Ozon
раньше 03-28 и позже 09-18 — 0.

Перед записью план перестроен заново (18:46 UTC) и сравнен с планом от 14:00
— помесячные строки совпали побайтно: таблица за 4,5 часа не менялась.

### 2. Как прошёл `--apply`

Старт 18:55:45 UTC, конец 18:59:17 — **3 мин 32 с** (оценка была 4–6 мин), за
5 ч 15 мин до ночного окна. Запускала с явным `--date-to 2026-09-18`: после
21:00 UTC локальная дата сменится, имя файла сырья по умолчанию стало бы
другим.

```
снимок        57 256 строк, все 15 колонок → data/snapshots/orders_rewrite_20260918T185616Z.json (30,2 МБ)
FBO  delete   одним запросом, 2,9 с     запись 39 531 строк, 80 пачек — 86,5 с     было 36 410 → в таблице 39 531 =
FBS  delete   одним запросом, 4,4 с     запись 21 123 строк, 43 пачки — 45,1 с     было 20 846 → в таблице 21 123 =
```

**`delete` в таймаут 8 с уложился, по месяцам делить не пришлось.** Но FBS —
4,4 с на 20 846 строк при 2,9 с на 36 410 у FBO: время не пропорционально
числу строк, запас меньше двух раз. Запасной путь `--delete-by-month`
написан и покрыт тестом, не использован. Запись — 1,05–1,08 с на пачку с
этой машины (86,5 с / 80 и 45,1 с / 43) против 1,0 с с Render.

Правки скрипта между планом и записью (на числа плана не влияют — проверено
повторным `--plan`): `observed_at` берётся из `fetched_at` файла, а не из
момента запуска; флаг `--delete-by-month`; печать времени `delete` и записи.
Тестов стало 7, весь набор — 597, OK.

### 3. Витрина: удалено 479 ключей `daily_sku_kpi`

`scripts/delete_stale_kpi_keys.py` (новый; без `--apply` не пишет). Список —
не приказ: перед удалением каждый ключ перепроверяется по живой БД, и
удаляется только тот, у которого сейчас нет строки ни в заказах, ни в
выкупах, ни в расходах, ни в органике.

```
до пересборки (18:45 UTC)   сухой прогон: с живым источником 479 из 479 (строка заказов старой датировки) → к удалению 0   ← защита сработала
после пересборки (19:01)    сухой прогон: с живым источником 0, значения в витрине = списку у 479 из 479 → к удалению 479
19:03 --apply               снимок 479 строк, все 29 колонок → data/snapshots/kpi_stale_keys_20260918T190338Z.json (380 КБ)
                            удалено 479 из 479, осталось 0; 539 шт, 17 031 211,00 ₽, даты 04-02 … 04-28; 43 с
```

Проверка SQL: строк `daily_sku_kpi` Ozon с 03-28 — 173 518 → 173 039
(−479); сумма заказов в витрине 1 434 985 797 → 1 417 954 586 = −17 031 211
ровно. В удалённых строках выкупы, расходы, реклама и органика — нули у всех
479. Остальные 372 ключа не трогала: сейчас в них 427 шт на 13 259 679,00 —
ровно прогноз проверки плана, ночь их обнулит.

### 4. Правило в `how-we-work.md`

Дописано в «Порядок обмена: inbox и outbox», текст советника без изменений,
плюс одна строка — откуда правило взялось. Ниже советник во время этой
сессии дописал свой абзац (источник «плато», `--date-to` в команде
необратимого шага, `index.lock`) — закоммичен как есть. По `--date-to` мы
сошлись независимо: я запускала с явной границей, §2.

### 5. Что проверить утром 09-19 — и какие числа должны сойтись

Ночное окно 09-19 начинается в ~03:20 МСК 08-20. Всё, что раньше, ночь не
трогает — это и есть проверка, что пересборка пережила первую ночь.

```
1. marketplace_orders, ozon, order_date ≤ 08-19 — НЕ ДОЛЖНО ИЗМЕНИТЬСЯ:
      строк 51 608;  подтв. 41 470 шт / 960 546 365;  отм. 30 182 шт / 566 478 624
      observed_at у всех — 2026-09-18 13:51:25 (fbo) или 13:59:59 (fbs)
2. Витрина = таблице после ночного KPI:
      Σ daily_sku_kpi.orders_amount_seller (ozon, ≤ 08-19) = 960 546 365          (сейчас витрина ещё старая)
      апрель: 195 807 652 → 179 911 343  (= таблица за апрель, она заморожена)
      daily_marketplace_kpi: 08-18 = 7 403 969, 08-19 = 7 655 901
      ad_share_of_orders за 08-10 … 08-18: 7,6–9,6 % (было 4,7–6,1 %)
3. 372 ключа старой датировки обнулены: запрос «в daily_sku_kpi заказы > 0, а строки в
      marketplace_orders за (дату, sku) нет» сейчас даёт 372 / 427 шт / 13 259 679 — утром должен дать 0
4. 479 удалённых не вернулись: строк daily_sku_kpi ozon с 03-28 = 173 039 + новые ключи ночи, не меньше;
      ни одного ключа из data/orders_history_kpi_stale_keys_20260918.json в витрине
5. Дефект первого дня окна — предсказание на эту ночь, дата 08-20:
      подтв. 7 786 688 → 7 639 623 (−147 065: 9 ключей FBO с отправлениями и до, и после 03:23 МСК)
      отм.   3 824 586 → 3 753 988 (−70 598: те же 9 FBO −30 845 и 1 ключ FBS −39 753)
      14 ключей 08-20 (7 FBO + 7 FBS, только ранние отправления) останутся с observed_at 09-18
   Сойдётся — механика доказана вперёд, а не только назад; это же цена каждой ночи без правки загрузчиков.
6. Лог Render: строки отправлений FBS/FBO и «записаны в marketplace_orders», блокер только органики,
      «Шаг не выполнен» нет. Прогон пересборка не трогала — деплой сменится только от этого коммита (документы и скрипты вне прогона).
```

Оговорка к п. 2: `reports_daily_sku_kpi.py` читает 84 тыс строк заказов
постранично без `ORDER BY` (известный дефект, `ozon_ad_spend_reconciliation.md`).
До сих пор витрина сходилась с таблицей до рубля на каждой проверенной дате;
если утром не сойдётся — первым подозревать чтение, а не пересборку.

### 5а. Утро 09-19 — проверено 05:54 UTC, сошлось всё, включая предсказание

Прогон 00:15 → 02:37 UTC, «Весь пайплайн успешно завершен», «Шаг не
выполнен» и Traceback — 0. FBS `/v4` 6 051 отправление / 61 страница
(подтв. 4 192, отм. 1 859, строк 4 726), FBO 5 528 / 56 (3 539 / 1 989,
строк 4 428), лог статусов 1 053 строки, KPI прочитал 84 558 строк заказов.

```
1. зона ≤ 08-19          51 608 строк, подтв. 41 470 шт / 960 546 365, отм. 30 182 шт / 566 478 624, observed_at не от 09-18 — 0   = ждали, не изменилась
2. витрина = таблице     ≤ 08-19: 960 546 365 = 960 546 365;  апрель 179 911 343 = 179 911 343 (было 195 807 652);
                         весь период 03-28 … 09-18: 1 119 555 903 = 1 119 555 903;  по дням 08-10 … 08-22 — Δ 0 на каждой дате
                         08-18 — 7 403 969, 08-19 — 7 655 901;  ДРР 08-10 … 08-18: 7,6–9,6 %                                        = ждали
3. 372 ключа             «заказы > 0 без строки заказов» — 0 (было 372 / 13 259 679)                                               = ждали
4. 479 удалённых         в витрине найдено 0 из 479                                                                               = ждали
5. 08-20, первый день    подтв. 7 786 688 → 7 639 623, отм. 3 824 586 → 3 753 988, с observed_at 09-18 осталось 14 ключей        = предсказание до рубля
```

П. 5 — дефект первого дня окна подтверждён вперёд: за одну ночь дата 08-20
потеряла 147 065 подтверждённых и 70 598 отменённых, ровно по тем 10 ключам,
что были названы заранее. Следующей ночью то же случится с 08-21. Утренний
алерт (07:30 UTC) на момент проверки ещё не ходил.

**Из чата разработчиков (поиск 09-19 по `posting/fbo/list`, `posting/fbs/list`,
глубине):** (а) жалоба 06-01 «`/v4` отдаёт пустой `postings`» закрыта самим
автором — чужой токен; «при неверном токене методы отдают пустые
`postings[]`, и ни слова про ошибку доступа» (#61875). Жалоба 06-04 «не все
заказы, с запозданием» осталась без подтверждения. Наша формулировка «в июне
`/v4` отдавал не все отправления» в `ozon_orders_one_rule.md` §3 была
сильнее источника — поправлено; (б) глубина: «fbo/list, fbs/list … дают
данные на требуемую глубину» (#64637), выгрузка `fbs/list` «списком за год»
работает (#60554, #60557) — сходится с нашим сбором на 175 дней и со спекой
(период до года); жалоб на пропажу старых отправлений в чате нет; (в)
2025-05-29 (#46672, без ответа): отправления появляются в `fbs/list` позже
своего `in_process_at` — для ночного окна в 30 дней неважно, для частого
опроса короткими окнами было бы; (г) 2025-07-22 (#48907): при разделении
отправления родитель остаётся в базе со статусом ожидания — у нас это
`cancelled_from_split_pending`, в истории таких 0.

Следствие из (а) для нас: отозванный или чужой ключ Seller API дал бы пустой
сбор, шаги заказов завершились бы «Нет заказов для записи» с кодом 0;
поймал бы это только утренний блокер `ozon_fbo/fbs_orders_missing`. Не
чинила — называю.

### 6. Не сошлось — отдельной строкой

* контрольные числа плана — расхождений 0, ни по суммам, ни по ключам, ни
  по значениям;
* время `--apply` 3 мин 32 с против оценки 4–6 мин — оценка была с запасом
  на медленную запись, её не понадобилось;
* побочно: ключей `daily_sku_kpi` без единого источника было 406 (двадцать
  вторая, §8), после пересборки стало 40 — у 366 появилась строка заказов
  (полностью отменённые ключи, которых старый FBO не писал). Заказов в
  оставшихся 40 — 0; не трогала;
* `delete` FBS занял 4,4 с из 8 — при росте таблицы одним запросом
  перестанет проходить; для следующих пересборок есть `--delete-by-month`.

### Коммиты

`main`, ветки нет: `b40290d` — скрипты, тест, правило, документы; номер
дописан следующим коммитом.
Файлы: `scripts/rebuild_ozon_orders_history.py`, `scripts/delete_stale_kpi_keys.py`
(новый), `tests/test_rebuild_orders_history.py`, `docs/how-we-work.md`,
документы. Снимки и сырьё — вне git (`data/snapshots/`, `data/postings_raw/`).

---

## 2026-09-18, двадцать вторая задача (пересборка истории заказов — план готов и проверен, `--apply` НЕ запускался)

К Ozon API — 794 обращения на чтение (`/v3/posting/fbo/list`,
`/v4/posting/fbs/list`), 429 — 0. В БД — только SELECT, `db_writes = 0`.
`--apply` не запускался. Витрины, устаревшие ключи, воркер, ветка, WB,
органика — не тронуты. Жду слова владельца по плану ниже.

До первого запуска прочитала скрипт и нашла три дефекта, два из них — в
необратимом шаге. Поправила до `--fetch`, покрыла тестами — §6.

### 1. `--estimate` против ориентиров

```
                 ориентир    --estimate (13:31 UTC)   факт --fetch
обращений          ~705            701                 794  (FBO 533, FBS 261)
время             ~24 мин         24 мин               25,9 мин (1 040 с + 513 с; 1,96 с на обращение)
строк в таблице   56 248          57 256               —
```

Строк +1 008 — ровно: 654 строки за даты 09-16 … 09-18 (новые дни) + 354
полностью отменённых ключа за старые даты, которых старый FBO не писал, а
ночь 09-18 записала (запрос по `created_at ≥ 09-16 20:30 UTC`, остальных
новых — 0). Расхождения с ориентирами нет, пошла в `--fetch`.

Обращений по факту +93 к оценке, и знак у схем разный: оценка брала
~197 отправлений/день по сентябрьскому сырью на обе схемы. FBO весной–летом
шёл по 224–389 в день (52 948 отправлений против оценки 34 498, +182
обращения), FBS начинался с 62 в день в апреле (25 837 против 34 364, −89).

### 2. Сырьё: 2 файла, по 6 окон

```
data/postings_raw/history_fbo_2026-03-28_2026-09-18.json   102,6 МБ   52 948 отправлений, номера уникальны
   окна (UTC 21:00 = локальная полночь): 03-27…04-26 6 724 | …05-26 8 539 | …06-25 11 657 | …07-25 11 252 | …08-24 10 907 | …09-18 13:34 3 869
   статусы: delivered 31 183, cancelled 20 925, delivering 660, awaiting_deliver 139, awaiting_packaging 41
data/postings_raw/history_fbs_2026-03-28_2026-09-18.json    95,3 МБ   25 837 отправлений, номера уникальны
   окна: 03-27…04-26 1 857 | …05-26 3 035 | …06-25 5 438 | …07-25 5 184 | …08-24 4 977 | …09-18 13:51 5 346
   статусы: delivered 13 657, cancelled 10 742, delivering 1 349, acceptance_in_progress 62, awaiting_packaging 25, awaiting_registration 1, awaiting_deliver 1
```

Незнакомых статусов, родителей разделённых, строк без даты / sku / количества
— 0 у обеих схем. Вне git (`.gitignore`).

**Полнота сырья проверена внешним арбитром — начислениями**
(`data/accrual_postings/`, тип 69, 44 229 отправлений с продажей за
03-28 … 09-15). Сравнение множеств номеров отправлений:

```
месяц первого начисления    продано    нет в нашем сборе
2026-05 … 2026-09            39 039          0
2026-04                       4 903         63   ← первые начисления 04-01 … 04-16: заказы до 03-28, вне окна
2026-03 и раньше                287        244   ← то же, заказы до границы проекта
```

Все проданные, что нашлись, в сборе `delivered` (42 659 с нетто > 0,
1 256 с нетто ≤ 0 — возвраты после доставки); `cancelled` — 7, все с нетто
≤ 0. Проданного с нетто > 0 и статусом `cancelled` — 0. То есть метод отдаёт
историю на всю глубину и статус сходится с деньгами.

### 3. План «было / станет»

`--plan` (14:00 UTC, 31 с) и `scripts/check_orders_history_plan.py` (68 с).
Август разрезан по 08-19 — границе ночного окна: до неё в таблице старые
правила, после — новое. Суммы — `orders_amount_seller`, ₽.

```
схема период      | ТАБЛИЦА строк  подтв шт   подтв ₽       отм ₽     | ПЛАН строк подтв шт  подтв ₽       отм шт   отм ₽        создано ₽      | отм шт / ₽    | Δ подтв ₽       Δ пары ₽
fbo  03 (28–31)   |    109      126    4 126 727           0          |    202     157    5 069 799      141   3 543 548    8 613 347    | 47,3 / 41,1 % |    +943 072    +4 486 620
fbo  04           |  4 251    5 297  146 648 506           0          |  5 377   4 842  141 833 978    3 483  80 684 532  222 518 510    | 41,8 / 36,3 % |  −4 814 528   +75 870 004
fbo  05           |  6 385    7 520  162 950 901           0          |  6 823   5 667  128 716 901    4 173  77 558 134  206 275 035    | 42,4 / 37,6 % | −34 234 000   +43 324 134
fbo  06           |  7 991    9 584  192 991 713           0          |  8 533   7 388  157 660 922    4 988  84 388 231  242 049 153    | 40,3 / 34,9 % | −35 330 791   +49 057 440
fbo  07           |  8 436   10 022  175 880 017           0          |  8 977   7 549  142 464 853    5 019  72 534 643  214 999 496    | 39,9 / 33,7 % | −33 415 164   +39 119 479
fbo  08 до 19     |  4 663    5 528   84 924 371           0          |  4 955   4 167   67 274 479    2 838  37 262 091  104 536 570    | 40,5 / 35,6 % | −17 649 892   +19 612 199
fbo  08 с 19      |  2 354    1 878   32 215 344  20 589 177          |  2 355   1 883   32 342 128    1 352  20 687 320   53 029 448    | 41,8 / 39,0 % |    +126 784      +224 927
fbo  09           |  2 221    1 968   33 205 831  14 895 876          |  2 309   2 036   34 836 994      870  15 603 096   50 440 090    | 29,9 / 30,9 % |  +1 631 163    +2 338 383
fbo  ИТОГО        | 36 410   41 923  832 943 410  35 485 053          | 39 531  33 689  710 200 054   22 864 392 261 595 1 102 461 649   | 40,4 / 35,6 % | −122 743 356 +234 033 186
fbs  03 (28–31)   |      0        0            0           0          |    173     120    3 339 859       88   2 374 123    5 713 982    | 42,3 / 41,5 % |  +3 339 859    +5 713 982
fbs  04           |  1 767    2 103   66 190 357           0          |  1 796   1 260   38 077 365    1 004  25 427 139   63 504 504    | 44,3 / 40,0 % | −28 112 992    −2 685 853
fbs  05           |  2 872    3 568   83 451 838           0          |  2 823   1 839   48 543 031    1 722  34 662 417   83 205 448    | 48,4 / 41,7 % | −34 908 807      −246 390
fbs  06           |  4 457    5 429  133 985 786           0          |  4 457   3 011   82 202 632    2 511  53 400 016  135 602 648    | 45,5 / 39,4 % | −51 783 154    +1 616 862
fbs  07           |  4 486    5 698  138 918 097           0          |  4 486   3 240   84 978 252    2 554  55 894 717  140 872 969    | 44,1 / 39,7 % | −53 939 845    +1 954 872
fbs  08 до 19     |  2 609    3 289   86 226 307           0          |  2 609   1 881   52 728 393    1 453  34 312 396   87 040 789    | 43,6 / 39,4 % | −33 497 914      +814 482
fbs  08 с 19      |  1 578    1 248   33 115 262  15 715 836          |  1 578   1 243   33 066 064      761  15 806 826   48 872 890    | 38,0 / 32,3 % |     −49 198       +41 792
fbs  09           |  3 077    3 142   60 154 740  25 832 961          |  3 201   3 238   63 664 779    1 207  26 512 615   90 177 394    | 27,2 / 29,4 % |  +3 510 039    +4 189 693
fbs  ИТОГО        | 20 846   24 477  602 042 387  41 548 797          | 21 123  15 832  406 600 375   11 300 248 390 249   654 990 624   | 41,6 / 37,9 % | −195 442 012  +11 399 440
```

Δ = план − таблица; «Δ пары» = (подтв + отм) плана − (подтв + отм) таблицы.
Строк станет 60 654 против 57 256 (+3 398). Подтверждённые заказы Ozon за
период: 1 434 985 797 → 1 116 800 429 (−318 185 368, −22,2 %).

**Ожидания задачи — по пунктам:**

* **FBS: сумма пары примерно прежняя, `orders_qty` падает — ДА** для мая …
  08-18: Δ пары от −0,3 % до +1,4 % (−246 390 … +1 954 872), штуки −43 … −48 %.
  Плюс в июне–августе (+4 386 216) — почти целиком дефект первого дня ночного
  окна, §5: на 05-16 … 08-18 он объясняет 222 ключа из 223 с недобором
  (4 672 035 из 4 677 999). Объяснение от 09-16 «отправления доехали в список
  после выхода даты из 14-дневного окна» **опровергнуто** — оно было
  догадкой, по значениям не проверялось. **Апрель −2 685 853 — НЕ «примерно
  прежняя», причина другая и найдена** — ниже, «ключи только в таблице».
* **FBO: `orders_qty` падает, сумма пары растёт — ДА** с апреля по 08-18:
  штуки −455 … −2 473 в месяц, пара +19,6 … +75,9 млн. Март (4 дня) — в
  другую сторону, штуки +31: в таблице он неполон (109 строк против 202 —
  FBO начали грузить 04-28, 30-дневное окно доставало лишь до конца марта).
* **Доля отмен растёт к плато на давних месяцах, ниже на свежих — ДА по
  форме, у FBS МИМО по уровню.** FBO: 39,9–42,4 % шт на дозревших месяцах
  против плато 41,7 % из среза 09-15 — сошлось; сентябрь 29,9 % — не дозрел.
  **FBS: 43,6–48,4 % шт / 39,4–41,7 % ₽ против ожидавшихся ~38 % / ~35 % —
  выше на 6–10 п.п. по штукам.** Это не хвост дозревания: между снимком
  09-16 20:09 UTC и сегодняшним сбором (41,7 ч) из живых отправлений
  возраста 21–30 дней отменилось 0 из 796 у FBS и 0 из 1 490 у FBO (младше:
  0–7 дней 6,3–6,7 %, 7–14 — 2,5 %, 14–21 — 1,6–1,8 %) — к 21-му дню дата
  закрыта, и та же когорта FBS 08-19 … 08-31 сегодня стоит на 38,0 % / 32,3 %,
  ровно на срезе. Значит, весенне-летние когорты FBS отменялись чаще
  августовских; плато 38 % было измерено по одной когорте и на историю не
  переносится.
* **Март и апрель не околонулевые** (FBO 47,3 % и 41,8 % шт, FBS 42,3 % и
  44,3 %) — метод старые отменённые отдаёт, пересборка осмысленна за все
  месяцы. Март у FBS появится впервые (173 строки, в таблице FBS с 04-12).

**Множества ключей (не счётчики).** Общих ключей 36 297 FBO и 20 075 FBS.

```
только в плане    FBO 3 234: 3 047 полностью отменённых ключей за 04-01…08-18 (старый код их не писал, 88 089 367 отм.) + 1 такой же за 08-19+ + 98 за 03-28…31 + 88 за сентябрь (заказы после ночи)
                  FBS 1 048: 173 за март + 751 за 04-01…04-17 (в таблице FBS с 04-12 и по другой дате) + 124 за сентябрь
только в таблице  FBO   113 на  2 709 395   даты 03-28 … 04-30
                  FBS   771 на 28 025 520   даты 04-12 … 05-01
```

«Только в таблице» — не пропавшие отправления, а **старая датировка, и это
проверено значениями, а не догадкой**. До `15fb56f` (2026-05-01) FBS ставил
дату по `shipment_date[:10]` — плановой отгрузке, а не заказу; FBO — по
UTC-дате `created_at`, а не локальной. Фикс 05-01 переписал даты внутри
тогдашних окон под новыми ключами, а upsert старые ключи не снял. Пересчёт
сегодняшнего сырья по старому правилу: FBS — найдено 746 ключей из 771, шт и
₽ совпали у 745; не найдены 25 на 532 993 (гипотеза: плановая отгрузка
сдвинулась после записи — не проверено); FBO — найдены все 113, у всех
отправления созданы в 21:00–24:00 UTC, значения совпали у 110 (у трёх
разница 44 457 в сумме — не разбирала). Отсюда апрель FBS: в таблице часть
заказов 04-18 … 04-30 лежит дважды, под обеими датами, и пара «падает».
Пересборка эти 884 ключа снимает — их удалит `delete` за окно.

### 4. Ступенька 08-18 / 08-19 — уходит

`daily_marketplace_kpi.orders_amount_seller` сейчас = сумме таблицы по обеим
схемам на каждой из 18 дат (проверено), поэтому «станет» = план подтв.

```
дата     сейчас (витрина)   станет (план подтв.)   отм ₽ %   реклама     ДРР сейчас → станет
08-15      8 351 850          5 402 445            39,9 %    473 561      5,7 % → 8,8 %
08-16      8 784 037          5 998 429            38,6 %    531 562      6,1 % → 8,9 %
08-17     11 077 554          7 581 721            40,1 %    617 253      5,6 % → 8,1 %
08-18      9 910 881          7 403 969            34,2 %    577 990      5,8 % → 7,8 %
08-19      7 566 374          7 655 901            36,7 %    620 819      8,2 % → 8,1 %   ← граница
08-20      7 786 688          7 786 688            32,9 %    598 416      7,7 % → 7,7 %
08-21      4 907 590          4 907 590            41,0 %    458 768      9,3 % → 9,3 %
```

08-18 опускается с 9 910 881 до **7 403 969** и встаёт в линию с 08-19
(7 655 901): перепад −2 344 507 (−23,7 %) превращается в +251 932 (+3,4 %).
ДРР за 08-10 … 08-18: было 4,7–6,1 %, станет 7,6–9,6 % — той же полосой, что
после границы (7,6–10,1 %). Даты 08-20 … 08-27 план не меняет ни на рубль.
08-19 вырастет на 89 527 = FBO +126 784 (дефект первого дня окна, §5: у 10
ключей пара плана больше таблицы на 161 084, у всех есть отправления и до, и
после 03:30 МСК) и FBS −37 257 (дозревание за 13 часов после ночи; тот же
дефект у 1 ключа, +41 792 к паре).

### 5. Найдено по дороге: ночное окно каждую ночь портит свой первый день

Окно сбора — «сейчас минус 30 дней», то есть начинается в ~03:20 МСК даты
D = сегодня − 30. Строки строятся из отправлений окна, и upsert
**замещает** ключ (D, sku) целиком. Если товар в день D продавался и до
03:20, и после — ранние заказы из строки исчезают; следующей ночью D уже вне
окна, и потеря остаётся навсегда. Измерено на 05-16 … 08-18, общие ключи,
где в таблице меньше, чем в сборе:

```
FBO   298 ключей на 5 093 441   из них 292 на 5 079 975 — ключи с отправлениями и до, и после 03:30 МСК; остальные 6 на 13 466   (база — подтверждённые, 1,14 % от 446 119 814)
FBS   223 ключа  на 4 677 999   из них 222 на 4 672 035 — то же; остальной 1 на 5 964                                      (база — созданные: старый FBS писал все статусы; 1,13 % от 414 973 102)
```

Встречная проверка: у FBO всего 322 ключа с отправлениями по обе стороны
03:30, их ранняя часть 5 544 730 — недобор 5 079 975 у 292 из них, то есть
теряется именно ранняя часть. ≈ 53 тыс ₽ в день у FBO и ≈ 49 тыс у FBS (окно
FBS было 14 дней, механика та же). Свежий пример — 08-19, первый день окна
ночи 09-18: §4. **Пересборка это лечит за всю историю, но
ночь продолжит портить по одной дате (сегодня − 30) каждый раз.** Чинится в
загрузчиках одной правкой — начинать окно с локальной полуночи либо не
писать строки первого, неполного дня. Не делала: это боевые загрузчики и
развилка, которую не обсуждали. Предлагаю отдельной задачей сразу после
`--apply`, иначе история снова начнёт осыпаться с первой же ночи.

### 6. Три дефекта в скрипте пересборки — исправлены до запуска

Найдены чтением, до первого обращения к API:

1. **Сбор терял три часа.** Окно начиналось в 00:00 UTC = 03:00 МСК, а
   `order_date` локальная и `--apply` переписывает день `date_from` целиком:
   03-28 ушёл бы в таблицу без ночных заказов. Теперь окно — от локальной
   полуночи (`2026-03-27T21:00Z`), конец — не позже «сейчас» (будущее методу
   не передаём). В сборе первое отправление — 03-27 21:09 UTC.
2. **У `delete` не было верхней границы.** `--date-to` в прошлом стёр бы все
   строки Ozon от `date_from` до сегодня, а записал бы только до `date_to`.
   Теперь `delete`, счётчики, статистика и отбор строк ограничены `date_to`.
3. **Снимок перед удалением держал 5 колонок из 15** (те, что читались для
   статистики) — восстановить по нему таблицу было бы нельзя. Теперь снимок
   читается отдельно, `select *`, и сверяется по числу строк со статистикой.

`tests/test_rebuild_orders_history.py` — 5 тестов (границы окна, обе границы
у delete, полный снимок, `--plan` ничего не пишет); весь набор — 595, OK.
Побочно, не правила: файл сырья пишется один раз на схему, в конце — отказ
на последнем окне теряет всю схему (~9–17 минут сбора, не квоты).

### 7. Что понадобится сразу после `--apply`

**Ключи витрины без заказов.** Исчезает 851 пара (дата, sku) — все от старой
датировки, §3; появляется 3 964 (ночной KPI создаст сам).

```
daily_sku_kpi          479 ключей под чистку, 539 шт, 17 031 211,00 ₽, даты 04-02 … 04-28
                       (421 от FBS по shipment_date, 58 от FBO по UTC-дате). У ключа нет ни выкупов, ни расходов,
                       ни органики — ночной KPI его не построит, upsert не удалит, заказы останутся навсегда
                       и задвоятся с теми же заказами под верной датой.
                       Список: data/orders_history_kpi_stale_keys_20260918.json
                       372 ключа — ночной KPI перепишет сам и заказы обнулит: есть расходы (129), расходы+выкупы (65),
                       расходы+органика (39), все три (23), только органика (116)
daily_marketplace_kpi  0 ключей: дат без заказов не возникает; суммы подтянутся из daily_sku_kpi ночью —
                       но апрель останется завышен на 17 031 211, пока 479 ключей не удалены
```

Оговорка по 116 ключам «только органика» (04-29 … 05-01): строка
`ozon_daily_sku_organic` посчитана от старых заказов, шаг органики выключен и
её не пересчитает — в витрине останется органика при нулевых заказах. Таблицу
не трогала. Прочие таблицы с заказами (`sku_decision_daily_input`,
`stock_data_quality_issues`, `intraday_snapshots`) ночью не строятся —
останутся как есть.

**Время `--apply`: ~4–6 минут, в окно попадает с запасом** (сейчас ~14:30
UTC, ночной прогон в 00:15). Слагаемые: чтение файлов + строки + статистика
31 с (замер `--plan`); полный снимок 57 256 строк ~40 с (оценка: узкое чтение
заняло 25 с); 122 пачки по 500 строк — с Render 1,0 с на пачку (лог ночи
09-18: 4 648 строк за 10,1 с, 4 569 за 8,9 с), с машины владельца кладу 1–2 с
→ 2–4 мин. Между `delete` и концом записи схемы (~2 мин) заказов этой схемы
в таблице нет — не запускать в 00:15 … 03:15 UTC (скрипт сам не даст) и около
07:30 UTC (утренний алерт).

**Риск, который стоит знать до запуска.** PostgREST ходит под ролью с
`statement_timeout = 8s` (`authenticator`; у `service_role` своего нет —
`pg_roles`, сегодня). `delete` 36 410 строк FBO одним запросом должен
уложиться, но замерить его без записи нельзя. Если не уложится — отказ
безопасный: запрос откатывается целиком, скрипт падает до записи, таблица
цела. Надёжнее другой порядок — upsert всех строк плана, затем удаление
только 884 ключей, которых в плане нет: тот же итог, без дыры в таблице и без
большого `delete`. Не делала — это смена согласованного алгоритма, решать
советнику и владельцу.

**Свежесть файлов.** Сегодня `--apply --date-from 2026-03-28` возьмёт файлы,
снятые в 13:34–14:00 UTC, и в API не пойдёт. Завтра `--date-to` по умолчанию
станет 09-19, имя файла изменится — будет новый сбор на 26 минут, и план
надо смотреть заново. Это согласуется с правилом, которое советник дописал в
`how-we-work.md` во время этой сессии («необратимый шаг выполняет та сессия,
которая показала план»); файл закоммичен как есть, я его не правила.

### 8. Не сошлось — отдельной строкой

* доля отмен FBS на истории 43,6–48,4 % шт против ожидавшихся ~38 % — не
  дозревание (0 отмен из 796 живых старше 21 дня за 41,7 ч), а сами когорты;
* апрель FBS: пара −2 685 853 вместо «примерно прежней» — задвоение от
  старой датировки по `shipment_date`, доказано значениями (745 из 771);
* 25 ключей FBS на 532 993 старым правилом не нашлись — гипотеза про
  сдвинувшуюся плановую отгрузку, не проверена; 3 ключа FBO расходятся на
  44 457 — не разбирала. Оба хвоста пересборка снимает вместе с ключами;
* объяснение от 09-16 «в сборе FBS на отправление больше — доехали после
  выхода даты из 14-дневного окна» опровергнуто: это дефект первого дня окна
  (222 ключа из 223), помечено в `ozon_orders_one_rule.md` §5;
* оценка обращений 701 против факта 794 — объём по месяцам неравномерен, §1;
* побочно, вне задачи: в `daily_sku_kpi` уже сегодня 406 ключей Ozon без
  единого источника (03-28 … 08-22), в них 235 471,38 прочих расходов и 12,75
  рекламы — к пересборке заказов отношения не имеют, заказов в них 0.

### Коммиты

Работа в `main`, ветки нет: `102ddf8` — скрипты (вне ночного прогона),
тесты, документы; номер коммита дописан следующим коммитом. Файлы: `scripts/rebuild_ozon_orders_history.py`,
`scripts/check_orders_history_plan.py`, `tests/test_rebuild_orders_history.py`,
`data/orders_history_kpi_stale_keys_20260918.json`, документы.

---

## 2026-09-18, двадцать первая задача (первая ночь с колонками отмен — новый код отработал, предсказание сошлось, поломок нет)

Только чтение: к Ozon API — 0 обращений, в БД — SELECT, Render — чтение
логов и деплоев, алерт — сухой прогон. Ничего не мержил. Этот отчёт —
коммит в `main`, только документы.

### 1. Новый код отработал — три признака из трёх

```
observed_at      ozon/fbo 4 569 строк (2026-09-18 00:23:06 UTC), ozon/fbs 4 648 (00:20:36); wb — 0 из 23 770     ДА
cancelled > 0    fbo 1 813 строк / 2 169 шт; fbs 1 612 строк / 1 916 шт                                            ДА
окно FBS 30 дн.  у fbs 2 023 строки с observed_at за даты раньше 09-04, самая ранняя 08-19                         ДА
```

Деплой: ночью жил `71a8dcb` (live с 09-17 20:11 UTC — отчёт двадцатой,
только документы поверх мержа `1ca569d`).

### 2. Предсказание: окно 09-03 … 09-16, ozon

```
            было (снимок 09-17 19:00)     стало (после ночи 09-18)
схема     строк    qty    отмен       строк   orders_qty  cancelled_qty  сумма пары   доля отмен шт / ₽
fbo       1 666   1 917     0         1 806      1 600         661          2 261      29,23 % / 32,08 %
fbs       2 590   3 679     0         2 590      2 678       1 006          3 684      27,31 % / 31,06 %
```

**FBS — как ждали:** сумма пары 3 684 против 3 679 (+5 — отправления,
доехавшие в список после снимка), `orders_qty` упал на отменённые.

**FBO — `orders_qty` упал с 1 917 до 1 600 (−317). По критерию задачи это
«поломка», по данным — нет; неверной была посылка.** 1 917 не были
чистыми подтверждёнными: в них сидел остаток отменённых ключей (та самая
вторая механика от 09-15 — старый код выбрасывал отменённые, а upsert не
снимал строку, у которой отправлений не осталось). Разложение:

```
старых ключей окна, ставших полностью отменёнными   297 строк, cancelled 326 шт, 4 813 450,00  ← их прежние qty и ушли из orders_qty
новых ключей (created_at = ночь 09-18)              140 строк, все полностью отменённые, 164 шт ← старый код их не писал вовсе
строк: 1 666 + 140 = 1 806 ✓;  −317 = −326 (остаток ушёл в отмены) + 9 (новые подтверждённые после снимка)
```

Независимая сверка: моё сырьё FBO от 09-16 20:11 UTC (шестнадцатая
задача) за то же окно давало 1 642 подтверждённых штуки при 1 401 живом
ключе — против 1 917 / 1 666 в снимке; разница 275 шт / 265 ключей
существовала уже тогда, за 28 часов дозрело ещё.

**Доля отмен против лога статусов** (последний статус каждого отправления
окна в `ozon_posting_status_log`):

```
          таблица, шт   таблица, ₽              лог, отправления          лог, ₽
fbo         29,23 %     32,08 % (13 008 846,00)  29,33 % (637 из 2 172)   32,08 % (13 008 846,00 из 40 550 670,00)
fbs         27,31 %     31,06 % (22 383 034,00)  27,94 % (960 из 3 436)   31,06 % (22 383 034,00 из 72 066 383,00)
```

Суммы отменённых в таблице и в логе совпали до рубля по обеим схемам —
два независимых пути от одного сырья дают одно число. Вилка 25–35 %
попала, подгонять нечего.

### 3. Сама ночь — чек-лист §9-4б

```
прогон                     00:15:57 → 02:57:12 UTC, «✅ Весь пайплайн успешно завершен»
FBS /v4                    Получено FBS postings: 5957 за 60 страниц (ждали ~5 900 / ~59), все 200
FBO /v3                    Получено FBO postings: 5736 за 58 страниц
подтв. / отмен.            FBS 4 119 / 1 838 (ждали ~4 100 / ~1 800); FBO 3 682 / 2 054 (ждали ~3 800 / ~2 100)
                           родителей разделённых 0, без даты 0, без sku 0, нулевых количеств 0, незнакомых статусов 0
записано                   FBS 4 648 строк, FBO 4 569 строк; сырьё сохранено оба раза
лог статусов               «✅ Готово», итого строк записано: 1021 — FBO 426, FBS 595 (первая ночь 1 013);
                           FBS delivering→delivered 172, ∅→awaiting_packaging 126, delivering→cancelled 92;
                           FBO delivering→delivered 84, ∅→awaiting_packaging 76, delivering→cancelled 47
тревоги «Шаг не выполнен»  0; Traceback 0
429                        1 — WB Sales Funnel, не наш шаг
реклама за 09-17           success, 484 из 484 кампаний, CPO success
блокеры (сухой прогон 09-17) только ozon_daily_sku_organic_missing; fbo/fbs_orders_missing не горят
```

### 4. Устаревшие ключи FBO

Внутри ночного окна их больше нет. За полные дни 08-20 … 09-17 строк
без `observed_at` — 0 у обеих схем; на 08-19 (первый, неполный по
UTC-границе день окна) — FBO 6 строк на 64 018,00 и FBS 7 на 137 760,00.
Новое правило новых устаревших не создало.

«Старые 1 108 никуда не делись» — внутри окна делись: 977 прежних ключей
FBO за 08-20 … 09-17 переписаны в полностью отменённые (`orders_qty` 0,
`cancelled` 1 119 шт, 15 693 292,00 — тот же порядок, что 1 108 / 15,78
млн замера 09-15 на окне 08-17 … 09-14). У FBS таких 1 168 строк на
30 367 482,00 — раньше они числились заказами целиком. Остаток — только
за даты раньше 08-19, и `observed_at` его не покажет (там он null у
всех): снимет пересборка истории.

### Что не сошлось — отдельными строками

- **Посылка про FBO:** «`orders_qty` должен остаться ≈ 1 917» неверна —
  в снимке сидел остаток отменённых ключей, 326 шт; падение до 1 600 —
  это исправление, не поломка.
- **«1 108 никуда не делись»** — внутри окна поглощены (977 ключей).
- **Ступенька в витрине уже стоит.** Ночной KPI пересчитал заказы от
  новых `orders_*`: Ozon 08-18 — 9 910 881 (старое правило), 08-19 —
  7 566 374, 08-20 — 7 786 688; `ad_share_of_orders` 5,6–6,0 % до 08-18 и
  7,7–10,1 % с 08-19. Это смена базы на подтверждённые, а не рост
  рекламы: у FBS старая колонка де-факто была «созданными», поэтому
  фраза «читатели не меняются» верна для кода и неверна для чисел —
  у читателей за последние 30 дней заказы FBS стали меньше на ~30 %.
  Ряд разорван на 08-19 до пересборки истории; каждую ночь граница
  будет сдвигаться на день вперёд вместе с окном.
- Вилка 25–35 % — попала; второй срез лога (1 021) — того же порядка,
  что первый (1 013).

### Не делал

Историю не пересобирал, устаревшие ключи не чистил, 09-14 по рекламе не
добирал, воркер не включал, ветку `recovery-resume-multiday` не мержил,
WB и органику не трогал.

---

## 2026-09-17, двадцатая задача (чат закоммичен; дыры в рекламе измерены; резюм починен на ветке, не включён)

Коммиты: чат — `531bba3` в `main`; ветка `recovery-resume-multiday` — один
коммит `e562c6d` (`git log main..recovery-resume-multiday`); этот отчёт —
коммит в `main`, только документы. К Ozon API — 0 обращений. В БД —
только SELECT. В `main` ничего не мержил, `--skip-recovery` на месте.
Тесты на ветке: 609 OK (590 + 19 новых).

### 1. Чат — сошёлся, закоммичен (`531bba3`)

Против версии в git (38 024): потеряно 0, новых 35 (id 66167 … 66258),
изменившихся 0; итого 38 059, последнее 2026-09-17T19:59:00, месяцев
без сообщений нет, формы `text` str/list/None, файл построчный
(840 262 строки), `\u`-экранирования кириллицы 0. Вложения под корнем
09-13: существуют 2 823, не найдены 0, пропущены 73;
`photo_2560@17-09-2026_19-36-41.jpg` на месте. `.bak` — прежняя база
38 024. Счётчики поправлены в `CLAUDE.md` и `README.md` (сообщения,
история слияний, 2 823 вложения, 2 532 фото).

### 2. Дыры в рекламе — измерено против второго источника, не оценено

**Как мерил.** Журнал `ozon_performance_daily_load_status` (235 строк,
target 05-05 … 09-16, 125 дат) даёт статусы, но статусы старые: бэкфиллы
(в том числе 09-06, когда одним прогоном взяли даты до 03-28) добирали
данные, не переписывая журнал — детектор `ozon_cpc_data_gap_report.py`
предупреждал об этом в шапке. Поэтому судья — независимый источник:
начисления Ozon за рекламу, типы 41 `PayPerClick` и 54 `Promotion` в
`accrual/by-day` (сырьё на диске за все даты 03-28 … 09-15), против
суммы трёх рекламных статей в `marketplace_expenses` за ту же дату. В
июле и сентябре эти два числа сходились до копейки; где расходятся —
там дыра или лишнее.

**35 дат, у которых последняя запись журнала не success или с pending —
и как они выглядят по данным:**

```
target      load        cpc_status        done/plan   stop_reason                          accrual 41+54   Performance     разница  строк
2026-05-05  2026-05-06  pending_backfill     1/169   -                                     318,036.21    318,036.19        +0.02   192
2026-05-23  2026-05-27  pending_quota      680/1323  daily_quota_exhausted                 540,470.69    540,470.70        -0.01   653
2026-05-24  2026-05-28  pending_quota       20/1323  daily_quota_exhausted                 490,534.88    490,534.85        +0.03   607
2026-05-25  2026-05-26  pending_quota     1390/1675  daily_quota_exhausted                 455,610.98    455,932.22      -321.24   588
2026-05-26  2026-05-27  pending_quota      160/1235  daily_quota_exhausted                 540,271.21    540,271.20        +0.01   734
2026-05-27  2026-06-02  pending_quota      850/1316  daily_quota_exhausted                 525,080.55    525,080.55        -0.00   742
2026-05-28  2026-05-29  pending_backfill    30/1250  exception_after_partial_progress      531,703.90    531,703.91        -0.01   778
2026-05-30  2026-06-05  pending_quota     1040/1250  daily_quota_exhausted                 575,386.82    575,386.82        -0.00   641
2026-06-01  2026-06-02  pending_backfill   270/965   exception_after_partial_progress      694,698.51    694,698.53        -0.02   588
2026-06-02  2026-06-04  pending_quota      420/960   daily_quota_exhausted                 633,160.23    633,160.23        -0.00   567
2026-06-07  2026-06-21  pending_quota     1110/1191  daily_quota_exhausted                 749,496.97    749,496.98        -0.01   624
2026-06-08  2026-06-09  pending_quota     1010/1149  daily_quota_exhausted                 717,565.17    717,565.17        -0.00   621
2026-06-09  2026-06-10  pending_quota      920/1091  daily_quota_exhausted                 758,883.73    758,883.72        +0.01   612
2026-06-10  2026-06-11  pending_quota     1010/1134  daily_quota_exhausted                 784,327.42    784,327.45        -0.03   614
2026-06-11  2026-06-12  pending_quota      990/1200  daily_quota_exhausted                 675,421.21    675,421.20        +0.01   596
2026-06-14  2026-06-15  failed               0/0     -                                     603,526.14    603,526.17        -0.03   639
2026-06-19  2026-06-20  pending_quota      960/1139  daily_quota_exhausted                 522,240.43    522,240.44        -0.01   597
2026-06-21  2026-06-22  pending_quota     1060/1183  daily_quota_exhausted                 564,891.11    564,891.10        +0.01   630
2026-06-22  2026-06-23  pending_quota      810/1180  daily_quota_exhausted                 401,035.26    401,035.25        +0.01   640
2026-06-23  2026-06-24  pending_quota     1040/1159  daily_quota_exhausted                 427,259.10    427,259.08        +0.02   608
2026-06-24  2026-06-25  pending_quota      890/1163  daily_quota_exhausted                 525,106.87    525,106.87        -0.00   616
2026-06-25  2026-06-26  pending_quota     1020/1147  daily_quota_exhausted                 476,437.44    476,437.42        +0.02   608
2026-06-26  2026-06-27  pending_quota      870/1139  daily_quota_exhausted                 467,541.36    467,541.35        +0.01   589
2026-06-27  2026-07-04  pending_quota     1050/1125  daily_quota_exhausted                 393,753.57    393,753.55        +0.02   570
2026-07-04  2026-07-05  pending_quota     1210/1246  daily_quota_exhausted                 500,866.38    500,866.37        +0.01   562
2026-07-05  2026-07-06  pending_backfill  1200/1263  -                                     529,615.61    529,615.62        -0.01   561
2026-08-12  2026-08-13  pending_quota     1210/2654  daily_quota_exhausted                 304,760.07    304,760.07        -0.00   911
2026-08-13  2026-08-14  pending_backfill   250/2646  exception_after_partial_progress      498,030.65    498,030.65        -0.00   409
2026-08-14  2026-08-15  pending_quota     1210/2308  daily_quota_exhausted                 522,819.09    522,819.09        +0.00   405
2026-08-17  2026-09-02  pending_backfill   720/1510  exception_after_partial_progress      617,253.16    617,253.16        +0.00   404
2026-08-18  2026-08-19  not_started          0/0     -                                     577,989.52    577,989.52        -0.00   404
2026-09-02  2026-09-03  pending_backfill   559/569   -                                     334,095.04    334,095.04        +0.00   397
2026-09-07  2026-09-08  pending_backfill   525/535   -                                     309,573.99    307,109.66    +2,464.33   386
2026-09-08  2026-09-09  pending_backfill   525/535   incomplete_without_reason:pending_    367,464.76    366,190.99    +1,273.77   383
2026-09-14  2026-09-15  pending_backfill   340/532   exception_after_partial_progress      486,566.66          0.00  +486,566.66     0
```

Все 35, кроме 09-14, сходятся с accrual с точностью до копеек (хвосты
09-07 +2 464,33 и 09-08 +1 273,77 — по 10 кампаний pending). То есть
дыр среди них одна.

**Итог по всем датам 03-28 … 09-15:** accrual 85 238 544,85, Performance
84 313 614,39 — **недобор 924 930,46 ₽ (1,09 %)**. Это измерение с
точностью до копеек на каждой дате, где есть оба источника (150+ дат
сходятся ровно), а не оценка. Слагаемые:

```
2026-09-14                 486 566,66   весь день: 41 = 128 948,51, 54 = 357 618,15; ночь 09-15 взяла 340 из 532,
                                        exception_after_partial_progress; в таблице 0 строк; блокер
                                        ozon_performance_partial_ads горел (сухой прогон 09-15, восьмая задача)
2026-03-28 … 03-31         505 449,64   тип 54 (CPO) не собран: 509 858,40 — наш отчёт CPO «Все» начинается 04-01
                                        (03-28: 79 976 из 115 637; 03-29 … 03-31: 0 из 206 257 / 132 581 / 135 360);
                                        клики сходятся (+4 409,76). Граница проекта, статусных строк и блокеров нет
обратный знак                −50 377,16  Performance ВЫШЕ accrual: 08-19 клики +26 828,86; 08-20/08-21 CPO +25 948,95.
                                        Не недобор; природа не установлена, версий не предлагаю
хвосты                       −16 708,68  09-07 +2 464,33, 09-08 +1 273,77, 09-15 +666,95, 05-25 −321,24 и копейки по датам
```

Оценка по соседям, как просил, для сверки метода: 09-14 по чистым дням
той же недели (09-15 — 472 080,65, 09-16 — 513 455,19) даёт 492 767,92,
на 1,3 % выше измеренного. Для марта соседей нет — там измерение
единственное. Детектор по CPC назвал ещё 07-02 (ratio 0,60) — не дыра:
accrual = Performance до копейки, это реальный спад.

**Граница добора.** «62 дня» в спеке — лимит длины периода в ОДНОЙ
выгрузке, не глубина истории. Ledger: прогон 09-06 успешно (HTTP 200)
запросил даты до 03-28 — глубина ≥ 162 дня. Навсегда не ушло ничего.
09-14 добирается штатно (532 кампании ≈ 54 выгрузки из 2 000). Март —
отчёт CPO «Все» (`all_sku_promo`), его глубину за март не проверял:
обращений сегодня нет.

### 3. Дефект резюма — починен на ветке `recovery-resume-multiday`, не включён

Корень подтверждён по коду: пять читателей прогресса требовали
`date_from == date_to == target_date` — `validate_cpc_resume_progress`,
`resolve_existing_cpc_backfill_progress`,
`resolve_daily_pending_cpc_progress_from_db`,
`read_exact_cpc_progress_from_db`, `find_cpc_progress_for_date` в
загрузчике и `resolve_worker_progress_candidate` в воркере. Прогресс
окна (cpc-backfill с `--date-from/--date-to`, например 07-13 … 08-30 у
прогонов 08-30/08-31) записывался, но для даты внутри окна был невидим.

Что сделано (`e562c6d`):

- запись прогресса подходит дате, если её окно накрывает дату
  (`cpc_progress_covers_date`); точный день предпочтительнее окна во
  всех читателях; два окна на одну дату — по-прежнему отказ, не
  угадывание;
- резюм по ключу (`--progress-key`) многодневной записи при запуске за
  один день внутри окна принимает окно записи для отчётов (ключ и батчи
  считались по нему), а пишет **только запрошенную дату**
  (`adopt_cpc_resume_window` → `--target-dates`). Писать всё окно из
  подмножества батчей нельзя: строка расхода агрегирует кампании по SKU,
  и здоровые даты окна получили бы усечённые суммы;
- batch 0 не повторяется: `next_batch_index` = первый pending, завершённые
  батчи остаются завершёнными (`seed_existing_cpc_progress_for_resume`).

Тесты `tests/test_ozon_performance_resume_multiday.py` (19): окно
накрывает дату и не накрывает соседнюю; точный день побеждает окно в
памяти, в БД и в воркере; два окна — неоднозначность; резюм по ключу с
середины окна и отказ для даты вне окна; `next_batch_index` = 5 при
завершённых 0–4; принятие окна с сохранением явных `--target-dates`;
прежние 590 тестов не тронуты.

### 4. Что нужно для включения и чем рискует

1. Первая чистая ночь на новом коде заказов (09-18) — сначала она.
2. Мерж `recovery-resume-multiday` в `main` (Render катит сам).
3. **Чистка устаревшего**: у 34 из 35 дат журнала статус `pending_*`
   при полных данных, а в `pipeline_runtime_state` лежат их записи
   `cpc_progress` с pending-батчами. Воркер, включённый как есть, пойдёт
   добирать эти «хвосты» первыми: pending у них в сумме тысячи
   кампаний (08-13 — 2 396, 08-12 — 1 444, 05-24 — 1 303, 08-14 — 1 098,
   05-26 — 1 075 …) — это сожгло бы всю квоту 2 000 на организацию за
   ночь на данных, которые уже полны, и ничего бы не добрало. До
   включения: либо пометить эти записи завершёнными по данным (скрипт с
   `--apply`, по слову владельца), либо научить воркер сверяться с
   accrual/детектором. Это отдельная задача, и без неё включать нельзя.
4. Первый запуск — вручную, `--dry-run --no-write` только для 09-14
   (`--ozon-recovery-current-day-only` не подходит: дата не вчерашняя),
   в окне после ночного прогона, не параллельно с ним (одновременных
   выгрузок с аккаунта — 1).
5. Затем снять `--skip-recovery` из команды Render — только владелец.

Риски: общая квота 2 000 на организацию с посторонним потребителем
(09-05 ушло ~1 400 чужих); антиспам 429 (пауза 60 с × 3 уже есть);
запись требует `--approve-cpc-recovery-write` — без него воркер только
планирует.

### Не сошлось — отдельными строками

- Журнал статусов против данных: 35 дат «незавершённых», реальная дыра
  одна — журнал не источник истины для добора.
- «62 дня назад» в задаче — неверная посылка: 62 — период выгрузки,
  глубина ≥ 162 дней по ledger.
- 08-19 и 08-20/21: Performance выше accrual на 50 377,16 — не
  объяснено.
- Блокеры прошлых ночей нигде не хранятся (`intraday_snapshots` — только
  заказы и выкупы), поэтому «какой блокер горел» известно лишь для 09-14.
- 09-16 accrual не снят (сырьё до 09-15), дата не сверена.
- Ночь 09-18 ещё впереди (отчёт 20:50 UTC), предсказание советника
  (fbo 1 666 / 1 917 / 0; fbs 2 590 / 3 679 / 0 за 09-03 … 09-16) не
  проверял — утром.

### Не делал

К API не обращался, в БД не писал, ничего не мержил, историю заказов,
устаревшие ключи, WB, органику, соинвест не трогал. `bank_coinvestment`
в `realization/by-day` — не потерян, записан в §9-10 CLAUDE как след.

---

## 2026-09-17, девятнадцатая задача (лимиты Performance API и товары с бонусами — разведка, только чтение)

Один коммит в `main`: обновлённая `spec/ozon-performance.json`, документы,
этот отчёт. К Ozon API — 0 обращений. В БД — только SELECT (ledger,
статусы прогонов). Документация читалась встроенным браузером: страница
`docs.ozon.ru/api/performance/`, `dev.ozon.ru/news/794`.

### 1. Спека — свежая, отличие одно

`curl` к `docs.ozon.ru` блокируется, но страница документации сама грузит
`https://docs.ozon.ru/api/performance/swagger.json` (200, 265 721 байт) —
забрал его `fetch`-ом из контекста страницы во встроенном браузере,
сравнил как JSON и положил в репозиторий в формате копии.

```
                       копия 09-13     живая 09-17
путей                        47              47
операций                     48              48     (у /campaign/{id}/products два метода: POST AddProducts, PUT UpdateProducts)
схем                         97              97
версия                      2.0             2.0
operationId и хеши описаний всех 48 операций — совпали
пометок «Не расходует лимиты»   2               2     (GET statistics/campaign/product, POST statistics/products/sku)
новых методов                                   0
отличий по JSON-путям                           1     → tags[3].description (тег Limits)
```

Изменение текста Limits: снята плашка `<aside class="warning">Введём
лимиты на работу с кампаниями 14 сентября 2026 года</aside>`, у названий
лимитов в обеих таблицах добавлена точка в конце; все числа те же.
`git diff --stat spec/ozon-performance.json`: 1 файл, +1 / −1. Ни один
метод, который мы зовём, не менялся.

**47 против 48:** путей 47, операций 48. В `docs/ozon_spec_refresh.md` и
`spec/README.md` «методов» считались пути (`len(paths)`); у одного пути
два метода. Оба числа верны в своих единицах — ориентиры переписаны как
«47 путей / 48 операций».

Новость 794 (14.09.2026): «действуют ограничения на количество запросов
к методам создания и копирования кампаний, добавления товаров, изменения
бюджетов и ставок, а также формирования отчётов … при превышении порога
методы будут возвращать ошибку до сброса счётчика». Сообщение 66094 в
чате — только ссылка, ответов и обсуждения нет.

### 2. Попадаем ли под лимиты

**Записи в кампании — 0 вызовов.** Grep всего репозитория по
`/api/client/` вне `tests/`, `venv/`, `spec/`, `knowledge/`: ни одного
вхождения `campaign/cpc/v2/product`, `activate`, `deactivate`, `PATCH
campaign`, `products` (add/update/delete), `bids/set`, `bids/delete`,
`search_promo/product/enable|disable`, `carrots`, `all_sku_promo/activate|
deactivate|set_bid`, `min/sku`, `dynamic_budget`; `requests.put/patch/
delete` не встречаются. Подозреваемые по названиям:
`scripts/ozon_search_promo_*` — отправляют только **отчёты** (`statistic/
orders|products/generate`, `all_sku_promo/…/generate`, token, список
кампаний; `ozon_search_promo_submit_probe.py` вдобавок называет три
несуществующих пути — это проба, в пайплайне её нет);
`scripts/ozon_performance_recovery_worker.py` — своих эндпоинтов нет,
зовёт функции загрузчика, то есть тот же `statistics/json`; выключен
`--skip-recovery`, при включении ограничен `--ozon-max-daily-cpc-units
1200` и порогом 1 800 у post-фазы. `scripts/ozon_cpc_data_gap_report.py`
— `GET /api/client/campaign`.

**Выгрузки статистики — за ночь** (лог Render 09-17 + ledger
`ozon_performance_statistics_json_usage` за 09-08 … 09-17):

```
эндпоинт                                          за ночь 09-17          лимит из таблицы                      запас
POST /api/client/statistics/json                  48 отправок × ≤10 =    2 000 выгрузок / 24 ч на организацию   474–535 из 2 000 = 24–27 %
                                                  474 выгрузки           ≤10 кампаний в отчёте                  10 из 10 — ровно на лимите
                                                  (09-08…09-14: 54 × 10  1 одновременная с аккаунта             1 — externallist перед каждой отправкой
                                                  = 533–535)             ≤62 дней в выгрузке                    1 день
GET  /api/client/statistics/{UUID}                231 опроса             100 000 запросов / сутки               —
GET  /api/client/statistics/report                48                     100 000                                —
GET  /api/client/statistics/externallist          48                     100 000                                — (ledger не считает)
GET  /api/client/campaign                         2                      100 000                                —
POST /api/client/token                            ~2                     100 000                                —
GET  /api/client/statistics/all_sku_promo/orders/generate   1 + опросы + 1   раздел Статистика → выгрузка         +1
POST /api/client/statistic/orders/generate        1 + опросы + 1         то же (Selected CPO, см. ниже)         +1
итого                                             ~395 запросов, 474–535 (+2) выгрузок
```

Формула «активных кампаний × 240»: 474 × 240 = 113 760, потолок всегда
табличные 2 000. Ночи 09-15 … 09-17: 350 / 440 / 474 выгрузок (кампаний
столько же), 429 — 0.

**Вывод одной строкой:** под лимиты на работу с кампаниями не попадаем
вовсе (записей нет), под выгрузки — 24–27 % от 2 000 в ночь, тонко ровно
там, где и было: общий на организацию потолок с посторонним потребителем
(09-05 забрал ~1 400) и «10 кампаний в отчёте» — мы ровно на лимите,
`DEFAULT_CAMPAIGN_BATCH_SIZE = 10` поднимать нельзя.

### 3. `products_with_bonuses` — бонусы на клики, не соинвест

Живая спека = копия (хеш описания совпал): `GET /api/client/products_with_bonuses`,
тег Campaign, параметров нет, ответ
`{"skus": ["<uint64 строкой>", …]}` — **только список SKU**, без сумм,
без единиц, без привязки к кампании и к дате. Описание: «список SKU
товаров, на которые начислены бонусы; за клик на товар в первую очередь
тратятся начисленные на него бонусы, если бонусы кончились — общий
бюджет кампании». Это **источник оплаты рекламы** (бонусы за клики), а
не соинвест в цене товара; соинвест живёт в `realization/by-day`
(`bank_coinvestment`) — отдельная задача этим не меняется.

- **Лимиты:** пометки «не расходует» нет; но метод не операция с
  кампанией и не отчёт раздела Статистика, поэтому по тексту Limits он
  под выгрузки не попадает — только общий 100 000. Живой вызов не делал
  (запрет задачи).
- **Стыковка:** `statistics/expense` даёт по кампании и дню три числа —
  расход, с абонентского счёта, бонусов; `statistics/products/sku` («не
  расходует лимиты») даёт по SKU и дню расход, клики, показы, заказы —
  без разреза по источнику; `products_with_bonuses` — только список.
  **Разложить рекламный расход по товарам с учётом источника оплаты
  нельзя:** ни один метод не отдаёт сумму бонусов по SKU. Доступно: по
  кампании — доля бонусов; по SKU — расход целиком; плюс флаг «у SKU
  есть бонусы».

### Что не сошлось — отдельными строками

- **CLAUDE §6 был устаревшим:** `APPROVE_OZON_SELECTED_CPO_DAILY_WRITE`
  в Render = `true` (прочитано `GET /v1/services/…/env-vars` 09-17),
  лог ночи 09-17: `write_approved: true, source_rows 21, db_writes 45,
  totals_match true`, отправка `statistic/orders/generate` в 02:54:32.
  Selected CPO собирается и пишется каждую ночь; с какого дня —
  не установлено. Помечено опровергнутым.
- **«CPO Все и Selected CPO — НЕ в лимите» (CLAUDE §4)** спекой не
  подтверждается: пометка «не расходует» стоит только у двух других
  методов, оба CPO-отчёта — в разделе Статистика. Практически +2
  выгрузки в ночь. Помечено как вывод из практики.
- **Ledger занижает счёт запросов:** `externallist` (48 за ночь) в
  `request_kind` не входит; против 100 000 это ничего не меняет.
- Живой вызов `products_with_bonuses` не делался — единицы ответа
  известны только по схеме.

### Дополнение: чат за 09-17 (файл дописан советником в 22:25, в git не положен — 35 сообщений, id 66167 … 66258, не проверял и не коммитил, это не в задаче)

- **66237, 17:52, Сергей Платонов** — первая живая жалоба на новые
  лимиты: сервис с продавцами по >2 000 кампаний упёрся в «1 200
  добавлений товаров в день» (Ozon сам учит «1 товар = 1 кампания»);
  просит массовые методы и расширение лимитов. Подтверждает, что лимиты
  на записи действуют; нас не касается — записей у нас нет.
- **66167 → 66189 → 66223: «рекламное API со вчера расход 0 отдаёт»**
  (10:04 и 12:52 МСК 09-17), «заработало» в 15:12. Наш прогон 09-17
  (03:15 МСК) попадал в это окно — проверил по `marketplace_expenses` за
  09-16: `advertising_clicks` 326 строк / 116 466,34, `advertising_order_5`
  171 / 211 240,05, `advertising_order_selected_cpo` 12 / 185 748,80 —
  порядок как в соседние дни, нулей нет. Сбой нас не задел.
- Попутно, не по задаче: **за 09-14 рекламных строк в таблице нет
  совсем** (0 строк всех трёх типов; 09-13 и 09-15 — по ~300). Это ночь
  09-15: `cpc_status pending_backfill`, 340 из 532 кампаний,
  `exception_after_partial_progress`; recovery-воркер выключен, дыру
  никто не добирает. Блокер `ozon_performance_partial_ads` за ту ночь
  горел. Отдельная задача.

### Не делал

К API не обращался, в БД не писал, ничего не внедрял. Историю,
устаревшие ключи, WB не трогал. Свежий инкремент чата не коммитил.

---

## 2026-09-17, восемнадцатая задача (чат проверен и закоммичен; ветка слита; ночь 09-17 — чисто)

Три коммита в `main`: чат `606a8d5`, текст задачи `ebad9dc`, мерж
`1ca569d` (`--no-ff`, ветка `orders-cancelled-columns`, коммиты `e173109`
и `3924a8b`). В БД не писал (только SELECT). Тесты на слитом дереве:
590 OK. К Ozon API не обращался.

### 1. Чат разработчиков — сходится, закоммичен

Проверено скриптом по файлу и по `.bak`, не на слово:

```
сообщений                     38 024, уникальных id 38 024
период                        2022-05-13 … 2026-09-17, месяцев без сообщений нет
формы text                    str 32 179, list 5 843, None 2 — читается тем же кодом
против .bak (37 986)          потеряно 0; новых 38 (id 66093 … 66166: 09-15 — 1, 09-16 — 34, 09-17 — 3);
                              изменилось 3 (65893, 66050, 66073) — только поле reactions
формат                        839 440 строк, начало `{\n "name"`, \u-экранирования кириллицы 0 — indent=1, ensure_ascii=False
```

Вложения под `~/Downloads/Telegram Desktop/ChatExport_2026-09-13/`: пройдены
все `photo` и `file`, пропущены начинающиеся со скобки — **существуют
2 822, не найдены 0, пропущены 73**. Ровно ожидаемые числа. Рядом лежат
корни 09-06, 09-13, 09-15, 09-17 — README теперь называет единственным
09-13, и пути под него разрешаются все.

Закоммичено отдельным коммитом `606a8d5` до мержа: `CLAUDE.md` (строка
про объём чата), `knowledge/telegram/README.md`, `ozon-dev-chat.json`
(+942 / −9 строк diff — файл остался построчным).

### 2. Мерж

До мержа `git log main..orders-cancelled-columns` — два коммита,
`3924a8b` и `e173109`; пересечений изменённых файлов с чужими правками
нет. Миграция подтверждена чтением: новые колонки читаются
(`cancelled_orders_qty = 0`, `observed_at = null` у выборочной строки),
строк с `cancelled_orders_qty > 0` и с `observed_at` — 0 у обеих схем,
то есть новый код до мержа в прод не уезжал. Первый мерж отбился о
незакоммиченный `docs/inbox.md` (тот же случай, что вчера с checkout) —
закоммитил текст задачи `ebad9dc` и повторил. После push:

```
git log --oneline -1 origin/main → 1ca569d Merge branch 'orders-cancelled-columns': cancelled orders next to confirmed, one rule for FBO and FBS, FBS on /v4
git log main..orders-cancelled-columns → пусто
```

Push ушёл ~18:50 UTC, Render катит сам; ночь 00:15 UTC 09-18 — первая с
колонками отмен и с FBS на `/v4`.

### 3. Ночь 09-17 — первая с логом статусов, прошла чисто

По логу Render (API) и БД, 18:45 UTC:

```
прогон                       00:15:52 → 03:08:18 UTC, «✅ Весь пайплайн успешно завершен»
тревоги «Шаг не выполнен»    0
сырьё                        FBS 3 439 отправлений (старый код, окно 14 дней), FBO 5 896 за 59 страниц
лог статусов                 «✅ Готово», итого строк записано: 1013
  в таблице                  nightly, observed_at 09-17 00:18–00:20: 1 013 = FBO 492 (211 новых + 281 переход)
                                                                            + FBS 521 (244 новых + 277 переходов)
  переходы                   delivering→delivered 222, awaiting_packaging→delivering 114,
                             delivering→cancelled 105, awaiting_deliver→delivering 81, awaiting_packaging→awaiting_deliver 14
заказы за 09-16              FBO 137 строк, FBS 179 строк
блокеры (сухой прогон 09-15) только ozon_daily_sku_organic_missing
```

**Что не сошлось — отдельной строкой:**

- записано 1 013 строк против прикидки ~1 500–1 700 из тринадцатого
  отчёта (FBO ~900, FBS ~700). Прикидка была грубой (переходов за 53 ч
  от посева ожидалось ~500 у FBO — вышло 281; FBS ожидалось ~700 —
  вышло 521). Поломки нет: обе схемы записали и новые, и переходы,
  переходы те, что ожидались;
- посев 12 700 строк лежит с `source='nightly'` и observed_at 09-14
  (6 321) / 09-15 (6 379), а не `source='seed'`; ночь от посева
  отличается только датой наблюдения. В чек-лист внесено, менять не
  стал — это метка, не данные.

### 4. Утром 09-18

Чек-лист в `CLAUDE.md` §9-4б переписан на 09-18: `Получено FBS
postings: … за … страниц` по `/v4` (ожидаю ~5 900 / ~59 при окне 30
дней), строки `Ozon FBO/FBS: отправлений подтверждённых …, отменённых
…`, в таблице за `order_date = 2026-09-17` `cancelled_orders_qty > 0` и
`observed_at` заполнен у обеих схем, у WB — null; лог статусов — второй
ночной срез. Признаки поломок — по трём вариантам из задачи.

### Не делал

Историю не пересобирал, устаревшие ключи не чистил, WB не трогал.
Витрины, флаги, Render, органику не трогал.

### Хвосты

- **Измеритель истории выкупов ждёт запуска владельцем**, обращений 0.
- Снимок 1С запрошен, «Аналитическая группа» — с ним.

---

## 2026-09-16, семнадцатая задача (шаг FBS нефатальный — сделано; миграция и мерж — ЗАБЛОКИРОВАНЫ, за владельцем)

Коммит `3924a8b` в ветке `orders-cancelled-columns` (запушена, `git log
main..orders-cancelled-columns` — два коммита); этот отчёт и чек-лист — в
`main`. В БД не писал. Тесты: 590 OK (585 + 5). Отчёт в 20:40 UTC, ночной
прогон в 00:15 UTC.

### 1. Шаг FBS нефатальный — сделано

`run_daily_pipeline.py`: «Ozon: загрузка FBS заказов» внесён в
`NON_FATAL_STEPS` с обоснованием рядом (цена отказа — одна ночь, окно 30
дней доберёт; цена фатального шага — ничего после него; молчание закрыто
тревогой и блокером). Порядок шагов не менялся. Охранный тест на узость
списка расширен (`tests/test_non_fatal_total_orders.py`), добавлен
`tests/test_fbs_step_non_fatal.py` по образцу FBO: нефатальность, порядок
перед «расходы и комиссии» и «реклама Performance API», текст тревоги,
`--skip-telegram` тревогу не глушит.

Числами:

- **Telegram при нефатальном отказе** — `send_failure_alert(…, fatal=False)`
  шлёт: `⚠️ Шаг не выполнен, прогон продолжен / Шаг: Ozon: загрузка FBS
  заказов / Код ошибки: 1` и хвост лога в `<pre>` (до 3 500 символов, в
  нём и «429 не прошёл за 3 попыток», и «HTTP 500»); слова «упал» нет.
  Проверено тестом с подменённым `requests.post`.
- **Блокер `ozon_fbs_orders_missing`** есть в `alerts_telegram.py`
  (строка 231, `table_has_rows(order_schema='fbs', order_date=вчера)`).
  Сухой прогон на живой БД: `--target-date 2026-09-15` — причины только
  `ozon_daily_sku_organic_missing`, блокер FBS не горит (строки есть);
  `--target-date 2026-04-01` (FBS в таблице с 04-12) — горят
  `ozon_daily_sku_organic_missing, ozon_fbs_orders_missing`. Работает.

### 2. Миграция и мерж — НЕ выполнены, причина не в задаче

Применить `sql/20260916_marketplace_orders_cancelled_columns.sql` через MCP
`apply_migration` не удалось: классификатор auto-режима Claude Code
запретил действие как «изменение общего ресурса» (Modify Shared
Resources). Обходить запрет тем же DDL через `execute_sql` я не стал — это
то же действие под другим именем; `.env` даёт только PostgREST-ключ, DDL
через него невозможен. Раз миграции нет, **мерж тоже не делал**:
загрузчики на ветке пишут `cancelled_orders_*` и `observed_at` и упали бы
на первом upsert — шаги нефатальные, но заказов за ночь не было бы.

**Что нужно от владельца, в этом порядке** (10 минут):

1. Выполнить файл `sql/20260916_marketplace_orders_cancelled_columns.sql`
   в SQL-редакторе Supabase (проект `pkrsrwjrlurlfpdyixei`) или через MCP
   из интерактивной сессии. Он добавляет 4 колонки (`default 0`, у
   `observed_at` — null), один check и комментарии; ничего не удаляет и
   не переименовывает.
2. Проверить, что старые колонки на месте и значения не тронуты —
   снимок ДО миграции (20:25 UTC 09-16):

   ```
   колонок 11: id, order_date, marketplace_code, marketplace_sku, article, product_name,
               orders_qty, orders_amount_buyer, orders_amount_seller, created_at, order_schema
   ozon/fbo        35 779 строк   qty 42 709   buyer 842 272 427,00   seller 842 272 427,00
   ozon/fbs        20 469 строк   qty 25 871   buyer 631 639 758,00   seller 631 639 758,00
   wb/marketplace  23 742 строки  qty 27 781   buyer 498 236 305,62   seller 711 337 760,07
   ```

   После: те же 11 + `cancelled_orders_qty`, `cancelled_orders_amount_buyer`,
   `cancelled_orders_amount_seller`, `observed_at`; суммы те же (ночной
   прогон меняет их — сверять до 00:15 UTC или с поправкой на вчера);
   `sum(cancelled_orders_qty) = 0` до первой ночи. Запрос — тот же, что в
   `docs/ozon_orders_one_rule.md` §7 / CLAUDE §9-4б.
3. `git merge --no-ff orders-cancelled-columns && git push origin main` —
   Render катит сам. Успеть до 00:15 UTC — первая ночь сегодня; не
   успеть — ничего не ломается, ночь пройдёт на старом коде.

### 3. Утром 09-17 — что смотреть

Перенесено в `CLAUDE.md` §9-4б, одной связкой с чек-листом лога
статусов. Коротко: если мерж успел — в логе Render `Получено FBS
postings: ~5 900 за ~59 страниц` (`/v4`), `Ozon FBS: отправлений
подтверждённых ~4 100, отменённых ~1 800`, `Ozon FBO: … ~3 800 / ~2 100`;
в таблице за `order_date = 2026-09-16` `cancelled_orders_qty > 0` и
`observed_at` заполнен у обеих схем; блокеры `ozon_fbo_orders_missing` и
`ozon_fbs_orders_missing` не горят, `ozon_daily_sku_organic_missing` горит
как всегда. Лог статусов — по прежнему чек-листу: блок «Ozon: лог
статусов отправлений», `итого строк записано: ~1 500–1 700`.

**Как различить два новшества.** Лог статусов читает сырьё, которое
кладут шаги заказов, поэтому порядок диагностики — сначала шаги заказов.
Тревога «Шаг не выполнен» с именем шага заказов и текстом про
`cancelled_orders_qty` — миграция не применена, шаг упал до сырья, «нет
сырья» в блоке лога — следствие. Шаги заказов прошли («Сырой ответ …
сохранён»), а блок лога пуст или код 1 — поломка лога. Шаги прошли, лог
записал, `cancelled_orders_qty` везде 0 — в прод ушёл старый код, мерж не
успел; не ошибка.

### 4. Не делал

Устаревшие ключи не удалял, историю не пересобирал (оба — после первой
чистой ночи, отдельными задачами). Витрины, флаги, Render, органику не
трогал. Полную модель не строил.

### Хвосты

- **Измеритель истории выкупов ждёт запуска владельцем**, обращений 0.
- Снимок 1С запрошен, «Аналитическая группа» — с ним.
- Соинвест и коэффициенты — в очереди.

---

## 2026-09-16, шестнадцатая задача (заказы в одну сторону — ветка `orders-cancelled-columns`, не слита)

Один коммит в ветке `orders-cancelled-columns` (`e173109`, `git log main..orders-cancelled-columns`),
запушена; этот отчёт — коммит в `main`. Миграция не применена, в БД не
писал (только SELECT), скрипты с записью не запускались, ветка не слита.
К Ozon API — 125 обращений, все чтение, 429 — 0. Тесты: 585 OK (557 + 28
новых). Разбор целиком — `docs/ozon_orders_one_rule.md` (в ветке).

### 1. Схема — `sql/20260916_marketplace_orders_cancelled_columns.sql`

Три колонки рядом со старыми на том же ключе `(order_date, sku, schema)`
(уникальный индекс проверен по `pg_indexes`): `cancelled_orders_qty`,
`cancelled_orders_amount_buyer`, `cancelled_orders_amount_seller`, все
`not null default 0` с check `≥ 0`, плюс `observed_at timestamptz` —
момент сбора, подтвердившего строку. Старые `orders_*` — «подтверждённые»,
читатели (девять модулей, берут `orders_qty` и `orders_amount_seller`) не
меняются; созданные = сумма пар.

- **Дозревание:** ночной сбор строит окно 30 дней заново, upsert
  переписывает обе пары у каждого ключа окна — отмена через две недели
  перекладывает суммы из `orders_*` в `cancelled_*` той же строкой. Upsert
  выдерживает, потому что обе пары пишутся вместе из одного полного сбора;
  ключ не исчезает — отменённое отправление остаётся в выборке.
- **Дата — когорта:** `created_at` у FBO, `in_process_at` у FBS (заполнен
  у всех 5 891 в `/v4`); дата отмены — в логе статусов.
- **Одно правило на обе схемы** — `loaders/ozon_orders_rows.py`:
  `cancelled` → `cancelled_*`; все прочие статусы спеки → `orders_*`;
  `cancelled_from_split_pending` (родитель разделённого) — пропуск и
  счётчик; незнакомый статус → `orders_*` и называется вслух. Цена —
  строка или объект `{amount, currency}`, чужая валюта и объект без
  `amount` — исключение, не ноль. Decimal, копейки.

Почему пары, а не строка со статусом: строка меняет ключ и заставляет
каждого читателя фильтровать; пары не трогают ни ключ, ни читателей.

### 2. Загрузчики

- **FBO** (`/v3`, без изменений в сборе): `build_order_rows` больше не
  выбрасывает `cancelled`, отменённые идут в `cancelled_*`.
- **FBS переведён на `/v4/posting/fbs/list`** по механике FBO: cursor,
  страница 100, пауза 1,5 с, на 429 — 60 с × 3, предел 400 страниц,
  любой не-200 — исключение (старый молча возвращал накопленное); статус
  читается, `quantity or 1` заменён явным правилом; окно 14 → 30 дней, как
  у FBO, иначе отмены второй недели не дозревают (решение дешёвое, можно
  вернуть).
- **Паритет `/v3` против `/v4` на одном окне 30 дней** (08-17T20:09Z …
  09-16T20:09Z): 5 891 = 5 891 отправление, `/v4` за 59 обращений
  (104 с), `/v3` за 6 (14 с); множества `posting_number` равны в обе
  стороны (0/0); значения `status`, `in_process_at`, `shipment_date`,
  `products.{sku, offer_id, quantity, price}` — расхождений 0, цена лишь
  сменила форму (6 087 товаров, строка → объект); `created_at` в `/v4`
  нет у всех; строки по новому правилу 4 588 = 4 588, итоги до копейки
  одинаковы; 31 дата из 31 равны. Июньские жалобы чата на неполный
  `/v4` на нашем окне не воспроизвелись.
- **Устаревшие ключи FBO** (1 108 на 15 778 719,00): с новым правилом
  новых не возникает. Со старыми — два варианта, предлагаю второй, не
  делал: (а) пометить — уже есть бесплатно, `observed_at is null` внутри
  окна после первой ночи; (б) удалять в окне сбора ключи схемы за полные
  локальные дни окна, которых нет среди построенных строк — безопасно,
  потому что оба загрузчика падают вместо частичного результата. Включать
  после первой чистой ночи и после пересборки истории (она сама снесёт
  все устаревшие ключи с 03-28), отдельной правкой с тестом.

### 3. Проверка до записи (свежий сбор 30 дней, `scripts/ozon_orders_precheck.py`)

```
                         FBO (/v3, 60 обращений, 105 с)     FBS (/v4, сырьё паритета)
отправлений                      5 914                              5 891
подтверждённых + отменённых      3 791 + 2 123 = 5 914 ✓            4 108 + 1 783 = 5 891 ✓
статусы                          delivered 2 961, cancelled 2 123,  delivered 2 615, cancelled 1 783,
                                 delivering 650, awaiting_* 180     delivering 1 408, awaiting_packaging 85
полные дни 08-18…09-16:
  подтверждённые                 3 959 шт   66 888 444,00          4 374 шт   92 870 441,00
  отменённые                     2 243 шт   36 835 201,00          1 857 шт   40 621 851,00
  созданные                                103 723 645,00                    133 492 292,00
  доля отменённых                35,5 %                             30,4 %
```

**FBO против таблицы (4 174 ключа), подтверждённые:** совпало 3 093;
1 076 ключей в таблице больше на 15 334 687,00 — заказы, отменённые
после последней ночной записи (тот самый остаток, 09-15 было 1 108 на
15 778 719,00); 5 меньше на 7 819,00 — добавились после ночи; только в
таблице 0; только в сборе 505 = 378 полностью отменённых ключей, которых
старый код не писал, + 127 с подтверждёнными (131 за 09-16 —
сегодняшние). Против созданных: 514 ключей в таблице меньше на
10 879 917,00 — отправления, отменённые ещё до первой записи.

**FBS против таблицы (4 425 ключей):** совпадать должны созданные, и они
совпали — 4 386 ключей, в таблице больше 0, меньше у 39 на 473 675,00
(в сборе на одно отправление больше по ключу, даты 08-18 … 09-02 и
09-16: появились после выхода даты из 14-дневного окна — 30 дней
закрывает), только в сборе 162 — все 09-16. Против подтверждённых:
1 540 ключей больше на 40 080 935,00 — отменённые внутри строк, ожидаемо
(09-15: 40 498 514 за 08-17 … 09-14).

### 4. История — только план (`scripts/rebuild_ozon_orders_history.py`)

`--estimate` без API: в таблице 56 248 строк ozon с 03-28 (FBO 35 779 на
842 272 427,00; FBS 20 469 на 631 639 758,00, FBS в таблице с 04-12).
Сбор 03-28 … 09-16, 173 дня окнами по 30, страница 100: FBO ~206
отправлений/день → ~35 600, ~362 обращения; FBS ~195/день → ~33 700,
~343; **итого ~705 обращений ≈ 24 мин** при 2,05 с на обращение, каждый
429 +60 с. Переписываются все 56 248 строк ozon обеих схем: снимок
(~15 МБ, `data/snapshots/orders_rewrite_<UTC>.json`) → delete по схеме
за окно → контроль «осталось 0» → запись (~50 000 строк) → контроль «в
таблице = записано». Витрины не трогает: ночной KPI строит из всех
заказов заново, ключи витрин без заказов удалить отдельно. Порядок:
`--fetch` (сырьё в файлы, перезапуск бесплатен) → `--plan` (помесячно
было/станет) → `--apply` после вашего слова, после первой чистой ночи на
новом коде, вне 00:15 … 03:15 UTC.

### 5. Порядок включения

Миграция (владелец) → мерж ветки → первая ночь → утром в логе Render
строки `Ozon FBO/FBS: отправлений подтверждённых …, отменённых …`, в
таблице `sum(cancelled_orders_qty) > 0` за вчера по обеим схемам →
пересборка истории → удаление устаревших ключей отдельной правкой.
Откат — откат ветки: миграция ничего не удаляет и не переименовывает.

**Дешёвый путь работает:** ключ выдержал, читатели не тронуты, стоп-сигнала
нет.

### Хвосты

- **Измеритель истории выкупов ждёт вашего запуска**, обращений 0.
- **Ночь 09-17** ещё не наступила (отчёт в 20:30 UTC 09-16); чек-лист в
  `CLAUDE.md` §9-4б.
- Снимок 1С запрошен, «Аналитическая группа» — с ним.
- В рабочем дереве появился не мой файл `data/ozon_orders_20260901_20260915.csv`
  (09-16) — не трогал, в git не клал.

### Чего не делал

Полную модель не строил. Витрины не трогал. Миграцию не применял, записи
не делал, ветку не мержил. Флаги, Render, органику не трогал.

---

## 2026-09-16, пятнадцатая задача (штуки — доказаны; сентябрь — сверен построчно; июльская логистика — объяснена)

Один коммит в `main`, номер в `git log`. В БД не писал (только SELECT
через клиент и MCP). К Ozon API — 616 (08-31 … 09-15: 16 дат by-day и 18 вызовов `accrual/postings`, 50 обращений, 110 с; 03-28 … 08-30: 144 даты by-day с пагинацией и 205 вызовов по 200 номеров, 566 обращений, 1 228 с) обращений, все чтение, 429 — 0.
Загрузчики, витрины, флаги, Render, органику не трогал. Лист «сверка» не
открывал. Тесты: 557 OK.

### 1. `quantity` в `accrual/postings` — это штуки, и они равны штукам Ozon

Продажа в `accrual/postings` — строка типа 69 `SaleCommission`: в ней
`seller_price` (цена за единицу), `quantity` и `accrued` (комиссия).
Доказательство построчное и со знаком: для каждой товарной строки by-day
(`sale_amount`, `sale_commission`) ищется строка 69 того же отправления,
SKU и даты с `seller_price × quantity = sale_amount` и `accrued =
sale_commission`. У возвратов `seller_price` отрицательный, тождество
держится. Мой первый прогон дал 114 «несошедшихся» — это была моя
ошибка (сравнивал по модулю и склеивал пары «продажа + возврат в один
день»); построчно со знаком промахов нет.

```
окно             строк by-day  сошлось  не сошлось  позиций нетто  штук нетто  штук − поз.  строк q≥2
1–9 июля               2 526    2 526          0          2 334       2 353      +19 (+0,81 %)     20
08-31 … 09-15          3 549    3 549          0          3 363       3 440      +77 (+2,29 %)     58
```

Третий источник — `ozon_realization_by_day`, 14 дат 08-31 … 09-13 (все,
где есть и реализация, и сырьё): **штуки нетто (продажи − возвраты)
совпали по каждому дню и по каждому из 2 418 SKU-дней, расхождений 0.**
За 08-31: реализация 217 продаж − 7 возвратов = 210, наши штуки 210,
позиций 201. Прежнее «+8 %» сравнивало 217 (только продажи) с 201
(нетто); нетто к нетто — +4,5 %.

Попутно: старый загрузчик считал записи `items[]`, и это были штуки —
за 09-07 «234 против 227 позиций» из комментария в коде равны 242 − 8
= 234 по реализации. Поэтому история выкупов до 08-17 в таблице лежит
в штуках (июль 1–9: таблица 2 357, штуки 2 353, позиции 2 334), а
позиции — только с перехода на accrual.

**За весь период 03-28 … 09-13 — штуки сняты целиком.** Сырьё by-day за
все даты (144 сняты сейчас, 12 уже были) и `accrual/postings` по 44 253
отправлениям. Построчная сверка за 172 даты 03-28 … 09-15: **46 510
строк by-day, сошлись 46 510, промахов 0.** В окне 170 дат позиций нетто
43 315, штук 43 814 (+499, +1,15 %), строк с `quantity ≥ 2` — 436.

Себестоимость (снимок 1С 05-20, sku → article из заказов, чтение с
`ORDER BY`), три базы рядом — `buyouts_qty` таблицы (по нему считалось
действующее 330 036 031,96), позиции by-day и штуки:

```
месяц     дней  qty табл.  позиций    штук  штук−поз   СС таблица        СС позиции        СС штуки          штуки − табл.
2026-03      4       261      261      261        0     2 396 929,10      2 396 929,10      2 396 929,10            0,00
2026-04     30     4 956    4 897    4 944      +47    46 564 945,38     46 365 199,44     46 526 233,61      −38 711,77
2026-05     31     6 808    6 706    6 784      +78    56 544 985,14     55 952 842,33     56 444 336,23     −100 648,91
2026-06     30    10 003    9 852    9 956     +104    80 038 320,54     79 293 528,28     79 732 009,38     −306 311,16
2026-07     31     9 806    9 680    9 778      +98    69 139 153,99     68 582 393,87     68 999 725,66     −139 428,33
2026-08     31     9 311    9 244    9 361     +117    61 797 092,82     61 506 911,52     61 723 324,58      −73 768,24
2026-09*    13     2 675    2 675    2 730      +55    13 554 604,99     13 554 604,99     13 603 919,35      +49 314,36
итого      170    43 820   43 315   43 814     +499   330 036 031,96    327 652 409,53    329 426 477,91     −609 554,05
```

\* по 09-13. Без цены — 36 позиций (те же 36 штук), как и прежде.

Слагаемые: **СС по штукам 329 426 477,91** = СС по позициям by-day
327 652 409,53 + **1 774 068,38** (499 добавочных штук в 436 строках,
+0,54 %). Против действующего 330 036 031,96 — −609 554,05 (−0,18 %),
и это не штуки: до 08-17 таблица заполнена старым API, который сам
считал штуки (43 820 против наших 43 814), но с другой раскладкой по
датам и SKU; эту разницу «старый API против accrual» меряет измеритель.
Прибыль с СС по штукам на той же базе выручки, что и действующие
числа: 455 275 671,42 − 329 426 477,91 = **125 849 193,51** (было
125 239 639,46, +609 554,05). Коридор ±3 % «позиции против штук» снят:
штуки точные до единицы. Остаются два допущения: один снимок цен
(05-20) и история старого API до 08-17.

Сырьё лежит в `data/accrual_history/<день>.json` (формат измерителя, все
даты 03-28 … 09-15) и `data/accrual_postings/<from>_<to>.json` (три
файла), оба в `.gitignore`. Скрипты: `scripts/fetch_accrual_postings_raw.py`
(сбор; есть файл — в API не идёт), `scripts/check_accrual_postings_quantity.py`
(сверка со знаком и с реализацией), `scripts/cogs_by_units_from_accrual.py`
(себестоимость по штукам помесячно). Все только читают.
**Измеритель истории выкупов теперь стоит 0 обращений** — сырьё by-day за
его окно уже на диске, он читает файлы; запуск по-прежнему ваш.

### 2. Сентябрь 1–15 против листа «Ozon - сентябрь»

Их лист снят из `data/manual_report_september.xlsx` в JSON (значения
ячеек как есть), сверка — `scripts/reconcile_manual_report_ozon.py`,
полный вывод по дням в отчёте прогона. 09-15 моложе двух суток; на
09-11, 09-12 и 09-15 сырьё by-day, снятое сегодня днём, отличается от
`marketplace_expenses` после ночной загрузки на 8,65 / 466,97 / 349,86
(логистика + прочее) — начисления доехали за день; ниже там, где важно,
взято сырьё.

```
строка                 у них (15 дн.)     у нас             разница        чем объясняется
Оборот — выкупы        60 522 754,00     60 522 754,00            0,00   все 15 дней до копейки
Комиссия с НДС         26 961 310,71     26 961 310,71            0,00   все 15 дней; у них 44,55 % от оборота, в июле 42,98 % — коэффициент дрейфует, оба числа из данных
Выручка                27 509 379,74     27 509 379,74            0,00   (оборот − комиссия) / 1,22
Себестоимость          18 785 449,09     16 106 886,04    −2 678 563,05   у нас позиции × снимок 1С 05-20; по штукам 16 158 082,60 (−2 627 366,49). У них 31,04 % оборота, у нас 26,70 % — их цены выше нашего снимка на 16 %; в июле было наоборот (их 32,73 % против наших 33,64 %), то есть их база цен сменилась. Снять можно только новым снимком 1С
Маржа                   8 723 930,65     11 402 493,70    +2 678 563,05   ровно разница себестоимости
Логистика                 587 944,34        709 585,02      +121 640,68   у них без НДС: × 1,22 = 717 292,10, тогда −7 707,08 (−1,1 %); остаток — граница с «Прочим», см. ниже
Реклама                 5 273 558,87      6 359 432,13    +1 085 873,26   accrual 41 + 54; **их число × 1,22 = типы 41 + 54 + 51 + 96 ровно, все 15 дней, остаток 0,00** — их «Реклама» = клики + продвижение + подписка Premium + ускоренный сбор отзывов, без НДС. Performance API у нас 5 870 173,41 за 14 дней (за 09-14 рекламы в таблице нет, известный блокер partial_ads), accrual за те же 14 дней 5 872 865,47
Эквайринг                 462 505,22        462 505,22            0,00   тип 1 / 1,22, все 15 дней до копейки — держится
Прочее                    580 768,24        630 997,63       +50 229,39   у нас («прочее» − эквайринг) / 1,22; из разницы 46 250,00 — тип 96, который у них в рекламе; остаток — граница с «Логистикой»
Фин. рез.               1 819 153,98      3 729 232,42    +1 910 078,44   их формула на наших числах; за 14 дней без 09-14: +1 331 224,97 = СС 2 491 690,75 − реклама 998 189,67 − логистика 111 519,00 − прочее 50 757,11, сходится в ноль
```

**Логистика и прочее — две строки одной группы.** Их «Логистика» +
«Прочее», умноженные на 1,22, равны нашим «логистика (16 типов) +
прочее − эквайринг − тип 96» **до копейки на 13 днях из 15**; 09-14
отличается ровно на тип 25 `ItemCompensation` 3 932,70 (у нас вне
расходов, у них внутри прочего), 09-01 — на 255,00 (не разложил).
Граница между двумя строками у них проведена иначе, чем наша
классификация по `type_id`: совместный подбор (каждый тип → логистика /
прочее / вне, покоординатный спуск с парными перестановками, 24 дня
июля и сентября) точного разбиения не даёт — лучший остаток 11 677,38
за 24 дня при неправдоподобном наборе (63 и 92 в логистике). Значит их
граница — не по типу начисления; какая — их источник.

### 3. Июльская логистика: не открытый вопрос, а та же граница

То же тождество на 1–9 июля: их (Логистика + Прочее) × 1,22 =
1 007 733,71, наша сумма 1 007 733,73, каждый из 9 дней ±0,01. Их
Реклама × 1,22 = 5 935 227,87 = типы 41 + 54 + 51 + 96 (5 935 227,88),
все 9 дней. Эквайринг — как было, до копейки.

Разрыв 232 132,30 по строке «Логистика» раскладывается без гипотез:
946 576,29 / 1,22 = 775 882,20 (НДС), и 775 882,20 − 714 443,99 =
61 438,21 — часть нашей логистики у них стоит в «Прочем» (в основном
07-02: тип 78 `TemporaryPlacement` 56 028,00 в тот день — у них
«другие услуги», как и в их ноябрьском своде). Это не версия, а
проверенное на 24 днях тождество; открытым остаётся только правило
границы между их двумя строками.

Что из прежних отчётов этим опровергнуто (помечено в документах):
«подписки нет в их отчёте» — она внутри их «Рекламы» (тип 51);
«реклама: ни один набор типов не даёт их число» — даёт 41 + 54 + 51 +
96 без НДС; «логистика — расхождение, закрыть может только их
источник» — сумма двух строк закрыта нами, источник нужен для границы.

### 4. Хвосты

- **Измеритель истории выкупов ждёт вашего запуска**
  (`scripts/measure_buyouts_history_vs_accrual.py`, вне 00:15 … 03:15
  UTC); сырьё для него уже на диске, обращений будет 0.
- **Первая ночь с логом статусов — 09-17, 00:15 UTC** — ещё не
  наступила (задача шла днём 09-16); чек-лист в `CLAUDE.md` §9-4б.
- Миграция «Аналитической группы» — со следующим снимком 1С; сентябрьский
  лист показывает, что их цены ушли от нашего снимка на +16 %, второй
  снимок нужен и для этого.
- Полная модель — не начинал.

### Чего не делал

В БД не писал, загрузчики и витрины не трогал (`buyouts_qty` по-прежнему
позиции). Флаги, Render, органику не трогал. Лист «сверка» не открывал.

---

## 2026-09-16, четырнадцатая задача (лог включён — документы; логистика — гипотеза проверена)

Один коммит в `main`, номер в `git log`. В БД не писал. К Ozon API — 13
обращений `accrual/postings` (п. 2), только чтение. Render — только чтение.

### 1. Документы

`STATUS.md` (таблица «Работает», хроника), `CLAUDE.md` (§0 и §9-4б — с
утренним чек-листом на 09-17, перенесённым из тринадцатого отчёта),
`reports_model.md` §5: лог включён 2026-09-16, посев 12 700 строк, мерж
`200e2ea` задеплоен 10:03 UTC (проверено по Render: `live`), продольная
кривая копится с 09-17.

**Снимки `data/postings_raw/` на Render не копятся, чистка не нужна.**
Проверил по `GET /v1/services/crn-…`: тип `cron_job`, поля `disk` нет —
постоянного диска у задачи нет, контейнер живёт один прогон. Обёртки
пишут два файла (~1,5 МБ), шаг лога читает их в том же прогоне, потом
контейнер исчезает. Обратная сторона: **архива сырых списков в проде
нет** — если он нужен для будущей модели, хранить в таблице (`raw jsonb`)
или на внешнем диске; отдельное решение. Локально файлы копятся только
при ручных запусках (сейчас три снимка, 12 МБ).

### 2. Логистика: гипотеза «по дате реализации» проверена и не подтвердилась

`accrual/postings` принимает до 200 номеров за вызов; отправлений с
продажей 1–9 июля — 2 420 (из сохранённого сырья by-day) → 13 обращений,
27 с, 0 × 429, 11 629 строк начислений, все 2 420 отправлений в ответе.
Для каждого отправления взяты все его начисления любой даты
(логистических вне окна 1–9 июля — 207 из 6 195), отнесены к дню
реализации, просуммированы по нашим типам `logistics`:

```
день        наша по дате реализации   их логистика    разница
2026-07-01          66 736,49           77 022,20    −10 285,71
2026-07-02          46 683,38           63 746,85    −17 063,47
2026-07-03          77 031,63           82 728,35     −5 696,72
2026-07-04          52 983,22           69 832,01    −16 848,79
2026-07-05          61 416,83           74 526,90    −13 110,07
2026-07-06          87 093,53           89 283,65     −2 190,12
2026-07-07          83 118,59           91 640,93     −8 522,34
2026-07-08          72 888,22           96 240,89    −23 352,67
2026-07-09          66 655,52           69 422,20     −2 766,68
итого              614 607,41          714 443,98    −99 836,57
```

По дате реализации наша логистика **меньше** их числа (по дате
начисления — больше: 946 576,29). Ни с НДС, ни без: 614 607,41 / 1,22 =
503 776,57. Подбор подмножеств типов (логистика и прочее, до 5 из 18) под
714 443,99 и под 871 621,67 — нет. **Опровергнуто:** их логистика — не
наши типы по дате реализации и не они же по дате начисления. Второй
гипотезы не предлагаю. Что проверено и не сошлось: дата начисления,
дата реализации, НДС, все подмножества типов до 5–6. Открытый вопрос,
последний из девяти строк сверки; закрыть его может только их источник
(откуда в листе колонка «Логистика» — из кабинета или из своего свода).

Попутный факт: в `accrual/postings` у каждой строки есть `quantity` —
штуки по начислению. Для выкупов это возможный источник настоящих штук
вместо позиций (`ozon_finance_migration.md`: «штуки взять неоткуда»).
Не проверял глубже — вне задачи.

### 3. Хвосты

- **Измеритель истории выкупов ждёт вашего запуска**
  (`scripts/measure_buyouts_history_vs_accrual.py`, вне 00:15…03:15 UTC).
- Миграция «Аналитической группы» — со следующим снимком 1С.
- Полная модель — следующий большой шаг, не начинал.

### Чего не делал

В БД не писал, загрузчики и витрины не трогал, полную модель не строил.
Флаги, Render, органику не трогал.

### Перепроверка второй сессией (2026-09-16, после коммита `aa24e05`)

Задача в inbox та же; отчёт выше уже был в `main`. Вторая сессия к Ozon
не ходила: пересчитала п. 2 из сохранённого сырья (`accrual/postings`,
2 МБ, 13 ответов; by-day за 1–9 июля — `data/accrual_history/`, файлы
побайтно равны тем, что читала первая сессия). Все числа сошлись:
2 420 отправлений, 11 629 начислений, 6 195 логистических, 207 вне окна,
**614 607,41** по дате реализации, **946 576,29** по дате начисления,
503 776,57 без НДС.

Уточнение к таблице по дням: 46 отправлений из 2 420 имеют продажу в
двух днях (частичная выдача), их логистика 13 001,95. Таблица выше
относит такие к **последнему** дню реализации — при этом правиле все
девять дневных строк воспроизводятся до копейки. Отнесение к первому
дню меняет только разбивку (07-01: 67 546,42 вместо 66 736,49; 07-09:
62 644,22 вместо 66 655,52), итог тот же 614 607,41. Вывод не зависит
от правила: разница с ручным отчётом отрицательна во все девять дней
при обоих.

Ручной отчёт прочитан из `data/manual_report_july.xlsx`, лист
«Ozon - июль», колонка «Логистика, руб.», строки 4–12: дневные значения
как в таблице; итог 714 443,9918 стоит в строке с подписью «Итого -
июнь:» (подпись в их листе ошибочна, лист июльский). Сумма дней,
округлённых до копейки, 714 443,98 против 714 443,99 итога — копейка
округления дневных ячеек, не расхождение. Дробные части ячеек
(77 022,204918…) — след деления на 1,22: 714 443,99 × 1,22 =
871 621,67, то самое число из inbox; это свойство листа, не вторая
гипотеза.

Чтобы проверка не жила в `/tmp`: сырьё `accrual/postings` перенесено в
`data/accrual_postings/2026-07-01_2026-07-09.json` (в `.gitignore`, как
остальное сырьё), пересчёт — `scripts/recheck_logistics_by_realization_date.py`,
только чтение файлов, типы логистики берёт из `TYPE_TO_EXPENSE`
загрузчика. Запуск воспроизводит обе таблицы. Коммит — один, номер в
`git log`. В БД не писал, к API не обращался, Render не трогал.

---

## 2026-09-16, тринадцатая задача (обёртки, миграция лога; посев и мерж — за владельцем)

Ветка `posting-status-log` дополнена, `main` — только этот отчёт и две
строки в STATUS/CLAUDE. В БД — только DDL (п. 2). Посев не запускал, ветку
не мержил.

```
git log main..posting-status-log
21414bd Save orders before dumping raw postings, and never let the dump take the fatal step down
69900ec Log posting status transitions nightly from the responses the order steps already fetch
```

### 1. Дефект исправлен в обеих обёртках

Было: `dump_raw` между получением и записью заказов в фатальном шаге.
Стало (`scripts/ozon_fbo_orders_step.py`, `scripts/ozon_fbs_orders_step.py`):
сначала `save_orders` / `save_ozon_orders`, потом `dump_raw` внутри
`try/except Exception` — при отказе печатает
`⚠️ сырой ответ FBO НЕ сохранён, лог статусов за эту ночь без FBO: OSError: …`
и завершает шаг успешно. Логика вынесена в `run(fbo_module, log_module)`,
чтобы это было проверяемо без API.

Тесты (`tests/test_ozon_posting_status_log.py`, +3): при бросающем
`dump_raw` заказы записаны и исключения нет — для FBO (порядок
fetch → build → save → dump) и для FBS (fetch → save → dump); при
успешном `dump_raw` он идёт после `save`. Весь набор в ветке: 556 OK.

**Шаг «Ozon: лог статусов отправлений» проверен руками** на двух отказах:
без файлов сырья — печатает `нет сырья в data/postings_raw`, код 1; без
таблицы — печатает `таблицы ozon_posting_status_log нет — применить
sql/20260916_…; шаг пропущен`, код 1. Оба видны в логе; код 1 у
нефатального шага даёт Telegram-предупреждение «Шаг не выполнен, прогон
продолжен» — это громко, как и нужно.

### 2. Миграция применена — таблица создана и пуста

`ozon_posting_status_log` через MCP `apply_migration` в проект
`pkrsrwjrlurlfpdyixei`, SQL тот же, что в
`sql/20260916_create_ozon_posting_status_log.sql`. Проверка запросом:
строк 0, колонок 13, ограничения `_pkey` и `_schema`, индексы
`idx_…_posting`, `idx_…_cohort`.

Посев — ваш, из ветки, в порядке 09-14 → 09-15 FBO → 09-15 FBS (команды в
отчёте двенадцатой задачи; ожидание 6 321 + 541 + 5 838 = 12 700 строк).
Мерж — после посева, по вашему слову.

### 3. Первая ночь после мержа (09-17, 00:15 UTC) — что должно появиться

В логе Render: в шагах заказов строки `Сырой ответ FBS сохранён: …` и
`Сырой ответ FBO сохранён: …`, затем блок «Ozon: лог статусов отправлений»
с двумя строками вида `fbo fbo_20260917T00….json: отправлений ~6 200,
впервые ~400, сменили статус ~500, без изменений ~5 300 → строк ~900`
и `fbs …: … → строк ~700`, и итог `итого строк записано: ~1 500–1 700`
(оценка: с посева 09-15 19:00 до сбора пройдёт ~53 часа; по измеренным
243 новых и 298 переходов за 31 час у FBO — около 900; у FBS первое
ночное наблюдение окна 14 дней — новые ~450 плюс переходы ~300).
Переходы — как в снимках: больше всего `delivering → delivered`, затем
`awaiting_deliver → delivering`, `delivering → cancelled`. В таблице:
`select count(*) … where source = 'nightly' and observed_at::date = '2026-09-17'`
больше нуля, `previous_status` заполнен у переходов и null у новых.
Промолчал шаг — если в логе нет блока «лог статусов» или в нём
«нет сырья» / «таблицы нет» с кодом 1 и утренний Telegram принёс
«Шаг не выполнен».

### 4. Хвосты

Измеритель истории выкупов ждёт запуска; миграция «Аналитической
группы» — со следующим снимком 1С; логистика −232 132,30 в сверке с
ручным отчётом — в очереди.

### Чего не делал

Полную модель не строил, `marketplace_orders` и загрузчики заказов не
менял. Посев и мерж не выполнял. Флаги, Render, органику не трогал.

---

## 2026-09-15, двенадцатая задача (лог статусов; дополнение из чата A–F; мелочи)

`main`: `97480a7` (документы, A–F, миграция «Аналитической группы», экспорт
чата), `4415229` (громкий `commission_ratio`, STATUS) и этот отчёт.
Код лога — **в ветке `posting-status-log`, не в main**, чтобы автодеплой
Render не увёз его до вашего слова:

```
git log main..posting-status-log
69900ec Log posting status transitions nightly from the responses the order steps already fetch
```

В БД не писал, миграции не применял, скрипты с записью не запускал, к
API не ходил. Тесты: 553 OK в ветке, 546 OK на main.

### 1. Дата отмены — записано как решение

`reports_model.md` §3: наблюдаемая дата = дата первого сбора со статусом
`cancelled`, точность сутки, колонка `observed_at` — «момент нашего
сбора, не момент отмены у Ozon». Условная отмена FBS — как возможное
уточнение, не сейчас.

### 2. Лог переходов статусов — собран, не запущен

**Таблица** `ozon_posting_status_log` (`sql/20260916_create_ozon_posting_status_log.sql`,
в ветке): `posting_number`, `observed_at` (ключ), `schema`, `order_date`
(когорта: FBO `created_at`, FBS `in_process_at` — как у загрузчиков),
`ordered_at`, `status`, `substatus`, `previous_status` (null = первое
наблюдение), `cancel_reason_id`, `cancellation_type`, `cancelled_after_ship`
(FBS), `amount` (Σ price × qty — взвешивать кривую деньгами), `source`.
Ваш минимум дополнен тремя полями: предыдущий статус (переход виден в
одной строке), причина отмены, сумма.

**Откуда данные без единого обращения к API.** Ночные шаги заказов идут
через обёртки `scripts/ozon_fbo_orders_step.py` и `scripts/ozon_fbs_orders_step.py`:
они вызывают функции тех же загрузчиков (загрузчики не изменены) и тот же
ответ кладут в `data/postings_raw/<schema>_<UTC>.json` (в `.gitignore`).
Отдельный нефатальный шаг «Ozon: лог статусов отправлений»
(`scripts/ozon_posting_status_log.py --apply`) читает последние файлы,
берёт последний известный статус каждого отправления из таблицы и пишет
**только изменения**: первое наблюдение и смену статуса. Нет таблицы или
файла — печатает и выходит с кодом 1, прогон идёт дальше. Побочная
польза: сырые списки отправлений копятся на диске (~1,5 МБ в сутки) —
история для будущей модели.

**Объёмы.** По двум снимкам FBO с интервалом 31 час: 243 новых + 298
переходов → ≈ 420 строк в сутки у FBO; FBS (окно 14 дней): первая ночь
≈ 2 800, дальше ≈ 350 в сутки. Итого ≈ 800 строк в сутки, ≈ 290 тысяч в
год по ≈ 400 байт — порядка 120 МБ в год в JSON-эквиваленте. Таблица
маленькая.

**Посев из снимков — два дня форы.** Снимки лежат в `data/postings_raw/`:
`fbo_20260914T115900Z.json` (6 321), `fbo_20260915T185911Z.json` (6 166),
`fbs_20260915T185911Z.json` (5 838). Прогон чистой функцией без БД: 09-14
→ 6 321 строка (все первые); 09-15 FBO → 541 (243 впервые + 298
переходов: delivering→delivered 116, awaiting_deliver→delivering 87,
delivering→cancelled 60, awaiting_deliver→cancelled 11 …); 09-15 FBS →
5 838 первых. Посев **12 700 строк**. По вашему слову, из ветки:

```
git checkout posting-status-log
# 1. миграция sql/20260916_create_ozon_posting_status_log.sql — SQL-редактор или MCP
venv/bin/python3 scripts/ozon_posting_status_log.py --raw data/postings_raw/fbo_20260914T115900Z.json --apply
venv/bin/python3 scripts/ozon_posting_status_log.py --raw data/postings_raw/fbo_20260915T185911Z.json --apply
venv/bin/python3 scripts/ozon_posting_status_log.py --raw data/postings_raw/fbs_20260915T185911Z.json --apply
# 2. мерж ветки в main → с ближайшей ночи шаг пишет сам
```

Порядок файлов важен: 09-14 до 09-15. Без `--apply` каждая команда
печатает план (db_writes = 0).

### 3. Дополнение из чата — A–F

**A. Правило записано** в `how-we-work.md` («Источники») и CLAUDE.md §4:
`code 8 «rate limit per second»` Seller API приходит и на отключённый
метод — 11.09 представитель Ozon так ответил про
`/v3/posting/fbs/unfulfilled/list` («давно отключён»). Сначала сверить
версию эндпоинта со спекой, потом лечить паузами.

**B. Инцидент 09-11 … 09-13** в `ozon_postings_migration.md`: добавлены
обе версии — частотный лимит и просроченный `/v2/posting/fbo/list` — и
что вторую опровергает: 13.09 и 14.09 живые пробы `/v2` давали 200
(14.09 — 6 324 отправления за 7 обращений), в логах Render 09-13 и 09-14
первые страницы `/v2` возвращали 200 и падала одна из следующих;
отключённый метод не отвечает 200 через раз. Что её не хоронит:
отключения у Ozon бывают постепенными. Версия не выбрана.

**C. `commission_ratio`.** Протечка protobuf `value:"0.450000"` живёт в
**accrual/by-day**, не в realization: в июльском сырье accrual 2 526
товарных строк из 8 306 несут такую строку, 5 780 — null. Мы это поле из
accrual не читаем (выкупы — `sale_amount`/`sale_commission`, расходы —
`type_id`). В `realization/by-day` поле числовое: во всех 31 файле
`float`, среди 7 619 загруженных строк null — 0. При строке-мусоре
загрузчик и раньше падал бы (`InvalidOperation`), теперь — явным
`RuntimeError` с датой и строкой; тест добавлен (`4415229`).

**D. Эквайринг `type_id 1` — сходится ровно, если снять НДС.** По дням
1–9 июля наш тип 1 из accrual, делённый на 1,22, даёт их «Эквайринг» до
копейки: 41 351,54 / 1,22 = 33 894,70 (07-01) … итого 484 320,98 / 1,22 =
396 984,41. Их эквайринг — без НДС при обороте и комиссии с НДС.
Отдельная статья дала бы полное совпадение при делении на 1,22; статья
не заведена (только расчёт). Та же идея на логистике не работает:
714 443,99 × 1,22 = 871 621,67 ≠ 946 576,29.

**E. Лимиты Performance.** Тег `Limits` спеки 09-13 совпадает с CLAUDE.md
§4 по всем числам выгрузок и добавляет одно: **одновременных выгрузок по
организации — 5**. С 14.09 — лимиты на операции с кампаниями
(создание/копирование 1000 в день, бюджет и стратегия 500, добавление
товаров 1200, ставки 6000), нас не касаются. Записано в §4 и в
`ozon_performance_limits.md`.

**F. `/v1/product/prices/details`** мы не используем — в коде ни одного
вызова. Не грозит ничем.

### 4. Мелочи

- «Аналитическая группа»: миграция `sql/20260916_add_analytics_group_to_article_unit_costs.sql`
  (не применена) и флаг `--analytics-group` у `load_article_unit_costs.py`;
  без флага поле не пишется, чтобы не падать на таблице без колонки.
- Экспорт чата (37 986 сообщений, 09-15 19:08) и README закоммичены в
  `97480a7`; диф огромный из-за форматирования — так и есть.
- Измеритель истории выкупов ждёт запуска.

### Чего не делал

Полную модель не строил, `marketplace_orders` и загрузчики заказов не
трогал (обёртки зовут их функции), миграции не применял, скрипты с
записью не запускал. Флаги, Render, органику не трогал.

---

## 2026-09-15, одиннадцатая задача (модель отчётности — документ и измерения)

Один коммит в `main`, номер в `git log`. В БД не писал, код не менял,
загрузчики не запускал, к API не ходил (все измерения — по сырью 09-14/09-15,
спекам и БД с чтением по сортировке). Тесты не затронуты (только документы).

### 1. `docs/reports_model.md` — целевая модель

Главное в документе:

- **зерно — отправление × товар** (`posting_number`, `sku`) со статусом,
  датой заказа и **наблюдаемой** датой отмены; «созданные» и
  «подтверждённые» — фильтр по флагу, все три отчёта и все срезы —
  запросы над одной таблицей; болезнь FBO (ключ без отправлений, который
  никто не удаляет) исчезает по построению — отмена обновляет ту же
  строку. Альтернатива «заказ + таблица отмен» разобрана и отклонена для
  факта, оставлена только как лог переходов статусов;
- **две даты** — оба взгляда описаны как два запроса (по дню заказа —
  настоящая экономика дня, дозревает; по дню отмены — закрывается сразу,
  смешивает когорты);
- **три отчёта**: созданные / выкупы / с дозреванием; формулы оригинала и
  наши — рядом, столбец к столбцу, с четырьмя сознательными
  расхождениями (подписка, измеренная комиссия, реклама по площадкам,
  ДРР от созданных и от подтверждённых);
- **кривая дозревания** измерена уже сейчас срезом по двум снимкам FBO и
  снимку FBS (§5 документа): 10 % отмен к концу первых суток, ~38 % к
  10-му дню, плато ≈ 41 % (FBO) и ≈ 38 % (FBS) к 21-му; два снимка с
  разницей в 31 час дали одну кривую. Продольно между снимками отменились
  75 из 3 770 открытых (2 %), 60 из них — из `delivering`, возраст до 17
  дней: прогноз нужен и двухнедельным датам. Коэффициент 0,65 оригинала
  ложится между плато как средняя;
- **срезы** — измерено на 55 870 строках заказов: префикс площадки у 100 %
  (FBO только F/T: 79,6 / 20,4 % ₽; FBS F 95,6 %, S 3,7 %, T 0,7 %),
  стыковка с 1С 99,99 % выручки, проба и вид изделия заполнены у всех
  найденных; реклама по SKU → префикс разносится на 99,1 % расхода (F 87,8,
  T 9,9, S 1,35 %). «Аналитическая группа» в `article_unit_costs` не
  грузилась — добавить колонку при следующем снимке.

### 2. Измерено — чего не хватает

- **Даты отмены у Ozon нет.** Ни в `/v3/posting/fbo/list` (15 полей, даты
  только `created_at`, `in_process_at`), ни в `/v4/posting/fbs/list` (44 поля;
  `cancellation` — причина, тип, инициатор, `cancelled_after_ship`, без
  времени), ни в `get`-методах. По всей спеке «дата отмены» встречается
  дважды и не про наше: `cancelled_at` у заявок на условную отмену (FBS,
  только заявки) и у возвратов с компенсацией. Начисление типа 6
  `Cancellation` как прокси не годится: в июле 1–9 таких строк ноль.
  **Развилка (не решал):** наблюдаемая дата — первый сбор со статусом
  `cancelled`, точность сутки; либо собирать чаще; либо для части FBS —
  заявки на условную отмену.
- **Кривая дозревания** измерима сейчас срезом (сделано), продольно — после
  лога переходов: первая когорта дозреет через ~30 дней после включения.
- **Площадка** — 100 % строк, **категория** — 99,99 % выручки, **реклама по
  площадкам** — 99,1 % расхода через SKU; признака площадки в Performance
  API нет (`extcampaignCampaign`: id, placement, paymentType, стратегия;
  `organisationTitle` только в подсказках Selected CPO).

### 3. Направление по загрузчикам — учтено, код не тронут

В модели оба загрузчика пишут всё со статусом по ключу отправление ×
товар, переходы — в лог; `marketplace_orders` становится представлением.
В `ozon_orders_cancellations.md` добавлена поправка: «завышение 27 %»
отозвано, измерения переименованы в «доля отменённых внутри созданных»;
в CLAUDE.md §0 и §9-4б формулировки заменены.

### 4. Хвосты

- Измеритель истории выкупов — ждёт вашего запуска, не трогал.
- «Дополнение из чата разработчиков» — **этой инструкции я не получал**:
  в десятой задаче, которую я читал, её не было, в истории git
  `docs/inbox.md` её тоже нет (проверено `git log -p`). По пяти темам, что
  вы перечислили, я нашёл в чате (файл на диске обновлён владельцем до
  37 986 сообщений, 09-15 19:08, не закоммичен — не трогал):
  `commission_ratio` у accrual объявлен string, но прилетает «протечка
  protobuf Decimal» (14.09, F1_GTR); эквайринг приходит как `type_id: 1`
  (14.09, Суетов Антон — про чужую таблицу); лимиты Performance
  анонсированы заново 08.09 со страницей Limits; про переименование
  колонок 08.09 в чате — `customer_price` в `/v1/product/prices/details`
  с 8 сентября отдаёт не клиентскую цену. Куда это записать и в какой
  форме — скажите, повторив инструкцию; сам не додумывал.

### Чего не делал

Схему не создавал, миграций не писал, загрузчики не менял, витрины не
трогал, историю не пересобирал, FBS на `/v4` не переводил. Флаги, Render,
органику не трогал.

---

## 2026-09-15, десятая задача (отменённые заказы — измерено; измеритель истории выкупов — написан)

Один коммит в `main`, номер в `git log`. В БД не писал. К Ozon API — только
свежий сбор отправлений за 30 дней (FBS 6 обращений, FBO 62, 0 × 429).
Измеритель не запускал (12 дат он прочитал из файлов, 0 обращений).
Тесты: весь набор 545 OK.

### 1. Отменённые заказы — 27,0 % выручки окна, и механик две

Свежий сбор 2026-09-15 18:59 UTC, полные локальные дни 08-17 … 09-14, оба
загрузчика воспроизведены как есть. Полный разбор —
`docs/ozon_orders_cancellations.md`.

```
схема  окно             ключей БД   выручка БД      ключей без отправлений  их сумма        доля    FBS: отменённые внутри строк
fbo    08-17…08-31        2 672    52 032 240,00       813         11 993 707,00   23,1 %
fbo    09-01…09-14        1 625    29 722 200,00       295          3 785 012,00   12,7 %   (+15 ключей с большей суммой, 263 465,00)
fbs    08-17…08-31        1 876    59 650 645,00         0                  0,00    0,0 %   19 503 510,00 (32,7 %)
fbs    09-01…09-14        2 489    68 365 910,00         0                  0,00    0,0 %   20 995 004,00 (30,7 %)
```

**FBO** — остаток, как и думали: 1 108 ключей, 15 778 719,00, 19,3 %; у
августа 23,1 %, у сентября пока 12,7 % — отмены накапливаются неделями.

**FBS — не остаток, а отсутствие фильтра:** `ozon_fbs_orders_loader.py`
статус не проверяет, отменённые отправления входят в строки наравне с
доставленными (в сборе 1 770 из 5 838, по деньгам 31,1 %). Ключей без
отправлений у FBS ноль именно поэтому — отменённый заказ никуда не
девается. Итого за окно заказы в таблице 209 770 995,00, не
подтверждено отправлениями **56 540 698,00 (27,0 %)**.

**Старше 30 дней.** 30 дней — наше окно, не предел API: списки
отправлений принимают период до года (спека, `PERIOD_IS_TOO_LONG` только
за год; парность 08-06 снималась 09-13 — 38 дней назад). Проверить
03-28 … 08-16 можно теми же методами: FBO ≈ 280 обращений, FBS ≈ 27,
итого ≈ 310, 8–10 минут, только чтение. Не запускал — вне разрешённых 30
дней. Без него известно: механика FBS не менялась с апреля, ~31 % стоит
на всей истории FBS; у FBO остаток накапливался с 04-26 — порядок
15–25 %, это оценка, не измерение.

**Витрины.** От `marketplace_orders` считают `daily_sku_kpi` →
`daily_marketplace_kpi` (заказы, ДРР `ad_share_of_orders`, ROAS,
buyout_rate), Telegram и management-отчёты, диагностика рекламы и
прогнозная экономика (TACOS), органика (выключена), decision-input.
Выкупы, расходы, себестоимость, маржа — не затронуты. **ДРР** за окно:
реклама 12 125 905,76 / заказы 209 770 995,00 = 5,78 %; без остатка FBO
— 6,26 %; без отменённых FBS — **7,91 %**. По подтверждённым заказам ДРР
на два процентных пункта выше, чем показывала витрина.

**Варианты починки** (описаны, не выбраны): A — ночью удалять ключи окна
без отправлений и научить FBS пропускать `cancelled` (история остаётся
завышенной, разрыв ряда на дате починки; FBS сначала перевести на `/v4`
с падением вместо частичного); B — отменённые отдельной строкой со
знаком (смена ключа и девяти читателей, и всё равно поверх A); C —
только вперёд (дешевле всего, разрыв ряда); D — A плюс разовая
пересборка истории с 03-28 из API (≈310 обращений чтения + один запуск с
записью и снимком, пересборка витрин за 170 дат) — единственный без
разрыва. Цены по каждому — в документе.

### 2. Измеритель истории выкупов — `scripts/measure_buyouts_history_vs_accrual.py`

Только чтение. По каждой дате 03-28 … 08-17 снимает `accrual/by-day`
(сырьё в `data/accrual_history/<дата>.json`, в `.gitignore`; при
повторном запуске читает файлы, не API), строит строки штатным
`build_buyout_rows` и сравнивает с `marketplace_buyouts` за дату (чтение
с `ORDER BY`). Печатает по датам Σ таблицы, Σ accrual, разницу; итог —
дат сверено, дат с расхождением по деньгам дня, суммарное расхождение,
таблицу расхождений с числом SKU только в таблице / только в accrual / с
разной суммой.

Что заложено: пауза 1,5 с, на 429 — 60 с × 3 с печатью числа пауз;
отказ стартовать в 00:15 … 03:15 UTC; стоп при превышении 250 обращений;
допуск 0,05 ₽ на float-округление таблицы (5 373 634,00 лежит как
5 373 633,99); нулевые строки старого загрузчика и иное распределение той
же суммы по SKU считаются отдельно, а не как расхождение — старый API
делил сумму операции поровну между позициями, accrual даёт `sale_amount`
по товару, поэтому у части SKU суммы разные при равном итоге дня (в июле
82 SKU на 9 дат). Тесты чистых функций — 8.

Прогон на 12 датах из файлов (0 обращений):

```
дат сверено: 9 (07-01…07-09) + 3 (03-30, 03-31, 04-01); расхождение по деньгам дня: 1
2026-07-08   таблица 5 944 937,00   accrual 5 935 375,00   Δ +9 562,00   SKU с разной суммой 13
нулевых строк в таблице 17 (не деньги), SKU с иным распределением 82
```

**Ожидание на полном окне:** 143 даты, из них 12 уже в файлах → ≈131 из
API, ≈150–170 обращений, 4–6 минут; расхождений по деньгам — единицы
дат, суммы порядка тысяч рублей на дату (по 12 датам: 1 из 12, 9 562,00).
Запуск — ваш, вне 00:15 … 03:15 UTC:

```
venv/bin/python3 scripts/measure_buyouts_history_vs_accrual.py
```

### Чего не делал

В БД не писал. Историю не переписывал. Витрины не пересобирал.
Измеритель не запускал. Флаги, Render, органику не трогал.

---

## 2026-09-15, девятая задача (комиссия 03-31, действующие числа, история выкупов)

Один коммит в `main`, номер в `git log`. В БД не писал. К Ozon API не
ходил. Все выборки — с `ORDER BY`.

### 1. Фальшивая комиссия в прибыль НЕ входила — 126 018 891,01 не рушится

**По коду.** `reports_daily_sku_kpi.build_kpi` берёт из `marketplace_buyouts`
только `buyouts_qty` и `buyouts_amount_seller`; `commission_amount` витрины
собирается из `marketplace_expenses` (`expense_type = commission`).
`marketplace_buyouts.commission_amount` не читает ни витрина, ни мой
расчёт прибыли (он брал комиссию из `daily_marketplace_kpi`).

**По данным** (запросы 09-15):

```
marketplace_expenses, commission, 03-31                           709 388,32
daily_marketplace_kpi, commission_amount, 03-31 — до перезаписи   709 388,32   (снимок rewrite_2026-03-31_20260915T180308Z.json)
daily_marketplace_kpi, commission_amount, 03-31 — после           709 388,32
marketplace_buyouts.commission_amount, 03-31 — до перезаписи   29 151 447,66   (только здесь; никто не читает)
marketplace_buyouts.commission_amount, 03-31 — после              709 388,32
Σ комиссии витрины 03-28…09-13                               432 129 622,41   — ровно то число, что вычиталось 09-14
```

Поправка прибыли за 03-31 — только выручка, −44 949 235,46. Здесь всё
как считали.

**Но число всё же меняется, и в последний раз, по другой причине.**
Проверяя, я сравнил сегодняшние помесячные суммы таблицы выкупов с
вчерашними и увидел ±12 млн по месяцам при равном итоге. Сегодняшние
суммы совпали с витриной, которую строит пайплайн; вчерашние — нет.
Причина — моя: вчерашние большие выборки (39 266 строк выкупов, 170 402
строки витрины по SKU) читались постранично без `ORDER BY`, и PostgREST
отдал страницы с повторами и пропусками. Итог по выкупам вчера вышел
1 011 373 398,68 против верных 1 012 928 981,10 (−1 555 582,42), а
себестоимость на них — 329 207 744,18 против 330 036 031,96. Сегодня всё
пересчитано с сортировкой и сверено тремя независимыми суммами:
таблица выкупов 1 012 928 981,10 = витрина по SKU 1 012 928 981,10 =
витрина по маркетплейсу 1 012 928 981,00.

**Действующие числа** (окно 03-28 … 09-13, данные 09-15 18:30 UTC):

```
выручка выкупов          1 012 928 981,00
− комиссия                 432 129 622,41
− логистика                 12 739 228,32
− прочее                    28 942 912,36
− реклама                   83 841 546,49
= прибыль без СС           455 275 671,42
− себестоимость выкупов    330 036 031,96   (32,58 %; без СС 1 814 073,00 = 0,18 %)
= прибыль с СС             125 239 639,46
```

От 126 018 891,01 отличие −779 251,55 = +49 036,23 (окно ≤09-13 вместо
≤09-14: вчера в окно входил неполный 09-14 с расходами 49 036,23 и
нулевой выручкой) − 828 287,78 (СС на 1 555 582,42 потерянной вчера
выручки). Цепочка 350,5 → 329,6 → 329,2 → **330,0 млн** записана с
причинами каждого шага в `cost_of_goods.md`, «Действующие числа».

Что ещё опиралось на чтение без сортировки — перепроверено с ней:

- «витрина по SKU 930,8 млн против 1 056,3 млн» и «article у 1 918
  товаров» — **опровергнуто**: суммы равны, article у 5 184 товаров, но
  лишь у 29,1 % выручки выкупов (эта часть находки стоит);
- «витрина отстаёт от таблицы по месяцам на ±12 млн» — **опровергнуто**;
- покрытие себестоимости за 90 дней — 4 630 из 4 631 ключей, 99,997 %,
  не найден один `F007780899-ТП` — **стоит**;
- остаток отменённых FBO 08-16…09-09 — 1 120 ключей, 15 933 652,00,
  21,3 % — **стоит** (вчера 1 119 / 15 843 585 / 21,0 %);
- сверка с ручным отчётом за 1–9 июля — все девять дней и 07-08 +9 562,00
  — **стоит** до копейки (2 170 строк, три страницы).

### 2. Документы приведены в согласие

`cost_of_goods.md` — раздел «Действующие числа» с помесячной таблицей и
историей числа; старые таблицы оставлены с пометкой. `STATUS.md` — деньги
и хроника: 03-31 перезаписан 09-15, загрузчик удалён 09-14, снимок в
`data/snapshots/rewrite_2026-03-31_20260915T180308Z.json`. `CLAUDE.md` —
§9-7 (опровержение), §9-7а (закрыт), §9-7б (действующие числа).
`how-we-work.md` — к правилу про `ORDER BY` добавлен потолок 1 000 строк
на ответ PostgREST (`range(0, 4999)` молча вернул 1 000 — поймано сегодня).

### 3. История выкупов 03-28 … 08-17 — измерено, что уже есть; дальше стоп

Без новых обращений уже известно по 12 датам: 03-30, 03-31 (после
перезаписи), 04-01 и 07-01 … 07-09 совпадают с `accrual/by-day` до
копейки, кроме **07-08: +9 562,00** (строка старого API). Полная
проверка — 143 даты, у `accrual/by-day` 1–3 страницы на дату: **≈160–190
обращений, 5–7 минут** с паузой 1,5 с. Это много против ~30 в ночь —
останавливаюсь, как просили; скрипт-измеритель по образцу июльского
готов написать за одну задачу.

### Чего не делал

Историю выкупов не переписывал, витрины не пересобирал, загрузчики не
запускал, к API не ходил. Флаги, Render, FBS, органику не трогал.

---

## 2026-09-14, восьмая задача (миграция реализации, перезапись 03-31, сверка с ручным отчётом, ночь)

Два коммита в `main` (код и документы — `3cea9c9`, этот отчёт — следующий).
Тесты: весь набор 537 OK. В БД писал только DDL (п. 1). Загрузчики не
запускал. К Ozon API: 3 вызова `accrual/by-day` за 03-31 (план
перезаписи), 10 вызовов за 1–9 июля и справочник типов (сверка);
`realization/by-day` не трогал.

### 1. Миграция `ozon_realization_by_day` — применена, таблица создана и пуста

Через MCP `apply_migration` в проект `pkrsrwjrlurlfpdyixei`, SQL тот же, что
в `sql/20260915_create_ozon_realization_by_day.sql`. Проверка запросом:
строк 0, колонок 26, ограничения `_pkey`, `_side`, `_quantity_positive`,
`_offer_id_norm_lower`, индексы `idx_…_sku`, `idx_…_offer`. Загрузчик не
запускал — ваш: `venv/bin/python3 scripts/load_ozon_realization_by_day.py --apply`
(сырьё 31 даты уже в `data/ozon_realization_by_day/`, в API не пойдёт).
`--check-only` и отчёт по нему — после вашего запуска.

### 2. 31 марта — скрипт готов, план сверен, загрузчик удалён

`scripts/rewrite_buyouts_20260331.py`. Без `--apply` — только план (прогнан
2026-09-14, db_writes = 0):

```
удалится   marketplace_buyouts 03-31: 940 строк, 940 SKU, 2 523 позиции,
           buyouts_amount_seller 46 842 023,46, commission_amount 29 151 447,66
           daily_sku_kpi 03-31 (ozon): 973 строки, Σbuyouts 46 842 023,46
           daily_marketplace_kpi 03-31 (ozon): 1 строка (2 523 / 46 842 023,46)
запишется  из accrual/by-day за 03-31: 427 начислений → 60 строк, 60 SKU,
           65 позиций нетто (69 продаж − 4 возврата), 1 892 788,00
           витрины за 03-31 — из штатных build_kpi / build_marketplace_kpi,
           отфильтрованных по дате
```

С `--apply`: снимок всех трёх наборов строк плюс сырые начисления в
`data/snapshots/rewrite_2026-03-31_<время>.json` (папка в `.gitignore`)
→ delete выкупов за дату, контроль «осталось 0» → upsert 60 строк,
контроль «записано = собрано» → delete витрин за дату → запись витрин
только за 03-31 → итоговые суммы. Если источник не совпадёт с ожиданием
(60 / 65 / 1 892 788,00), скрипт не пишет.

Ожидание «80 SKU, 121 позиция» из вечернего отчёта было неверным: я
считал товарные строки источника, а штатный сборщик пропускает 48 строк
с нулевой суммой и комиссией и вычитает 4 возврата. Сумма та же. Отсюда
уточнение чисел (разница 437 265,55 = СС этих 56 строк):

```
лишнее в 03-31             позиций 2 458, выручка 44 949 235,46, СС 21 318 860,16 (было 20 881 594,61)
СС за 03-28…09-13          329 207 744,18 (было 329 645 009,73; утром 350 526 604,34)
прибыль с СС               126 018 891,01 (было 125 581 625,46; утром 149 649 266,31)
март: выручка / СС / доля  8 042 295,00 / 2 396 929,10 / 29,80 %
```

Старые числа в `cost_of_goods.md` оставлены в утренних таблицах с
пометкой сверху «действующие — в поправке»; в `STATUS.md`, `CLAUDE.md`,
`ozon_finance_migration.md` заменены. `loaders/ozon_realization_loader.py`
удалён (`git rm`); импортов не было.

### 3. Сверка с ручным отчётом, 1–9 июля

Их лист «Ozon - июль» против наших таблиц за те же даты (выкупы —
`marketplace_buyouts`, комиссия и расходы — `marketplace_expenses`,
СС — позиции × `article_unit_costs`, реклама — Performance API):

```
строка                                        у них             у нас           разница      объяснение
Оборот — выкупы                                50,654,708.00    50,664,270.00       9,562.00  07-08: наша строка старого API 5 944 937,00 против 5 935 375,00 в accrual и у них; остальные 8 дней — до копейки
Комиссия (с НДС)                               21,772,052.30    21,772,052.30           0.00  все 9 дней до копейки: наша sale_commission уже с НДС
Выручка = (оборот − комиссия)/1,22             23,674,307.95    23,682,145.66       7,837.71  ровно 9 562,00 / 1,22 от 07-08
Себестоимость                                  16,579,969.06    17,042,610.35     462,641.29  у нас позиции × снимок 1С 05-20; их штуки и источник цен неизвестны — см. ниже
Маржа = выручка − СС                            7,094,338.89     6,639,535.31    -454,803.58  = разница выручки + разница СС
Логистика                                         714,443.99       946,576.29     232,132.30  у нас 11 типов accrual за дату начисления; ни один набор типов не даёт их число
Реклама                                         4,864,940.89     4,567,607.44    -297,333.45  у нас Performance API (клики + CPO + Selected CPO); accrual 41+54 = 4 566 972,05; ни один набор типов не даёт их число
Эквайринг                                         396,984.41       484,320.98      87,336.57  у нас тип 1 Acquiring внутри «прочее»; ни один набор типов не даёт их число
Прочее                                            111,567.26       111,787.44         220.18  у нас «прочее» без эквайринга — почти сошлось
Фин. рез. (их формула)                          1,006,402.34       529,243.16    -477,159.18  по нашим числам в тех же статьях
… минус подписка Premium (51), которой у них нет               —      -788,382.67              —  1 317 625,83 за 9 дней в accrual, статья subscription; в их отчёте строки нет
```

**«Оборот - выкупы» = наша выручка выкупов** — проверено, не принято:
`accrual/by-day` за каждый из девяти дней даёт ровно их число
(50 654 708,00 итого), а наша таблица отличается только 07-08 на
9 562,00 — там лежит строка старого API, записанная 07-09 до миграции.
Комиссия с НДС — до копейки все девять дней: их база и наша одна и та
же, `sale_commission` из accrual. Выручку считал их формулой.

**Себестоимость: +462 641,29 у нас, и это НЕ разница в штуках.** Их
штуки в листе не показаны (колонки количества нет), точно разложить
нельзя. Направление известно: у нас позиции, штук больше или равно
позициям (31.08: 217 штук на 201 позицию, +8 %), значит по штукам наша
СС была бы ещё выше, а их — ниже нашей. Источник разницы — цены: их
себестоимость на единицу ниже нашего снимка 1С 05-20 (32,73 % оборота
против 33,64 %). Размер «позиции против штук» за июль по нашим данным
не оценить: `realization/by-day` начинается с 08-14; порядок по 31.08 —
около +1,3 млн к нашей СС за 9 дней, тогда разрыв в ценах ещё больше.

**Логистика, реклама, эквайринг — другая база, не ошибка разбора.** Наш
разбор accrual полон: по всем девяти дням `total_amount` начислений
минус (продажи + комиссия + все услуги) = 0,00. Но ни одна комбинация
типов (до 6 из 24) не даёт ни 714 443,99, ни 396 984,41, ни 4 864 940,89,
ни разрыв рекламы 297 333,45. Гипотеза, не проверена: у них расходы
отнесены к дате реализации отправления, у нас — к дате начисления
(`accrual/by-day` даёт только её; `accrual/postings` мог бы дать
привязку к отправлению, не вызывал). Реклама: наш Performance-итог
4 567 607,44 и accrual 41 + 54 = 4 566 972,05 сходятся между собой
(Promotion 54 = наши CPO + Selected CPO до копейки), их число выше обоих.

**Подписки нет в их отчёте вовсе:** тип 51 PremiumMembership за 9 дней —
1 317 625,83, категория ITEM, наша статья `subscription`. В их листе
строки нет; в их ноябрьском своде «Подписка Premium Plus» — 24 990 за
месяц внутри рекламы. Их фин. рез. 1 006 402,34 этой суммы не содержит;
по нашим данным те же 9 дней с подпиской — минус 788 382,67.

**Правила разнесения (свод за ноябрь).** Их статьи — группы Ozon из
выгрузки кабинета («Группа услуг»), не своя ВПР. Сравнение по смыслу
имён, наши типы — по `type_id`:

```
их группа                    их тип                               наш type_id → статья        правило
Продажи                      Выручка + Баллы + Программы партнёров sale_amount → выкупы        то же (amount + bonus + bank_coinv)
Возвраты                     те же три типа с минусом             sale_amount < 0 → выкупы    то же, у нас нетто в одной таблице
Вознаграждение Ozon          за продажу / возврат вознаграждения  sale_commission → commission то же, нетто
Услуги доставки              Логистика, обратная, обработка отмен 32 59 6 9 40 29 16 17 45 98 → logistics   то же
Другие услуги                Временное размещение, ОВХ, ошибки    78 82 → logistics; 93 94 92 → other   РАСХОДИТСЯ: 78 и 82 у них «другие»
Услуги агентов               Эквайринг, realFBS                   1 → other; 63 → other; 62 64 → logistics   РАСХОДИТСЯ: realFBS-доставка у нас логистика
Продвижение и реклама        Оплата за клик, за заказ             41 54 → Performance API     то же по смыслу, источник другой
Продвижение и реклама        Реклама в сети Интернет              23 → external_promo         РАСХОДИТСЯ: у нас вне рекламы намеренно
Продвижение и реклама        Подписка Premium Plus                51 → subscription           РАСХОДИТСЯ: у нас отдельная статья
Продвижение и реклама        Баллы за отзывы, Закрепление отзыва  47 61 → other               РАСХОДИТСЯ
Услуги FBO                   Размещение товаров на складах        46 → other                  РАСХОДИТСЯ
Компенсации и декомпенсации  —                                    25 10 → вне классификации   то же по сути: отдельно, не в расходы
Прочие начисления            Перечисление за доставку, недовложение  тип не установлен, в июле не встречались
```

В их ноябрьской выгрузке у всех строк «Услуги доставки» сумма 0,00 —
логистика в кабинетной выгрузке лежит не в этой колонке; согласуется с
тем, что их «Логистика» в июльском листе считается не из этого свода.

**Можем ли воспроизвести их отчёт по выкупам целиком.** Оборот, комиссию
и выручку — да, до копейки, из `accrual/by-day` (наша таблица выкупов за
старые даты ещё содержит строки старого API, 07-08 тому пример;
пересборка истории из accrual закроет). Себестоимость — считаем, но не
их: позиции против штук и снимок 1С 05-20 против их источника цен; за
июль штуки взять неоткуда. Логистику, эквайринг и рекламу их числами не
воспроизводим — база отнесения другая и по нашим данным не
восстанавливается без `accrual/postings`. Подписку они не учитывают, мы
учитываем. Итог: три строки из девяти — точно, одна — с известной
оговоркой, четыре — нет.

### 4. Ночь 09-15 — прошла чисто (дополнено 2026-09-15)

Cron 00:15 → 02:29 UTC, `successful`. Шаг FBO: **6 257 отправлений за 63
страницы, все 200, 429 — 0** (единственный 429 в окне — у WB Sales Funnel,
не наш шаг), «записаны 3 385 строк» в 00:20:55. В БД FBO за 09-14 — 119
строк (днём 14-го было 60), за 09-15 — 7 (день не закончен). Утренний
алерт за 09-14: `ozon_fbo_orders_missing` **нет**; горят
`ozon_daily_sku_organic_missing`, `ozon_ads_layer_missing`,
`ozon_performance_partial_ads`, `ozon_performance_cpc_incomplete` —
реклама за 09-14 неполная, это Performance, не заказы.

### Дополнение 2026-09-15: после вашего запуска загрузчика реализации

Таблица была создана пустой (0 строк, проверено сразу после миграции);
на 16:27 UTC 09-15 в ней **7 619 записей, 31 дата 08-14 … 09-13** — ровно
план. Тождества `amount = price × qty` и формула `total` проверяются
загрузчиком на каждой записи до записи; раз запись прошла — нарушений 0.

`--check-only` против `marketplace_buyouts` (вывод в отчёте прогона):

- множества SKU: совпали на 25 датах из 31; расходятся на 1–2 SKU:
  08-20, 08-21, 08-25 — SKU есть только в выкупах (accrual), 09-08, 09-09,
  09-10 — только в реализации;
- тождество `amount + bonus + bank_coinvestment` нетто = выручка выкупов:
  точно на 12 датах (08-29, 08-31, 09-01, 09-03, 09-05…09-08, 09-11…09-13);
  за период реализация 146 847 233,55 против выкупов 147 334 711,00 —
  **−487 477,45 (−0,331 %)**, дат с расхождением 19.

Разложение по SKU-дням (6 368 всего, расходятся 62):

```
только в выкупах (accrual), в отчёте о реализации нет       4   −304 005,50   (08-25 sku 1621630016 −171 227; 08-21 sku 1448785140 −92 432; 08-20; 08-21)
в обоих, сумма реализации ≈ половина accrual               39   −157 763,36   (например 08-14 sku 989430811: price 1,00, bonus 5 757,27 → 5 758,28 против 12 346,00)
в обоих, иное                                              19    −25 708,59
```

Причины не устанавливал: «половина» выглядит как строки с ценой 1 ₽ и
крупным бонусом — механика Ozon, не наша; «только в выкупах» — SKU,
проданный по accrual в этот день, но отсутствующий в отчёте о реализации
за ту же дату. Оба вида — открытый вопрос к источнику, не к загрузчику.

**Ошибка в моём измерении, найдена и исправлена.** Первая сводная
таблица по дням показала расхождения на 09-01 (−23 468) и 09-08
(+53 497), а разбор тех же дней по SKU дал ноль. Виновато чтение:
`range()` PostgREST без `order()` отдаёт страницы с повторами — сумма
7 619 строк вышла другой. Пересчёт с сортировкой дал числа выше. Правило
записано в `how-we-work.md`; в `load_article_unit_costs.py` и
`rewrite_buyouts_20260331.py` постраничное чтение теперь с `ORDER BY`
(на их прошлые результаты это не повлияло: сверка себестоимости дала те
же 4 621 из 4 622, что и разбор без пагинации, а план 03-31 читает
меньше страницы).

**31 марта не перезаписан:** в `marketplace_buyouts` по-прежнему 940
строк на 46 842 023,46 — `scripts/rewrite_buyouts_20260331.py --apply`
ждёт вашего запуска.

### Чего не делал

Загрузчики с записью не запускал (оба — ваши). Витрины не трогал —
только план. Флаги, Render, FBS, органику не трогал. Лист «Заказы» и
Wildberries не разбирал.

---

## 2026-09-14, седьмая задача (realization/by-day — план и сырьё; 31 марта — разобрано; ночь)

Один коммит в `main`, номер в `git log`. В БД не писал. К Ozon API: 8 проб
`realization/by-day` (границы и лимит), 31 вызов того же метода в режиме
плана (сырьё в файлы), 3 вызова `accrual/by-day` за 03-30 / 03-31 / 04-01
для разбора аномалии. Тесты: весь набор 537 OK.

**Отдельной строкой, как просили: запросов мало и лимит не мешает —
можно запускать сегодня.** 31 дата, 31 обращение, 0 × 429, 55 секунд,
`ratelimit-remaining` после последнего вызова 43. Сырьё за все 31 дату
уже снято в `data/ozon_realization_by_day/` (4,1 МБ, в `.gitignore`);
запись из файлов не ходит в API. Порядок по вашему слову: (1) миграция
`sql/20260915_create_ozon_realization_by_day.sql` — в SQL-редакторе или
через MCP, скажите какой; (2)
`venv/bin/python3 scripts/load_ozon_realization_by_day.py --apply`.

### 1. `realization/by-day` — план

**Сколько дат доступно — опытом.** Спека: 32 дня. Пробы 2026-09-14:
08-12 и 08-13 — 400 code 3 «no earlier than 32 days before the current
day» (08-13 = 09-14 − 32, отказ — граница на день строже текста), 08-14 —
200, 286 строк; 09-13 — 200, 253 строки; 09-14 — 404 code 5 «Report for
the requested date not found» (день не закрыт). **Доступно 31 дата,
08-14 … 09-13.** Каждые сутки в полночь самая ранняя пропадает.

**Запросы и лимиты.** Один запрос на дату, пагинации нет: 31 запрос,
0,1 … 0,8 с и 100 … 150 КБ каждый. В спеке о лимитах ни слова; пометка
«не расходует лимиты» в seller-спеке есть у одного метода
(`/v1/delivery/check`), у этого нет — неизвестно, не «бесплатно». В
ответе единственный заголовок лимита — `ratelimit-remaining`: 44 → 43 →
41 при вызовах через 2 с, 40 через минуту, 46 после паузы 65 с, 35 через
15 минут без наших вызовов, 43 после 31 вызова подряд. Окно и потолок
неизвестны, счётчик общий на аккаунт; 31 вызов с паузой 1,5 с в него
укладывается, 429 не было. В чате разработчиков про этот заголовок и
про лимиты метода — ничего (5 совпадений по «ratelimit», все не о том).

**Схема** — `sql/20260915_create_ozon_realization_by_day.sql`, таблица
`ozon_realization_by_day`, единица хранения — одна сторона одной строки
отчёта:

```
ключ (realization_date, row_number, side)      side: 'sale' | 'return'
sku, offer_id, offer_id_norm, barcode, product_name,
seller_price_per_instance, commission_ratio,
price_per_instance, quantity, amount, bonus, commission, compensation,
standard_fee, bank_coinvestment, stars, pick_up_point_coinvestment, total,
raw_row jsonb, source, fetched_at, loaded_at
```

Почему так: один SKU встречается в отчёте несколько раз с разной ценой
или долей комиссии (31.08 — 173 SKU на 209 строк, до 4 на SKU), поэтому
ключ — номер строки; номер стабилен только внутри ответа, поэтому дата
при записи перезаписывается целиком (delete + insert); продажи и
возвраты — отдельные записи, чтобы суммы считались фильтром. Тождества
(`amount = price × qty`, `total = amount + bonus + bank + stars + apvz +
compensation − standard_fee − commission`) сняты на 210 блоках 31.08 без
расхождений и проверяются скриптом на каждой записи — падает при первом
нарушении; на 7 619 записях плана нарушений 0.

**Скрипт** — `scripts/load_ozon_realization_by_day.py`: без `--apply` снимает
в файлы и печатает, с `--apply` пишет из файлов, `--check-only` сверяет
таблицу с `marketplace_buyouts` по множествам SKU и по тождеству
`amount + bonus + bank_coinvestment` нетто = выручка выкупов accrual
(31.08 сошлось до копейки). Пауза 1,5 с, на 429 — 60 с × 3, число пауз
печатается. Тесты чистых функций — 7, `tests/test_load_ozon_realization_by_day.py`.
Разбор — `docs/ozon_realization_by_day.md`.

Вывод плана (31 дата): записей 7 619, продаж 7 720 шт на 73 604 483,60,
`bonus` продаж 77 130 758,71 — bonus снова больше amount, как в отчёте за
август; выводов не делаю. Возвратов 220 шт.

### 2. 31 марта — гипотеза опровергнута, причина найдена

**Не начисления.** `accrual/by-day` за 03-31 сегодня: 427 начислений
(ITEM 143, POSTING 115, NON_ITEM 169), 92 отправления с товарами, 121
товарная строка, `sale_amount` 1 892 788,00, комиссия −709 388,32 —
обычный день между 03-30 (1 680 863,00) и 04-01 (2 312 400,00), причём
за те два дня таблица совпадает с источником до копейки. Диапазон id
заказов 03-31 такой же, как у соседей. Отправления датировать не
понадобилось: 46,8 млн в источнике нет.

**Что это.** Все 940 строк 03-31 записаны 2026-04-25 в 19:33:12 — за
сутки до бэкфилла 04-26 (21:31), который записал 03-28 … 04-03.
Писал их `loaders/ozon_realization_loader.py`: берёт **месячный** отчёт
`/v2/finance/realization` за прошлый месяц и кладёт весь месяц в
`marketplace_buyouts` одной датой — `stop_date` отчёта, для марта 03-31.
В пайплайне его нет, лежит с первого коммита, запускался один раз.
Отсюда и настоящие штуки (2 523 — месячная реализация), и «комиссия
1,5 %»: расходы за 03-31 из accrual нормальные, месяц лёг поверх дня.
Других таких дней нет: 04-30, 05-31, 06-30, 07-31, 08-31 обычные.

**На что влияет** (лишнее против источника: 2 402 позиции):

```
выручка выкупов за март          52 991 530,46 → 8 042 295,00   (−44 949 235,46)
себестоимость марта              23 715 789,26 → 2 834 194,65   (−20 881 594,61)
доля СС марта                    44,75 % → 35,24 %
прибыль без СС за 03-28…09-13   500 175 870,65 → 455 226 635,19 (−44 949 235,46)
СС за период                    350 526 604,34 → 329 645 009,73 (−20 881 594,61)
прибыль с СС                    149 649 266,31 → 125 581 625,46 (−24 067 640,85)
```

Утреннее «завышает прибыль марта ~на 19 млн» неверно: завышение — вся
лишняя выручка, 44 949 235,46. **Число из шестой задачи исправлено:
329 645 009,73 вместо 350 526 604,34**, коридор 319,5 … 331,5 млн.
Поправка записана в `cost_of_goods.md`, CLAUDE.md, STATUS.

**Рекомендация:** перезаписать 03-31 из accrual/by-day — удалить 940
строк за дату и записать день штатным сборщиком выкупов (80 SKU, 121
позиция, 1 892 788,00), а `ozon_realization_loader.py` удалить из
репозитория. «Разносить по исходным датам» невозможно: в месячном
отчёте нет дат, а март до 03-28 вне границы проекта. Цена — таблица
выше. После перезаписи пересобрать витрины за 03-31. Решение ваше.

Заодно: вся история выкупов 03-28 … 08-17 записана старым API до
миграции 09-11 (по created_at), новым сборщиком перезаписаны 7 дат с
08-18; выборочно 03-30 и 04-01 совпадают с accrual до копейки.

### 3. Ночь 09-15 — ещё не было

Задача говорит «прогон уже был» — по Render нет: последний запуск cron
09-14 00:15 UTC (закончен 03:13, successful), логов шага FBO после
09-14 12:00 UTC нет, время на момент проверки 20:22 UTC 09-14. Прогон
будет в 00:15 UTC 09-15.

### Чего не делал

В БД не писал, миграцию не применял, витрины не трогал. FBS, органику,
флаги, Render не трогал. Сбор с записью не запускал. В коммит попал и
`docs/ozon_organic_sales.md` с чужой правкой (раздел «Что говорят в чате
разработчиков»), лежавшей незакоммиченной — я её не менял.

---

## 2026-09-14, шестая задача (маржа по выкупам с себестоимостью — только чтение)

Один коммит в `main`, номер в `git log`. В БД не писал, витрины не
трогал, к Ozon API не ходил. Все расчёты — чтением `marketplace_buyouts`,
`marketplace_orders`, `article_unit_costs`, `daily_marketplace_kpi`,
`daily_sku_kpi`, `marketplace_expenses`. Полный разбор — в
`docs/cost_of_goods.md`, раздел «Маржа по выкупам».

### 1. К чему приложено — к выкупам, через SKU

Выкупы несут SKU, но не offer_id (`article` в `marketplace_buyouts` пустой).
Приложил через SKU → offer_id из `marketplace_orders` за тот же период
(5 129 SKU; у четырёх по два article — взят тот, где больше заказов) →
`article_unit_costs` без учёта регистра. Заказами выкупы не подменял.
Развилка, которую называю вслух: **штуки в выкупах — это позиции
начислений, не единицы товара** (модель accrual количества не отдаёт).
Проверка «позиция ≈ штука»: отношение (выручка выкупа / позиций) к
средней цене заказа за штуку — медиана 0,978, 76,7 % строк в пределах
±10 %, выше 1,5 — 0,5 %. Считал по позициям; оценка по подразумеваемым
штукам ниже на 3,08 % (см. п. 3).

### 2. Помесячно и итогом, 2026-03-28 … 2026-09-13

```
месяц      позиций    выручка выкупов    себестоимость     валовая маржа    СС/выручка   без СС
2026-03      2 719     52 991 530,46     23 715 789,26     29 275 741,20    44,75 %      5,93 %
2026-04      4 827    146 977 403,68     45 103 200,75    101 874 202,93    30,69 %      1,19 %
2026-05      6 355    155 525 246,25     52 271 499,75    103 253 746,50    33,61 %      0,64 %
2026-06     10 520    249 340 654,75     84 361 507,76    164 979 146,99    33,83 %      0,05 %
2026-07      9 815    205 279 472,00     69 173 629,67    136 105 842,33    33,70 %      0,00 %
2026-08      9 368    194 968 549,00     62 183 706,65    132 784 842,35    31,89 %     −0,06 %
2026-09      2 710     51 239 778,00     13 717 270,50     37 522 507,50    26,77 %      0,00 %
итого       46 314  1 056 322 634,14    350 526 604,34    705 796 029,80    33,18 %      0,56 %
```

**Без себестоимости: 183 позиции, 5 898 526,73 ₽ = 0,56 % выручки** —
порог 1 % не превышен. Из них 149 SKU на 5 736 380,73 не встречаются в
заказах с 03-28 (заказаны до границы проекта — ключа взять неоткуда) и
один SKU на 162 146,00, чей ходовой article `F007775341-старый` в файле
отсутствует (второй article `F007775341` есть; стыковка по нему дала бы
ещё 70 053,60 СС — не подгонял).

Март: 44,75 % и 5,93 % без СС — искажены одним днём, см. находки.

### 3. Одно число: отчётная прибыль выше на 350 526 604,34 ₽

Из чего складывается (витрина `daily_marketplace_kpi`, 171 дата
03-28…09-14; колонки `gross_margin_amount` и `ebitda_amount` там всегда 0,
их никто не считает — прибыль собрана из компонентов):

```
выручка выкупов          1 057 878 216,46
− комиссия                 432 129 622,41
− логистика                 12 740 103,08
− прочее                    28 991 073,83
− реклама                   83 841 546,49
= прибыль без СС           500 175 870,65
− себестоимость выкупов    350 526 604,34
= прибыль с СС             149 649 266,31
```

Перепроверка: (а) агрегат по SKU вместо дат — 350 596 657,94, разница
70 053,60 = тот самый SKU с двумя article (второй путь подставил цену по
запасному article); (б) доля СС стабильна 30,7–33,8 % в апреле–августе;
(в) выручка выкупов в витрине и в таблице расходится на 1 555 582,32
(0,15 %) — витрина захватывает 09-14, таблица заканчивается 09-13.

Коридор числа: по подразумеваемым штукам 339 717 962,93 (−3,08 %);
ненайденные 0,56 % при средней доле 33 % добавили бы ~1,96 млн. Итого
339,7 … 352,5 млн, центральное 350,5 млн.

### 4. Допущение: одна цена на весь период

До снимка 05-20 — 268 125 633,39 ₽ выручки = 25,4 % (март 52,99, апрель
146,98, май до 19-го 68,16 млн) и 91 599 761,49 ₽ себестоимости (26,1 %).
Каждый 1 % отличия цены на этом отрезке = 915 997,61 ₽; 10 % = 9,16 млн.
Если металл к маю подорожал, СС ранних месяцев завышена и прибыль
занижена на эту величину. Размер неизвестен до второго снимка.

### 5. Золотой артикул — заменено в §8

Прогон диагностического правила за 2026-05-16 (только чтение, кампании
24375352 и 24375331): **net estimate 42 425,56 ₽** при COGS 29 390,06 из
таблицы (`cogs_source = article_unit_costs`, предупреждение
`cost_snapshot_newer_than_date` — 05-16 раньше снимка, так задумано).
Компоненты: выкупы 2 × 29 390,06 = 58 780,12; выручка 223 513,00;
комиссия 93 875,46; логистика 156,03; прочее 2 858,69; реклама 25 417,14.
Прежние 36 161,71 при 32 963 на сегодняшних расходах дают 35 279,68 —
разница 882,03 от переклассификации расходов 09-11, а не от COGS;
вклад COGS ровно 2 × 3 572,94 = 7 145,88. Через `--cogs 32963` старое
значение уже не воспроизвести: таблица имеет приоритет над флагом.

### 6. Ночь 09-15 — ещё не было

Сейчас 2026-09-14 ~20:00 UTC, прогон в 00:15 UTC.

### Находки по ходу (не чинил)

- **31 марта: 46 842 023,46 выкупов одним днём**, 2 523 позиции при
  обычных 60–80 и 2,6 млн; комиссия за день 709 388,32 = 1,5 % при норме
  ~42 %. Гипотеза: сводное начисление за период до границы проекта.
  Завышает прибыль без СС за март ~на 19 млн; в «одно число» не входит.
- **Витрина по SKU не готова к себестоимости:** в `daily_sku_kpi` article
  есть у 1 918 товаров, 68 % выручки выкупов без него; её сумма выкупов
  930,8 млн против 1 056,3 млн в таблице. Второй путь перепроверки через
  неё не работает; встраивать СС туда нельзя без SKU → offer_id.
- Помесячно витрина расходится с таблицей выкупов на ±12 млн при почти
  равном итоге — витрина не пересчитана (§9-7).

### 7. `/v1/finance/realization/by-day` за 2026-08-31 — доступен, один вызов

Пункт добавлен в inbox после того, как я его прочитал, поэтому в первой
редакции отчёта его нет; выполнен следом, отдельным коммитом. Один POST
без ретраев (`requests` напрямую, не `http_retry`), тело `{day: 31,
month: 8, year: 2026}`, ответ сохранён в scratchpad сессии.

**Доступен по нашей подписке:** HTTP 200, 107 715 байт, 209 строк, поле
только `rows`. Спека: данные не старше 32 календарных дней от текущей
даты — историю за март–июль этим методом не взять.

**Суммы за дату (по всем строкам):**

```
                              продажи (delivery)   возвраты (return)   нетто
amount                            2 177 006,78         134 161,36     2 042 845,42
bonus                             2 209 726,25         149 403,90     2 060 322,35
bank_coinvestment                    21 688,97           1 189,74        20 499,23
standard_fee                      1 964 086,66         127 855,53     1 836 231,13
total (итого к начислению)        2 444 335,34         156 899,47     2 287 435,87
quantity                                   217                   7             210
commission, compensation, stars, pick_up_point_coinvestment — нули во всех строках
```

**Соотношение с 2 060 322 из 4 123 667 в кабинете:** `bonus` нетто
(продажи − возвраты) = **2 060 322,35** — число владельца до копейки.
А `amount` нетто + `bonus` нетто + `bank_coinvestment` нетто =
2 042 845,42 + 2 060 322,35 + 20 499,23 = **4 123 667,00** — это наша
выручка выкупов за 08-31 из accrual (`marketplace_buyouts`, сверена с
кабинетом ранее). Тождество снято на одной дате, выводов из него не
делаю — только факт.

**SKU и стыковка:** в каждой строке `item.sku`, `item.offer_id`, `barcode`,
`name`; строк без sku нет. 173 различных SKU — ровно те же 173, что в
`marketplace_buyouts` за 08-31 (только в ответе 0, только у нас 0);
все 173 offer_id есть в заказах и в `article_unit_costs`.

Ещё факт, без вывода: в ответе есть `quantity` — 217 единиц продаж
против 201 позиции в наших выкупах за ту же дату (accrual количества
не отдаёт, см. п. 1 выше).

### Чего не делал

В БД не писал. Витрины и KPI не пересчитывал. Таблицу себестоимости не
менял. FBS, органика, флаги, Render — не трогал. К Ozon API — один
вызов из п. 7, больше ничего.

---

## 2026-09-14, пятая задача (`article_unit_costs`: схема и скрипт — без запуска)

Один коммит в `main`, номер в `git log`. В БД не писал, таблицу не
создавал, к Ozon API не ходил, файл не редактировал. Тесты: 530 OK.

### `.gitignore` — проверено

`git check-ignore -v data/cost_20260520.xlsx` → `.gitignore:18:data/cost_*.xlsx`.
Файл untracked и в коммит не попадает; изменение `.gitignore` закоммичено.

### 1. Схема — `sql/20260914_create_article_unit_costs.sql`

Заменяет `sql/20260518_create_article_unit_costs.sql` (никогда не
применялась, модель `article + valid_from/valid_to` без даты снимка) —
старый файл удалён из репозитория, чтобы не применили его по ошибке.

```
primary key (offer_id_norm, snapshot_date)
marketplace_code   text  default 'ozon'    читатель фильтрует по нему
offer_id           text                     как в файле и кабинете, с регистром
offer_id_norm      text                     lower(offer_id); check offer_id_norm = lower(offer_id)
snapshot_date      date                     для этого файла 2026-05-20
unit_cost          numeric(14,2) > 0        «Себестоимость»
unit_cost_uniform  numeric(14,2)            «единая», справочно
variant_type       main | select | discount префикс F / S / T
article_1c, size, insert_category, product_kind, metal_fineness, weight_g,
discontinued, source_file, source_row, loaded_at
индексы: (marketplace_code, offer_id_norm, snapshot_date desc); (article_1c, snapshot_date)
```

**Регистр: выбрал нормализованную колонку с первичным ключом по ней**,
а не сравнение при стыковке. Почему: читатель ищет списком через `in_`,
а регистронезависимый `ilike` в PostgREST списка не принимает — вышло бы
по запросу на ключ; уникальность на уровне стыковки должна держать база,
а не договорённость; коллизий по `lower()` в файле нет (86 799 уникальных
до и после). Исходный `offer_id` хранится рядом. Это отклонение от
формулировки «ключ (offer_id, snapshot_date)» ровно в регистре.

### 2. Не грузятся 33 offer_id = 11 номеров × 3 префикса

Скрипт находит их сам (разная `Себестоимость` у одного offer_id), не
грузит, не усредняет, печатает поимённо со строками файла. Список в
`cost_of_goods.md`; в заказах за 90 дней их нет.

### 3. Скрипт — `scripts/load_article_unit_costs.py`, прогнан в режиме плана

```
venv/bin/python3 scripts/load_article_unit_costs.py --file data/cost_20260520.xlsx --snapshot-date 2026-05-20            # план, db_writes = 0
venv/bin/python3 scripts/load_article_unit_costs.py --file data/cost_20260520.xlsx --snapshot-date 2026-05-20 --apply    # запись
venv/bin/python3 scripts/load_article_unit_costs.py --check-only --snapshot-date 2026-05-20                              # только сверка
```

Вывод плана на файле (выполнено 2026-09-14, без БД):

```
строк прочитано: 28965
offer_id получено (три колонки): 86895, уникальных по lower(): 86799
пропущено: пустой offer_id 0, пустая/нулевая себестоимость 0,
           конфликтных offer_id 33, дублей с той же ценой (свёрнуто) 63
к записи: 86766 строк   (main 28922, select 28922, discount 28922)
```

86 895 − 63 дубля − 33 конфликта = 86 799 уникальных; 86 799 − 33 = 86 766 к
записи. Дубли — один и тот же 1С-артикул дважды в файле с одной ценой,
берётся первая строка.

С `--apply` скрипт: проверяет, что таблица есть (иначе стоп с указанием
на миграцию); отказывается писать, если за этот `snapshot_date` строки
уже есть (нужен `--force`, upsert по ключу); пишет батчами по 500;
печатает «строк записано», сравнивает с числом строк в таблице за снимок
и, если не равны, говорит это вслух; затем сверка с `marketplace_orders`
за 90 дней: ключей, найдено, доля выручки, первые 20 ненайденных.
Ожидание в самом выводе: 4 621 из 4 622, не найден `F007780899-ТП`
(26 389,00, новее снимка) — 99,997 % выручки. Ваши 99,97 % считались с
учётом регистра; без регистра выше, разница — четыре ключа `-Изгт`
на 205 835 ₽.

**Порядок запуска после вашего слова:** (1) миграцию — в SQL-редакторе
Supabase (из кода DDL применить нечем, только ключ PostgREST; могу
через MCP `apply_migration`, если скажете); (2) скрипт с `--apply`;
(3) прислать вывод.

**Читатель адаптирован** — `load_article_unit_costs` в
`reports_ozon_ad_diagnostic_rule.py`: искал `article`/`valid_from`,
которых в новой схеме нет, и на живой таблице молча вернул бы
«таблицы нет». Теперь ищет по `offer_id_norm`, берёт последний снимок
не новее даты расчёта, а если все снимки новее (проект с 03-28, снимок
05-20) — самый ранний с предупреждением `cost_snapshot_newer_than_date`.
Шесть тестов в `tests/test_article_unit_costs_reader.py`. В ночной
прогон этот код не входит (диагностическое правило — CLI, decision —
`--skip-decision`).

### 4. Золотой артикул — заменено; что опиралось на 32 963

Заменено в CLAUDE.md §8 и в коде: `KNOWN_SKU_COGS["1300079194"]` =
29 390,06 (`reports_ozon_ad_diagnostic_rule.py`), тест обновлён.

Что сверялось против эталона и теперь под вопросом (список, без
пересчёта):

1. **CLAUDE.md §8, эталон 2026-05-16: net estimate 36 161,71 ₽** — считан
   при COGS 32 963; при 29 390,06 не пересчитан, помечено в §8. TACOS
   (7,64 % и 1,01 %) от COGS не зависят — остаются.
2. **`KNOWN_SKU_COGS` как fallback диагностического правила
   (`reports_ozon_ad_diagnostic_rule.py`)** — любые dry-run отчёты по
   SKU 1300079194 с `cogs_source = known_sku_cogs` считали чистую оценку
   от 32 963. В БД они не писались: таблицы `daily_sku_order_forecast_economics`
   не существует (проверено 2026-09-14, PGRST205), в
   `sku_decision_daily_input` себестоимости нет. Под вопросом только то,
   что читали глазами из консоли.
3. **`reports_sku_order_forecast_economics.py`** — та же цепочка
   `resolve_cogs_for_sku`; результаты никуда не сохранялись (см. п. 2).
4. **Тесты** — `test_ozon_ad_diagnostic_rule.py` (16 мест) и
   `test_sku_order_forecast_economics.py` (2 места) передают 32 963 как
   явный параметр фикстуры, а не как истину о товаре; на выводы не
   влияют, менять не стал.

Вне этого списка эталон никуда не входил: `grep` по `32963`, `36161`,
`net estimate` в коде и документах даёт только §8 и перечисленное.

### Дополнение 2026-09-14, по слову владельца: миграция применена

Через MCP `apply_migration` в проект `pkrsrwjrlurlfpdyixei` (совпадает с
`SUPABASE_URL` в `.env`), SQL — тот же, что в `sql/20260914_create_article_unit_costs.sql`.
Проверка запросом к базе: строк 0, колонок 18, ограничения
`article_unit_costs_pkey`, `_unit_cost_positive`, `_variant_type`,
`_offer_id_norm_lower`, индексы `idx_article_unit_costs_lookup` и
`_article_1c`, комментарий на таблице есть. Загрузчик не запускал —
владелец запускает сам.

### Чего не делал

Скрипт с `--apply` не запускал, маржу не считал,
витрины не трогал. FBS, органика, флаги, Render — не трогал.

---

## 2026-09-14, четвёртая задача (провал 09-11 — измерено; себестоимость — стыковка)

Один коммит в `main`, номер в `git log`. К Ozon API — один прогон FBO на
чтение (разрешён п. 0), записей в БД — 0, `article_unit_costs` не создана,
файл не редактирован.

### 0. Провал 09-11: отмен столько же, заказов было меньше

Прогон `get_fbo_postings(days_back=30)` в 11:59 UTC: 6 321 отправление
(утром 6 324 — окно сдвинулось на час), 64 обращения, 0 × 429, 133 с.
По датам (дата — локальная, как считает загрузчик; суммы — qty × price
по товарам):

```
дата        всего  cancelled  доля    сумма неотменённых  сумма отменённых  ср. чек неотм.
2026-09-09    159      45     28,3 %     2 749 539,00       1 128 001,00      24 119
2026-09-10    174      33     19,0 %     2 397 653,00       1 041 066,00      17 005
2026-09-11    117      19     16,2 %     1 217 551,00         885 012,00      12 424
2026-09-12    152      23     15,1 %     2 226 889,00         539 628,00      17 263
2026-09-13    142      18     12,7 %     2 281 726,00         284 844,00      18 401
2026-09-14     69       3      4,3 %     1 490 605,00          17 101,00      22 585
```

Суммы неотменённых за 09-11 … 09-13 совпадают с БД после добора
(1 217 551 / 2 226 889 / 2 281 726) — ключи те же. Доля отмен убывает к
свежим датам, потому что отмены накапливаются; 09-11 в этом ряду на
месте (16,2 % между 19,0 и 15,1). **Отмен не больше.** Всего отправлений,
включая отменённые, 117 против 174 и 152 у соседей; сумма с отменёнными
2,10 млн против 3,44 и 2,77. Заказов 11-го действительно было меньше, и
чек ниже. Не наша поломка. Причину не называю; арбитр — кабинет.

### 1. Стыковка — ключ offer_id, покрытие 99,97 % выручки

Ключей в `marketplace_orders` за 90 дней (с 2026-06-16): 4 622, выручка
803 564 138,00.

```
ключ                         найдено          выручка
offer_id (три колонки)       4 617 (99,9 %)   803 331 914,00 (99,97 %)
числовой id из файла             2 ( 0,0 %)       572 494,00 ( 0,07 %)
ни по одному                     5 ( 0,1 %)       232 224,00 ( 0,03 %)
```

Три колонки нужны все: у нас в `article` есть T-префикс (490 ключей) и
S-префикс (143). По offer_id ни один ключ не даёт двух разных
себестоимостей. Числовой id в файле — это `product_id` Ozon, а не SKU
заказов: проверено по `sku_catalog` (737405231 → F000283615, наш SKU
заказов 1300079194); потому и 2 совпадения из 3 228.

Пять ненайденных — в документе поимённо: четыре — регистр (`-Изгт` у
Ozon против `-ИЗгт` в 1С), без регистра находятся; один
(`F007780899-ТП`, 26 389 ₽, единственный заказ 09-12) в файле
отсутствует — гипотеза: новее снимка 05-20. Список «первых 20» короче
двадцати, потому что ненайденных всего пять.

### 2. Неоднозначности — описаны

`Себестоимость` ≠ `единая` в 22 888 строках (у советника 22 893 —
округление). Наши ключи — варианты с размером, `Себестоимость` ложится
1:1; `единая` сотрёт размеры. На 90-дневных продажах: разница у 3 116
ключей (52,9 % выручки), Σ(Себестоимость − единая) × qty = −7 026 683,04.
Пример с кольцом по девяти размерам — в документе.

«33 offer_id с двумя себестоимостями» — это 11 номеров × 3 колонки. Все
11 перечислены; в наших заказах за 90 дней их нет.

### 3. Чего нет — зафиксировано

История; новые товары после 05-20; составляющие не сходятся в 16 359
строках по формуле с вычетом экономии (без вычета — в 3 491: у части
строк экономия вычтена, у части нет); `Маркетплейс` пуст в 11 447 строках.

### Попутно: хардкод COGS золотого артикула не из файла

§8 CLAUDE.md: `F000283615`, COGS 32 963. В файле у него 29 390,06.
Числа 32 963 в файле нет ни в одной колонке-итоге; 32 963,25 — сумма
четырёх составляющих **первой строки файла**, другого товара
(`F007752682-ИЗгт`). Гипотеза: взято не из той строки. Пометил в §8 как
гипотезу, эталон не пересчитывал.

### 4. Результат

`docs/cost_of_goods.md` (новый), CLAUDE.md §8 (пометка), §9-1, §10.
`data/cost_20260520.xlsx` в git **не положил**: 8 МБ закупочных цен,
класть или нет — решает владелец; сейчас файл лежит untracked.

### Чего не делал

Таблицу не создавал, в БД не писал, маржу не считал, файл не редактировал.
FBS, органика, флаги, Render — не трогал.

---

## 2026-09-14, третья задача (закрытие дыры FBO в документах, вопрос про 09-11, ночь)

Один коммит в `main`, номер в `git log`. Обращений к Ozon API — 0, записей в БД — 0;
только чтение `marketplace_orders` и `/tmp/backfill_fbo_write.json`.

### 1. Документы — сделано

- `docs/ozon_postings_migration.md`: добор — четвёртой строкой таблицы
  замеров (135 с, 64 обращения, 0 × 429, с записью, 603 строки), с
  условиями и сверкой; раздел про инцидент помечен «закрыт 2026-09-14».
  Точное время окна не сохранено — скрипт печатал только `days_back`;
  так и записано.
- `docs/STATUS.md`: строка из «Сломано» убрана, в «Работает» — заказы FBO
  с датой закрытия, две строки в хронике сентября.
- `CLAUDE.md`: §0 и §6 — «закрыто 2026-09-14», §9-4а — что смотреть утром.

**Скрипт удалил** (`git rm`, восстановим из `4d82f40`, если понадобится).
Почему: это скрипт с записью в боевую таблицу и зашитой датой. Полежав в
`scripts/`, он через месяц выглядит как рабочий инструмент, и кто-нибудь
запустит его «добрать» — а он перепишет 09-09 … сегодня агрегатами
текущего окна. Пометка в шапке от этого не защищает: how-we-work прямо
говорит, что пробная запись — тоже запись. Его логика (окно 30 дней,
запись только за полностью покрытые даты, сверка множествами после
записи) описана в документе миграции и в outbox от второй задачи.

### 2. Провал 09-11 — по FBS не просел, причина неизвестна

Доля `cancelled` по датам из JSON **извлечь нельзя**: в файле только итоги
(отправлений, обращений, строк по датам), самих отправлений там нет, а
мои утренние прогоны их тоже не сохраняли. Без нового обращения к API
эту цифру не получить — по условию задачи не делал.

FBS за те же даты из БД (отказов у FBS не было):

```
дата        FBO строк   FBO сумма       FBO ср./шт   FBS строк   FBS сумма       FBS ср./шт
2026-09-09     113      3 074 024,00      22 115        175      5 638 575,00      19 179
2026-09-10     124      2 427 843,00      16 629        168      5 164 717,00      23 691
2026-09-11      88      1 217 551,00      12 055        173      5 215 202,00      25 073
2026-09-12     123      2 226 889,00      16 495        215      6 463 325,00      20 454
2026-09-13     115      2 281 726,00      17 552        236      7 743 787,00      22 316
```

Средний чек здесь — сумма на штуку (`orders_amount_buyer / orders_qty`),
не на строку, поэтому числа отличаются от inbox (там сумма на строку).

**FBS 11-го не просел**: 173 строки против 168 накануне и 215 назавтра,
сумма 5,2 млн между 5,2 и 6,5. Просел только FBO, и по числу строк, и по
сумме, и по среднему чеку. Что известно, а что нет:

- строки за 09-11 в БД отражают статусы на момент добора (09-14); у
  09-09 и 09-10 отмен успело накопиться ещё больше, а они не просели —
  «старше, поэтому больше отмен» провал не объясняет;
- 09-11 — день, когда у Ozon начались 429 на методах заказов, но наш
  сбор за эту дату сделан 09-14 полным окном без единого отказа, то есть
  недобора с нашей стороны нет: ключей от API 603, в БД все 603.

Открытый вопрос: FBO за 2026-09-11 на 29 % ниже соседей по строкам и на
45–50 % по сумме при ровном FBS. Причина неизвестна. Проверить дёшево
можно кабинетом Ozon за 11-е (заказы FBO по дням) — это внешний арбитр,
API не нужен.

### 3. Перед ночью — одной строкой

Изменится: шаг «Ozon: загрузка FBO заказов» пойдёт на `/v3` (64 страницы
по 100 с паузой 1,5 с вместо 7 по 1000 без пауз; на 429 — 60 с × 3, затем
исключение вместо частичной записи; нефатальный). Смотреть утром: в
логе шага строки «Ozon FBO postings страница N HTTP status: 200» и
«Получено FBO postings: … за … страниц», затем «✅ … записаны … строк»;
в БД строки FBO за 2026-09-14 (сейчас 60 за неполный день — станет
больше); в утреннем алерте нет `ozon_fbo_orders_missing`. Если шаг упал —
в Telegram «⚠️ Шаг не выполнен, прогон продолжен / Шаг: Ozon: загрузка
FBO заказов» с причиной в хвосте, остальной прогон идёт.

### Чего не делал

FBS не трогал, пайплайн не запускал, остаток отменённых не чинил,
`--skip-organic` не снимал, флаги и Render не трогал, к API не ходил.

---

## 2026-09-14, вторая задача (нефатальный FBO, добор, замеры, `--skip-organic`)

Код и документы в `main`, один коммит — номер ниже в `git log`. Тесты:
весь набор 524 OK (`PYTHONPATH=. venv/bin/python3 -m unittest discover -s tests`).

### 1. Шаг FBO нефатальный — сделано, отказ громкий проверен

`"Ozon: загрузка FBO заказов"` внесён в `NON_FATAL_STEPS` с обоснованием
в комментарии рядом; порядок шагов не менялся. Охранный тест
`test_the_list_is_narrow` расширен на третью запись,
`tests/test_fbo_step_non_fatal.py` добавлен (4 теста).

**Telegram.** `send_failure_alert` после `b1e9dff` уже различает
фатальность: нефатальный отказ уходит текстом

```
⚠️ Шаг не выполнен, прогон продолжен
Шаг: Ozon: загрузка FBO заказов
Код ошибки: 1
<последние 20 строк вывода, в них RuntimeError с причиной: «429 не прошёл за 3 попыток» / «HTTP 4xx» / «превышен предел страниц»>
```

«Пайплайн упал» в нём нет — закреплено тестом. `--skip-telegram` в
команде Render пропускает только шаг «Telegram: …», тревога об отказе им
не гасится (тест `test_alert_is_sent_even_with_skip_telegram`).
`TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` на Render стоят (список имён
переменных снят через API). Живую отправку не проверял — §2.

**Утренний блокер — НЕ замечал, починено.** `get_ozon_report_completeness`
не спрашивал про заказы вовсе: только KPI, органика, реклама и статус
Performance. Именно поэтому 09-11 … 09-13 прошли молча. Добавлены две
проверки `marketplace_orders` за целевую дату по схемам: блокеры
`ozon_fbo_orders_missing` и `ozon_fbs_orders_missing` (FBS — та же
проверка, загрузчик FBS не трогал). Dry-run за 09-13 на живой БД:

```
venv/bin/python3 alerts_telegram.py --dry-run --no-send --skip-snapshot --target-date 2026-09-13
Причины: ozon_daily_sku_organic_missing, ozon_fbo_orders_missing.
```

FBS-блокер за 09-13 не горит — строки FBS есть (236). Тесты
`test_alerts_telegram_ozon_completeness.py` переведены с позиционных
списков `side_effect` на выбор по имени таблицы (10 мест): порядок
вызовов — деталь реализации, и с шестым вызовом позиционные списки
рассыпались бы.

### 2. Добор 09-09 … 09-14 с записью — НЕ выполнен, заблокирован средой

Остаток перед записью (count=exact, после всех утренних чтений — не
изменился, чтения были только чтениями):

```
09-08 104   09-09 113   09-10 7   09-11 0   09-12 0   09-13 0   09-14 0
```

Скрипт добора готов: `scripts/ozon_fbo_backfill_write_20260914.py` — штатные 30 дней новым кодом, запись **только**
строк с `order_date >= 2026-09-09` (даты, покрытые окном целиком; старые
не трогаются), затем сверка записанного с построенным из API по
множествам ключей и по значениям, и итог по датам.

Запуск отклонён классификатором режима auto («Modify Shared Resources»)
дважды — разрешение владельца в inbox для него не аргумент. Обходить не
стал. Запустить руками из корня проекта:

```
venv/bin/python3 scripts/ozon_fbo_backfill_write_20260914.py /tmp/backfill_fbo_write.json
```

Скрипт печатает: отправлений, обращений, 429, время выгрузки, строк к
записи по датам, время записи, и после записи — ключей от API / в БД /
нет в БД / лишних в БД / расхождений значений, плюс строки и суммы по
датам. Ожидание по утреннему замеру: ~6 300 отправлений, ~64 обращения,
0 отказов, ~130 с, к записи ~500 строк за 09-09 … 09-14. «Лишние в БД»
ожидаются ненулевыми за 09-09 и 09-10 — это те же остатки отменённых,
что описаны утром; их не чинить.

Если не запустить до ночи, ночной прогон 09-15 сделает то же самое
30-дневным окном — при условии, что шаг пройдёт.

### 3. Контрольные числа — записаны

`docs/ozon_postings_migration.md`, раздел «Контрольные замеры»: таблица
из трёх строк с окном, датой, условиями. Вчерашний замер отмечен как
«окно не записано, со слов советника 38 дней»; сегодняшний — с точным
окном `2026-08-15T10:47:49Z … 2026-09-14T10:47:49Z`, паузой и размером
страницы. Утверждение вчерашней сессии «записала» не подтверждается
ни репозиторием, ни историей git — так и написано.

### 4. `--skip-organic` — зафиксировано, не чинил

`docs/ozon_organic_sales.md`, новый раздел. Коротко: флаг добавлен
коммитом `6a9705b` 2026-09-03 намеренно (гейт был случайным), в команду
Render выставлен владельцем в тот же день; точнее момент не виден — в
git команды нет, события Render фиксируют только билды и запуски.
Условие снятия из help флага («Selected CPO собран») выполнено 09-05,
флаг стоит девять дней сверх него. Записанная в STATUS.md причина (гейт)
верна до 09-03 и устарела после; строки в STATUS.md и CLAUDE.md §6
поправлены. Снятие — отдельное решение, вместе с пересчётом с 05-22 и
разбором трёх определений рекламных заказов.

### Чего не делал

FBS-загрузчик не трогал (блокер на строки FBS — это проверка, не
загрузчик). Полный пайплайн не запускал. Флаги Ozon и Render не трогал.
Остаток отменённых не чинил. Записей в БД — 0.

---

## 2026-09-14, по задаче от 2026-09-13

Всё в `main`, запушено 2026-09-14 (`8a091eb..b2fdfa8`), Render деплоит сам.
Ветки нет, поэтому `git log main..HEAD` пуст. Коммиты:

```
b2fdfa8  Fail on a posting price object without amount instead of reading zero
948dbc5  Merge branch 'migrate-fbo-cursor'
85f69b6  Describe the knowledge sources and the inbox/outbox workflow in CLAUDE.md
aba8228  Add the Ozon knowledge sources: developer chat export and the full API spec set
```

### 1. Коммит источников — сделано, `aba8228`

21 файл, 890 974 строки, без кода. Перед коммитом проверено:
`ozon-dev-chat.json` — 37 863 сообщения (37 180 `message` + 683 `service`),
первое 2022-05-13T15:20:12, последнее 2026-09-13T19:52:48. Архив `.gz`
игнорируется (`git check-ignore` → `.gitignore:15`).

### 2. Раздел в CLAUDE.md — сделано, `85f69b6`

§11 «Источники знаний и порядок работы» плюс три строки в §10.

### 3. Мерж FBO — сделано, `948dbc5`; ночь ещё не отработал

Тесты после мержа: `tests/test_fbo_cursor_migration.py` 13 OK, весь набор
517 OK (`PYTHONPATH=. venv/bin/python3 -m unittest discover -s tests`).

**Первого ночного сбора на новом коде ещё не было** — он в 2026-09-15
00:15 UTC. Вместо него замер локально, тем же кодом, без записи в БД:

```
                     контроль из inbox   2026-09-14, /v3, db_writes = 0
отправлений              8 775              6 324
обращений к API             88                 64
отказов 429                  0                  0
время                     156 с              131 с (повтор: 130 с)
```

Разницу 8 775 против 6 324 объяснить нельзя: контрольного замера в
`docs/ozon_postings_migration.md` нет — числа есть только в inbox
(`grep` по репозиторию и по истории git), окно и дата замера неизвестны.
По правилу из `how-we-work.md` сравнивать рано. Что сверено вместо этого:

- тот же 30-дневный интервал (2026-08-15T10:47:49Z … 2026-09-14T10:47:49Z)
  старым `/v2`: 6 324 отправления за 7 обращений; множества
  `posting_number` совпали в обе стороны (0 / 0);
- строки, которые построит загрузчик: 3 428 и 3 428, расхождений по qty 0,
  по сумме 0, итог `orders_amount_buyer` 69 482 228,00 в обоих;
- все 6 529 цен в `/v3` — объекты `{amount, currency}`, строк нет;
- на обращение 98,8 отправления и 2,05 с (пауза 1,5 с + сам запрос).

### Найдено по ходу: заказы FBO не пишутся с 2026-09-11

Пока сверял строки с БД, оказалось, что `marketplace_orders` для FBO
пуста за 09-11 … 09-13. Логи Render (прочитаны через API, ключ в `.env`):

```
ночь   шаг FBO                                     записано строк
09-10  7 страниц, все 200                              3 656
09-11  429 rate_limit_per_second на offset 0                0
09-12  429 на offset 0                                      0
09-13  1 страница 200, 429 на offset 1000                 508
09-14  3 страницы 200, 429 на offset 3000              1 443
```

Механизм: старый загрузчик обрывался на первом 429 и писал накопленное с
кодом 0 — это загрузчик из списка молчунов (`loader_partial_data_contract.md`,
строка 111). Сортировка ASC, поэтому доезжали только старые даты окна.
В БД по датам: 09-09 fbo=113, 09-10 fbo=7, 09-11 … 09-13 fbo=0. FBS
не пострадал: 09-11 173, 09-12 215, 09-13 236 строк, в логах отказов нет.

Начало совпадает с чатом разработчиков: с 2026-09-11 несколько продавцов
пишут, что методы заказов отдают `rate limit per second` при единичных
запросах («Заказы со вчерашнего дня не отдает», «до этого годами
работала»); 2026-08-31 текст отказа называет `max rate per sec.: 2`.
Ответа Ozon в чате до 09-13 нет. Наш шаг FBO идёт сразу после FBS
(четыре обращения подряд без пауз) — этого стало достаточно.

Новый код: пауза 1,5 с перед каждой страницей, на 429 — 60 с и до трёх
попыток, затем исключение вместо частичной записи. Днём 14-го при таких
паузах 0 отказов на 64 обращения. Если ночью пройдёт, окно в 30 дней само
доберёт 09-10 … 09-14.

**Риск, не решён:** шаг FBO в пайплайне фатальный. Если три паузы по 60 с
не хватит, прогон остановится до расходов, рекламы и KPI. Это развилка,
которую не обсуждали, поэтому не трогал. Варианты: внести шаг в
`NON_FATAL_STEPS` (частичной записи теперь нет, терять нечего) или
переставить FBO после рекламы.

### Найдено по ходу: отменённые заказы остаются в БД

Из 6 324 отправлений окна 2 299 (36 %) со статусом `cancelled`. Загрузчик
их пропускает, но upsert по ключу `(date, sku, schema)` не удаляет строки,
у которых не осталось отправлений. За 08-16 … 09-09 в БД 3 956 строк FBO
на 75 485 481,00; из них 1 119 ключей свежий сбор не даёт вовсе —
15 843 585,00, то есть 21,0 % суммы. Ещё у 52 ключей сумма в БД больше на
474 356,00. Тот же класс, что осыпание WB, только в плюс. Не чинил —
вне задачи, и это решение про модель данных.

### 4. `posting_price` — сделано, `b2fdfa8`

Объект без `amount` (нет ключа, `None`, пустая строка) — `RuntimeError`
с sku и ценой. Тест на три формы добавлен. Не менял: `price` вообще
отсутствует → по-прежнему 0,0, это закреплено тестом
`test_missing_price_is_zero` из ветки; по спеке поле не `required`.

### Попутные правки документов (в этом же коммите с отчётом)

- CLAUDE.md §1: живая команда Render содержит `--skip-organic`, файл
  этого не знал; снята через API 2026-09-14. Там же: логи читаются
  через API, «только по панели» неверно.
- CLAUDE.md §0, §6, §9 и STATUS.md: поломка FBO и остаток отменённых.
- `docs/ozon_postings_migration.md`: замер 14-го и инцидент 11-го.

### Чего не делал

FBS не трогал. Полный пайплайн не запускал. Флаги не трогал. Render не
менял. Все проверки без записи: к Seller API 64 + 7 + 7 + 7 обращений
(один сбор `/v3`, три `/v2`), к Supabase только чтение.
