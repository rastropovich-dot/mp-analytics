# Отчёт рабочей сессии — WB

Сюда WB-сессия пишет результат по задаче из `docs/inbox_wb.md`. Советник
читает этот файл сам. Требования к отчёту те же, что в `docs/outbox.md`:
что сделано с номерами коммитов; что не сделано и почему; числа, а не
оценки; любое расхождение объясняется в той же строке; «готово» — только
о проверенном; работа в ветке — с `git log main..HEAD`.

---

## 2026-09-25, девятая задача WB — §2–§4 слиты в `main` по слову (`8e3dba3`, Render live 13:20 UTC; ярлыки одни с Ozon, коэффициент выкупа `buyout_rate_for_finrez`, время `build_rows` 988 → 915 с); §1 — утром 09-26 по будильнику 07:47 UTC

Обращений к WB API — **0**, 429 — **0**; **db_writes = 0** (всё — чтение базы через PostgREST и `execute_sql`, файлы на диске).
Будильник поставлен: одноразовый cron в сессии на 07:47 UTC 09-26 (10:47 МСК), §1 — по чек-листу восьмой. Ветка `wb-fixes`
(`git log origin/main..HEAD` — в конце блока); тесты модуля **18** (было 13), полный набор **1 077 OK**. Другим сессиям
не писал. Замер §4 — до правок на коде `d4fb322`, после — на этом коммите; приёмка §2 — логи
`logs/finrez_wb_check_sep_v5_20260925.out`, `…_aug_v5_…`, коэффициент — `logs/finrez_wb_buyout_rate_apr_sep_20260925.out`.

### §2. Ярлыки категории и бренда — одни на обе площадки

1. **Категория** — `OWNER_CATEGORIES = ("кольца", "серьги", "подвески", "цепочки", "браслеты", "пирсинг", "колье", "броши")`
   продублирован в модуле (не импорт), тест `test_owner_categories_equal_the_ozon_dictionary` разбирает
   `scripts/ozon_product_catalog.py` (`ast`, из того же дерева — после мержа `e78b822` файл в worktree = основному) и
   сверяет кортеж; `CATEGORY_BY_SUBJECT` переписан на строчные. Предметов в данных 11 (воронка) / 10 (отчёт): 8 ложатся в
   словарь; в «прочее» — «Ювелирные иконы» (воронка 31 nmId, заказов на 4 739 ₽; отчёт 3 строки, 2 450 ₽), «Упаковки для
   украшений» (1 nmId; отчёт 72 строки, 17 712,15 ₽), «Ювелирные запонки» (3 nmId, 0). `CATEGORY_UNKNOWN`
   («прочее (нет предмета)») оставлен, число строк с ним печатается в приёмке: сентябрь 1 … 21 — 0, август — 0 (см. ниже).
2. **Бренд** — `brand_of` перевёрнут: первая буква артикула первична (`t` → **«Топаз»**, иначе KARATOV); бренд WB
   (`brand_name` строки / `brand` карточки) — только когда артикула нет, и тогда нормализуется (`BRAND_NORMALIZE`:
   «КОЮЗ Топаз» → «Топаз», неизвестный → KARATOV). Строк с nmId без артикула в отчёте и в воронке — **0**, так что
   откат по бренду WB в данных не срабатывает. По данным правило буквы держится: в воронке все 16 824 строки буквы `t`
   (101 nmId) — «КОЮЗ Топаз», буква `f` — KARATOV или пусто; в отчёте буквы `t` 32 455 строк «КОЮЗ Топаз», и **3 строки
   буквы `f` с брендом «КОЮЗ Топаз»** — nmId 258805843 `f000280828` (цепочка, продажа 08.04 на 67 763 ₽ + 2 возмещения):
   по букве это KARATOV, WB считает Топазом; единичный случай, по правилу владельца — буква.
3. **«Неопознанный Товар»** — nmId 99866376, артикул так и называется («неопознанный товар»); в отчёте 02-03 … 07-27 —
   **239 строк**, продаж 0: логистика 101 строка на **20 570,00**, возмещение издержек 138 строк на **374,75**. Теперь —
   как строки без товара (`is_unidentified` по артикулу или бренду): деньги уходят в строку «(без товара)» того же дня,
   отдельных строк с этим nmId в «Данных» больше нет; сборка печатает счётчик («… строк за N дн., логистика …»). В окне
   книги 04-01 … 09-24 — **141 строка отчёта за 39 дней** (логистика 13 090,00, rebill 244,10, продаж 0) слились в «(без товара)»: строк «Данных» стало 71 532 → **71 493, −39** — ровно те 39 строк с брендом «Неопознанный Товар», что советник видел в книге; суммы книги не изменились (контроль в §4) (было 39 строк «Данных» с брендом «Неопознанный Товар» — как у советника).
4. Применено везде, где ярлыки выходят из модуля: `build_rows` → «Данные WB выкупы»; `build_split` по бренду и категории
   (группы — из строк); `orders_rows_for_finrez` → строки WB в «Данных заказы». `SHOP` = «KARATOV» не тронут.
   Множества ярлыков — `BRAND_LABELS` = {KARATOV, Топаз, (без товара)}, `CATEGORY_LABELS` = словарь ∪ {прочее, прочее (нет
   предмета), (без товара)}; `main` печатает «⊆ владельца — да/нет» и возвращает код 1 при нарушении, тесты — на фикстурах
   и на строках заказов; `build_rows(..., stats=)` отдаёт счётчики ярлыков и «неопознанного».
5. **Приёмка** (после правок): сентябрь 1 … 21 (`--check --buyout-rate --xlsx`, 84 с) — мост к «WB - месяц» **остаток 0,00 во всех 9 строках**, итоги те же, что в v4 (оборот 16 667 220,00, комиссия 6 994 883,76, СС 5 359 751,81, фин. рез. 1 965 675,95); «ярлыки: бренды ['(без товара)', 'KARATOV', 'Топаз'] ⊆ владельца — да; категории ['(без товара)', 'браслеты', 'колье', 'кольца', 'пирсинг', 'подвески', 'серьги', 'цепочки'] ⊆ владельца — да; строк «прочее (нет предмета)» 0»; «Σ категорий по месяцам = общему по месяцам: да». Август (143 с) — остаток 0,00 в 9 строках, итоги = v4 (фин. рез. 3 796 599,54), ярлыки ⊆ да, «прочее (нет предмета)» 0, категория у 10 231 из 10 231. Окно книги 04-01 … 09-24 (`--buyout-rate`, 644 с): «Данные» 71 493 строк, категория у 71 316 из 71 316 (100 %), «прочее» 170 строк (иконы, упаковки, запонки), броши 53; бренды {(без товара), KARATOV, Топаз}; Σ категорий = общему по всем 6 месяцам; Σ продаж, комиссии, СС, логистики, рекламы, эквайринга, прочего и фин. реза за окно до копейки равны прогону до правок (§4). Книги — `data/reports/finrez_wb_2026-09.xlsx`, `…_2026-08.xlsx`.

### §3. Коэффициент выкупа WB — измерено четырьмя способами, в контракт добавлен `buyout_rate_for_finrez`

Только чтение (`execute_sql`): продажи отчёта реализации (Продажа − Возврат, `retailPriceWithDisc`; штуки — `quantity`),
заказы воронки (`marketplace_orders_analytics` = Σ `wb_funnel_products_daily` по месяцам до рубля — проверено на
04 … 09), `marketplace_orders` для ночного правила. Сентябрь незрелый — справочно.

| месяц | заказы воронки ₽ / шт | (а) календарный: продажи по `saleDt` / заказы месяца, ₽ / шт | **(б) когорта месяца заказа (`orderDt` МСК), ₽ / шт — принят** | (в) воронка `buyoutSum` / `orderSum`, `buyoutCount` / `orderCount` по дню заказа | (г) ночное правило `buyouts_qty` / (`orders_qty` + `cancelled`) из `marketplace_orders` |
|---|---:|---:|---:|---:|---:|
| 04 | 150 786 934 / 7 066 | 0,5432 / 0,5633 | **0,5188** / 0,5504 | 0,5188 / 0,5504 | 0,5194 (3 980 / 7 663) |
| 05 | 137 311 494 / 7 736 | 0,4959 / 0,5101 | **0,5131** / 0,5430 | 0,5133 / 0,5432 | 0,4333 (3 946 / 9 107) |
| 06 | 139 016 221 / 7 612 | 0,3739 / 0,4463 | **0,4760** / 0,5126 | 0,4760 / 0,5126 | 0,4144 (3 397 / 8 197) |
| 07 | 281 675 661 / 12 890 | 0,5234 / 0,5507 | **0,5261** / 0,5566 | 0,5265 / 0,5567 | 0,6086 (7 099 / 11 665) |
| 08 | 131 804 895 / 4 714 | 0,5353 / 0,6555 | **0,4221** / 0,4652 | 0,4253 / 0,4652 | 0,7385 (3 090 / 4 184) |
| 09 (незрелый) | 20 289 276 / 716 | 0,8433 / 0,8422 | 0,3746 / 0,3799 | 0,3746 / 0,3799 | 0,9422 (603 / 640) |
| **итого 04 … 08** | 840 595 205 / 40 018 | 0,5231 / 0,5337 | **0,4981** (418 677 011,08 / 840 595 205) / **0,5337** (21 359 / 40 018) | | |

Расхождения объяснены: **(а) против (б) — лаг доставки.** По ₽ на заказы прошлого месяца приходится 13,8 % продаж апреля,
10,8 % мая, 18,6 % июня, 16,3 % июля, **34,8 % августа, 56,1 % сентября**; лаг `saleDt − orderDt`: медиана 3,0 / 3,1 / 3,1 /
3,9 дн. (апрель … июль), **7,0** в августе, **8,3** в сентябре, p90 — 7,8 / 7,7 / 8,5 / 9,0 / **15,1 / 23,8**: с августа
заказ едет к продаже вдвое дольше, и календарный способ завышает август (0,5353 против 0,4221 — июльские заказы
продались в августе) и сентябрь (0,8433). **(б) против (в)** — одно и то же определение (воронка тоже относит выкуп ко
дню заказа): апрель до рубля (78 223 111 = 78 223 111), дальше воронка выше на 25 152 / 0 / 122 237 / 423 179 ₽
(0,04 … 0,8 %) — воронка не снимает возвраты и считает продажу раньше отчёта; в штуках равны или +1. **(г)** — ночное
правило (`reports_daily_sku_kpi.buyout_rate` = `buyouts_qty` / `created_orders_qty`, `created` = `orders_qty` +
`cancelled_orders_qty` из `marketplace_orders`; в алерте 0,9562 = 589 / 616 за 1 … 22.09): числитель календарный
(по `saleDt`), знаменатель — другой источник (`supplier/orders`, история восстановлена 09-22 по `flag=1`), помесячно
7 663 / 9 107 / 8 197 / 11 665 / 4 184 созданных против 7 066 / 7 736 / 7 612 / 12 890 / 4 714 в воронке — 0,41 … 0,74,
для коэффициента листа не годится.

**Зрелость когорты** (доля итога по ₽, собранная к N-му дню после конца месяца): к 10-му — апрель 99,4 %, май 99,3 %,
июнь 97,6 %, июль 98,2 %, август 98,2 %; к 25-му — 100,06 / 100,10 / 100,14 / 99,95 % (выше 100 — поздние возвраты),
август на 09-25 = свой 25-й день. Отсюда `BUYOUT_RATE_MATURE_DAYS = 25`: месяц зрел, когда конец месяца + 25 дней ≤
сегодня — на 09-25 это апрель … август, сентябрь зреет с 10-25.

**Функция** `buyout_rate_for_finrez(month_from, month_to, sb=None) -> {ярлык месяца: Decimal, "итого": Decimal}` —
способ (б) по ₽ (`by="qty"` — штуки), только зрелые месяцы, «итого» = Σ числителей / Σ знаменателей по ним; незрелые не
входят (в `details` — с `mature=False` и датой зрелости). Читает отчёт узким `SELECT` (7 колонок) по ключу с 1-го числа
`month_from` по сегодня и сводки дней `marketplace_orders_analytics` — одна страница вместо 490 тыс. строк воронки.
В `main` — `--buyout-rate` печатает таблицу ₽ и штук с числителями. Тест — на фикстуре (когорты, возврат, строка без
даты заказа, зрелость по дате). Живой прогон 04 … 09 на 09-25: ₽ — апр 0,5188, май 0,5131, июн 0,4760, июл 0,5261, авг 0,4221, **итого 0,4981**; шт — 0,5504 / 0,5430 / 0,5126 / 0,5566 / 0,4652, итого 0,5337; сентябрь незрелый (0,3745 / 0,3799 на сегодня, зрел с 10-25); прочитано 250 461 строк отчёта (251 страница по ключу) и 6 месяцев сводок. «Свод» WB считается по-прежнему с выкупом 100 %.

**Вопрос владельцу (одной строкой):** «Свод» WB оставить с выкупом 100 % (K = `orderSum` × 0,58 / 1,22) или, как у Ozon,
умножать на измеренный коэффициент (₽ по когорте: апрель 0,5188, май 0,5131, июнь 0,4760, июль 0,5261, август 0,4221;
итого апрель … август 0,4981)?

### §4. `build_rows` — где время (окно книги 04-01 … 09-24, как у Ozon-сборки)

Замер скриптом сессии (обёртка над `execute` PostgREST считает обращения; всё по шагам, в памяти; только чтение):

| шаг | до: с / обращений | после: с / обращений | строк | что изменено |
|---|---:|---:|---:|---|
| `load_report_rows` (rr_date 04-01 … 10-01) | 190,4 / 251 | 151,6 / 251 | 250 461 | страницы по ключу (`rr_date`, `rrd_id`) вместо `offset` — это и есть 190,4 → 151,6 (замер «после» читал полный `SELECT`); плюс `BUILD_SELECT` — 21 колонка вместо 24 (без `doc_type`, `for_pay`, `cashback_discount`: их читает только мост через `build_daily`, `main --check` берёт полный `SELECT`), по умолчанию в `build_rows`; замерен отдельно на том же окне: **выигрыша нет** — 146,4 с против 145,3 с у полного `SELECT` (251 страница в обоих случаях): время страницы держит сервер и RTT, не ширина строки; оставлен, числа не трогает |
| `load_costs` (снимок 1С целиком) | 32,7 / 87 | 36,0 / 87 | 86 766 | — |
| `load_ads_db` | 8,6 / 23 | 8,5 / 23 | 139 дней | — |
| `load_product_dictionary` (keyset) | 301,3 / 493 | 291,0 / 493 | 492 128 → 5 202 nmId | — |
| `load_ads_nm_db` | 108,5 / 258 | **73,8 / 172** | 257 464 → 171 599 | фильтр `sum > 0` на сервере: нули в доли не входят (веса берут только v > 0), отрицательных нет (min 0) |
| сборка `build_rows` | 1,3 / 0 | 1,6 / 0 | 71 532 | — |
| листы (`build_month_sheet`, `build_split` × 2) | 2,0 / 0 | 2,2 / 0 | | — |
| `load_funnel_products` (для заказов) | 342,6 / 493 | 349,4 / 493 | 492 128 | — |
| `orders_rows_for_finrez` | 0,9 / 0 | 0,8 / 0 | 50 006 | — |
| **итого** | **988,3 / 1 605** | **914,7 / 1 519** | | |

`build_rows` целиком (отчёт + СС + реклама + словарь + nm + сборка) — 642,8 с до (Ozon-сессия намерила 615,9 — тот же
порядок), 562,5 с после; **контроль чисел до / после**: Итого продажи 437 081 974,08, комиссия 161 991 111,33, СС 170 558 054,11, логистика + хранение 2 802 088,02, реклама 13 996 818,03, эквайринг 9 942 762,93, прочее 10 870 707,47, фин. рез. 17 313 883,17 — **до и после равны до копейки**; строк заказов 50 006 = 50 006 (stats equal = True); строк «Данных» 71 532 → 71 493 (слияние «неопознанного», §2.3); бренды до ['(без товара)', 'KARATOV', 'КОЮЗ Топаз', 'Неопознанный Товар'] → после ['(без товара)', 'KARATOV', 'Топаз']; категории до с заглавной → после словарь строчными. Разброс между прогонами есть и без правок: словарь 301,3 → 291,0 с, воронка 342,6 → 349,4 с при тех же 493 страницах — сеть, не код; выигрыш keyset по отчёту 190,4 → 151,6 с и фильтра nm 108,5 → 73,8 с (258 → 172 страницы) больше этого шума. Сама сборка — 1,3 с: всё время — чтение.

**Страница 1 000 против 10 000** (обращений на тех же строках): отчёт 251 → 26, СС 87 → 9, реклама 23 → 3, словарь
493 → 50, nm 258 → 26, воронка 493 → 50 — **1 605 → 164** (−90 %); настройку «Max rows» не трогал.

**Что ещё можно ускорить без изменения чисел моста** (не делал — либо нужен объект в базе, либо меняет строки заказов):
1. **Словарь товаров 301 с / 493 страницы** — читается только ради «Наименования» (предмет и бренд теперь идут из
   строк отчёта, откат по артикулу не срабатывает: строк с nmId без артикула 0). Вьюха / RPC «последняя карточка по
   nmId» (`distinct on (nm_id) … order by day desc`) отдала бы 5 202 строки за 6 страниц вместо 493 → −295 с; это
   объект в базе — по слову. Без базы: читать последние 14 дней окна (2 900 nmId из 5 202, 27 страниц) — у 2 302 nmId
   пропадёт название, числа не изменятся; не делал без решения.
2. **Воронка для заказов 342,6 с / 493 страницы** — строк с заказами 22 719 (23 страницы при фильтре
   `or(order_count.gt.0,order_sum.gt.0)` на сервере); остальные нужные строки — пары (день, nmId) с рекламой без
   заказов: 27 287 на 8 825 099,88 ₽ рекламы (51,7 % списаний апрель … август), у всех `buyoutCount` = `cancelCount` = 0,
   то есть их можно собрать из словаря без чтения строки. Единственное число, которое сдвинется: 83 пары с рекламой
   без строки воронки в тот день (33,07 ₽) сейчас теряются, при сборке из словаря войдут. Ожидаемо 493 → ~24 страницы,
   −320 с; делать — по слову, с приёмкой Σ по месяцам до = после и +33,07 по рекламе.
3. **`load_costs` 32,7 с / 87 страниц** — весь снимок 1С (86 766 строк) ради ~2 200 артикулов WB; адресное чтение по
   артикулам пачками ≈ 45 обращений и в 40 раз меньше данных → ~−20 с; мелко, не делал.
4. Один проход за окно книги для всех месяцев — уже так: Ozon-скрипт зовёт `build_rows` один раз на всё окно.

### §5. Мерж в `main` — готово, main = `8e3dba3`, Render live 13:20 UTC (слово владельца 13:1x UTC)

`origin/main` перед мержем — `37c3ecc` (тридцать седьмая Ozon-сессии, 13 коммитов после `e78b822`, live 12:07:57 UTC);
влит в ветку без конфликтов (`92dba3e`, 12 файлов Ozon). Новый `report_finrez.py` зовёт модуль так: `build_rows(…, sb=)`,
`build_month_sheet`, `build_split`, `load_funnel_products(d, d, sb=)` по дню, `orders_rows_for_finrez(d1, d2, sb=,
funnel_rows=, costs=, stats=)` — все сигнатуры и ключи `stats` на месте. Полный набор на голове ветки — **1 088 OK**.
Мерж-коммит `--no-ff` плюмбингом **`8e3dba3`** (родители `37c3ecc` + `92dba3e`, дерево = ветке) — **push в `main`
13:19:52–13:19:55 UTC**; ветка `wb-fixes` переведена на него и запушена. **Render по API (6 обращений `GET
/v1/services/{id}/deploys`): `mp-analytics` — live 13:20:41 UTC, `mp-analytics-telegram-report` — live 13:20:37 UTC**, оба
на `8e3dba3`, за 10 ч 55 мин до 00:15. Ночь 09-26 для модуля ничего нового не несёт (ночной пайплайн его не зовёт; книга
утром 09-26 в 07:30 UTC соберётся уже на ярлыках владельца и с `--buyout-rate`-функцией в контракте — Ozon-скрипт её
подхватит своей задачей).

### `git log origin/main..HEAD`

```
(этот коммит)  Ninth WB report §5: merged into main (8e3dba3), Render live 13:20 UTC
```
После мержа `origin/main..HEAD` — только этот коммит отчёта; до мержа ветка несла `c0f9413` (§2–§4) и `d4fb322` (восьмая, §7):

```
c0f9413 Ninth WB task: §2 one set of brand/category labels with Ozon, §3 buyout_rate_for_finrez (order-month cohort, mature months), §4 keyset report read + sum>0 nm filter (988 → 915 s, numbers equal; narrow SELECT no gain); 18 module tests, 1 077 total
d4fb322 Eighth WB report §7: §6 merged into main (e78b822), Render live 09:40 UTC
```


### Что не сошлось — одной строкой

3 строки отчёта буквы `f` с брендом «КОЮЗ Топаз» (nmId 258805843, продажа 67 763 ₽ 08.04) — по правилу буквы уходят в
KARATOV; воронка выше отчёта по когорте на 0,04 … 0,8 % (возвраты, ранняя продажа); лаг заказа до продажи вырос вдвое с
августа (медиана 3 → 7 дн.); «Данных» стало на 39 строк меньше при тех же суммах (слияние «неопознанного товара» — по задаче); шум замера между прогонами до 10 с на шаг без правок кода.

---

## 2026-09-24 … 25, восьмая задача WB — закрыта: пять слов 09-24 (`main` `353acae`), **§1 утро 09-25 — ночь чистая**, **§6 владельца слито 09-25 (`main` `e78b822`, Render live 09:40 UTC)**

Записей в базу — две, по слову (ответ советника ~13:00 UTC): миграция двух колонок и посев предмета/бренда; остальное
`db_writes = 0`. Обращений к WB API — **567**: `sales-funnel/products` 538 (съём 04-01 … 08-31, в том числе 8 повторов после 429),
`adv/v3/fullstats` 2 (проба 1 … 7 июля) + 26 (съём февраль–август), `adv/v1/upd` 1 (живой dry-run шага «WB: реклама»);
**429 — 8 (все у воронки, изжиты), таймаутов — 0** по логам. Книга-образец читалась на месте.
Ветка: текст задачи `1b28019`; тесты **1 053 OK** (1 032 + 21).

### §1. Утро 09-25 — первая ночь на всём новом: чисто

Лог Render (`GET /v1/logs`, 00:00 … 09:00Z, 2 153 строки — `logs/render_20260925/night.jsonl`) и база (`execute_sql`).
Прогон **00:15:36 → 02:51:30 UTC**, «✅ Весь пайплайн успешно завершен», **«Шаги не выполнены: нет»**, `pipeline_run:last`
— `failed_steps: []`. Тревог «Шаг не выполнен» и Traceback нет. 429 за ночь — 5 повторов `http_retry`, все в шагах Ozon
(«штуки выкупов» 1, «total orders analytics по SKU» 4); **в шагах WB — 0**.

| шаг WB | время UTC | по логу | по базе |
|---|---|---|---|
| загрузка заказов | 00:15:40–00:15:46 | как прежде | 09-24: 12 строк, 12 шт / 195 812, отменённых 0 |
| Analytics Sales Funnel | 00:15:45–00:27:43 (12 мин) | окно **09-11 … 09-24**, 14 дней, **28 обращений** (по 2 страницы), `HTTP: 200` — 28, 429 — 0, ⏳ — 0; карточек 25 903, записано 25 903; **застрявших удалено 10** — две карточки с нулём заказов, исчезнувшие из ответа после бэкфилла 09-24 (nmId 926793558 на 09-11 … 09-17, 833714159 на 09-12 … 09-14), чистка по правилу | `wb_funnel_products_daily`: с `observed_at` 09-25 — **25 903 строки, 14 дней 09-11 … 09-24**; всего 492 128 строк, 177 дней; сводка 09-24 — 14 шт / 231 418, `loaded_at` 00:27:39 |
| продажи/выкупы | 00:27:42–00:28:17 | внутри шага отчёт: `HTTP 200, строк 12122`, `обращений 1, 429 — 0`, `upsert 12122`, застрявших нет; **`источник: wb_sales_report_rows (отчёт собран этой ночью: строк 12122, обращений 1, 429 — 0; окно 2026-09-04 … 2026-09-24)`**; выкупы окна: ключей 408, **добавить 7, переписать 0, без изменений 401, застрявших 0** → `записаны … из отчёта реализации: 7 строк` | `marketplace_buyouts` WB 09-24: **7 строк, 7 шт, 232 565,00** = отчёт по `saleDt` МСК 09-24 (7 nmId, 7 шт, 232 565) |
| отчёт реализации | 00:28:16–00:28:18 | **«отчёт за 2026-09-24 уже собран 00:27:43 UTC этой ночью (моложе 6 ч) — повторно в API не хожу; обращений 0»** | строк с `observed_at` 09-25 — 12 122, окно 09-04 … 09-24, `max(rr_date)` = **09-24**; 1 … 21.09 = 16 667 220,00; **предмет и бренд у всех 16 продаж 09-23 / 09-24** (9 продаж 09-23 без предмета после посева — дописаны ночью) |
| реклама | 00:28:17–00:28:28 | `adv/v1/upd` окно 08-25 … 09-24: HTTP 200, строк 0, обращений 1; по номенклатурам: «списаний в окне нет … обращений 0» | `wb_ad_spend_nm_daily` с `observed_at` 09-25 — 0 (ожидаемо) |
| остатки | 00:28:26–00:29:18 | 2 739 строк ответа | как 09-24 |

**Витрина после ночного KPI (пересчёт 02:47 UTC):** Σ выкупов WB `daily_sku_kpi` по месяцам = `marketplace_buyouts` на
всех 8 месяцах до копейки (02: 123 169 045,57 … 08: 70 561 190,29, 09: 17 110 228,00), штуки равны. **`buyout_rate` WB
1 … 22.09 — 0,9562 = 589 / 616** (вчера 0,9608 = 589 / 613: ночь переписала окно заказов, созданных стало 616; выкупы те же).
Обращений к WB API этим утром — 0.

**Дополнение к §1 — три замера второй WB-сессии (запущена владельцем 09:25 UTC как «продолжение»; только чтение),
проверены мной заново:**

* **Пересмотра воронки за окно нет:** Σ `order_sum` и Σ `order_count` в `wb_funnel_products_daily` по дням 09-11 … 09-23
  после ночи = файлам съёма 09-24 (`data/wb_funnel_raw/`) на всех 13 днях до рубля (421 043 / 24 … 457 292 / 18); карточек
  на день (таблица − файл) −1 … +3 (09-11: 1 813 → 1 814; 09-12: 1 872 → 1 871; 09-22: 1 802 → 1 805) — новые карточки с
  нулями и выбывшие; в первой редакции стояло «−2 … +2» — написано на глаз, поправлено по замеру второй сессии.
  Десять удалённых застрявших ключей — карточки, продавшие последнюю штуку 09-24 (остаток WB 1 → 0); суммы дней от
  чистки не изменились.
* **Алерт 07:30 ушёл** (лог `mp-analytics-telegram-report`): `sendMessage` 07:31:09 UTC, `message_id 289`; книга Ozon с
  листом WB доставлена 07:33:23 (`sendDocument` HTTP 200, 406 272 байт, sha256 `af53be9e2b5c…`): «лист WB: строк отчёта
  15 194, дней 24, молодых 1; оборот 17 110 228,00, комиссия 7 179 936,88, логистика 215 761,39, фин. рез.
  1 906 709,75» — оборот сентября = таблице выкупов.
* **Знаменатель `buyout_rate` 613 → 616:** шаг заказов WB переписал все 490 строк 09-01 … 09-22 (`observed_at` ночи у
  490 из 490), подтверждённых 354 + отменённых 262 = 616.

### §6 инбокса (дополнение владельца 24.09) — сделано в ветке

1. **«Данные WB выкупы» — 15 полей ровно как в кэше сводной владельца**, в этом порядке и с этими именами: Дата · Месяц ·
   Магазин · Артикул поставщика · Продажи, ₽ · Реклама, ₽ · Комиссия, ₽ · Логистика, ₽ · Себес-ть, ₽ · Хранение, ₽ ·
   Ост.расходы и компенсации МП, ₽ · Наименование · Бренд · **Статус (= категория)** · **Месяцы (= номер месяца)**;
   наши добавки справа: nmId · Эквайринг, ₽ · Соинвест, ₽ · Штуки · справочные (возмещение ПВЗ, rebill) · реклама
   разнесена по · без СС, шт. `DATA_COLS` модуля — в этом порядке (`OWNER_DATA_FIELDS`, тест на слово в слово);
   «Площадка» из первых колонок убрана (в кэше владельца её нет; ключ `platform` в строках остался).
2. **Словарь товаров без `statement_timeout`**: `read_keyset` — страницы по первичному ключу (`day`, `nm_id`) с фильтром
   «после последнего ключа», без `offset`, только колонки `day, nm_id, vendor_code, title, brand, subject_name`, окно дней =
   диапазон книги. Страница — **1 000 строк, не 10 000**: PostgREST режет ответ до 1 000 независимо от `limit` (первый
   прогон с 10 000 молча получил одну страницу и 1 000 nmId — в августе 608 строк «Данных» остались без категории, поймано
   приёмкой). Замер: сентябрь 1 … 21 — **39 964 строки, 3 218 nmId, 19,7 с, 40 страниц**; август — **81 187 строк,
   4 421 nmId, 37,5 с, 82 страницы**. Предмет для nmId без карточки в воронке: сначала из любой строки того же nmId в
   загруженных строках отчёта (`subject_name`/`brand_name`), затем адресно из `wb_sales_report_rows` по списку nmId
   (`fetch_subjects_for`, пачки по 200); **категория есть у 100 % строк «Данных» с товаром** — сентябрь 3 257 из 3 257,
   август 10 231 из 10 231 (остатка нет). Откат на `marketplace_buyouts.product_name` убран (не нужен).
3. **Строки заказов без пустых**: `orders_rows_for_finrez` не отдаёт карточки с `orderSum` 0, заказов 0 и рекламой
   номенклатуры 0 (`keep_empty=True` возвращает всё); реклама номенклатуры = `updSum` дня × доля `fullstats`
   (`nm_ads_by_day_and_nm`). Замер: **сентябрь 1 … 21 — строк 39 964 → 525 (1,3 %)**, Σ orderSum 19 117 898 до = после,
   по месяцам равны; **август — 81 187 → 5 065 (6,2 %)**, Σ 131 804 895 до = после, строк с рекламой 3 218 на
   1 019 872,97 (Σ `updSum` августа 1 019 881,00: 8,03 ₽ — nmId без карточки в воронке в тот день, в строки заказов не
   попали; в «Данных WB выкупы» реклама разносится по строкам продаж и Σ = списаниям). Чтение воронки за месяц — 50 … 74 с.
   Запись xlsx — на стороне Ozon-скрипта.
4. Приёмка прежняя: мост к «WB - месяц» — остаток 0,00 за 1 … 21.09 и за август (`logs/finrez_wb_check_*_v4_20260925.out`);
   заголовок «Данных» = 15 полям образца (тест). Тесты `tests/test_report_finrez_wb.py` — 16 (+5: 15 полей, страницы по
   ключу, откаты предмета, фильтр пустых с суммами). Побочно: тест «уже собран этой ночью» был привязан к календарю и упал
   утром — переведён на относительное время.

### Выполнено по слову (три слова, ~16:20 UTC) — таблица и запись по номенклатурам, запись воронки, мерж

1. **Миграция `wb_ad_spend_nm_daily` применена ~16:23 UTC** через MCP (`create_wb_ad_spend_nm_daily`, DDL =
   `sql/20260924_create_wb_ad_spend_nm_daily.sql`: PK (`day`, `advert_id`, `app_type`, `nm_id`), 2 индекса). **Запись
   `scripts/wb_ads_nm_backfill.py --apply --approve-wb-ads-nm-write --date-from 2026-02-01 --date-to 2026-08-31` —
   16:24:15–16:31:36 UTC** (`logs/wb_ads_nm_backfill_apply_20260924.out`): по 7 месяцам upsert 54 972 / 48 786 / 55 777 /
   66 323 / 66 307 / 45 954 / 23 103, «застрявших ключей нет» × 7, итог `upsert 361222 строк, застрявших удалено 0`; к WB — 0.
   **Контроль SQL 16:32 UTC:** `count(*)` = **361 222**, Σ `sum` = **32 343 490,37**, один `observed_at` (16:24:29), дни
   02-01 … 08-31; по месяцам строки и Σ — ровно план (02: 54 972 / 7 048 145,31 … 08: 23 103 / 1 041 860,55); кампаний в
   месяце 125 / 118 / 168 / 313 / 216 / 78 / 94 = спрошенным. Дней со статистикой 212 против 198 дней со списаниями: лишние
   14 — 08-18 … 08-31 (после остановки рекламы), Σ `sum` на них 633,97 — хвосты кампаний без биллинга; при нормировке к
   `updSum` = 0 в деньги не попадают.
2. **Запись воронки апрель–август помесячно** (`--apply --approve-wb-funnel-write`, пять запусков **16:24:19–16:37:40 UTC**,
   `logs/wb_funnel_backfill_apply_apr_aug_20260924.out`): upsert 85 101 / 91 953 / 91 027 / 97 459 / 81 187 строк, сводок
   дней 30 / 31 / 30 / 31 / 31, «застрявших ключей нет» на всех 153 днях, удалено 0; к WB — 0. **Контроль SQL 16:38 UTC:**
   по месяцам строки / Σ `order_sum` / дни = плану до рубля (04: 85 101 / 150 786 934 / 30 … 08: 81 187 / 131 804 895 / 31),
   один `observed_at` на месяц; `marketplace_orders_analytics` по месяцам = колонке «Σ orderSum сырьё» (150 786 934,0 …
   131 804 895,0; заказов 7 066 / 7 736 / 7 612 / 12 890 / 4 714); таблица целиком — **490 307 строк, 176 дней 04-01 … 09-23**.
   Истина — сегодняшний сбор (август вверх на 19 днях принят владельцем).
3. **Мерж** — `origin/main` влит в ветку дважды за вечер: `ea37ca3` (тридцать седьмая, 10 коммитов; конфликт только в
   `.gitignore`, обе стороны оставлены; `report_finrez.py` Ozon-сессии мой модуль пока не импортирует — «блок WB пусто»)
   и `0f7a6ee` (+2), полный набор **1 069 OK**; мерж-коммит, push и Render — §6.

### Выполнено по слову (ответы советника, ~13:00 UTC)

1. **Ответ 1 — `rebillLogisticCost` из «Логистика ₽» убран** (инструкция владельца «возмещение издержек по перевозке — не
   берём»): справочная колонка в «Данных», в форме не участвует; мост пересчитан — ниже.
2. **Ответ 2 — «Магазин» = KARATOV** (константа `SHOP`).
3. **Ответ 3 — миграция применена 13:15 UTC** через MCP (`add_wb_sales_report_subject_brand`; DDL =
   `sql/20260924_add_wb_sales_report_subject_brand.sql`: две nullable-колонки `subject_name`, `brand_name` в
   `wb_sales_report_rows`); загрузчик отчёта пишет обе с первой ночи после мержа (`FIELDS`). **Посев из сырья на диске**
   (`scripts/seed_wb_sales_report_subjects.py --apply --approve-wb-subjects-write`, 13:16:36–13:21:21 UTC,
   `logs/wb_subjects_seed_apply_20260924.out`): сырьё 17 файлов, 329 522 строки, с предметом/брендом **228 182**, без —
   101 340 (операции без товара: возмещение издержек по перевозке 76 777, возмещение ПВЗ 24 118, удержание 283, хранение 148 …);
   запись `update … in_(rrd_id)` пачками по 300 одинаковых значений — **770 запросов, 285 с**, ничего кроме двух колонок не
   тронуто (upsert целых строк из старого сырья затёр бы ночные значения окна). **Контроль SQL 13:22 UTC:** строк с предметом
   **228 182**, пар предмет × бренд **18** (= плану, все 18 «обновлено N из N»), даты 02-01 … 09-22; без предмета 101 513 =
   101 340 без товара + 173 строки за 09-23 (сырья на диске нет — доберёт ночь: окно отчёта); продаж/возвратов без
   предмета — **9, все 09-23, 174 465 ₽**. Категория формы — из колонок строки, карточки воронки — откат.
4. **Ответ 4 — съём `fullstats` за февраль–август**: `scripts/wb_ads_nm_backfill.py --fetch`, **26 обращений 13:15:22–13:26:00 UTC,
   429 — 0, 5xx — 0**, 1 112 кампаний-месяцев спрошено = 1 112 в ответах, 77 МБ (`data/wb_ads_nm_raw/`, вне git). **Февраль
   отдал → глубина ≥ 235 дней.** План из файлов (`logs/wb_ads_nm_backfill_plan_20260924.out`):

| месяц | пачек | кампаний | строк nm | Σ nms.sum | Σ updSum | отношение |
|---|---:|---:|---:|---:|---:|---:|
| 02 | 3 | 125 | 54 972 | 7 048 145,31 | 6 942 541,00 | 1,0152 |
| 03 | 3 | 118 | 48 786 | 8 026 249,47 | 7 985 817,00 | 1,0051 |
| 04 | 4 | 168 | 55 777 | 6 187 446,06 | 6 142 629,00 | 1,0073 |
| 05 | 7 | 313 | 66 323 | 3 818 215,45 | 3 760 961,00 | 1,0152 |
| 06 | 5 | 216 | 66 307 | 4 121 000,42 | 4 085 750,00 | 1,0086 |
| 07 | 2 | 78 | 45 954 | 2 100 573,11 | 2 066 897,00 | 1,0163 |
| 08 | 2 | 94 | 23 103 | 1 041 860,55 | 1 019 881,00 | 1,0216 |
| итого | 26 | | **361 222** | 32 343 490,37 | 32 004 476,00 | **1,0106** |

   nms без nmId — 0, дней вне месяца — 0, все месяцы полные. **Правило чтения принято и записано в модель**
   (`wb_report_model.md` §14): истина по деньгам — `updSum`; доли по номенклатурам из `fullstats` нормируются к
   списаниям дня, Σ по дню = списаниям; в `report_finrez_wb.build_rows` — `ads_allocated = fullstats`, в дни без
   статистики — по продажам дня (тест на нормировку есть).

### §2. «Выкупы WB» книги «Фин рез» — `scripts/report_finrez_wb.py`, мост к «WB - месяц» сошёлся

**Образец** (`owner_finrez_wb_buyouts.xlsx`, лист «Свод») — сводная без формул: строки «апр … июл», затем дни «01.июл …
20.сен», «Общий итог»; колонки B … H с НДС и K … Z. **Тождества проверены по значениям на четырёх месяцах** (остаток
0,00): N = (K − L)/1,22; R = **(Логистика + Хранение)/1,22** — у второго кабинета хранение сидит внутри «Логистика без
НДС»; T = E/1,22; X = H/1,22; Y = P − R − T − V − X; доли M = L/K, остальные от N; V = 0. Форма воспроизведена ровно
так; единственное отличие — V (эквайринг) заполнен, сказано в подвале.

**Модуль**: договорные сигнатуры `build_rows(month_from, month_to, date_to=None, sb=None)`, `build_month_sheet(rows)`,
`build_split(rows, by='brand'|'category')`, `SHEET_COLS`, `DATA_COLS` (+ §4: `load_funnel_products`, `orders_rows_for_finrez`);
свой `--xlsx` (листы «Выкупы WB», «… по брендам», «… по категориям», «Данные WB выкупы») и `--check`. «Данные» — строка
на (день продажи `saleDt` МСК, nmId) по колонкам задачи; операции без nmId (возмещение ПВЗ 7 015 строк за 1 … 21.09,
хранение 21, удержание 1) — строка «(без товара)» за день; категория — предмет карточки из `wb_funnel_products_daily`
(откат — `product_name` выкупов, куда старый загрузчик писал предмет): покрыто 1 614 из 1 636 nmId с продажами,
непокрыто 1 661 932 ₽ из 702,0 млн (0,24 %) → «прочее (нет предмета)»; бренд — карточка, иначе буква (`t` → КОЮЗ
Топаз); реклама — списания дня из `wb_ad_spend_daily`, разнесённые по артикулам пропорционально продажам дня (флаг
«разнесено»; день со списаниями без строк отчёта — строка «(без товара)», Σ по дню = списаниям дня).

**Приёмка** (`logs/finrez_wb_check_sep_20260924.out`, `…_aug_…`; книги `data/reports/finrez_wb_2026-09.xlsx`, `…_2026-08.xlsx`):

| строка | форма 1 … 21.09 | «WB - месяц» | разница | чем объясняется (слагаемые моста, остаток 0,00) |
|---|---:|---:|---:|---|
| Оборот | 16 667 220,00 | 16 667 220,00 | 0,00 | |
| Комиссия (Σ цена × кВВ) | 6 994 883,76 | 6 994 883,76 | 0,00 | |
| Себестоимость | 5 359 751,81 | 5 359 751,81 | 0,00 | |
| Эквайринг без НДС | 330 065,83 | 330 065,83 | 0,00 | |
| Выручка без НДС | 7 928 144,46 | 7 839 198,00 | +88 946,47 | НДС за возмещение вычитается только у владельца |
| Логистика без НДС | 261 839,40 | 207 256,16 | +54 583,24 | хранение/НДС внутри R у второго кабинета (после ответа 1: `rebillLogisticCost` 201 747,72 с НДС — справочно, в форму не входит) |
| Прочее без НДС | 10 811,48 | 65 971,76 | −55 160,29 | −хранение/НДС 54 583,24; −577,05 НДС штрафов (у владельца штрафы без НДС, форма делит H на 1,22); additionalPayment 0 |
| Фин. рез. | 1 965 675,95 | 1 876 152,43 | +89 523,52 | +88 946,47 (НДС за возмещение) + 577,05 (НДС штрафов); additionalPayment 0 |

Август: оборот 70 561 190,29, комиссия 29 577 379,27, СС 25 840 720,50, эквайринг 1 468 257,75 — до копейки; выручка
+210 687,41 (НДС за возмещение), логистика +60 268,38 (хранение), прочее −62 601,82, фин. рез. 3 796 599,54 против
3 583 578,68 (+213 020,86 = 210 687,41 + 2 333,44) — остаток моста 0,00 везде (`logs/finrez_wb_check_*_v2_20260924.out`;
первые прогоны с rebill в логистике — `…_sep_…`/`…_aug_…` без `v2`, там фин. рез. 1 800 308,96 / 3 692 079,18). Σ брендов = Σ категорий = общий
(16 667 220,00 / 70 561 190,29). Строк «Данные»: сентябрь 3 278 (3 257 с товаром + 21 без), август 8 788 (8 757 + 31).
Категории 1 … 21.09 (строко-дней, предмет из строк отчёта после посева): Кольца 1 286, Серьги 825, Подвески 461, Цепочки 369,
Браслеты 251, Колье 34, Пирсинг 31, «прочее (нет предмета)» 0 (до посева было 8); без СС 17 шт. Тесты —
`tests/test_report_finrez_wb.py` (10: + нормировка к списаниям дня, + категория из строки).

### §3. Реклама по номенклатурам — проба, таблица, загрузчик, план бэкфилла (миграция, съём и запись — по слову)

* **Спека** (`08-promotion.yaml`): `ids` ≤ **50** (в задаче «≤ 100» — неверно), период ≤ 31 день, 3 запроса/мин,
  статусы 7 / 9 / 11; ответ — кампания → `days[]` → `apps[]` (`appType`) → `nms[]` (14 полей). Глубина спекой не названа.
* **Проба** 1 … 7 июля, 78 кампаний июля, 2 обращения (50 + 28), HTTP 200, 1,75 МБ + 0,23 МБ, 429 — 0
  (`logs/wb_fullstats_probe_20260924/`): 78 из 78 кампаний, 10 696 nm-строк, 884 nmId. **Тождество:** Σ `nms.sum` = Σ
  `days.sum` до копейки; против Σ `updSum` того же дня — **+1,6 … 1,8 % каждый день** (68 374,79 против 67 194 …
  69 005,68 против 67 768; неделя 480 022,89 против 471 869, +8 153,89, 1,0173). Не НДС (было бы 22 %), не кэшбэк
  (все списания недели «Баланс»), не без-nm списания (тогда upd > nms). Не объяснено; гипотеза — статистика кампаний
  против биллинга. Глубина ≥ 85 дней; февраль (235) — только пробой бэкфилла.
* **Таблица** `sql/20260924_create_wb_ad_spend_nm_daily.sql` — зерно (день, кампания, `appType`, nmId), как в ответе, 14 полей;
  **миграция по слову**. **Загрузчик** `loaders/wb_ads_nm_loader.py` — зовётся из шага «WB: реклама» после списаний (строка
  шага та же; `wb_ads_loader.main` дополнен): только кампании со списаниями в окне 31 день, пачки по 50, застрявшие ключи по
  спрошенным кампаниям — по правилу `stale_keys`; сейчас списаний нет → **0 обращений** (живой dry-run шага: `upd` 1
  обращение, 0 строк; «списаний в окне нет … обращений 0», `logs/wb_ads_step_dryrun_20260924.out`).
* **Бэкфилл** `scripts/wb_ads_nm_backfill.py --estimate` (из базы, 0 обращений): 02 — 125 кампаний / 3 пачки, 03 — 118 / 3,
  04 — 168 / 4, 05 — 313 / 7, 06 — 216 / 5, 07 — 78 / 2, 08 — 94 / 2 → **26 обращений ≈ 9 мин**, ответы ~7,5 МБ на пачку
  за месяц; `--fetch` (по слову), `--plan` из файлов с тождеством по месяцам, `--apply --approve-wb-ads-nm-write` по слову.
  Тесты — `tests/test_wb_ads_nm.py` (11), включая «шаг «WB: реклама» зовёт загрузчик по номенклатурам после списаний».

### §4. Воронка апрель–август — снята, план готов, запись по слову

`scripts/wb_funnel_products_backfill.py --fetch --date-from 2026-04-01 --date-to 2026-08-31` — **10:26:54 → 14:11:19 UTC,
153 дня, 538 обращений, 429 — 8** (четыре дня по два подряд: 04-16 стр. 2, 05-28 стр. 3, 07-05 стр. 3, 08-11 стр. 1 —
каждый изжит третьей попыткой после пауз 60 с; лог `logs/wb_funnel_backfill_fetch_apr_aug_20260924.out`, леджер
`data/wb_funnel_raw/calls.json`), сетевых отказов 0, неполных дней 0; страниц на день — 2: 6 дней, 3: 70, **4: 77**
(карточек 1 925 … 3 368; в задаче ждали 2 … 3 страницы и 2 … 2,5 ч — вышло 3 ч 44 мин), 1,0 ГБ сырья вне git. Глубина
метода 365 дней — апрель внутри, спека не ограничила.

**План по месяцам** (`--plan` помесячно, `logs/wb_funnel_backfill_plan_apr_aug_20260924.out`; сводка в базе —
`marketplace_orders_analytics`, что писала ночь D+1 … D+4 прежним загрузчиком):

| месяц | строк по товарам | заказов | Σ orderSum сырьё | Σ сводок в базе | разница | дней: сводки нет / равны / сырьё ниже / выше |
|---|---:|---:|---:|---:|---:|---|
| 04 | 85 101 | 7 066 | 150 786 934 | 75 522 278 | +75 264 656 | **14 нет (04-01 … 04-14, шаг воронки начался с 04-15)** / 5 / 11 (Σ −4 437 399) / 0 |
| 05 | 91 953 | 7 736 | 137 311 494 | 142 417 525 | −5 106 031 | 0 / 6 / 25 (Σ −5 106 031) / 0 |
| 06 | 91 027 | 7 612 | 139 016 221 | 140 980 684 | −1 964 463 | 0 / 14 / 16 (Σ −1 964 463) / 0 |
| 07 | 97 459 | 12 890 | 281 675 661 | 286 421 858 | −4 746 197 | 0 / 11 / 20 (Σ −4 746 197) / 0 |
| 08 | 81 187 | 4 714 | 131 804 895 | 121 632 539 | **+10 172 356** | 0 / 12 / 0 / **19 (Σ +10 172 356)** |
| итого | **446 727** | 40 018 | 840 595 205 | 766 974 884 | | сводок дней к записи 153 |

Апрель–июль: сырьё ниже базы на 72 днях — пересмотр воронки вниз задним числом (дыра 15; крупнейшие 05-24 −978 607,
07-06 −845 933, 04-19 −824 320), выше — ни одного дня. **Август — наоборот:** сырьё выше базы на 19 днях
(08-14 +1 097 237, 08-16 +1 030 418, 08-12 +1 005 082), ниже — ни одного; равны 12. Не объяснено: либо ночи августа
писали день до того, как воронка его дособрала («малая часть данных появляется в течение нескольких дней» — спека),
либо сводки августа в базе не с тех ночей; по правилу советника (окно, а не «последние N») истиной берётся
сегодняшний сбор — запись перепишет все 153 сводки. Карточки с буквой не `f`/`t` (`к`, `ц`, `б`, `т`, `c`, `r`, `1`) —
единицы в день, заказов у них 0. Строк «(без nmId)» — 0. `--apply --approve-wb-funnel-write` — **по слову** (по
месяцам, 5 запусков; обращений 0).

### §6. Мерж в `main` — готово, main = `353acae`, Render live 16:39 UTC

`origin/main` перед мержем — `dfa0d69` (тридцать седьмая), ветка его содержит (`0f7a6ee`), полный набор на голове
**1 069 OK**; мерж-коммит `--no-ff` плюмбингом **`353acae`** (родители `dfa0d69` + `7e612bf`, дерево = ветке) —
**push в `main` 16:39:06–16:39:09 UTC**; ветка `wb-fixes` переведена на него и запушена, `origin/main..HEAD` пуст.
**Render по API: оба сервиса на `353acae`, live 16:39:50 UTC** (`mp-analytics`) и **16:39:52 UTC**
(`mp-analytics-telegram-report`) — за 7,5 часов до 00:15.

Ночь 09-25 — первая для: выкупов из отчёта внутри шага продаж и шага отчёта без обращения (седьмая), колонок предмета и
бренда в новых строках отчёта, статистики по номенклатурам в шаге «WB: реклама» (списаний нет → 0 обращений), воронки
окном 14 дней в таблицу по товарам. Чек-лист — §1 (утром 09-25).

### §7. Мерж §6 в `main` — готово, main = `e78b822`, Render live 09:40 UTC (09-25)

Слово владельца 09-25 ~09:37 UTC («мерж wb-fixes (c3b4f87) в main --no-ff»); голова ветки на тот момент — `8c4eb6e`
(= `c3b4f87` + два коммита только с текстом отчёта), влита она. `origin/main` `fc5e771` (тридцать седьмая: книга «Фин рез»
уже зовёт мой модуль по договору — `build_rows / build_month_sheet / build_split / orders_rows_for_finrez`) влит в ветку без
конфликтов (`b564000`), полный набор на голове **1 072 OK**; мерж-коммит `--no-ff` плюмбингом **`e78b822`** (родители
`fc5e771` + `b564000`, дерево = ветке) — **push в `main` 09:39:29–09:39:31 UTC**; ветка `wb-fixes` переведена на него и
запушена, `origin/main..HEAD` пуст. **Render по API: оба сервиса на `e78b822`, live 09:40:08 UTC** (`mp-analytics`) и
**09:40:14 UTC** (`mp-analytics-telegram-report`). Утренняя книга 09-26 соберёт «Данные WB выкупы» уже в 15 полях владельца
и словарём по ключу.

### Вопросы (заданы ~12:00 UTC, ответы получены ~13:00 UTC и применены — см. «Выполнено по слову» выше)

1. **`rebillLogisticCost` в «Логистика ₽»** (задача §2.1) даёт +165 366,98 к логистике формы за 1 … 21.09 и −165 366,98 к
   фин. резу против «WB - месяц» владельца, где это возмещение участвует только через «НДС за возмещение». Оставить по
   задаче (сделано так) или вынести в «Ост. расходы»/справочно? Мост печатает слагаемое в любом случае.
2. **«Магазин (кабинет)»** — константа `SHOP = "KARATOV"`; как называет наш кабинет владелец в книге «Фин рез»?
3. **`subjectName` / `brandName` есть в каждой строке отчёта реализации** — предлагаю две колонки в `wb_sales_report_rows`
   (миграция по слову) и посев из сырья на диске, чтобы категория не зависела от карточек воронки.
4. **Съём `fullstats` за февраль–август** — 26 обращений, по слову (запись — отдельным словом после `--plan`).

### Слова (получены ~16:20 UTC, выполнены — см. выше)

1. **Таблица `wb_ad_spend_nm_daily`** (`sql/20260924_create_wb_ad_spend_nm_daily.sql`, через MCP) и **запись бэкфилла**
   `scripts/wb_ads_nm_backfill.py --apply --approve-wb-ads-nm-write --date-from 2026-02-01 --date-to 2026-08-31` — 361 222
   строки из файлов, обращений 0; контроль SQL после: `count(*)` = 361 222, Σ `sum` = 32 343 490,37, по месяцам = плану.
2. **Запись воронки апрель–август** — `scripts/wb_funnel_products_backfill.py --apply --approve-wb-funnel-write` помесячно
   (04-01…04-30, 05, 06, 07, 08): 446 727 строк по товарам + 153 сводки дней (14 новых, 72 пересмотр вниз, 19 вверх, 48 равны),
   обращений 0. Контроль SQL после: `count(*)` по дням 04-01 … 08-31 = 446 727, Σ `order_sum` = 840 595 205,
   `marketplace_orders_analytics` по месяцам = колонке «Σ orderSum сырьё».
3. **Мерж в `main` до 00:15 UTC** — нужен сегодня: без него ночь не запишет `subject_name`/`brand_name` в новые строки
   отчёта (9 продаж 09-23 и все с 09-24 останутся без предмета до мержа); загрузчик по номенклатурам в шаге «WB: реклама»
   без таблицы даст именованный отказ шага (нефатально) — если таблица не будет создана к ночи, скажу.

### Что не сошлось — одной строкой

Σ `nms.sum` выше Σ `updSum` на 0,5 … 2,2 % по месяцам (1,0106 за февраль–август) — в деньги не попадает (нормировка
к `updSum`); форма второго кабинета отличается от листа владельца тремя правилами (хранение в логистике, штрафы с НДС,
без вычета НДС за возмещение) — мост показывает, остаток 0,00; съём воронки дольше оценки задачи из-за третьей и
четвёртой страниц (июль — до 3 361 карточек).

---

## 2026-09-24, седьмая задача WB — четыре слова выполнены: выкупы переписаны, миграция воронки, бэкфилл воронки, мерж в `main` (`5c2535c`, Render live 10:03 UTC); §1 — утром 09-25

Записей в базу — одна, по слову владельца в окне (§2.1, выкупы WB); всё остальное `db_writes = 0`.
Обращений к WB API — **49**: `sales-funnel/products` 48 (съём 23 дней 09-01 … 09-23 по всем страницам,
09:10:43–09:30:19 UTC, `data/wb_funnel_raw/calls.json`) и finance-api `sales-reports/detailed` 1 (живой
dry-run нового пути продаж, `logs/wb_sales_loader_dryrun_20260924.out`); **429 — 0, таймаутов — 0** (по
логам, не по итогам шагов; в логе съёма `HTTP: 200` — 48 строк, `⏳` — 0). Книга владельца читалась на месте.
Ветка: текст задачи закреплён (`c4770b7`), `origin/main` `dfe3683` влит без конфликтов (`d02168d`), полный
набор — **995 OK** (961 после мержа + 34 новых; в сообщении коммита `e41194e` написано «46 tests» — неверно, 34).

### Выполнено по слову владельца (окно, ~08:58 UTC) — §2.1: выкупы WB переписаны из отчёта

План пересобран из живой базы и совпал с шестой (таблица — в блоке вопроса ниже, оставлен как был).
`--apply --approve-wb-buyouts-write --date-from 2026-02-01 --date-to 2026-09-23` — **09:01:37–09:01:56
UTC**, `logs/wb_buyouts_rebuild_apply_20260924.out`: снимок затрагиваемых строк
`data/snapshots/marketplace_buyouts_wb_before_rebuild_2026-02-01_2026-09-23_20260924_090137.csv.gz` — 15 710
строк, **sha256 7595f43e8f7c8882c7a351704ff16b41b0240b78ad4e3c8803f4730d36064926** (`_meta.json` рядом; вне
git); `✅ marketplace_buyouts: upsert 7319 строк (добавить 5518, переписать 1801); без изменений 13909 не
трогались`; `удалено 0`. К WB API — 0 обращений.

**Контроль SQL 09:03 UTC** (`execute_sql`, только чтение) — сошёлся по всем пунктам задачи:

| что | ожидание | в таблице |
|---|---:|---:|
| строк WB / различных ключей (дата, sku) | 21 228 | **21 228 / 21 228** |
| Σ `buyouts_amount_seller` / Σ `buyouts_qty` | 694 631 118,64 / 31 398 | **694 631 118,64 / 31 398** |
| по месяцам 02 … 09 (₽) | 123 169 045,57; 134 612 663,99; 81 901 574,05; 68 090 297,09; 51 977 150,28; 147 441 534,37; 70 561 190,29; 16 877 663,00 | **все 8 до копейки** (строк 2 947 / 3 075 / 2 654 / 2 770 / 2 466 / 4 614 / 2 206 / 496) |
| сентябрь 1 … 22 | 16 704 622,00 | **16 704 622,00** (489 строк, 589 шт) |
| `buyouts_qty` за 09-01 | 48 | **48** (35 строк, 1 255 921) |
| строк с `created_at` ≥ 09:00 UTC | 5 518 (добавленные) | **5 518** |
| строк раньше 02-01 | 0 | 0 |

Витрина `daily_sku_kpi` / `daily_marketplace_kpi` по WB подтянет новые выкупы ночным KPI (02-01 … 03-25 —
новые дни, апрель … август — +8,66 млн): утром 09-25 сверить Σ выкупов WB витрины с таблицей по месяцам.

### Выполнено по слову владельца (окно, ~09:45 UTC) — три слова

1. **Миграция применена 09:47 UTC** через MCP (`apply_migration create_wb_funnel_products_daily`; DDL =
   `sql/20260924_create_wb_funnel_products_daily.sql` — таблица, PK (`day`, `nm_id`), комментарий, 2 индекса;
   шапка комментариев в вызове сокращена, колонки и типы те же). `success: true`.
2. **Бэкфилл воронки записан 09:47:45–09:49:21 UTC** (`--apply --approve-wb-funnel-write --date-from 2026-09-01
   --date-to 2026-09-23`, `logs/wb_funnel_backfill_apply_20260924.out`): по каждому из 23 дней — сводка дня,
   `upsert` строк по товарам, блок застрявших «застрявших ключей нет» (23 из 23); итог
   `✅ wb_funnel_products_daily: upsert 43580 строк за 23 дней, застрявших удалено 0; сводок дней 23`. К WB API — 0.
   **Контроль SQL 09:50 UTC:**

| что | ожидание | в таблице |
|---|---:|---:|
| `count(*)` / различных (`day`, `nm_id`) | 43 580 / 43 580 | **43 580 / 43 580** |
| Σ `order_sum`, дней, границы | 20 057 858, 23, 09-01 … 09-23 | **20 057 858, 23, 2026-09-01 … 2026-09-23** |
| `observed_at` | один у всех | **один: 09:47:48 UTC** |
| `marketplace_orders_analytics` 09-07 / 09-14 | 1 778 872 / 876 746 | **1 778 872 / 876 746** (были 1 799 118 / 1 006 834) |
| сводок дней 09-01 … 09-23 / Σ | 23 / 20 057 858 | **23 / 20 057 858** (09-01 = 2 052 178, 09-23 = 457 292 — как ночью) |
| артикулы `t` (Дискаунтер) | Σ 4 479 847 (план) | **2 111 строк, Σ 4 479 847, заказов 134** |

   `loaded_at` у переписанных сводок остался ночным (у колонки только default при вставке, upsert его не
   трогает) — читать время записи по `observed_at` строк по товарам.
3. **Мерж в `main`** — ниже, §6 (после полного набора на голове ветки: **995 OK**, `origin/main` = `97e8bc2`,
   ничего нового с утра — `HEAD..origin/main` пуст).

### §2.2. Ночной источник выкупов — отчёт реализации (код в ветке, мерж по слову)

**Развилка, которой в задаче не было:** «WB: загрузка продаж/выкупов» идёт РАНЬШЕ «WB: отчёт реализации»
(строки шагов не менялись), поэтому «если шаг отчёта этой ночи прошёл» в момент шага продаж не выполнимо —
отчёт за вчера ещё не собран. Сделано так: шаг продаж (`loaders/wb_sales_loader.py`, та же команда
`python3 loaders/wb_sales_loader.py`) **сам собирает окно отчёта** (`wb_sales_report_loader.run`: 21 день,
1 обращение, upsert `wb_sales_report_rows`, чистка застрявших) и строит выкупы окна из таблицы
(`loaders/wb_buyouts_from_report.run` — те же функции, что у пересборки: Продажа − Возврат по `saleDt` МСК,
зерно (день, nmId), штуки со знаком; пишутся add / rewrite, same не трогаются; застрявшие ключи —
`stale_keys` в днях с продажами). Строка лога: `источник: wb_sales_report_rows (отчёт собран этой ночью:
строк …, обращений 1, 429 — 0; окно … )`. Шаг «WB: отчёт реализации», идущий следом, видит строки за вчера с
`observed_at` моложе 6 ч и **в API не ходит** («уже собран … повторно в API не хожу; обращений 0»; `--force`
снимает) — и остаётся вторым шансом, если первый сбор отказал. Отказ отчёта (429 / сеть не изжиты, HTTP) →
`источник: supplier/sales (отчёт не собран: …)` и старый путь без изменений (окно записи 30 дней).
`--source report|sales`, `--dry-run` (db_writes = 0). Что меняется для читателей: день D появляется
ночью D+1 (частичный «сегодня» больше не пишется), окно перезаписи 21 день вместо 30.

**Живой dry-run 09:2x UTC** (1 обращение, 429 — 0): отчёт 09-03 … 09-23 — 12 490 строк, застрявших 0;
выкупы окна — **ключей 430 = 430 в базе, добавить 0, переписать 0, застрявших 0** (история только что
переписана тем же правилом — сходится с самой собой, как и должно).
Тесты: `tests/test_wb_buyouts_from_report.py` (5), `test_wb_sales_loader_source.py` (7: строка шага не
менялась и идёт раньше шага отчёта; auto → отчёт; отчёт уже собран → выкупы из таблицы; отказ → supplier/sales
с пометкой; `--source report` падает; dry-run в оба пути), `SameNightSkipTests` (2) в
`test_wb_sales_report_loader.py`. `scripts/wb_buyouts_rebuild_from_report.py` теперь импортирует правило из
загрузчика (8 его тестов проходят без правок). **Первая ночь после мержа — §1 п. 3.**

### §3. Воронка по товарам и окном — таблица, загрузчик, бэкфилл (таблица и запись — по слову)

* **Таблица** `sql/20260924_create_wb_funnel_products_daily.sql`: ключ (`day`, `nm_id`); карточка —
  `vendor_code`, `title`, `brand`, `subject_id`, `subject_name`, `product_rating`, `feedback_rating`,
  `stocks_wb` / `stocks_mp` / `balance_sum`; статистика дня — `open_count`, `cart_count`, `order_count`,
  `order_sum`, `buyout_count`, `buyout_sum`, `cancel_count`, `cancel_sum`, `avg_price`, `add_to_wishlist`;
  `observed_at`, `loaded_at`; индексы по `day` и `vendor_code`. Не хранятся `past` / `comparison`
  (производные), `wbClub`, `conversions`, `timeToReady`. Выкупы и отмены воронки — по дню заказа (спека).
  **Миграция — по слову** (применю через MCP, как таблицы 09-23).
* **Загрузчик** `loaders/wb_sales_funnel_orders_loader.py` (тот же шаг, строка не менялась): окно **14 дней**
  до вчера, за день все страницы по 1 000, пауза 21 с между любыми запросами (лимит 3/мин по спеке); из
  тех же ответов одним проходом — сводка дня в `marketplace_orders_analytics` (как прежде, читают алерт и
  `report_management`) и строки по товарам; застрявшие карточки дня — по правилу `stale_keys` (день с
  повтором nmId между страницами — неполный, не чистится, вслух); таблицы нет — сводки дней записаны, шаг
  падает в конце с перечнем дней (нефатален по `FATAL_STEPS`, алерт покажет); `--dry-run`, `--days-back`;
  `load_dotenv` — от `__file__` (§5). **Цена ночи по факту съёма:** карточек **1 798 … 2 040** на день, у
  дней с > 2 000 карточек **3 страницы** (09-06, 09-07) → 14 дней = **28 … 42 обращения ≈ 10 … 15 мин**;
  «≤ 2 обращения на день» из задачи держится не всегда — карточек уже больше 2 000. Старые тесты шага и
  загрузчика (10) проходят без правок; новых — `tests/test_wb_funnel_products_loader.py` (8).
* **Бэкфилл** `scripts/wb_funnel_products_backfill.py`: `--fetch` снял 09-01 … 09-23 заново по всем
  страницам (снимок 09-23 из шестой — только первая страница, дни из него не собрать) — **23 дня, 48
  обращений, 429 — 0, неполных дней 0**, 95 МБ в `data/wb_funnel_raw/` (вне git); `--plan` — из файлов
  против сводок дней в базе и снимка первой страницы 09-23 (`logs/wb_funnel_backfill_plan_20260924.out`):

| | значение |
|---|---|
| строк по товарам к записи | **43 580** (23 дня × 1 798 … 2 040 карточек), сводок дней 23 |
| Σ orderSum 09-01 … 09-23 | 20 057 858 (заказов 702); Дискаунтер (`t`) 4 479 847; карточки не `f`/`t` (`ц` 42, `к` 8, `т` 20, `б` 8 карточко-дней) — заказов 0 |
| сводки дней в базе = сырью | **21 из 23**; 09-07: база 1 799 118 → 1 778 872 (**−20 246**), 09-14: 1 006 834 → 876 746 (**−130 088**) — пересмотр воронки задним числом (дыра 15), бэкфилл перепишет сводки |
| против снимка первой страницы 09-23 (по тем же nmId) | 09-01, 09-07, 09-13, 09-14, 09-15, 09-16 — **+0**: за сутки 09-23 → 09-24 orderSum карточек не менялся |

  `--apply --approve-wb-funnel-write --date-from 2026-09-01 --date-to 2026-09-23` — **по слову, после
  миграции**; тесты `tests/test_wb_funnel_products_backfill.py` (5).

### §4. Лист «Заказы WB» — в `report_wb_month.py`, приёмка сделана

Три листа в той же книге: **«Заказы WB»**, **«Заказы WB Standard»** (артикулы не `t`), **«Заказы WB
Дискаунтер»** (`t`) — колонки: День · Заказано шт / на сумму (с НДС) · **Выручка = Σ orderSum × 0,58 / НДС**
(K владельца) · Себестоимость = Σ orderCount × СС снимка 05-20 по базовому артикулу · Маржа · М-ть · Реклама
(`wb_ad_spend_daily` / НДС, только общий лист — списания на площадки не делятся) · % ДРР от заказов ·
**Соинвест = (Σ retailPriceWithDisc − Σ retailAmount) / Σ retailPriceWithDisc по строкам «Продажа» отчёта за
день (saleDt МСК), по площадке товара** — так и написано в шапке · справочно выкупы и отмены воронки по дню
заказа · карточек · заказов без СС. Источник — таблица `wb_funnel_products_daily` или `--funnel-from-files
data/wb_funnel_raw` (сегодня). Дни моложе двух суток — жёлтые и вне приёмки. Сигнатуры `build_daily`,
`total_row`, `write_xlsx` не менялись (у `write_xlsx` — новый необязательный `orders=None`); новые функции —
`load_funnel_db/files`, `coinvest_by_day`, `build_orders_daily`, `orders_total_row`, `check_orders`.

**Приёмка** `--month 2026-09 --date-to 2026-09-21 --funnel-from-files data/wb_funnel_raw --check
--check-orders --xlsx … --sheet "WB - сентябрь"` (09:3x UTC, `logs/wb_month_check_orders_20260924.out`,
книга `data/reports/wb_2026-09_to_2026-09-21.xlsx`, 4 листа). Месячный лист — те же 6 обязанных колонок
**21 из 21**, «не сошлось 0». Листы заказов против блока J–P «Заказы», K–P «Заказы Standard» и B–D «WB
Дискаунтер»:

| лист / колонка | у него | у нас | разница | дней 0,00 | ожидание задачи | чем объясняется |
|---|---:|---:|---:|---:|---|---|
| «Заказы» K Выручка | 9 902 795,43 | 9 088 836,75 | −813 958,68 | **16 из 21** | 16 из 21 | 09-07, 09-13, 09-14, 09-15, 09-16 — его снимки выше любой воронки (WB-6 §2), не искать |
| «Заказы» L Маржа | 3 004 124,41 | 3 516 610,04 | +512 485,63 | 0 из 21 | 0 | его цены 1С против снимка 05-20 |
| «Заказы» N Реклама | 0,00 | 0,00 | 0,00 | 21 из 21 | 21 из 21 | |
| «Заказы» P Соинвест | — | — | — | **14 из 21** в пределах 0,005 | 15 из 22 | то же множество: 22-й день (09-22) вне листа до 09-21; 09-03 и 09-14 — его рука |
| «Заказы Standard» K | 7 650 441,51 | 6 972 300,76 | −678 140,75 | **17 из 21** | — | те же дни снимков минус 09-16 |
| «Заказы Standard» P | — | — | — | 19 из 21 | — | 09-03, 09-14 |
| «WB Дискаунтер» B | 2 252 353,94 | 2 116 536,01 | −135 817,93 | **18 из 21** | 9 из 21 | источник теперь воронка по товарам, а не созданные из `marketplace_orders`; 09-07, 09-14, 09-16 — снимки |
| «WB Дискаунтер» C Маржа | 459 834,25 | 541 843,63 | +82 009,38 | 1 из 21 | — | его цены 1С |

Итог заказов 1 … 21.09: 661 заказ на 19 117 898,00, выручка 9 088 836,75 (Standard 6 972 300,75 +
Дискаунтер 2 116 536,00), СС 5 572 226,71, маржа 3 516 610,04; без СС 30 заказов из 661. Тесты —
`tests/test_report_wb_month.py` +6 (27).

### §5. Попутно

`load_dotenv` в загрузчике воронки — от `__file__` (вошло в переписанный загрузчик). Реестр дыр после
мержа `main`: 13 — закрыта (запись 09-24), 2 и 15 — статусы с числами приёмки и плана (в этом коммите).

### §6. Мерж в `main`, push, Render — live 10:03 UTC

Первая попытка 09:50–09:51 UTC не прошла и **правильно**: пока собирался мерж-коммит `cc5f99c` на
`origin/main = 97e8bc2`, Ozon-сессия запушила в `main` `99c1b7a` (тридцать шестая: три ветки + правка веса
покрытия СС), Render выкатил его в 09:51:54; push отклонён как non-fast-forward (плюс zsh съел `:r` в
refspec `$MERGE:refs/…` — модификатор; в `main` это не попало, ветка `wb-fixes` на `cc5f99c` была
запушена). Дальше: `origin/main` `99c1b7a` влит в ветку без конфликтов (`8b6708f`; в `main` пришёл
`scripts/book_wb_sheet.py` — он импортирует из генератора `month_days`, `load_rows_db`, `load_costs`,
`unit_cost_for`, `load_ads_db`, `build_daily`, `total_row`, `COLS` — сигнатуры не менялись), полный набор
на голове **1 032 OK**; мерж-коммит `--no-ff` плюмбингом **`5c2535c`** (родители `99c1b7a` + `8b6708f`,
дерево = ветке) — **push в `main` 10:02:54–10:02:56 UTC**; ветка `wb-fixes` переведена на него и запушена;
`origin/main..HEAD` пуст. **Render по API: оба сервиса на `5c2535c`, live 10:03:41 UTC** (`mp-analytics`) и
**10:03:42 UTC** (`mp-analytics-telegram-report`) — за 14 часов до 00:15. Промежуточный `cc5f99c` остался в
истории `main` как предок `8b6708f` — пустой по дереву относительно `c3aa3a6`, вреда нет.

Ночь 09-25 — первая для трёх вещей разом: выкупы из отчёта в шаге продаж, шаг отчёта без обращения к API,
воронка по товарам окном 14 дней (28 … 42 обращения) с записью в `wb_funnel_products_daily`. Чек-лист — §1.

### §1. Утро 09-25 — что смотреть (по логу Render и базе; допишу утром)

1. «WB: отчёт реализации» — если мерж состоится: **«уже собран … повторно в API не хожу; обращений 0»**
   (сбор сделал шаг продаж); без мержа — окно 09-04 … 09-24, ≈ 12 000 строк, обращений 1, 429 — 0;
   `max(rr_date) = 2026-09-24`; 1 … 21.09 = 16 667 220,00.
2. «WB: реклама» — обращений 1, строк 0.
3. «WB: загрузка продаж/выкупов» с мержем: `источник: wb_sales_report_rows (отчёт собран этой ночью: …)`,
   `WB выкупы из отчёта: окно 2026-09-04 … 2026-09-24 … добавить N, переписать M, застрявших 0`;
   `marketplace_buyouts` WB за 09-24 = отчёт по `saleDt` 09-24 (шт и ₽); без мержа — старый лог, сказать.
4. Funnel с мержем: окно 09-11 … 09-24, **28 … 42 обращения**, все `HTTP: 200`, `wb_funnel_products_daily`
   за 14 дней ≈ 26 000 строк (если миграция применена), сводки дней переписаны; без мержа — 4 дня, как
   09-24. Заказы WB за 09-24; `buyout_rate` WB 09-01 … 22 (вчера 0,9608) — с новыми выкупами из отчёта
   числитель за сентябрь тот же (589), измениться может только знаменатель.
5. Витрина: Σ выкупов WB `daily_sku_kpi` по месяцам = `marketplace_buyouts` (02 … 09 из таблицы контроля выше).
6. «Шаги не выполнены: нет».

### Три слова — получены ~09:45 UTC, выполнены (см. «Выполнено по слову … три слова» и §6)

1. **Миграция** `sql/20260924_create_wb_funnel_products_daily.sql` — через MCP (`apply_migration`).
2. **Бэкфилл воронки** `scripts/wb_funnel_products_backfill.py --apply --approve-wb-funnel-write --date-from
   2026-09-01 --date-to 2026-09-23` — 43 580 строк по товарам + 23 сводки дней (две меняются: 09-07
   −20 246, 09-14 −130 088), обращений 0. Контроль SQL после: `count(*)` = 43 580, различных (day, nm_id) —
   столько же, Σ `order_sum` = 20 057 858, 23 дня, `marketplace_orders_analytics` 09-07 = 1 778 872 и 09-14 = 876 746.
3. **Мерж в `main` до 00:15 UTC** (`origin/main` в ветке, 995 OK) — после миграции: без таблицы шаг воронки
   ночью запишет сводки и упадёт с тревогой «Шаг не выполнен» (нефатально), выкупы из отчёта заработают и без
   неё. Push — как 09-23 (моей рукой по слову или владельцем).

### `git log origin/main..HEAD`

```
350eff1 Seventh WB report: buyouts rebuilt by the owner's word (21 228 rows, +246 477 715,50, SQL control equal), nightly buyouts from the report, funnel by product (48 requests, 43 580 rows planned), «Заказы WB» sheets checked (K 16/21, Discounter B 18/21); three words pending
e41194e Seventh WB task: buyouts from the sales report nightly (same step line; report step skips a repeat collection), funnel by product with a 14-day window and stale keys, funnel backfill, «Заказы WB» sheets with --check-orders; 46 tests
ce951c0 Seventh WB task, question: the buyouts rebuild plan rebuilt from the live base equals the sixth (Feb–Aug line by line, September +7 keys of 09-23 unchanged); snapshot goes to data/snapshots
d02168d Merge remote-tracking branch 'origin/main' into wb-fixes
c4770b7 Keep the seventh WB task text as placed by the adviser
60fe43f Sixth WB report §1: the first night of the sales-report and ads steps ran clean (12 490 rows, max rr_date 09-23 — the report for D is there on night D+1), buyout_rate on the new formula 22 of 22, no skipped steps
(+ коммит с этим логом)
```
Общие файлы: `run_daily_pipeline.py` — не трогался (строки шагов те же); `docs/owner_workbook_gaps.md` —
статусы 2, 13, 15; `.gitignore` — `data/wb_funnel_raw/`.

### Что не сошлось — одной строкой

K владельца на пяти днях (09-07, 13, 14, 15, 16) выше нашей воронки — его снимки; L и C — его цены 1С; P на
09-03 и 09-14 — его рука; сводки воронки в базе за 09-07 и 09-14 старее сырья на 20 246 и 130 088 (пересмотр,
перепишет бэкфилл); карточек на день уже до 2 040 — ночь воронки до 42 обращений, не 28; «46 tests» в
сообщении коммита `e41194e` — на деле 34.

### Вопрос сессии (как был задан до слова) — план выкупов против шестой

План построен заново 08:5x UTC из живой базы (`scripts/wb_buyouts_rebuild_from_report.py
--plan --date-from 2026-02-01 --date-to 2026-09-23`, обращений к API 0; вывод —
`scratchpad/buyouts_plan_20260924.out`). Против плана шестой (02-01 … 09-22,
`logs/wb_buyouts_rebuild_plan_20260923.out`) — **месяцы февраль … август совпали строка в
строку** (добавить / переписать / без изменений / удалить, штуки и ₽ до и после); **разница —
только сентябрь, и это ровно день 09-23**, которого в шестой не было:

| | шестая (… 09-22) | сегодня (… 09-23) | разница | чем объясняется |
|---|---:|---:|---:|---|
| добавить | 5 518 | 5 518 | 0 | |
| переписать | 1 801 | 1 801 | 0 | |
| без изменений | 13 902 | **13 909** | +7 | 7 ключей 09-23 — ночь 09-24 записала их из `supplier/sales`, отчёт даёт то же |
| удалить | 0 | 0 | 0 | |
| шт до → после | 22 317 → 31 391 | 22 324 → 31 398 | +7 / +7 | те же 7 ключей 09-23, 7 штук |
| ₽ до → после | 447 980 362,14 → 694 458 077,64 | 448 153 403,14 → 694 631 118,64 | +173 041,00 / +173 041,00 | оборот 09-23 по отчёту = базе (шестая §1: «за 09-23: 173 строки, оборот 173 041») |
| **разница ₽** | **+246 477 715,50** | **+246 477 715,50** | 0 | |
| сентябрь | 5 / 484 / 589 шт / 16 704 622,00 → то же | 5 / 491 / 596 шт / 16 877 663,00 → то же | | 09-01 … 09-22 как в шестой; 09-23 добавился «без изменений» |
| дни, где база выше отчёта | 9 (04-06 −0,01 … 08-30 −36 544,00) | те же 9, те же числа | 0 | |
| крупнейшие переписывания | 10 ключей | те же 10, те же числа | 0 | |

Строк Продажа/Возврат с датой продажи в периоде — 32 016 (было 32 007: +9 строк 09-23), строк
базы WB — 15 710 (было 15 703: +7). Ключей на удаление — 0.

**Что случится ночью 09-25, если новый путь (§2.2) не будет слит до 00:15:** старый шаг
продаж переписывает 30-дневное окно (с 08-26) из `supplier/sales`; внутри этого окна план
трогает всего 9 ключей — 1 добавить (08-30, возврат −36 544,00, которого в `supplier/sales`
нет — старый путь его не затрёт, ключ ему неизвестен) и 8 переписать с **теми же ₽ и штуками**
(нули против null-ов) — то есть откатить ночь ничего материального не сможет
(`--plan --date-from 2026-08-26 --date-to 2026-09-23`: 1 / 8 / 704 / 0, −36 544,00).

Снимок затрагиваемых строк перед записью — `data/snapshots/marketplace_buyouts_wb_before_rebuild_
2026-02-01_2026-09-23_<ts>.csv.gz` + `_meta.json` с sha256 (путь переведён с `snapshots/` в
корне на `data/snapshots/` — как в задаче и как у Ozon; `data/snapshots/` в `.gitignore`,
корневой `snapshots/` — нет).

**Команда, которую выполню по слову (границы явные, = плану):**

```
venv/bin/python3 scripts/wb_buyouts_rebuild_from_report.py --apply --approve-wb-buyouts-write --date-from 2026-02-01 --date-to 2026-09-23
```

Ожидание после записи (контроль SQL): строк WB в `marketplace_buyouts` 15 710 + 5 518 = **21 228**,
Σ `buyouts_amount_seller` по месяцам = колонке «₽ после» (02: 123 169 045,57; 03: 134 612 663,99;
04: 81 901 574,05; 05: 68 090 297,09; 06: 51 977 150,28; 07: 147 441 534,37; 08: 70 561 190,29;
09 (1 … 23): 16 877 663,00), сентябрь 1 … 22 = **16 704 622,00**, `buyouts_qty` за 09-01 = **48**.

**Слово получено ~08:58 UTC («Сошлось — записывай»), выполнено 09:01 — см. «Выполнено по слову» выше.**

---

## 2026-09-23, шестая задача WB — §1 (утро 09-24 чисто), §2, §3, §3а сделаны; реклама в базе и в проде по слову (мерж `385aafb`)

`db_writes = 0`. Обращений к WB API — **15**: воронка `sales-funnel/products` 6 (пять дней
расхождения + контрольный 09-01, зачем — §2), `adv/v1/upd` 8 (съём истории рекламы по 31 дню,
§3а) и 1 (dry-run загрузчика); **429 — 0, таймаутов — 0** (леджеры
`logs/wb_funnel_recheck_20260923/calls.json`, `data/wb_ads_raw/calls.json`,
`logs/wb_ads_loader_dryrun_20260923.out`). Книга владельца читалась на месте. Тесты после
мержа `origin/main` (`4e6f0bf`, тридцать пятая Ozon) — **949 OK**; дыра 4 в
`docs/owner_workbook_gaps.md` переведена в «площадка найдена».

### Выполнено по слову владельца (три «да» в чате ~20:50 UTC)

1. **Миграция применена 20:51 UTC** через MCP (`apply_migration create_wb_ad_spend_daily`,
   текст = `sql/20260923_create_wb_ad_spend_daily.sql`): таблица `wb_ad_spend_daily`, 12
   колонок, PK `(advert_id, upd_time)` + 2 индекса.
2. **Бэкфилл рекламы записан 20:52:28–20:53:02 UTC** (`--apply --approve-wb-ads-write`,
   `logs/wb_ads_backfill_apply_20260923.out`): `✅ wb_ad_spend_daily: upsert 33781 строк`, к
   API — 0 обращений. **Контроль SQL 20:53 UTC:** `count(*)` = **33 781**, различных пар
   `(advert_id, upd_time)` = **33 781**, `observed_at` один у всех (20:52:28), `upd_day`
   02-01 … 08-17; июль / 1,22 = **1 694 177,87** (лист 1 694 178), 08-01…08-17 / 1,22 =
   **835 968,03** (лист 835 968), по месяцам строки и Σ = плану на всех 7 месяцах
   (02: 4 488 / 6 942 541 … 08: 1 146 / 1 019 881), итого 32 004 476,00; `upd_num` null — 0.
3. **Строка шага «WB: реклама»** (`python3 loaders/wb_ads_loader.py`) добавлена в
   `run_daily_pipeline.py` сразу после «WB: отчёт реализации» (нефатален по правилу
   `FATAL_STEPS`), 4 теста `tests/test_wb_ads_step.py`. Первая ночь — 09-24: в логе ждать
   «WB реклама: окно 2026-08-24 … 2026-09-23», «HTTP 200, строк 0», «списаний в окне нет —
   писать нечего» (реклама остановлена 18.08).
4. **Мерж в `main`:** `origin/main` `4e6f0bf` влит в ветку (конфликт только в `.gitignore`,
   обе строки оставлены), полный набор **949 OK**, мерж-коммит **`385aafb`** собран плюмбингом
   и запушен **20:54:45 UTC**; ветка `wb-fixes` переведена на него. Render — ниже, §5.
5. **`--apply` по выкупам** — не запускался: после утра 09-24 и решения по §3.3.

### §1. Утро 09-24 — первая ночь шагов «WB: отчёт реализации» и «WB: реклама» прошла чисто

Проверено 08:0x UTC по логу Render (`GET /v1/logs`, 00:00…08:00Z, 1 856 строк —
`logs/render_20260924/`) и по базе (`execute_sql`, только чтение). Прогон 00:15:43 →
02:39:02 UTC, «✅ Весь пайплайн успешно завершен», **«Шаги не выполнены: нет»**, итог записан в
`pipeline_runtime_state` (не выполнено шагов 0), cron «finished successfully» 02:39:06.

1. **«WB: отчёт реализации» 00:19:18 → 00:19:50 UTC (32 с)**, сразу после «WB: загрузка
   продаж/выкупов» (00:19:13 → 00:19:18): `WB sales report 2026-09-03…2026-09-23: HTTP 200,
   строк 12490 (всего 12490)`, `обращений 1, 429 — 0, сетевых отказов 0`, `✅
   wb_sales_report_rows: upsert 12490 строк`, блок застрявших: «окно 2026-09-03 … 2026-09-23,
   строк окна в таблице 12490, построено ключей 12490 … застрявших ключей нет». Строк не
   ~14 000, а 12 490: окно уехало на день — 09-02 (1 674 строки операций) вышел, 09-23 (173)
   вошёл. 429 по строкам `http_retry` в этом шаге — 0 (все 16 `rate_limit_per_second` ночи —
   в «Ozon: штуки выкупов», `accrual/postings`, изжиты повторами).
   **«WB: реклама» 00:19:49 → 00:20:01 UTC:** `WB реклама: окно 2026-08-24 … 2026-09-23`,
   `HTTP 200, строк 0`, `обращений 1, 429 — 0, 5xx — 0`, «списаний в окне нет — писать
   нечего» — ноль ожидаем (реклама остановлена 18.08), таблица не тронута (строк с
   `observed_at` 09-24 — 0, всего 33 781).
2. **База:** `count(*) where observed_at::date = '2026-09-24'` = **12 490** — ровно окно
   09-03…09-23, все строки окна переписаны (с `observed_at` бэкфилла в окне осталось 0);
   `count(*)` всего 329 695 = 329 522 + 173 строки за 09-23; **`max(rr_date) = 2026-09-23`** —
   отчёт за день D **есть уже в ночь D+1** (в 00:19 UTC), лага в сутки нет — это ответ на
   §3.3; Σ оборота 1…21.09 по `rr_date` = **16 667 220,00** — закрытые дни ночное окно не
   сдвинуло; строки за 09-22: было 207 → **стало 207** (все 207 переписаны, состав тот же);
   за 09-23: 173 строки, оборот 173 041.
3. **Funnel:** пятый день без 429 и таймаутов — 4 дня × 2 страницы, все `HTTP: 200`
   (00:15:54 → 00:19:16, пауза 21 с), 09-23: 18 шт / 457 292 ₽. **Заказы WB за 09-23** в
   `marketplace_orders`: 12 строк, подтверждённых 12 шт / 293 578 ₽, отменённых 0 (все с
   `observed_at` 09-24; за ночь переписано 711 строк на 31 дате 08-25…09-24 — лог: 1 619
   строк ответа, 705 без отмены / 914 отменённых, окно с 08-25). **Остатки WB** — шаг прошёл:
   `stock_daily` 2026-09-24 — 1 433 строки / 1 433 SKU, единиц 6 979, с ненулевым остатком 1 166.
4. **`buyout_rate` WB по новой формуле** — работает: на всех 22 днях 09-01…22
   `buyout_rate` = `buyouts_qty / (подтв. + отмен.)` (09-01: 48 / (24 + 14) = 1,2632), по старой
   (`/ подтв.`) — 0 дней. Сумма периода **0,9608 = 589 / (355 + 258)** против 0,9624 = 589 /
   (356 + 256) сухого прогона Ozon-сессии: выкупы те же 589, а созданные 612 → 613 — ночное
   окно переписало 09-01…22 (с 08-25): один подтверждённый стал отменённым и две отмены
   доехали (356 → 355, 256 → 258). Ozon-сессия должна увидеть те же 355 / 258.
5. **«Шаги не выполнены: нет»** (02:39:02 UTC). Тревог «Шаг не выполнен, прогон продолжен» и
   Traceback в логе нет.

Обращений к WB API этим утром — 0 (Render читался по API, база — MCP).

### §2. Блок J–P листа «Заказы»: K = воронка × 0,58 / 1,22; P ≈ доля СПП по продажам дня

**K «Выручка» = `orderSum` воронки продаж (`sales-funnel/products`, «Заказали на сумму»,
`timezone: Europe/Moscow`) × 0,58 / 1,22.** Наш `marketplace_orders_analytics` (тот же
метод, ночь D+1) — до копейки на **16 из 21** законченных дней: 09-01…06, 08…12, 17…21
(09-01: 2 052 178 × 0,475410 = 975 625,61). Множитель 0,58 / 1,22 = 0,475410 — «после
комиссии 42 % и НДС», как у Ozon 0,53 / 1,22 (тридцать первая). Это созданные заказы, а
не дозревание: спад K к молодым дням — спад самих заказов (воронка 09-13…22: 765 121 →
482 668), 09-22 у него снят неполным (K / м = 26 743 против наших 482 668 за полный день).

Пять дней не сходятся — **у него воронка выше, чем мы видели когда-либо**:

| день | K владельца | K / м = его воронка | наша воронка (ночь D+1) | перезапрос 09-23 | разница K |
|---|---:|---:|---:|---:|---:|
| 09-07 | 952 249,70 | 2 003 008 | 1 799 118 | 1 776 529 | +96 931,31 |
| 09-13 | 937 730,21 | 1 972 467 | 765 121 | 761 747 | +573 984,16 |
| 09-14 | 510 811,23 | 1 074 465 | 1 006 834 | 841 205 | +32 152,44 |
| 09-15 | 183 878,54 | 386 779 | 318 025 | 316 709 | +32 686,33 |
| 09-16 | 133 637,23 | 281 099 | 266 934 | 266 934 | +6 734,18 |

Перезапрос показал, что **воронка пересматривает `orderSum` вниз задним числом** (09-14: −16 %
за 9 дней, 09-07: −1,3 %) — наш загрузчик берёт 4 последних дня и старое не перечитывает, это
дыра сама по себе (по правилу заказов: окно, а не «последние N»). Но его числа выше и ночного
снимка, и сегодняшнего: это его рука или снимок другого дня. 09-13 разложился так: Дискаунтер
97 885,46 / м = 205 897 = **ровно** сегодняшняя воронка по артикулам `t…`, а Standard
839 844,75 / м = 1 766 570 при 555 850 по остальным — Standard 09-13 у него не из воронки
этого дня.

**Площадки — догадка советника подтверждена: буква `t` = «WB Дискаунтер».** K = 'Заказы
Standard'!K + 'WB Дискаунтер'!B на всех 22 днях (проверено по книге). «WB Дискаунтер» B =
`orderSum` воронки по товарам с `vendorCode` `t…` × м: 09-01 — 236 672 (= и воронка `t`, и
созданные `t` из `marketplace_orders`), 09-13 — 205 897 (= воронка `t`, а `supplier/orders`
дал 186 771). Против созданных `t` из `marketplace_orders` — до копейки 9 из 21 (09-01, 07,
10, 11, 12, 15, 17, 20, 21); остальное — сдвиги даты (09-18 / 09-19: 276 848 у него в 18-м,
у нас в 19-м) и разница воронки с `supplier/orders` по товару. В сырье воронки товар несёт
`vendorCode` — площадку можно ставить прямо из ответа. Сырьё шести дней —
`logs/wb_funnel_recheck_20260923/funnel_<день>.json` (первая страница, 1 000 товаров;
`orderSum` совпал с ночным снимком на контрольном 09-01).

**L «Маржа» = K − СС** (M = L / K): СС / воронка 0,378 (09-01), 0,341 (09-02), 0,306 (09-10),
0,236 (09-17) — его цены по товарам; из базы не воспроизвести: воронка у нас хранится днём,
не товаром. Кандидат 4 (отчёт по `orderDt`): 09-01 оборот 611 120, `forPay` 339 042 — K
975 626 больше обоих, не он. Побочно: **`buyoutSum` воронки = выкупы по дате ЗАКАЗА** (09-01:
611 120 = оборот отчёта по `orderDt` 09-01 до рубля; спека дату отнесения не называет,
данные назвали).

**P «Соинвест, %».** Ближе всего — **отчёт реализации по `saleDt` МСК: (Σ retailPriceWithDisc −
Σ retailAmount) / Σ retailPriceWithDisc** (то же, что Σ p·spp / Σ p): до 0,0005 — 4 дня
(09-01, 05, 18, 21), в пределах 0,005 — 15 из 22. Заказы ((`orders_amount_seller` −
`orders_amount_buyer`) / seller, созданные и подтверждённые) — 0 точных, с 09-08 ниже его на
0,01…0,07 (скидка на созданных заказах меньше, чем на проданных). По `orderDt` — 4 в
пределах 0,005. Таблица по дням — `scratchpad/kp_analysis.out` этой сессии; в отчёт — итог:

| день | P | отчёт по saleDt (p−ra)/p | заказы созданные | день | P | отчёт по saleDt | заказы созданные |
|---|---|---|---|---|---|---|---|
| 09-01 | 0,4298 | **0,4295** | 0,4232 | 09-12 | 0,4078 | 0,4139 | 0,3651 |
| 09-02 | 0,4311 | 0,4384 | 0,4200 | 09-13 | 0,3912 | 0,4068 | 0,3675 |
| 09-03 | **0,1928** | 0,4257 | 0,4200 | 09-14 | **0,2820** | 0,3952 | 0,3602 |
| 09-04 | 0,4216 | 0,4306 | 0,4227 | 09-15 | 0,3942 | 0,3899 | 0,3702 |
| 09-05 | 0,4396 | **0,4395** | 0,4193 | 09-16 | 0,4181 | 0,4195 | 0,3563 |
| 09-06 | 0,4348 | 0,4360 | 0,4174 | 09-17 | 0,4000 | 0,3993 | 0,3643 |
| 09-07 | 0,4287 | 0,4321 | 0,4074 | 09-18 | 0,4209 | **0,4209** | 0,3632 |
| 09-08 | 0,4386 | 0,4368 | 0,3742 | 09-19 | 0,3898 | 0,3852 | 0,3608 |
| 09-09 | 0,4292 | 0,4284 | 0,3668 | 09-20 | 0,3778 | 0,3917 | 0,3598 |
| 09-10 | 0,4301 | 0,4294 | 0,3571 | 09-21 | 0,3761 | **0,3763** | 0,3710 |
| 09-11 | 0,4330 | 0,4337 | 0,3683 | 09-22 | 0,3750 | 0,3732 | 0,3665 |

09-03 (0,1928) и 09-14 (0,282): ни в одном нашем источнике провала нет — отчёт 0,4257 /
0,3952, заказы 0,4200 / 0,3602, воронка не хранит цен. Это его рука (или его источник в тот
день), не данные. Точного правила P нет; «доля СПП по проданному за день» — рабочее.

**Вывод §2:** K = воронка `orderSum` × 0,58 / 1,22 (16 из 21 до копейки, остальное — его
снимки); площадка — первая буква артикула `t`; P ≈ (p − retailAmount) / p по продажам дня
(saleDt), точного нет. Для листа «Заказы WB» в генераторе нужны: воронка по товарам с
`vendorCode` (не хранится — только день) и перечитывание воронки окном, а не 4 днями.

### §3. `marketplace_buyouts` WB против отчёта реализации — план готов, запись не делалась

**Дата.** Старый загрузчик берёт `date` из `supplier/sales` (`loaders/wb_sales_loader.py`,
`aggregate_sales`: `sale_date = item["date"][:10]`) — это московская дата продажи, то есть
`saleDt` МСК: сентябрь по `saleDt` **22 из 22** до рубля, по `rr_date` — 20 из 22. Знак:
возврат в `supplier/sales` — отрицательная строка (qty −1), в отчёте — операция «Возврат»
с положительной суммой; сборка — Σ × (+1 / −1), зерно то же: (день, nmId).

**Сверка 03-26 … 09-22 по месяцам** (SQL, только чтение; отчёт — Продажа − Возврат по `saleDt` МСК):

| месяц | дней | база ₽ | отчёт ₽ | разница | шт база / отчёт | дней = | дней > 1 % |
|---|---:|---:|---:|---:|---|---:|---:|
| 03 (26…31) | 6 | 19 926 393,00 | 20 149 748,00 | −223 355,00 | 728 / 747 | 2 | 3 |
| 04 | 30 | 78 293 393,06 | 81 901 574,05 | −3 608 180,99 | 3 772 / 3 980 | 8 | 18 |
| 05 | 31 | 66 693 926,11 | 68 090 297,09 | −1 396 370,98 | 3 838 / 3 946 | 11 | 14 |
| 06 | 30 | 51 770 478,29 | 51 977 150,28 | −206 671,99 | 3 378 / 3 397 | 22 | 4 |
| 07 | 31 | 145 357 194,39 | 147 441 534,37 | −2 084 339,98 | 6 997 / 7 099 | 8 | 10 |
| 08 | 31 | 69 196 318,29 | 70 561 190,29 | −1 364 872,00 | 3 011 / 3 090 | 21 | 9 |
| 09 (1…22) | 22 | 16 704 622,00 | 16 704 622,00 | 0,00 | 589 / 589 | 22 | 0 |
| итого | 181 | 447 942 325,14 | 456 826 116,08 | **−8 883 790,94** | 22 313 / 22 848 | 94 | 58 |

**Дни с разницей > 1 % — 58, по классам** (список — `logs/wb_buyouts_rebuild_plan_20260923.out`
и SQL-выборка в сессии):
* **недобор старого окна — 52 дня:** те же ключи (дата, sku), штук и ₽ меньше — flag=0
  переписывал агрегат ключа частичным ответом (04-22 sku 218209851: 47 406 / 1 шт → 336 097 /
  7; 07-06 sku 776186804: 70 635 → 337 377; 08-07 sku 17641481: 13 700 → 207 187 / 14 шт);
* **возврат, которого старый загрузчик не увидел — 5 дней:** 05-23 и 07-02 — продажа и
  возврат в один день (нетто 0, в базе продажа); 08-24 — sku 17641480: продажа 12 133 и
  возврат 9 347 (в базе только продажа); 06-20 (−37 389) и 08-30 (−36 544) — возврат без
  ключа в базе; в плане это переписывания и добавления с отрицательными штуками;
* **цена в отчёте отличается — 3 ключа:** 04-25 sku 244516773 32 666 → 16 266 (штука одна),
  05-21 sku 51421290 36 699 → 24 955, 04-25 sku 17641480 46 272 → 46 157 (4 шт).

**Скрипт `scripts/wb_buyouts_rebuild_from_report.py`** — `--plan` из таблицы отчёта, обращений
0; `--apply --approve-wb-buyouts-write` — снимок затрагиваемых строк → upsert → удаление
ключей списка по id; в ночном окне отказ; 8 тестов. **План 02-01 … 09-22
(`logs/wb_buyouts_rebuild_plan_20260923.out`):**

| месяц | добавить | переписать | без изм. | удалить | шт до → после | ₽ до → после | разница ₽ |
|---|---:|---:|---:|---:|---|---|---:|
| 02 | 2 947 | 0 | 0 | 0 | 0 → 4 439 | 0 → 123 169 045,57 | +123 169 045,57 |
| 03 | 2 568 | 69 | 438 | 0 | 732 → 4 851 | 19 964 430,00 → 134 612 663,99 | +114 648 233,99 |
| 04 | 0 | 465 | 2 189 | 0 | 3 772 → 3 980 | 78 293 393,06 → 81 901 574,05 | +3 608 180,99 |
| 05 | 0 | 397 | 2 373 | 0 | 3 838 → 3 946 | 66 693 926,11 → 68 090 297,09 | +1 396 370,98 |
| 06 | 1 | 603 | 1 862 | 0 | 3 378 → 3 397 | 51 770 478,29 → 51 977 150,28 | +206 671,99 |
| 07 | 1 | 207 | 4 406 | 0 | 6 997 → 7 099 | 145 357 194,39 → 147 441 534,37 | +2 084 339,98 |
| 08 | 1 | 55 | 2 150 | 0 | 3 011 → 3 090 | 69 196 318,29 → 70 561 190,29 | +1 364 872,00 |
| 09 | 0 | 5 | 484 | 0 | 589 → 589 | 16 704 622,00 → 16 704 622,00 | 0,00 |
| итого | 5 518 | 1 801 | 13 902 | **0** | 22 317 → 31 391 | 447 980 362,14 → 694 458 077,64 | +246 477 715,50 |

Из них 02-01 … 03-25 — новые дни, +237 817 279,56 (в базе их не было); 03-26 … 09-22 —
+8 660 435,94. **Ключей на удаление — 0**: старый загрузчик не писал ключей, которых нет в
отчёте (застрявших нет); дней без единой продажи в отчёте при строках в базе — 0. 09-01…22 —
5 переписываний на 0,00 ₽ (нули против null-ов, суммы те же). Февраль по `saleDt` —
123 169 045,57 против 123 172 593,57 по `rr_date` (пятая): граница суток. Ozon не тронут.

**§3.3 — что ночью после замены (предложение, код шага не менялся).** Что видит
`supplier/sales` в ночь D+1: у 737 из 766 строк 08-24…09-21 `created_at` — ночь D+1
(96,6 % ₽), остальные 29 — весь день 08-24, созданный ночью 08-26 (ночь 08-25 его не
записала); 09-22 в ночь 09-23 = 37 402 = отчёт за 09-22 (снят 09-23 10:00 UTC) — то есть
день D в `supplier/sales` полон уже в ночь D+1. Чего отчёт ещё не отдал в 00:15 UTC ночи D+1
— скажет утро 09-24: `max(rr_date)` = 09-23 (отчёт за D есть в ночь D+1) или 09-22 (лаг
сутки). Предложение: шаг «WB: загрузка продаж/выкупов» пишет из отчёта реализации всё, что
отчёт отдал (окно 21 день, уже в базе после шага отчёта), а `supplier/sales` остаётся только
для дней, которых в отчёте ещё нет (D и, если лаг, D−1); ключи дня, попавшего в отчёт,
переписываются отчётом. Решение — советника после утра.

### §3а. Реклама WB — история снята, загрузчик и таблица написаны, запись ждёт слова

`adv/v1/upd` (спека `08-promotion.yaml`: интервал ≤ 31 дня, 1 запрос/с; поля `updNum`,
`updTime`, `updSum`, `advertId`, `campName`, `advertType`, `paymentType`, `advertStatus`,
`currency`). Съём `scripts/wb_ads_backfill.py --fetch` 02-01 … 09-22 кусками по 31 дню:
**8 обращений, 33 781 строка, 429 — 0** (`data/wb_ads_raw/`, вне git). План из файлов:

| месяц | строк | Σ updSum (с НДС) | / 1,22 | лист владельца (без НДС) |
|---|---:|---:|---:|---:|
| 02 | 4 488 | 6 942 541,00 | 5 690 607,38 | — |
| 03 | 7 231 | 7 985 817,00 | 6 545 751,64 | — |
| 04 | 7 492 | 6 142 629,00 | 5 034 941,80 | — |
| 05 | 5 245 | 3 760 961,00 | 3 082 754,92 | — |
| 06 | 5 499 | 4 085 750,00 | 3 348 975,41 | — |
| 07 | 2 680 | 2 066 897,00 | **1 694 177,87** | 1 694 178,00 |
| 08 | 1 146 | 1 019 881,00 | **835 968,03** (все 1…17) | 835 968,00 |
| 09 | 0 | 0,00 | 0,00 | 0 |
| итого | 33 781 | 32 004 476,00 | 26 233 177,05 | |

`updSum` — с НДС (июль и август сходятся с листом после / 1,22 до рубля); день — `updTime` по
МСК. `paymentType`: Баланс 25 019 060, Кэшбэк 6 985 416; `advertType` только 9; кампаний 415.
**Зерно:** `updNum` ключом быть не может — 37 различных значений на 33 781 строку, 5 610 нулей;
пара **(advertId, updTime) уникальна** (повторов 0, без `updTime` 0) — она и ключ.

Написано: `loaders/wb_ads_loader.py` (окно 31 день до вчера, одно обращение, повторы 429 /
сеть 3 × 65 с, дубль пары и строка без обязательного поля — отказ до записи, `--dry-run`; 11
тестов), `sql/20260923_create_wb_ad_spend_daily.sql` (имя — по задаче, зерно — списание,
`upd_day` для сумм по дням), бэкфилл с `--apply --approve-wb-ads-write` (не запускался).
Dry-run загрузчика по живому API: окно 08-23 … 09-22, HTTP 200, строк 0, 1 обращение —
ноль сентября подтверждён. `report_wb_month.py` теперь берёт рекламу из таблицы (Σ updSum /
НДС, как у владельца без НДС), `--ads api` — ручной путь, `--ads none` — пусто; пока таблицы
нет, печатает «реклама не получена: не прочитать wb_ad_spend_daily» и оставляет колонку
пустой, не нулём (приёмка сентября после правки — те же 6 из 6, код 0). Шаг в
`run_daily_pipeline.py` не добавлен (§5) — вопрос 3 выше.

### §5. Render — live до 00:15 UTC

`GET /v1/services/{id}/deploys?limit=1`: оба сервиса на `385aafb`, **live 20:55:25 UTC**
(`mp-analytics`) и **20:55:26 UTC** (`mp-analytics-telegram-report`) — за три с лишним часа до
00:15 UTC. Ночь 09-24 — первая для двух шагов: «WB: отчёт реализации» и «WB: реклама».

### §4. Решения советника — как применены

Дата листа `saleDt` МСК и комиссия по кВВ — не трогались. Февраль–март выкупов — в плане §3
(5 515 новых ключей, +237,8 млн). Лист WB в Telegram — не делался.

### `git log` ветки до мержа (всё вошло в `385aafb`)

```
dfecd84 Merge origin/main (thirty-fifth Ozon task) into wb-fixes; gap 4 of the workbook register: the t-letter Discounter confirmed
4876b25 Nightly step «WB: реклама» after the sales report step, by the owner's word; 4 tests
2acd479 Sixth WB report so far: K = funnel orderSum × 0,58/1,22, the t-letter Discounter, P ≈ SPP share by sale day; …
d75ee59 Sixth WB task: buyouts rebuild plan from the sales report, WB ads loader with table and backfill plan, sheet ads from the table
52de1a3 Keep the sixth WB task text as placed by the adviser
```
Общие файлы: `run_daily_pipeline.py` — только строка шага «WB: реклама» (по слову);
`docs/owner_workbook_gaps.md` — статус дыры 4; `.gitignore` — `data/wb_ads_raw/`.

### Что не сошлось — одной строкой

K владельца на 5 днях выше любой нашей воронки (его снимки / рука; 09-13 Standard — не из
воронки дня); P — точного правила нет (лучший кандидат 4 из 22 до 0,0005), 09-03 и 09-14 —
его рука; выкупы WB в базе ниже отчёта на 8 883 790,94 за 03-26…09-22 (недобор старого окна
на 52 днях, невиденные возвраты на 5, цены на 3 ключах), февраль–март 25 в базе нет —
план готов, запись по слову; воронка пересматривает `orderSum` вниз задним числом, а наш
загрузчик старые дни не перечитывает — дыра, не чинилась.

---

## 2026-09-23, пятая задача WB — три слова получены: миграция применена, бэкфилл записан, мерж в `main` (см. «Выполнено по слову»)

`db_writes = 0`. Обращений к WB API за задачу — **24** (по логам: 5 пробы поведения
отчёта 09:35–09:39 UTC, 17 съём бэкфилла 09:44–10:10, 1 dry-run загрузчика, 1
`adv/v1/upd` в первой приёмке), **429 — 0, сетевых отказов — 0** (`calls.json`,
`logs/wb_sales_report_probe2/run.out`, `logs/wb_sales_report_dryrun_20260923.out`).
Файл владельца читался на месте. Ветка `wb-fixes` включает `origin/main` дважды за день:
`39754e2` (тридцать третья задача Ozon: правило фатальности перевёрнуто, §2) и `b3ba9a2`
(тридцать четвёртая: тесты без сети, `buyout_rate` по созданным, retention сырья) —
после второго мержа тесты **925 OK** (0,6 с: сеть в тестах закрыта).

### Выполнено по слову владельца (три «да» в чате, ~11:10 UTC)

1. **Миграция применена 11:13:13 UTC** через MCP (`apply_migration
   create_wb_sales_report_rows`, текст = `sql/20260923_create_wb_sales_report_rows.sql`):
   таблица `wb_sales_report_rows`, **43 колонки** (в прежней записи «40» — считала без
   `rrd_id`, `report_period`, `loaded_at`), 4 индекса (PK + 3), строк 0 до записи.
2. **Бэкфилл** `scripts/wb_sales_report_backfill.py --apply --approve-wb-sales-report-write`
   из снятых файлов, запуск 11:13:29 UTC (`logs/wb_sales_report_backfill_apply_20260923.out`)
   — итог и SQL-контроль ниже, в §2а.
3. **Решение советника — колонка «Комиссия» листа = Σ цена × кВВ строки (как у
   владельца), «оборот − forPay» — справочно «удержано из выплаты всего»** — сделано
   до мержа (`fd9a98d`): выручка, маржа, фин. рез. и Ebitda идут от его
   комиссии; удержано всего — справочная колонка и необязательная строка приёмки.
   Приёмка после правки — §3.
4. **Мерж в `main` — сделан 11:31:50 UTC, Render live 11:32:29 / 11:32:31 UTC** — §5.

Ниже — что сделано и с какими числами.

### Две поправки к четвёртому отчёту

1. **«429 — 0» за ручной прогон — неверно.** По логу `logs/manual_run_20260923.out`
   строк `http_retry` «транзиентный отказ reason=rate_limit_per_second» — **10, все в
   шаге «Ozon: штуки выкупов»** (`accrual/postings`, повтор через 1 с, все изжиты;
   итоговая строка шага «пауз 429 — 0» считает свои паузы, а не повторы
   `http_retry`). В остальных шагах — 0. Впредь считаю по логу.
2. **`buyout_rate` WB после смены правила** (1,005 на плато, 1,843 в окне) — что ещё
   зависит от `orders_qty` / `orders_amount_seller` WB, по коду:
   * `reports_daily_sku_kpi.py`: `buyout_rate = buyouts_qty / orders_qty`,
     `ad_share_of_orders = ad_spend / orders_amount_seller`, `ad_share_orders =
     ad_orders_qty / orders_qty`, `roas = orders_amount_seller / ad_spend`; веса
     карты sku → article — уже по `orders_qty + cancelled_orders_qty` (созданные);
   * `reports_daily_marketplace_kpi.py`: те же четыре плюс `ad_share_revenue`;
   * `alerts_telegram.py`: «WB вчера: Заказы», отклонение к 7 дням, триггер
     риска «заказы ниже среднего» и срез «сегодня против вчера» — все по
     funnel-overlay (созданные из `marketplace_orders_analytics`), витрину не
     читают — их правило не тронуло; выкупы алерта — из `marketplace_buyouts`;
   * остальные читатели `orders_qty` (`report_7_days`, `report_60_days`,
     `export_management_excel`, `report_management`, decision-скрипты) — витрину
     как есть, без своего знаменателя.
   **Предложение знаменателя (решение советника):** для WB и Ozon одинаково —
   `buyout_rate = buyouts_qty / (orders_qty + cancelled_orders_qty)`, то есть
   выкупы по дате продажи к **созданным** по дате заказа: это ровно прежняя
   семантика показателя до смены правила (у WB до 09-23 знаменатель и был
   созданными), и она одна на обе площадки; когорта (доля заказов дня D, у
   которых потом была продажа, по `srid` — у WB `srid` есть и в заказах, и в
   продажах) — правильнее, но это другой показатель и отдельная задача. Пока
   `buyout_rate` в витрине читать нельзя — да.

### §1. Мерж починки funnel — сделан до ночи

См. конец четвёртого отчёта: merge-коммит `d56acb4`, push 09:25:51 UTC, Render live
09:26:31 / 09:26:32 UTC; тесты на тот момент 838. `main` с тех пор не двигался
(`origin/main = d56acb4`), ветка `wb-fixes` содержит его.

### §2. Отчёт реализации — код, таблица (применена), съём бэкфилла; запись и контроль — §2а

**Как отчёт живёт во времени** (пять обращений 09:35–09:39 UTC, все 200/204):
неделя 09-01…07, снятая повторно через сутки, — **те же 5 979 rrdId, поле в поле**
(задним числом за сутки не менялась); у закрытой недели `period=daily` и `weekly` —
**одни и те же rrdId и значения** (5 979 из 5 979, суммы по дням равны); незакрытая
неделя: `weekly` → 204, `daily` → день D появляется D+1 (09-21 — 225 строк, 09-22 —
207; `reportId 6338020260922`, `createDate 09-23`); оборот 09-21 из daily = 236 850 =
лист. Отсюда: **загрузчик ходит всегда с `period=daily`**, upsert по `rrd_id`.
Подробно — `docs/wb_report_model.md` §6а.

**Таблица** `sql/20260923_create_wb_sales_report_rows.sql` — зерно `rrd_id`, 43
колонки (номер и период отчёта, `rr_date`, `sale_dt`, `order_dt`, `nm_id`,
`vendor_code`, `tech_size`, `srid`, операция, тип документа, `quantity`, все суммы
листа: `retail_price_with_disc`, `retail_amount`, `for_pay` (поле `forPay` спеки — «К
перечислению продавцу за реализованный товар»), `commission_percent`, `ppvz_reward`,
`rebill_logistic_cost`, `delivery_service`, `acquiring_fee`, `paid_storage`, `penalty`,
`deduction`, `additional_payment`, `paid_acceptance`, `vw`, `vw_nds`, `cashback_*`,
`observed_at`, `loaded_at`). Деньги — строкой из ответа в `numeric` без точности (у `vw` 16 знаков).
**Миграция применена по слову 11:13:13 UTC** (выше).

**Загрузчик** `loaders/wb_sales_report_loader.py`: окно 21 день до вчера, одна
страница на 100 000 строк → **1 обращение за ночь** (страницы по `rrdId`, пауза 65 с —
лимит метода по спеке `13-finances.yaml` 1 запрос/мин; 429 / таймаут / сеть — 3
попытки по 65 с, затем именованный отказ); дубли `rrdId` и строки вне периода — отказ до
записи; upsert по `rrd_id`; **застрявшие строки окна** (в базе есть, в свежем полном
ответе нет) — `loaders/stale_keys.py`: только при полном сборе, порог ≤ 200 и ≤ 2 % окна,
список до удаления, удаление после записи. Задним числом: закрытая неделя за сутки не
менялась (проба выше), поэтому окно 21 день — три недели с запасом, а не «две
последние». Dry-run по живому API 10:13 UTC: окно 09-02…09-22, **13 991 строка, 1
обращение, 429 — 0**, оборот нетто 15 448 701,00; чистка застрявших пропущена вслух:
таблицы нет (`logs/wb_sales_report_dryrun_20260923.out`).

**Шаг «WB: отчёт реализации»** — после «WB: загрузка продаж/выкупов», перед остатками
WB, Ozon и KPI. В ветке он был внесён в `NON_FATAL_STEPS` с причиной; в `origin/main`
`39754e2` (тридцать третья задача Ozon, `c2e0cd6`) правило перевёрнуто — фатальны
только два шага KPI (`FATAL_STEPS`), `NON_FATAL_STEPS` теперь производное от списка
шагов. После мержа `main` в ветку (`bcb995c`, конфликт только в
`tests/test_non_fatal_total_orders.py`, взята версия `main`) моя запись с причиной
осталась в `NON_FATAL_BEFORE_20260923` как история; шаг нефатален по правилу, тесты
шага (`tests/test_wb_sales_report_step.py`, 4) проходят без правок. Общий файл
`run_daily_pipeline.py` в ветке отличается от `main` только этим: строка шага в
`build_steps` и запись в историю (10 строк).

**Бэкфилл** `scripts/wb_sales_report_backfill.py`: `--fetch` снят **09:44:19–10:10:16
UTC: 17 обращений (куски по 14 дней), 17 файлов, 329 522 строки, 429 — 0, сетевых
отказов — 0**, 715 МБ в `data/wb_sales_report_raw/` (вне git, `.gitignore`); файл есть —
в API не идёт, ночное окно — стоп. `--plan` из файлов (`plan_20260923.out`), оборот =
Σ `retailPriceWithDisc` (Продажа − Возврат) по `rrDate`, против `marketplace_buyouts`
(`buyouts_amount_seller` по дате):

| месяц | строк | продаж / возвр. | оборот по отчёту | `marketplace_buyouts` | разница | почему |
|---|---:|---|---:|---:|---:|---|
| 2026-02 | 44 269 | 4 490 / 51 | 123 172 593,57 | 0,00 | 123 172 593,57 | сбор WB начат 03-26, базы нет |
| 2026-03 | 35 106 | 4 898 / 47 | 134 612 663,99 | 19 964 430,00 | 114 648 233,99 | база с 03-26 |
| 2026-04 | 30 814 | 4 026 / 46 | 81 901 574,05 | 78 293 393,06 | **3 608 180,99** | база ниже отчёта |
| 2026-05 | 40 190 | 3 976 / 30 | 68 090 297,09 | 66 693 926,11 | **1 396 370,98** | то же |
| 2026-06 | 54 100 | 3 426 / 29 | 51 977 150,28 | 51 770 478,29 | **206 671,99** | то же |
| 2026-07 | 72 820 | 7 169 / 71 | 147 428 123,37 | 145 357 194,39 | **2 070 928,98** | по дням: 6 равны, 23 база ниже, 2 выше |
| 2026-08 | 37 343 | 3 118 / 27 | 70 574 601,29 | 69 196 318,29 | **1 378 283,00** | то же |
| 2026-09 (1…22) | 14 880 | 597 / 8 | 16 704 622,00 | 16 704 622,00 | **0,00** | сходится до рубля |
| итого | 329 522 | | 694 461 625,64 | 447 980 362,14 | 246 481 263,50 | |

### §2а. Бэкфилл записан 11:13:44–11:28:35 UTC, контроль SQL сошёлся

`--apply --approve-wb-sales-report-write` из 17 файлов: план напечатан заново (те же
числа, что в таблице ниже), затем **`✅ wb_sales_report_rows: upsert 329522 строк`**,
один `observed_at` = `2026-09-23 11:13:44.534273+00` у всех строк; к WB — 0 обращений,
к Supabase ≈ 660 upsert-пачек по 500, отказов 0, 15 минут
(`logs/wb_sales_report_backfill_apply_20260923.out`).

**Контроль SQL 11:29 UTC** (`execute_sql`, только чтение):

| что | план | в таблице |
|---|---:|---:|
| `count(*)` | 329 522 | **329 522** |
| `count(distinct rrd_id)` | = `count(*)` | **329 522** |
| дней `rr_date` | 234 (02-01 … 09-22) | **234, 2026-02-01 … 2026-09-22** |
| Σ оборот (Продажа − Возврат) по `rr_date` 1…21 сентября | 16 667 220,00 (лист, `marketplace_buyouts`) | **16 667 220,00** |
| по дням 09-01 … 09-07 | 1 255 921; 1 621 368; 792 237; 1 084 193; 1 066 137; 1 264 609; 1 092 148 (проба и лист) | **те же 7 из 7** |
| по месяцам 02 … 09 | 123 172 593,57; 134 612 663,99; 81 901 574,05; 68 090 297,09; 51 977 150,28; 147 428 123,37; 70 574 601,29; 16 704 622,00 | **все 8 до копейки** |

**Генератор из таблицы = из файлов:** `report_wb_month.py --month 2026-09 --date-to
2026-09-21 --no-fetch --check` без `--rows-from-files` (11:31 UTC,
`check_2026-09_20260923_v4_db.out`) — 14 880 строк из `wb_sales_report_rows`, таблица
приёмки совпала с прогоном из файлов строка в строку (`diff` пуст), «не сошлось 0»,
книга `data/reports/wb_2026-09_to_2026-09-21_from_db.xlsx`.

Апрель–август: `marketplace_buyouts` **ниже отчёта на 8,66 млн** — старый загрузчик
продаж (`supplier/sales flag=0`, окно 30 дней) недобирал так же, как заказы
(`docs/wb_data_integrity.md` §1); сентябрь на новом окне сходится. Это не расхождение
двух источников за один день, а недостача базы — после записи отчёта у выкупов WB
появится полный источник с февраля; правило замены `marketplace_buyouts` — отдельная
задача, здесь не делалось. Записи в этой задаче не было: `--apply` — по слову.

### §3. Лист «WB - месяц» — `scripts/report_wb_month.py`: шесть колонок 21 из 21

**Что нашлось за час, и это меняет карту** (`docs/wb_report_model.md` §0–§2,
опровергнутое помечено, не удалено):

1. **Дата у владельца — `saleDt` в московском времени у всех строк**, не `rrDate`
   (в задаче: «Дата реализации (= rrDate)» — отклонение, названо). По `rrDate` оборот
   расходился 09-09 / 09-10 на ∓96 641 (четыре продажи `saleDt` 09-09 16:50–18:44 UTC с
   `rrDate` 09-10 у него в 09-09), логистика — на −1 523…+1 892 в день; по `saleDt` МСК
   всё до копейки. `rrDate` ≠ дата продажи у 65 строк из 32 009 (02-01…09-22): 60 на
   1…3 дня, 5 на 16…115 дней — генератор читает по `rrDate` с запасом 7 дней и
   **считает вслух** продажи с датой вне листа (за 1…21.09: раньше 0, позже 3 на
   37 402,00 — это 22-е и дальше).
2. **Комиссия владельца — не константа 42 %**: Σ `retailPriceWithDisc` ×
   `commissionPercent` / 100 по строкам, Продажа +, Возврат − (кВВ строки: 42 у
   большинства, 38 / 39,5 / 43 у части). 21 из 21 до копейки; в ячейках листа C —
   числа, не формулы (проверено openpyxl 2026-09-23). Прежнее «C = 0,42 × B» держалось на
   14 днях из 21.
3. **Эквайринг — возврат со знаком минус**: «известные» 09-03 и 09-07 (и новый 09-14)
   — это была наша формула, не его сторона. **09-17 — его сторона:** два возврата
   (441,25 + 936,24 с НДС) он не вычел, на трёх других днях вычел; единственное известное
   расхождение, −2 258,18.
4. **Прочее — штрафы без НДС**: `paidStorage / 1,22 + penalty + deduction / 1,22`.
   «Известный» 09-02 (+563,20) — наша формула делила штраф 2 560 на 1,22; 09-18 —
   `deduction` 9 990 (удержание) / 1,22 = 8 188,52. 21 из 21.
5. **Логистика — `deliveryService / 1,22` по `saleDt` МСК** — 21 из 21 (в задаче: «не
   искать дальше часа» — нашлось внутри часа вместе с датой). Теперь обязана сходиться.

Лист: колонки владельца; **комиссия — Σ цена × кВВ строки, как у него (решение
советника, выше)**; справочно — «удержано из выплаты всего» (`оборот − forPay`, 44,39 %)
и его доля; выручка, маржа, фин. рез., Ebitda — от его комиссии; НДС — `VAT_RATES` из
`report_ozon_month.py`; СС — снимок 05-20 по базовому артикулу (`unit_cost_uniform`,
безразмерные — точной строкой; `--cogs-by variant` есть, выключен — решение 4); реклама
— `adv/v1/upd` одним обращением (`--no-fetch` — пусто, не ноль); накладные
74 002,00/день с 09-01; удержания — своя колонка; дни моложе двух суток — жёлтые и вне
приёмки; строки без `commissionPercent` — счётчик вслух (в сентябре 0); позиции без СС
— счётчик и доля оборота вслух (1…21.09: 17 из 586, 1,12 %). Источник —
таблица или `--rows-from-files` (пока таблицы нет). Тесты — 17.

**Приёмка 1…21 сентября после решения советника** (`--date-to 2026-09-21
--rows-from-files … --no-fetch --check`, 11:16 UTC; вывод —
`data/wb_sales_report_raw/check_2026-09_20260923_v3.out`, книга —
`data/reports/wb_2026-09_to_2026-09-21.xlsx`; строк по `rrDate` 09-01…09-28 — 14 880):

| колонка | у него | у нас | разница | дней 0,00 |
|---|---:|---:|---:|---:|
| Оборот (с НДС) | 16 667 220,00 | 16 667 220,00 | 0,00 | **21 из 21** |
| Выручка (оборот − комиссия) / НДС − НДС за возмещение | 7 839 198,01 | 7 839 198,01 | 0,00 | **21 из 21** |
| Эквайринг (возврат со знаком минус) / НДС | 332 324,00 | 330 065,82 | −2 258,18 | **20 из 21** — 09-17, его сторона (п. 3) |
| Прочее (хранение / НДС + штрафы + удержания / НДС) | 65 971,76 | 65 971,76 | 0,00 | **21 из 21** |
| Комиссия (Σ цена × кВВ строки, знак возврата) | 6 994 883,76 | 6 994 883,76 | 0,00 | **21 из 21** |
| Логистика (deliveryService / НДС, по дате продажи МСК) | 207 256,15 | 207 256,15 | 0,00 | **21 из 21** |
| справочно: удержано из выплаты всего (оборот − forPay) против его комиссии | 6 994 883,76 | 7 398 472,70 | +403 588,94 | 0 из 21 — не одно и то же: удержано ещё согласованная скидка и доплаты |
| Себестоимость (снимок 05-20, базовый артикул) против его | 5 943 927,07 | 5 359 751,81 | −584 175,26 | 0 из 21 — его цены выше на 10,9 % |
| Фин. рез. против его | 1 289 719,02 | 1 876 152,46 | +586 433,44 | 0 из 21 — = СС 584 175,26 + эквайринг 09-17 2 258,18 |

«приёмка: колонок, обязанных сходиться до копейки, — 6; не сошлось 0», код 0. НДС за
возмещение 88 946,47 = колонка AB его свода. Итог наш: оборот 16 667 220,00, комиссия
6 994 883,76 (41,97 %), выручка 7 839 198,00, СС 5 359 751,81, маржа 2 479 446,19,
логистика 207 256,16, реклама — (не запрашивалась), эквайринг 330 065,82, прочее
65 971,76, фин. рез. 1 876 152,46; справочно удержано из выплаты всего 7 398 472,70
(44,39 %). Первая приёмка того же дня (10:30, `…_v2.out`) была с комиссией факт в
основной колонке — те же шесть тождеств, фин. рез. 1 545 341,83.

Доставка в Telegram — не делалась (по задаче).

### §4. Решения советника — как применены

Комиссия: сначала — факт в листе, его по кВВ справочно (решение 4); затем решение
советника в чате — наоборот: **в листе его (кВВ строк), удержано всего — справочно**;
сделано до мержа.
Логистика — поле отчёта, по дате продажи МСК — тождество. «Три дня» — два из трёх были
наши формулы (сняты), третьего нет; единственный день его стороны — 09-17 (эквайринг).
СС — базовый артикул; **без СС 17 из 586 позиций — 186 330,00, 1,12 % оборота**
(артикулов нет в снимке 05-20; стыковка: `uniform` 405, `plain` 180) — печатается в
выводе и в подвале книги.

### §5. Мерж в `main`, push, Render

Полный набор на голове ветки `b40982e` — **925 OK** (0,5 с). `origin/main` перед мержем
= `b3ba9a2` (проверено в той же команде; предок ветки, конфликтов нет). Мерж-коммит
собран плюмбингом (`git commit-tree`, родители `b3ba9a2` + `b40982e`, `--no-ff`
по смыслу): **`25d5534` «Merge wb-fixes (fifth WB task) by the owner's word»**, push в
`main` **11:31:50 UTC** моей рукой (классификатор пропустил, как 09-23 утром).
Render по API (`GET /v1/services/{id}/deploys?limit=1`): оба сервиса на `25d5534`,
**live 11:32:29 UTC** (`mp-analytics`) и **11:32:31 UTC** (`mp-analytics-telegram-report`)
— до 00:15 UTC с запасом. Ветка `wb-fixes` переведена на `25d5534` (fast-forward),
`git log origin/main..HEAD` — пусто.

**Ночь 09-24 — что смотреть:** в логе Render шаг «WB: отчёт реализации» сразу после
«WB: загрузка продаж/выкупов»: `WB sales report 2026-09-03…2026-09-23: HTTP 200, строк
~14 000`, `обращений 1, 429 — 0`, `✅ wb_sales_report_rows: upsert …`, блок застрявших —
«удалять нечего» или список ≤ 200 строк до удаления; в таблице
`select count(*) from wb_sales_report_rows where observed_at::date = '2026-09-24'` ≈
14 000 и `max(rr_date) = 2026-09-23`. Упал — нефатально по правилу `FATAL_STEPS`,
тревога «Шаг не выполнен, прогон продолжен» с именем шага; таблицу пустой сбор не портит.

### `git log` ветки до мержа (`b40982e`, всё вошло в `25d5534`)

```
b40982e Fifth WB report: migration applied, backfill written and checked by SQL (329 522 rows, Sept 1…21 = 16 667 220,00, 7 of 7 days, 8 of 8 months), the advisor's commission decision applied
34a5663 WB sales-report step test: stub record_pipeline_run like the sibling step tests (no network in tests, thirty-fourth Ozon task)
4fdef49 Merge remote-tracking branch 'origin/main' into wb-fixes
fd9a98d WB month sheet: «Комиссия» = Σ price × row kVV as on the owner's sheet; «оборот − forPay» kept as the reference «удержано из выплаты всего»
0fe0174 Fifth WB report: branch head named exactly
d936876 Fifth WB report: six columns of the owner's sheet match 21 of 21, the backfill plan by month, two words pending (migration, --apply)
bcb995c Merge origin/main (thirty-third Ozon task: fatality rule inverted) into wb-fixes
f01d1ab WB month sheet: the owner's rules found — day = saleDt in Moscow time, commission by row kVV, returns signed in acquiring, penalties without VAT
1276272 Fifth WB report, interim: corrections (429 by log, buyout_rate dependents), sales-report loader and sheet in the branch
424ab50 Record how the WB sales report behaves over time and how the loader uses it
fa2587b Generate the «WB - month» sheet from the sales report with a check against the owner's sheet
b738b7d Load the WB sales report (finance-api) into wb_sales_report_rows: migration, nightly non-fatal step, backfill by word
c5ba76f Merge remote-tracking branch 'origin/main' into wb-fixes
22d31fa Funnel fix merged and live on Render at 09:26 UTC by the owner's word; what to check on the morning of 09-24
```

Файлы мержа против `b3ba9a2`: `.gitignore`, `docs/outbox_wb.md`, `docs/wb_report_model.md`,
`loaders/wb_sales_report_loader.py`, `run_daily_pipeline.py` (+10: строка шага и запись в
историю `NON_FATAL_BEFORE_20260923`), `scripts/report_wb_month.py`,
`scripts/wb_sales_report_backfill.py`, `sql/20260923_create_wb_sales_report_rows.sql`, три файла
тестов — 11 файлов, +1 803 / −14. Ozon не тронут. **Замечание Ozon-потоку:**
`tests/test_ozon_performance_recovery_worker.py` грузит воркер по абсолютному пути
`/Users/mihaileliseev/mp-analytics/scripts/…` — из другого worktree набор ломается, как
только деревья расходятся (сегодня 11:1x, до мержа `b3ba9a2`); лечится мержем `main`,
но путь стоит вычислять от `__file__`.

### Что не сошлось — одной строкой

Удержано из выплаты всего 44,39 % против его комиссии по кВВ 41,97 % (+403 588,94 за
1…21.09 — справочная колонка, не комиссия); СС −584 175,26 (его цены выше снимка 05-20 на
10,9 % — как у Ozon +16 %, нужен свежий снимок 1С); фин. рез. +586 433,44 (= СС +
эквайринг 09-17);
эквайринг 09-17 −2 258,18 (его сторона); `marketplace_buyouts` апрель–август ниже
отчёта на 8,66 млн (недобор старого загрузчика продаж, февраль–март базы нет); дата
листа — `saleDt` МСК, не `rrDate` из задачи (иначе оборот 19 из 21).

---

## 2026-09-22, четвёртая задача WB — отчёт (§2–§3 вечером 09-22, §1 утром 09-23; ночь упала на funnel — разобрано, прогон повторён, починка слита)

`db_writes = 0`. Обращений к WB API — **7**, 429 — 0, все в §5 карты
(`docs/wb_report_model.md`). Файл владельца читался на месте, не копировался.
Шапка третьего отчёта поправлена: вместо «db_writes = 0 … не тронуты» —
фактическая запись (151 дата, 22 647 строк upsert-ом).

### ⚠️ Вопрос, задача не закончена — ночь 09-23 упала на шаге funnel, прогон не дошёл до KPI

**Что случилось** (лог Render, проверено 05:14 UTC 09-23): шаг «WB: загрузка
заказов» прошёл на новом коде чисто (ниже), следующий шаг **«WB: загрузка
заказов Analytics Sales Funnel» упал в 00:18:21 UTC**: `sales-funnel/products` за
09-21 → HTTP 429 → пауза 60 с → повторный запрос — `ReadTimeout` (60 с) от
`seller-analytics-api.wildberries.ru`, исключение не перехвачено
(`loaders/wb_sales_funnel_orders_loader.py:82`, `requests.post(..., timeout=60)`),
код 1. **Шаг фатальный** (его нет в `NON_FATAL_STEPS`) → «❌ Your cronjob failed:
Exited with status 1» в 00:18:25. **После него не выполнилось ничего:** ни продажи
и остатки WB, ни один шаг Ozon (заказы FBS/FBO, лог статусов, выкупы, штуки,
расходы, реклама), ни KPI обеих площадок — в логе после 00:18 строк нет. В БД:
`daily_sku_kpi` за ночь не обновлялась (последняя запись WB — 09-22 02:51 UTC),
витрина по WB — старая (26 288 созданных на 151 дате вместо 22 239 подтверждённых).

Не моя правка: файл funnel в ветке не менялся, тот же шаг накануне (ночь 09-22)
тоже ловил 429 на `offset 0` и пережил его (200 через 63 с); сегодня повтор
после паузы не дождался ответа. Разница ночей — везение, не код. Шаг питает
только `marketplace_orders_analytics` (funnel-overlay утреннего алерта,
`docs/wb_funnel_consumers.md`), витрина от него не зависит — фатальным ему быть
не за что.

**Решено владельцем в окне сессии ~05:30 UTC:** «Запусти прогон сейчас» и «Да,
почини в ветке». **Прогон перезапущен 05:56:31 UTC** из этого worktree (код =
`main` `6ff7587` + починка funnel ниже; лог `logs/manual_run_20260923.out`), команда
— живая команда Render. Шаг заказов WB прошёл повторно так же (1 706 строк,
733 / 973, записано 745). Итог прогона — допишу по завершении (~2,5–3 ч).

**Починка в ветке** (коммит ниже): `loaders/wb_sales_funnel_orders_loader.py` —
сетевой отказ (`Timeout`, `ConnectionError`) повторяется как 429 (те же 5 попыток
по 60 с, по исчерпании `RuntimeError` с именем), между запросами 21 с (лимит
метода 3 запроса/мин, 20 с — прежние 3–5 с и давали 429 каждую ночь);
`run_daily_pipeline.py` — шаг внесён в `NON_FATAL_STEPS` с причиной;
`tests/test_wb_funnel_step_non_fatal.py` — 7 тестов (шаг нефатальный и стоит до
продаж/Ozon/KPI, таймаут повторяется и завершается именованной ошибкой, пауза
под лимит); закрепляющий список дополнен. **827 OK.** Мерж — по слову.

Что было предложено до решения:

1. **Перезапустить ночной прогон руками** — `python3 run_daily_pipeline.py` с
   командой Render (все флаги) из чистого checkout `main` = `6ff7587`. Upsert-ы
   идемпотентны, окна 30 дней, квота Performance за ночь не тронута (шаг рекламы
   не дошёл). По §2 CLAUDE.md полный прогон — только с подтверждением; по слову
   запущу (~2,5–3 ч; ночное окно скриптов 00:15…04:30 уже позади, а с алертом
   07:20…07:45 прогон пересечётся — либо запуск сразу, либо после 07:45).
   Альтернатива — Ozon-поток запускает у себя; прошу советника развести.
2. **Сделать шаг funnel нефатальным** и перехватывать таймаут/сеть в загрузчике
   (перевести `requests.post` на `loaders/http_retry.post`, который повторяет 429
   и 5xx; таймаут `requests` он не ловит — добавить). Загрузчик — мой файл;
   `NON_FATAL_STEPS` — общий (`run_daily_pipeline.py`), нужно разрешение. Без
   этого ночь падает каждый раз, когда WB медлит на второй попытке.

### §1 Утро 09-23 — ночная часть проверена 05:14 UTC, алерт — после 07:30

Лог Render, шаг «WB: загрузка заказов» (00:15:40–00:15:47 UTC), против предсказания:

| строка лога | предсказание | факт |
|---|---|---|
| `Получено строк WB orders` | ~1 800 | **1 706** |
| `заказов без отмены / отменённых; строк агрегата` | ~780 / ~1 040; ~1 360 | **734 / 972; 1 293** |
| `Окно записи: с 2026-08-24` | 08-24 | **08-24** |
| `Не записано (старше окна …)` | ~34 дат, ~580 строк, ~800 заказов | **35 дат, 548 строк, 754 заказа, 07-11…08-23** |
| `✅ … записано` | ~780 строк | **745 строк** |
| «ВНИМАНИЕ, пропущено» / тревога «Шаг не выполнен» | нет | **нет**; `✅ Готово` |

Разница в числах — суточный сдвиг окна (ответ на день короче), не поломка.

SQL (`marketplace_code = 'wb'`, 05:14 UTC):

| группа | строк | Σ `orders_qty` / ₽ | Σ `cancelled_*` / ₽ | `observed_at` не null | доля отмен шт / ₽ |
|---|---:|---|---|---:|---|
| ≥ 08-24 (окно ночи) | 745 (31 дата, 08-24…09-23) | 536 / 14 492 945 | 416 / 14 174 614 | **745 из 745, все — этой ночью** | **43,7 % / 49,4 %** (ждали ≈42 / 50) |
| 08-04…08-23 | 1 851 | 1 638 / 41 333 269,29 | 1 465 / 50 731 303,51 | 1 851 (со вчера) | 47,2 % / 55,1 % |
| 03-26…08-03 | 20 796 | 20 601 / 397 818 830,85 | 18 344 / 468 624 128,53 | 20 796 (со вчера) | 47,1 % / 54,1 % |
| < 03-26 | 455 | 498 / 16 607 482 | 0 | **0** | — (созданные, как и было) |

`count(*)` WB: 23 847 = 23 827 + **20 новых ключей** (09-22 и хвост 09-23), в
предсказанном коридоре +30…+60 — чуть ниже, ночь в 00:15 захватывает 09-23
только начатым. Ни одной строки старше 08-24 ночь не тронула (`observed_tonight`
0 вне окна). Созданных в окне 952 (было 928 + новые).

**Витрина ночью не пересчиталась** (прогон упал до KPI): на 05:14 UTC
`daily_sku_kpi` WB за 03-26…08-23 держала старую таблицу (26 288 / 666 524 305,07,
расхождение с новой на 12 069 ключах), окно — старые 928 / 27 783 043.

**Ручной прогон 05:56:31 → 07:01:07 UTC — «Весь пайплайн успешно завершен»**,
15 шагов, тревог «Шаг не выполнен» нет, 429 — 0 (`logs/manual_run_20260923.out`).
Funnel с починкой — 4 дня, все страницы 200, ни одного 429 (пауза 21 с). Реклама
Performance 06:08–06:46 UTC: submit 48, poll 121, 478 campaign units за 09-22.
**Остатки WB ожили впервые с 07-14:** `stock_daily` 2026-09-23 — 2 741 строка ответа
→ **1 435 SKU, 6 938 единиц**, один агрегатный «Склад WB», 108 SKU без артикула
(нет в `sku_catalog`) — как в `wb_stocks_migration.md`.

**Витрина после пересчёта (07:01 UTC) — сверено SQL:**

| группа | `daily_sku_kpi` WB: ключей / расхождений с таблицей | `orders_qty` / ₽ в витрине = таблице | против витрины 09-22 (созданные) |
|---|---|---|---|
| 03-26…08-03 (плато, 131 дата) | (по подгруппам ключи не считала) / **0** | **20 601 / 397 818 830,85** | было 24 094 / 594 156 177 → **−14,5 % шт / −33,0 % ₽** (ждали −15,4 / −34,1 на 151 дате вместе) |
| 08-04…08-23 (20 дат) | (то же) / **0** | **1 638 / 41 333 269,29** | было 2 194 / 72 368 128 → −25,3 % / −42,9 % |
| 03-26…08-23 вместе (151 дата) | **31 312 ключей, расхождений 0** | **22 239 / 439 152 100,14** | было 26 288 / 666 524 305 → **−15,4 % шт / −34,1 % ₽** — ровно предсказание |
| ≥ 08-24 (окно, 31 дата) | 1 404 ключей, расхождений 0 | 535 / 14 467 624 | было 928 / 27 783 043 → **−42,3 % / −47,9 %** (ждали −42 / −50) |
| < 03-26 (27 дат) | 457, расхождений 0 | 498 / 16 607 482 | без изменений — созданные, как записано |

Ключей витрины без строки в таблице — 8 665 на 151 дате и 659 в окне: это
ключи с выкупами без заказов (были и раньше), `orders_qty` у них 0.
`daily_marketplace_kpi` WB пересчитана (390 строк обеих площадок, 07:01 UTC): по
группам те же суммы; **выкупаемость по WB прыгнула** — средний `buyout_rate` на
плато 1,005, в окне 1,843 (выкупы по дате продажи против подтверждённых по дате
заказа) — та самая смена базы, читать с этим в уме. Ozon-строки таблицы — те же
61 709 до прогона; после — свои ночные изменения.

**Ozon по датам до 08-24 — без изменений:** `scripts/kpi_ozon_snapshot.py --compare
data/snapshots/kpi_ozon_by_date_20260922T1559Z.json --until 2026-08-24` (07:05
UTC): расхождений до 08-24 — 2, и это ровно ожидаемые из CLAUDE.md §9-4к: 08-21
`buyouts_qty` 317 → 316, `buyouts_amount_seller` 6 592 194,5 → 6 586 264,0 (−5 930,50,
ручная чистка выкупов Ozon-потока 09-22 15:55, снимок снят до ночного пересчёта
витрины). В окне ночи — 62 законных изменения.

**Алерт 07:30 — ушёл** (Render `mp-analytics-telegram-report`, `sendMessage`
07:31:04 UTC, `message_id 285`, тревог «Шаг не выполнен» нет). Текст воспроизведён
без отправки (`alerts_telegram.py --dry-run --no-send --skip-snapshot --skip-sheet
--target-date 2026-09-22`, `db_writes = 0`): **«🟣 WB вчера — Заказы: 23 / 482 668
руб.»** — это funnel-overlay (`marketplace_orders_analytics` 09-22: 23 / 482 668),
витрина `daily_marketplace_kpi` WB 09-22 после пересчёта — **17 / 278 251**
(подтверждённые), созданных в таблице 22 / 816 956. Ждали «примерно вдвое» — по
рублям funnel выше витрины в 1,73 раза, по штукам в 1,35; относительно созданных
funnel по штукам совпал (23 против 22), по рублям — вдвое ниже (482 668 против
816 956) — funnel считает деньги по-своему, как и записано в
`wb_funnel_consumers.md`. Там же «Отклонение заказов к 7 дн: 77 %» и «главный
риск: выкупы ниже среднего на −76 % (3 против 12)» — выкупы 09-22 из
`supplier/sales` (шаг продаж, правило не менялось) на 06:00 UTC ещё не доехали;
к правилу заказов отношения не имеет. Блокер Ozon — только вечная органика.

**Итог §1:** всё, что предсказывалось, сошлось по числам; единственное отклонение
ночи — падение на funnel, разобрано и починено в ветке; прогон повторён руками.

**Починка funnel — в проде (09:26 UTC 09-23).** Слово владельца в окне сессии
~09:20 UTC: «Да, сливать … push main вашей рукой, как вчера. Это до 00:15 UTC».
`main` за день ушёл на 11 коммитов (Ozon-поток) — слит в ветку (`586b80e`, без
конфликтов), **838 OK** = 738 `main` + 100 `test_wb_*`. Merge-коммит `--no-ff`
**`d56acb4`** (родители `5f63ff0` = `origin/main`, `586b80e` = `wb-fixes`, дерево =
ветке) — на этот раз push прошёл моей рукой: `5f63ff0..d56acb4 → main` 09:25:51
UTC. **Render, по API: оба сервиса live на `d56acb4`** — `mp-analytics` 09:26:32,
`mp-analytics-telegram-report` 09:26:31 UTC. Ночь 09-24 пойдёт с нефатальным
funnel и паузой 21 с. Ветки `wb-fixes` / `wb-fixes-merge` на `origin` = `586b80e` /
`d56acb4`.

**Что смотреть утром 09-24:** в логе Render шаг funnel — четыре дня, все страницы
`HTTP: 200`, ни одного `429` и «⏳»; «Весь пайплайн успешно завершен»; блок «WB:
загрузка заказов» — как 09-23 (окно с 08-25); если funnel всё же откажет — в логе
«⚠️ … шаг не удался … витрину строим дальше» и тревога «Шаг не выполнен, прогон
продолжен», а KPI обеих площадок на месте.

### §2 Карта листа «WB - сентябрь» — `docs/wb_report_model.md`

Главное:

* **Дата реализации у владельца = `rrDate`** («Дата операции») отчёта
  `finance-api /api/finance/v1/sales-reports/detailed`: штраф 2 560 (`saleDt`
  09-01 / `rrDate` 09-02) в листе стоит в 09-02.
* **Оборот** = Σ `retailPriceWithDisc` (Продажа − Возврат) — 7 из 7 дней до
  рубля; это же наш `marketplace_buyouts.buyouts_amount_seller` (1…21.09 —
  16 667 220 = лист).
* **Комиссия у владельца — константа 42 %** (`D = 0,42` на всех днях), из API не
  берётся. Факт по отчёту `B − forPay` = **44,42 %** за неделю (44,25…44,61 по
  дням): владелец недосчитывает ~198 тыс. за неделю 1…7.
* **«НДС за возмещение» (свод AB) = (`ppvzReward` + `rebillLogisticCost`) ×
  22/122** — 7 из 7 до копейки (5 261,53 … 5 029,47). «Возмещение» — это
  возмещение за выдачу/возврат на ПВЗ и издержек перемещения.
* **Эквайринг = `acquiringFee`/1,22** — 5 из 7 до копейки; **09-03 и 09-07 у
  владельца меньше** на 4 276,54 и 3 531,88 — не объяснено; `acquiring/detailed`
  → 204.
* **Прочее = (`paidStorage` + `penalty`)/1,22** — 6 из 7; **09-02 остаток +563,20**
  не объяснён.
* **Логистика ≈ `deliveryService`/1,22** — не тождество: по дням −1 523…+1 172 ₽,
  неделя 112 392,48 против 113 690,54 (−1,1 %); правило владельца не найдено.
* **Реклама 0** сходится: `adv/v1/upd` за сентябрь `[]`, `fullstats` 09-21 —
  копейки; кампаний 548 (171 завершённых, 377 на паузе, активных 0).
* **У нас есть** только оборот (`marketplace_buyouts`); `marketplace_expenses`
  по WB — 0 строк; отчёта реализации нет нигде.
* **Себестоимость:** `supplierArticle` WB — базовый артикул, размер в `techSize`;
  в снимке 1С варианты `артикул-размер` с разной ценой по размерам. Базовый
  артикул есть у **225 из 257** (94,6 % оборота 1…21, 15 765 032 / 16 667 220),
  точный вариант без размера — 24,7 %; размера в `marketplace_buyouts` нет, он
  есть в отчёте реализации (`vendorCode` + `techSize`).
* **Доступ и горизонт:** finance 200 (5 979 строк за неделю, 12,1 МБ; данные с
  **2024-01-29** — самые ранние строки отчёта 251047423 получены одним
  запросом), advert 200, analytics 200. Новых прав не нужно.
* **План пятой** — карта §7: загрузчик отчёта реализации в новую таблицу
  `wb_sales_report_rows` (зерно `rrdId`, миграция по слову), генератор
  `report_wb_month.py` с `--check`, три вопроса владельцу (комиссия 42 % или
  факт; правило логистики; эквайринг 09-03/09-07 и прочее 09-02).

### §3 `isCancel` — лента различает

`order-feed` за 09-15 (по времени статуса): 49 заказов, отмен 21 — **`receipt`
(отказ при получении) 15, `app` (до получения) 4, `expire` 2**. `isCancel` в
`supplier/orders` покрывает всё это; гипотеза 09-21 подтверждена. Правило
`cancelled_orders_*` не меняется; разложение по типу — только join по `srid` с
лентой (31 день, 1 запрос/мин), отдельная задача. Записано в
`docs/wb_orders_write_window.md` и в карте §6.

### Не сошлось — отдельной строкой

* эквайринг 09-03 / 09-07 и прочее 09-02 — три расхождения с листом, не
  объяснены (см. выше);
* логистика — близко, не тождество;
* комиссия владельца — модель (42 %), не факт (44,4 %);
* `acquiring/detailed` — 204: отчёта эквайринга у аккаунта нет, эквайринг —
  внутри отчёта реализации;
* «4 765 кампаний» в логе пробы — счётчик по спискам, верно 548 по `count`.

---

## 2026-09-22, третья задача WB — отчёт (мерж в `main`, Render live 18:40 UTC; **запись закончена 19:20:45 UTC**, контроль сошёлся)

**ЗАПИСЬ ЗАКОНЧЕНА 2026-09-22 19:20:45 UTC** — `marketplace_orders` по WB, 151 дата
03-26…08-23, по слову владельца (инбокс 19:15 UTC + подтверждение в окне сессии
19:1x UTC). Ozon-поток может продолжать свои чистки. Контроль SQL — §5.


**Запись в БД — одна: `marketplace_orders` по WB, 151 дата (03-26…08-23), 22 647
строк upsert-ом** (20 796 плато + 1 851 вышедшие из окна), 19:18:05–19:20:45 UTC, по
слову владельца; до неё за всю задачу `db_writes = 0`. `marketplace_buyouts` не
тронута, продажи в базу не писались, канал Ozon не тронут. Обращений к WB API за
задачу — **14** (1 утренняя проверка + 2 края + 11 продаж), 429 — 0. Общие файлы
сверх разрешённых: `spec/wb/` добавлен (разрешение советника),
`run_daily_pipeline.py` — только то, что слито в ветку вчера.

### Слово на `--apply` — получено дважды, запись выполнена

Инбокс, дополнение 19:15 UTC (советник по прямому поручению владельца) — слово по
числам плана §4 с оговоркой «если по правилу сессии слово нужно в её окне —
владелец продублирует». Запись в боевую таблицу необратима, поэтому спросила в
окне: владелец ответил **«Да, выполняй»** (~19:15 UTC). Push в `main` — владельцем
18:39 UTC; **Render, проверено по API 18:46 UTC: оба сервиса live на `6ff7587`**
(`mp-analytics` создан 18:39:36, live 18:40:19; `mp-analytics-telegram-report`
18:39:36 / 18:40:20 UTC). Проверка «загрузчик в проде» у скрипта прошла
(`origin/main` содержит оба маркера).

### 1. Горизонт — скользящий, подтверждено

`logs/wb_horizon_check_20260922/capture_calls.json`: 03:20:18 UTC, `flag=1` за
2026-03-15, HTTP 200, **0 строк** (накануне 16:16 UTC — 218). Ровно 190 дней,
2026-03-26 выпадет 2026-10-03. `docs/wb_data_integrity.md` §1, на помеченном месте
(`257492b`). API заново не вызывался.

### 0. Слова владельца — в окне сессии 2026-09-22 ~16:00 UTC, дословно

1. «Личное «да» владельца: WB приводим к правилу Ozon до восстановления —
   orders_* = подтверждённые заказы, отменённые отдельно в cancelled_orders_*,
   observed_at, Decimal.»
2. «Слово владельца на мерж wb-fixes в main (--no-ff) после того, как покажешь
   git log main..wb-fixes и прогон тестов.» — показано в §2, мерж-коммит построен
   по нему; push — см. вопрос 1.
3. «Что такое isCancel у WB (отмена до выдачи или ещё и отказ на ПВЗ) — проверь по
   spec/wb/ и напиши в отчёт; если по спеке не видно, так и напиши, не угадывай.»
4. «Слово на --apply пока не даю: сначала план из файлов (--from-files --plan) с
   числами в docs/outbox_wb.md, снимок, и только потом спросишь отдельно.»

**`isCancel` по спеке — не видно.** `spec/wb/12-reports.yaml`, схема ответа
`/api/v1/supplier/orders`, строки 2298–2305, дословно: `isCancel` — «Отмена заказа:
`true` — заказ отменен»; `cancelDate` — «Дата и время отмены заказа. Если заказ не
был отменен, то "0001-01-01T00:00:00". Если часовой пояс не указан, то берётся
Московское время UTC+3». Описание метода (строки 84–100) про отказ при получении
молчит; слов «отказ», «ПВЗ», «невыкуп» в описании заказов нет («ПВЗ» в файле —
только у возвратов и удержаний). По спеке отмену до отгрузки и отказ на ПВЗ не
различить — как и по веб-документации 09-21. Записано в
`docs/wb_orders_write_window.md` ещё 09-21 (там же лаги отмен: 47 % на 3-й день и
позже — гипотеза «покрывает и отказ на ПВЗ» остаётся гипотезой). Подсказка на
будущее из той же спеки: «Чтобы получить все оформленные заказы, используйте
Ленту заказов» (`11-analytics.yaml`, orderFeed) — возможно, статусы там подробнее;
не проверяла.

### 2. Мерж

`git merge main` в ветку — без конфликтов (`b2ff17b`), `git log HEAD..main` пуст.
Общее окно `loaders/pipeline_window.py` — скрипт сырья переведён на него (тест:
04:30 внутри, 04:31 снаружи), своё 00:15…03:15 снято.

Тесты на `803691d`: **820 OK** = 727 `main` + 93 `test_wb_*`
(`PYTHONPATH=. venv/bin/python3 -m unittest discover -s tests`).

```
$ git log --oneline main..wb-fixes
803691d Capture supplier/sales flag=1 into files with --source sales
ea3860e Interim third WB report: horizon confirmed, owner's words, the night prediction before the merge
257492b Confirm the sliding flag=1 horizon, adopt the shared nightly window, show cancellations in repair, add WB specs
b2ff17b Merge branch 'main' into wb-fixes
d9a39cc Report the second WB task: raw captured, Ozon rule in the loader, plan from files in corridor
51be775 Merge branch 'main' into wb-fixes
9ed3f05 Record the from-files restore plan and the one-call sales horizon check
2c54bd7 Interim WB report: the fatal-step blocker is closed in the branch
b19653d Make the WB orders step non-fatal: a strict parser must not take the night down
74f91d6 Merge branch 'main' into wb-fixes
dcde9bd Interim WB report: rule answered, capture running, fatal-step blocker named
ac7ed2e Rewrite WB docs for the 190-day horizon, the Ozon rule and the from-files path
daf8723 Bring WB orders to the Ozon rule and restore history from captured files
e00ff9c Ask the owner about the WB orders rule before touching the loader
2332a91 Report the first WB task: merge, re-measurement, restore plan, manual sheets
6fd1c7f Re-measure WB erosion after 18 days and make the restore plan trustworthy
426ed29 Merge branch 'main' into wb-fixes
c1b8a5c Stop WB order history from eroding and prepare the repair path
```

Diff `main..wb-fixes`: 44 файла, +67 191 / −146 (из них `spec/wb/` — 2,6 МБ спек);
по коду — `loaders/wb_orders_rows.py` (+172, новый), `loaders/wb_orders_loader.py`
(+97/−55), `loaders/wb_sales_loader.py` (окно), `loaders/wb_stocks_loader.py`
(новый эндпоинт), `run_daily_pipeline.py` (+11), `scripts/wb_*` (6),
`tests/test_wb_*` (8), `docs/wb_*` (6).

**Merge-коммит `6ff7587`** (`git commit-tree` с двумя родителями — то же, что
`git merge --no-ff`; `main` выписан в `~/mp-analytics`, поэтому мерж собран
плюмбингом, чужой worktree не тронут). На `origin`: `wb-fixes` = `803691d`,
`wb-fixes-merge` = `6ff7587`. На 17:12 UTC `origin/main` был `bc3dea6` и Render на
нём; **владелец запушил `6ff7587` в `main`, Render live 18:40:19 / 18:40:20 UTC**
(см. вопрос выше). Этот отчёт (и снимок) — коммитами поверх `803691d`, в `6ff7587`
не входят; советник подберёт при следующем мерже.

### 3. Край — 2 файла

16:12–16:13 UTC, 2 обращения `flag=1`: **08-22 — 44 строки, 08-23 — 35**, чужих
дат 0. В `logs/wb_flag1_raw/` теперь 162 файла заказов, 03-15…08-23, дыр 0.

### 4. План из файлов — заново, на слово владельца

`python3 scripts/wb_orders_history_restore.py --from-files logs/wb_flag1_raw
--days-back 29 --plan`, 16:18 UTC, обращений к WB — 0
(`logs/wb_flag1_raw/plan_20260922T1618.out`). `--days-back 29` — окно **ближайшей**
ночи 09-23 начинается с 08-24: всё, чего ночь не перепишет, идёт в очередь, включая
08-22 и 08-23 (с 30 по умолчанию 08-23 выпал бы и остался в старом правиле).

Очередь: 178 дат старше окна, 26 786 созданных / 683 131 787 ₽. **Не пишем 27 дат
раньше 03-26** (498 / 16 607 482; 17 из них за горизонтом). **К записи 151 дата**,
в базе 26 288 / 666 524 305 ₽. Дат без файла 0, чужих 0, пустых 0.

| группа | дат | строк станет / в базе | созданных станет, шт / ₽ | в базе | прирост | → `orders_*` | → `cancelled_orders_*` |
|---|---:|---|---|---|---|---|---|
| плато 03-26…08-03 | 131 | 20 796 / 20 796 | 38 945 / 866 442 959,38 | 24 094 / 594 156 177,27 | **+14 851 / +272 286 782,11** | 20 601 / 397 818 830,85 | 18 344 / 468 624 128,53 |
| вышедшие из окна 08-04…08-23 | 20 | 1 851 / 1 851 | 3 103 / 92 064 572,80 | 2 194 / 72 368 127,80 | +909 / +19 696 445,00 | 1 638 / 41 333 269,29 | 1 465 / 50 731 303,51 |

Коридор плато (созданные) +10 454…+19 715 шт / +152…+351 млн ₽ → **в коридоре**.
Новых ключей (дата, SKU) 0, «в базе есть, в сырье нет» 0; `daily_sku_kpi` по WB на
151 дате — 31 312 ключей, пересчёт перепишет все, новых 0. Против вчерашнего плана:
плато в базе 24 096 → 24 094 (−2 заказа / −27 996 ₽ за ночь 09-22 — старый
загрузчик всё ещё в проде), прирост +14 849 → +14 851; вторая группа +2 даты.

**Для читателя:** `orders_*` по WB на 151 дате станут 22 239 шт / 439 152 100,14 ₽
при нынешних 26 288 / 666 524 305 — история «упадёт» на 15,4 % шт / 34,1 % ₽,
хотя восстановлено +15 760 созданных: отменённые уходят в свою пару колонок.

### 5. `--apply` — выполнен 19:18:05 → 19:20:45 UTC; контроль сошёлся

`python3 scripts/wb_orders_history_restore.py --from-files logs/wb_flag1_raw
--days-back 29 --approve-wb-orders-write` (лог — `logs/wb_flag1_raw/apply_20260922.out`):
план в режиме записи совпал с §4 до копейки, коридор — в коридоре, дат без файла
0; **снимок скрипта** 19:18:36–19:19:36 UTC —
`snapshots/marketplace_orders_wb_before_restore_20260922_191836.csv.gz`, 23 102
строки, 178 дат, sha256 `d2ee5400…1071`; содержимое строка в строку равно снимку
16:19 (`…_161941.csv.gz`, sha256 `effc4e1f…c68a7`) — за три часа между ними таблица
по этим датам не менялась. Оба в этом коммите. Записано **151 дата из 151, отказов
0**, прирост созданных **+15 760 / +291 983 227 ₽** = 14 851 + 909 / 272 286 782,11 +
19 696 445,00. По дням недобор от 0 до 52 % шт, отрицательных нет. Прогресс —
`logs/wb_orders_history_restore_progress.json`: done 151, failed 0.

**Контроль после записи — SQL по таблице, 19:21 UTC** (не счётчики скрипта):

| группа | строк | Σ `orders_qty` / ₽ | Σ `cancelled_orders_qty` / ₽ | `observed_at` не null | = плану |
|---|---:|---|---|---:|---|
| WB 03-26…08-03 (плато) | 20 796 | 20 601 / 397 818 830,85 | 18 344 / 468 624 128,53 | 20 796 | **да, до копейки** |
| WB 08-04…08-23 | 1 851 | 1 638 / 41 333 269,29 | 1 465 / 50 731 303,51 | 1 851 | **да** |
| WB < 03-26 | 455 (27 дат) | 498 / 16 607 482 | 0 / 0 | 0 | не тронуты, как до записи |
| WB ≥ 08-24 (окно ночи) | 725 (29 дат) | 928 / 27 783 043 | 0 / 0 | 0 | не тронуты; их перепишет ночь |
| Ozon, всё | 61 709 | 50 338 / 1 135 589 290 | 34 714 / 651 102 386 | 61 709 | те же, что в 19:12 до записи |

`observed_at` у восстановленных строк — момент съёма сырья: плато 09-21
17:37–19:59 UTC, вторая группа 09-21 20:00 … 09-22 16:13 UTC. Строк с нулём
подтверждённых и ненулём отменённых — 7 502 на плато и 684 во второй группе:
ключи целиком отменённых ячеек остались с `orders_qty = 0`, как задумано (ключ не
исчезает). Покупательская сумма `orders_amount_buyer` на плато 271 902 119,88.

Прежний текст контроля (ожидания до записи):

```sql
-- плато: 20 796 строк, orders 20 601 / 397 818 830,85, cancelled 18 344 / 468 624 128,53, observed_at у всех
select count(*), sum(orders_qty), sum(orders_amount_seller), sum(cancelled_orders_qty),
       sum(cancelled_orders_amount_seller), count(observed_at)
from marketplace_orders where marketplace_code='wb' and order_date between '2026-03-26' and '2026-08-03';
-- вышедшие из окна: 1 851 строк, 1 638 / 41 333 269,29, 1 465 / 50 731 303,51, observed_at у всех
… and order_date between '2026-08-04' and '2026-08-23';
-- до 03-26 не тронуты: 455 строк, 27 дат, Σ orders_qty 498, cancelled 0, observed_at 0
… and order_date < '2026-03-26';
-- Ozon — те же счётчики, что до записи (16:10 UTC 09-22): 61 709 строк, orders_qty 50 338, cancelled 34 714
select count(*), sum(orders_qty), sum(cancelled_orders_qty) from marketplace_orders where marketplace_code='ozon';
```

**Запись закончена 2026-09-22 19:20:45 UTC** (строка для советника — и в шапке
раздела).

### 6. `spec/wb/` — diff пуст, закоммичен

`curl` с Мака получает **498** от антибота даже с браузерными заголовками (README
обещал «обычно отдаёт» — не отдаёт). Сняла из встроенного браузера: `fetch` тех же
адресов внутри `dev.wildberries.ru` + sha256 в браузере — **`12-reports.yaml`
`5024a559…7566b`, `13-finances.yaml` `1df88047…fdf24`, оба байт в байт равны копиям
зеркала** (157 618 и 90 857 байт). Набор доверенный, 15 спек + README + CHANGELOG
закоммичены в `257492b`, строка проверки дописана в README. Все 15 sha256 зеркала
совпали с таблицей README.

### 7. Продажи 03-15…03-25 — 4 файла, горизонт продаж короче

11 обращений `supplier/sales flag=1`, 16:14–16:25 UTC, все HTTP 200: **03-15…03-21
— семь дат подряд `[]`**, 03-22 — 106 строк (2 возврата, `priceWithDisc` 3 238 172),
03-23 — 110, 03-24 — 123, 03-25 — 110. Файлы `logs/wb_flag1_raw/sales_flag1_*.json`
(4), в базу не писались. **Горизонт продаж — 184 дня** (03-22 первая с данными),
на 6 дней короче заказов; семь пустых подряд при ~200 заказах в день в те же дни —
горизонт, не затишье. Скользящий ли — не проверялось. Записано в
`wb_data_integrity.md` §1; прежний вывод 09-21 («03-14 пусто — согласуется с тем же
горизонтом») был неполон. Для глубокой истории продаж — не statistics-api, а
`finance-api /api/finance/v1/sales-reports/detailed` (`13-finances.yaml`).

### 8. Мелочи

`wb_orders_repair.compare` показывает обе пары колонок (`cancelled_*_truth /
_stored / _delta`), `stored_day` читает их с сортировкой; 3 теста (`257492b`).
`--source sales` у скрипта сырья, тест (`803691d`).

### Что смотреть утром 09-23

Деплой до 00:15 UTC состоялся — действует этот сценарий (в логе Render шаг «WB:
загрузка заказов»):
`Окно записи: с 2026-08-24`, `WB orders: заказов без отмены ~780, отменённых ~1 040`,
`Не записано (старше окна …): ~34 дат`, `✅ … записано ~780 строк`; тревоги «Шаг не
выполнен» нет; шаг продаж прошёл как обычно. SQL: `observed_at` не null у **всех**
строк WB с `order_date ≥ 2026-08-24` и ни у одной старше; доля отмен в окне ≈ 42 %
шт / 50 % ₽; `count(*)` WB = 23 827 + новые ключи 09-22 (порядка +30…+60); даты
< 08-24 — не тронуты (если `--apply` не было: 23 102 строки, Σ `orders_qty` 26 786).
Витрина: `daily_sku_kpi` / `daily_marketplace_kpi` WB за ≥ 08-24 — заказы −42 % шт /
−50 % ₽, выкупаемость и ДРР WB — скачок; алерт 07:30 — «WB вчера» из funnel
(созданные) примерно вдвое выше витрины. **Если `--apply` был:** `daily_sku_kpi` WB
за 151 дату = таблице (SQL §5), `daily_marketplace_kpi` WB по датам 03-26…08-23 —
заказы −15 % шт / −34 % ₽ против 09-22; Ozon по датам до 08-24 — без изменений
(Ozon-сессия сверяет `data/snapshots/kpi_ozon_by_date_20260922T1559Z.json`).
`--apply` был — смотреть оба списка: и окно (первая ночь нового загрузчика), и
историю (151 дата после пересчёта KPI). Даты < 03-26 в витрине — по-прежнему
созданные (498), это не ошибка пересчёта.

### Не сошлось — отдельной строкой

* **push в `main` от моего имени не прошёл** — классификатор, три отказа; сделал
  владелец в 18:39 UTC, Render live 18:40;
* **горизонт продаж 184 ≠ 190 у заказов**; вывод 09-21 неполон;
* **плато осыпалось за ночь ещё на 2 / 27 996 ₽**, план пересчитан;
* **`--days-back 29`** вместо 30 по умолчанию — чтобы 08-23 не остался в старом
  правиле; в runbook записано;
* **`curl` с Мака — 498**, README ошибался; проверка сделана из браузера;
* `git merge --no-ff` буквально невозможен из этого worktree (`main` выписан в
  другом) — эквивалент через `commit-tree`, родители и дерево проверены.

---

## 2026-09-21, вторая задача WB — отчёт

`db_writes = 0`. Ветка не слита и не запушена, `--apply` не запускался,
`marketplace_orders` и `marketplace_buyouts` не тронуты, канал Ozon не тронут. Из
общих файлов правлен один — `run_daily_pipeline.py`, по решению советника,
переданному в окно (ниже). **Одно не закрыто:** утренняя проверка горизонта —
она по условию после 03:15 UTC 09-22; процесс заведён, результат впишет та
сессия, что будет жива утром.

### 1. Сырьё `flag=1`

| | |
|---|---|
| файлов | **160**, по одному на дату, 2026-03-15 … 2026-08-21, дыр 0 |
| строк | **44 298**, из них `isCancel` 20 909 (47,2 %); повторов `srid` между файлами 0 |
| проверка на месте | HTTP 200 у всех; строк чужой даты — 0 файлов; пустых ответов — 0 |
| обращений | **155** в этом прогоне (17:30:37 – 20:18:59 UTC, пауза 65 с) + 5 файлов от первой задачи (3 съёмом, 2 из поиска горизонта) |
| 429 / отказов | **0 / 0** |
| самая старая снятая дата | **2026-03-15** (190 дней; 03-14 пуста с первой задачи) |
| где | `logs/wb_flag1_raw/` (вне git, 35,7 МБ), журналы `capture_calls.json`, `depth_calls.json` |

Скрипту добавлено ночное окно: в 00:15–03:15 UTC не стартует и останавливается
(код 3), повторный запуск продолжает; пустой или чужой ответ — «ОТКАЗ», в файл
не кладётся, серия идёт дальше. Ещё одно обращение — `supplier/sales flag=1` за
03-14, 20:20 UTC → `[]`. **Всего обращений к WB за задачу — 156.**

**Горизонт у продаж:** пустой ответ согласуется с тем же горизонтом (не глубже
190 дней), но одним обращением не доказан: мельче ли он — не проверялось, «день
без продаж» исключён по смыслу (в середине марта ~200 заказов в день), не по
данным. Историю продаж 03-15…03-25 надо снимать так же срочно — дата в сутки.

### 2. Ответ по правилу — кто его дал

Вопрос положен блоком в этот файл (`e00ff9c`) и задан в окно. Ответ пришёл в окно
с пометкой источника, дословно: «Решение советника …, передаёт Codex по поручению
Михаила (**не новое личное подтверждение владельца**): да, правило Ozon … В этой
задаче — только код и тесты в ветке wb-fixes … Мержа нет, записи в БД нет, --apply
нет». Так и сделано. **Личного «да» владельца по правилу у меня нет — оно нужно
перед мержем**, вместе со словом на сам мерж: после мержа правило начнёт писать в
прод первой же ночью.

### 3. Загрузчик: правило Ozon, Decimal, нефатальный шаг

`loaders/wb_orders_rows.py` — одна функция `build_order_rows`, ею строят строки
ночной загрузчик, ремонт (`wb_orders_repair`) и восстановление. `isCancel = true`
→ `cancelled_orders_qty / _amount_buyer / _amount_seller`, остальное → `orders_*`,
`observed_at` — момент сбора (при восстановлении из файлов — момент съёма сырья
из журнала, а не момент записи). Копится в Decimal, в строку кладётся один раз
округлённым до копейки — как в `ozon_orders_rows.py`. Нет ключа цены, `None`, все
три цены нулевые, `isCancel` не bool — **отказ сборки**; строки без `nmId` / даты
считаются и печатаются, не глотаются. Окно записи не менялось
(`split_by_write_window`, 30 дней). `wb_sales_loader.py` не тронут.

Проверено на живом ответе (ночной `flag=0` от 16:03 UTC, разобран как ночью, без
`parse_float`): внутри окна создано 1 005 шт / 30 960 564 ₽ = подтверждённых 579 /
15 572 856 + отменённых 426 / 15 387 708; со старым правилом на тех же строках
совпало до рубля; значений с хвостом длиннее копейки — 0.

**Предсказание для читателей — чтобы утром после мержа это не читалось поломкой:**
* внутри окна записи `orders_*` по WB упадут в первую же ночь на **42,4 % шт /
  49,7 % ₽**, `cancelled_*` заполнятся, у строк появится `observed_at`;
* после восстановления на 149 датах истории `orders_*` станут 22 193 шт /
  437 928 043,14 ₽ при нынешних 26 231 / 664 008 773 — заказы WB в витрине на
  истории **упадут** (−15,4 % шт, −34,0 % ₽), хотя восстановлено +15 738 заказов:
  отменённые уезжают в свою пару колонок;
* выкупаемость и ДРР по WB вырастут скачком — смена базы, как у Ozon 08-19;
* до восстановления ряд WB разорван на границе окна записи (внутри —
  подтверждённые, старше — созданные огрызки); граница едет на день в сутки;
* утренний алерт берёт заказы WB из funnel-overlay (`wb_funnel_consumers.md`), а
  funnel считает созданные — алерт и витрина разойдутся сильнее, чем сейчас.

**Нефатальный шаг** (`b19653d`, 18:26 UTC; решение советника, передано в окно:
«исполнитель — ты, в wb-fixes»). Сначала `git merge main` (`74f91d6`, без
конфликтов), затем «WB: загрузка заказов» внесён в `NON_FATAL_STEPS` с причиной в
комментарии. Закрепляющий список дополнен. **Расхождение с буквой поручения:** тот
же файл тестов держал «WB: загрузка заказов» ещё и в списке «обязаны оставаться
фатальными» — оттуда шаг убран (иначе тесты противоречат решению), на его место
поставлен «WB: загрузка продаж/выкупов». Новый
`tests/test_wb_orders_step_non_fatal.py`, 8 тестов: после сбоя идут продажи WB,
заказы / расходы / реклама Ozon и оба KPI; тревога «Шаг не выполнен, прогон
продолжен» называет шаг, слова «упал» нет, уходит и при `--skip-telegram`;
продажи WB фатальны; порядок шагов закреплён; разбор действительно падает на
строке без цены.

Тесты: **787 OK** = 698 `main` + 89 `test_wb_*` (было 51). `main` за вечер ушёл
вперёд ещё раз (лист заказов и доставка в Telegram) — слит повторно в 20:21 UTC
(`51be775`, без конфликтов), `git log HEAD..main` пуст.

```
$ git log --oneline main..HEAD
51be775 Merge branch 'main' into wb-fixes
9ed3f05 Record the from-files restore plan and the one-call sales horizon check
2c54bd7 Interim WB report: the fatal-step blocker is closed in the branch
b19653d Make the WB orders step non-fatal: a strict parser must not take the night down
74f91d6 Merge branch 'main' into wb-fixes
dcde9bd Interim WB report: rule answered, capture running, fatal-step blocker named
ac7ed2e Rewrite WB docs for the 190-day horizon, the Ozon rule and the from-files path
daf8723 Bring WB orders to the Ozon rule and restore history from captured files
e00ff9c Ask the owner about the WB orders rule before touching the loader
2332a91 Report the first WB task: merge, re-measurement, restore plan, manual sheets
6fd1c7f Re-measure WB erosion after 18 days and make the restore plan trustworthy
426ed29 Merge branch 'main' into wb-fixes
c1b8a5c Stop WB order history from eroding and prepare the repair path
```
(коммит с этим отчётом — следующий поверх.) Diff против `main` по коду: новые
`loaders/wb_orders_rows.py` (+172), `scripts/wb_flag1_raw_capture.py` (+114),
`wb_flag1_depth_probe.py`, `wb_remeasure_probe.py`, `wb_remeasure_analyze.py`;
`loaders/wb_orders_loader.py` +97 / −55 (окно записи из первого коммита ветки и вынос
сборки строк);
`scripts/wb_orders_history_restore.py` — 668 строк; `run_daily_pipeline.py` +11;
тесты — 5 новых файлов WB и правка `test_non_fatal_total_orders.py`.

### 4. План восстановления из файлов

`python3 scripts/wb_orders_history_restore.py --from-files logs/wb_flag1_raw --plan`,
20:21 UTC, обращений к WB — 0; вывод целиком — `logs/wb_flag1_raw/plan_20260921T2021.out`.

Очередь: 176 дат старше окна, 26 729 / 680 616 255 ₽. **Не пишем 27 дат раньше
03-26** (498 / 16 607 482 ₽; 16 из них за горизонтом). **К записи 149 дат**, в базе
26 231 / 664 008 773 ₽. Дат без файла — **0**, чужих — 0, пустых — 0.

| группа | дат | строк станет / в базе | созданных станет, шт / ₽ | в базе | прирост | → `orders_*` | → `cancelled_orders_*` |
|---|---:|---|---|---|---|---|---|
| плато 03-26…08-03 | 131 | 20 796 / 20 796 | 38 945 / 866 442 959,38 | 24 096 / 594 184 173,27 | **+14 849 / +272 258 786,11** | 20 601 / 397 818 830,85 | 18 344 / 468 624 128,53 |
| вышедшие из окна 08-04…08-21 | 18 | 1 795 / 1 795 | 3 024 / 88 803 487,80 | 2 135 / 69 824 599,80 | +889 / +18 978 888,00 | 1 592 / 40 109 212,29 | 1 432 / 48 694 275,51 |

Сравниваются **созданные** (подтверждённые + отменённые) против `orders_* +
cancelled_orders_*` базы — `stored_dates` складывает обе пары, поэтому сравнение
верно и до записи, и после. **Коридор плато** зашит в код: +10 454 … +19 715 шт,
+152 … +351 млн ₽ → **в коридоре**; вне коридора запись отказывает (код 4), плато
с дырой в сырье — тоже; на куске очереди коридор не проверяется, и об этом
печатается строка. Предсказание по четырём пробам (+14 356 / +218 млн): по штукам
сошлось в 3,4 %, **по рублям занизило на 24,9 %** — доля недобора на всём плато
38,13 % шт / 31,42 % ₽, а не 37,33 / 26,84: четыре даты оказались по рублям
легче среднего. Коридор это покрыл, но рублёвую оценку по четырём датам больше
точечной не считать.

**Ключи `daily_sku_kpi`** (WB, 149 дат): 31 213, пересчёт перепишет все; **новых
ключей 0** на всех 149 датах, не только на пробах; ключей «в базе есть, в сырье
нет» — 0; ключей без источника восстановление не создаёт (только upsert).

Обновлено под горизонт: `KNOWN_GOOD_DEPTH_DAYS = 190`, шапка скрипта, runbook
(переписан под `--from-files`), `wb_data_integrity.md` §1 — опровергнутое помечено
с причиной, не удалено. Проверка «загрузчик починен» теперь смотрит
`origin/main`, а не свой checkout: в ветке он починен всегда, и прежняя проверка
пропустила бы запись при непочиненном проде.

### 5. Что WB называет `isCancel` — не установлено, вопрос

Документация (dev.wildberries.ru → Отчёты → `GET /api/v1/supplier/orders`,
прочитана 2026-09-21) дословно: `isCancel` — «Отмена заказа: true — заказ
отменен»; `cancelDate` — «Дата и время отмены заказа. Если заказ не был отменен,
то "0001-01-01T00:00:00"». **Отмену до отгрузки и отказ на ПВЗ она не различает.**
По данным (3 538 отмен на 29 датах): в день заказа 34,7 %, 1–2 дня 18,0 %, 3–7
дней 30,0 %, 8–14 — 10,1 %, 15–21 — 5,9 %, дальше 1,3 %; время в `cancelDate`
всегда 00:00:00. 47 % отмен — на третий день и позже; **гипотеза**, что `isCancel`
покрывает и отказ при получении, — по смыслу, не по документу. Записано в
`docs/wb_orders_write_window.md`. **Вопрос владельцу/советнику:** кто-нибудь знает
по кабинету WB, входит ли отказ на ПВЗ в отмену? Правило от ответа не меняется,
меняется чтение `cancelled_orders_*` у WB: «не выкуплено по любой причине» против
«отменено до отправки».

Там же в документации: «Информация о заказе хранится 90 дней» (на деле 190),
лимит 1 запрос в минуту, 1 строка = 1 заказ = 1 единица товара.

### 6. Утренняя проверка горизонта — заведена, результата ещё нет

Отдельный процесс (PID 62734, `logs/wb_horizon_check_20260922/run.sh`) ждёт
03:20 UTC 09-22 и делает одно обращение `flag=1` за 03-15. Итог —
`logs/wb_horizon_check_20260922/capture_calls.json`: `rows: 0` — «скользящий»
подтверждён, `rows: 218` — опровергнут. Строку в `docs/wb_data_integrity.md` §1
(место помечено «Результат — строкой ниже») впишет сессия, живая утром. Если
ноутбук спал — процесс выстрелит при пробуждении, ночное окно скрипт сам обойдёт.

### Не сошлось — отдельной строкой

* **рублёвый прирост плато**: предсказано +218 млн, по файлам +272,3 млн (в
  коридоре); штучный +14 356 против +14 849;
* **«обязаны оставаться фатальными»** в `test_non_fatal_total_orders.py` держал
  шаг заказов WB — поручение про него не знало, список поправлен;
* **ответ по правилу** — советника, не личное «да» владельца;
* **`isCancel`** документацией не определён;
* **27 дат раньше 03-26 остаются по старому правилу** (498 созданных огрызками):
  после восстановления в `marketplace_orders` по WB будут жить два смысла
  `orders_*`, разделённые датой 03-26. Развести можно только записью этих дат из
  сырья — а это и есть «заказы без выкупов», от которых отказались. Оставлено до
  истории продаж; любая сумма по WB «с начала года» эти 498 зацепит;
* **диагностика ремонта** (`wb_orders_repair.compare`) сравнивает только
  `orders_qty` — под новым правилом позднюю отмену она увидит как −1
  подтверждённый, а `cancelled_*` в отчёте не покажет; пишет при этом строку
  целиком. Скрипт в прогон не включён, правка мелкая, в задачу не входила;
* **`main` дважды ушёл вперёд за вечер** — к завтрашнему мержу понадобится ещё
  один `git merge main`, если Ozon-поток что-то сольёт ночью или утром.

### К завтрашнему мержу — что должно быть сказано словами

1. личное «да» владельца по правилу Ozon для WB;
2. слово на мерж после чистой ночи 09-22;
3. после деплоя — доснять сырьё за даты, вышедшие из окна с 08-22 по день
   мержа − 30 (дата в сутки), затем план из файлов **заново той сессией, что
   пойдёт писать**, и слово владельца по его числам; час пересчёта KPI — с
   Ozon-потоком.

---

## 2026-09-21, первая задача WB — отчёт

`db_writes = 0`. Ветка не слита, восстановление не запускалось, загрузчики не
выкачены, общие файлы и канал Ozon не тронуты. Сырьё и разбор:
`logs/wb_probe_20260921/` (вне git), повторить разбор без обращений к WB —
`python3 scripts/wb_remeasure_analyze.py --raw logs/wb_probe_20260921`.

### Главное, до таблиц

1. **Окно записи держится** (п. 2а сошёлся на двух датах, по множествам и по
   значениям) — план ниже показан.
2. **У `flag=1` есть горизонт, 190 дней, и он едет вперёд.** 2026-03-01,
   отдававшая 09-03 362 строки, сегодня отдаёт `[]`. Двоичный поиск: 03-14
   пусто, 03-15 — 218 строк. 16 дат очереди уже невосстановимы. Если горизонт
   скользящий (гипотеза, два замера: 09-03 — 186 дн. отдаёт; 09-21 — 190
   отдаёт, 191 нет), то **2026-03-26, первый день полного сбора WB, выпадет
   2026-10-03**, дальше — по дню истории в сутки. Это срок для всего порядка
   «мерж → снимок → восстановление». Вопрос 1 внизу — как от срока отвязаться.
3. **Ожидание в рублях в runbook завышено в 1,6 раза**: «+360 млн ₽» получено
   переносом доли недобора в штуках (37 %) на рубли. Недобор в рублях на тех
   же четырёх датах измерен — 26,84 %. Осыпаются дешёвые ходовые SKU.

### 1. Слияние и тесты

`git merge main` — **без конфликтов**, мерж-коммит `426ed29`. Файлы ветки в
`main` не менялись, пересечений нет.

Тесты (`PYTHONPATH=. venv/bin/python3 -m unittest discover -s tests`):
**687 OK** сразу после слияния = 640 из `main` + 47 `test_wb_*` (ожидание
«около 680» — в ветке 47 тестов WB, а не ~40). После правок этой сессии —
**691 OK** (+4 теста на чтение плана). Попутно: полный прогон печатает 13
`ResourceWarning: unclosed ssl.SSLSocket` к `104.18.38.10:443` — это тесты
`main`, у `test_wb_*` отдельно их 0; не разбирал, чужие файлы.

```
$ git log --oneline main..HEAD
6fd1c7f Re-measure WB erosion after 18 days and make the restore plan trustworthy
426ed29 Merge branch 'main' into wb-fixes
c1b8a5c Stop WB order history from eroding and prepare the repair path
```
(коммит с этим отчётом и `inbox_wb.md` — следующий поверх.)

### 2. Перепроверка измерений 09-03

Что с чем сравнивалось: `flag=1` за дату — все заказы с этой датой заказа, как
WB отдаёт их сейчас; срез `flag=0` — тот же день внутри ответа с
`dateFrom = 2026-08-22`; база — агрегат (дата, nmId), записанный последней
ночью. «Заказ» везде = строка ответа, **отменённые входят**.

| | было 09-03 | сейчас 09-21 |
|---|---|---|
| **a) полнота `flag=0` внутри окна** | 08-10 (24 дн.): 218 = 218, множества `srid` совпали, в базе 218 | **09-05 (16 дн.): 58 = 58**, множества `srid` совпали (только в `flag=1` — 0, только в `flag=0` — 0), дублей `srid` 0, наборы ключей строк равны, **различий значений ни в одном поле — 0**; база по SKU: 39 = 39, 58 шт, продавец 1 853 100, покупатель 1 076 105 — всё равно. **08-24 (28 дн., у края окна): 66 = 66**, то же самое, SKU 47 = 47, 2 218 629 / 1 290 612. Копеек на этих двух датах нет вовсе — «до копейки» здесь не проверялось. В ответе `flag=0` (1 812 строк, 65 дат, из них 34 даты / 807 строк старше окна — их и затрёт ближайшая ночь) строк с `lastChangeDate < date` — 0, как и 09-03 |
| **b) глубина `flag=1`** | 03-01 (186 дн.) → 362 строки | **03-01 (204 дн.) → `[]`, HTTP 200.** Не сошлось: у `flag=1` есть горизонт. Поиск (5 обращений): 03-17 → 206, 03-09 → 0, 03-13 → 0, 03-15 → 218, 03-14 → 0. **Первая дата с данными 03-15 (190 дн.), последняя пустая 03-14 (191 дн.)**; в базе на 03-09, 03-13, 03-14 заказы есть (13, 10, 14), то есть пусто — это горизонт, а не день без продаж. Вывод 09-03 «пустой 02-04 — начало истории аккаунта, границей считать нельзя» теперь под вопросом: 02-04 мог быть уже за горизонтом |
| **c) остатки** | старый `GET` — 404 deprecated; новый `POST` — 200, `data.items[]`, всё под `warehouseId = -999999` | старый — **тот же 404**, тот же текст и ссылка `release-notes?id=494`. Новый — **200, структура та же**: `data.items[]`, 1 000 строк на странице (снята одна, `limit=1000`), все `-999999 / «Склад WB»`. Ключи строки: `chrtId, inWayFromClient, inWayToClient, nmId, quantity, regionName, warehouseId, warehouseName`; `regionName` (везде «Склад WB») в документах 09-03 не назван — новый он или тогда не записали, по документам не установить, разбор его не читает. Разбор ветки на сегодняшней странице отработал: 1 000 строк → 386 SKU, 4 033 шт |
| **d) недобор на четырёх датах** | 280/160, 271/189, 366/226, 291/188 — 1 208/763, 37 % | **280/154**, 271/189, 366/226, 291/188 — **1 208/757, 37,33 %**. `flag=1` не изменился ни на одной. База: 07-21 ушла 160 → 154 (−6 за 18 дней, осыпание продолжается); три старые даты стоят — плато. По датам: 45,0 / 30,3 / 38,3 / 35,4 % |

Новое по d), чего 09-03 не мерили:

* **рубли** (продавец): истина 25 589 666,98 против 18 722 179 в базе, недобор
  6 867 487,98 = **26,84 %**; по датам 37,15 / 20,40 / 28,18 / 23,39 %. На 04-02
  в истине есть копейки (7 649 699,98) — у WB они бывают;
* **множества SKU совпали на всех четырёх датах** (143 = 143, 158, 190, 158; ни
  одного SKU только в истине или только в базе). Осыпание режет ячейку
  (дата, SKU) до числа недавно менявшихся заказов, но целых ячеек не убивает:
  различаются 47, 32, 49 и 36 ячеек — 164 из 649;
* **отменённых в истине 617 из 1 208 — 51 %** (42 / 59 / 51 / 52 % по датам); в
  ночном `flag=0` — 1 036 из 1 812.

### 3. План восстановления

`python3 scripts/wb_orders_history_restore.py --plan` — ни одного обращения к WB:

```
Окно записи загрузчика: с 2026-08-22. Восстанавливаем всё, что старше.
Дат старше окна: 176 (2026-02-04 … 2026-08-21)
В базе по ним: 26,729 заказов / 680,616,255 ₽
В очередь этого прогона: 176 дат ≈ 190 мин при паузе 65 с
⚠️  Глубже проверенных 186 дней: 20 дат (2026-02-04…2026-03-18), 135 заказов / 5,206,964 ₽
```

Перед запуском пришлось чинить сам план: `stored_dates` читал 23 тыс. строк
`range()` без `order()` — дефект из how-we-work от 09-15. Теперь сортировка по
полному ключу, повтор ключа — отказ, Decimal, пустая сумма роняет (`6fd1c7f`).
Сверка с независимым SQL: 176 дат, 26 729, 680 616 255,07 — **совпало**.

Против runbook (158 дат, 24 919 / 617 416 438): **дат 176** — за 18 дней из окна
вышли ещё 18 (08-04…08-21); **те же 158 дат сегодня — 24 594 / 610 791 655,27**,
то есть за 18 дней осыпалось ещё 325 заказов / 6 624 782,73 ₽.

Очередь неоднородна, одной долей её мерить нельзя:

| группа | дат | в базе, шт / ₽ | что ждать |
|---|---:|---|---|
| до 03-15 — **за горизонтом** | 16 | 74 / 3 118 493 | **не восстановить.** `flag=1` пуст; скрипт по защите «пустой ответ» их не тронет. Истина была большой: 03-01 — 362 заказа при 1 в базе |
| 03-15…03-25 — до начала сбора WB | 11 | 424 / 13 488 989 | в базе огрызки, модель 37 % неприменима. Измерено: 03-15 — 218 шт / 7 401 461 ₽ против 13 / 385 918; 03-17 — 206 / 6 308 740 против 19 / 779 270. Порядок: +1,9 тыс. заказов, +62 млн ₽ (оценка: среднее двух измеренных дат × 11 минус база; не измерение). Выкупов WB до 03-26 в базе 4 штуки — см. вопрос 3 |
| 03-26…08-03 — плато | 131 | 24 096 / 594 184 173,27 | **+14 356 заказов** (коридор 10 454…19 715 при 30,3…45,0 %) и **+217 952 871 ₽** (коридор 152…351 млн при 20,4…37,2 %) |
| 08-04…08-21 — вышли из окна за 18 дней | 18 | 2 135 / 69 824 599,80 | осыпались не до плато: 08-10 была 218 (09-03), сейчас 154, −29 %. Доли не мерил — оценки не даю |

Ожидание runbook «+14 600 заказов» для плато подтверждается (+14 356).
**«≈ +360 млн ₽, коридор 265–466» — нет**: это штучная доля, применённая к
рублям; по измеренной рублёвой — +218 млн. Порог «остановиться, если вне
коридора» в скрипте и runbook надо переписать до запуска, иначе прогон
остановится на верных данных.

Время: к восстановлению 160 дат (176 − 16 за горизонтом) × 65 с =
**2 ч 53 мин**; вся очередь с пустыми — 3 ч 11 мин. Порядок по умолчанию — от
старых к свежим, при горизонте это правильно; `--newest-first` теперь вреден.

**Витрины.** Ключей `daily_sku_kpi` по WB на этих 176 датах — **31 670**: 23 046
с заказами (ровно число строк `marketplace_orders`), 8 624 — выкупы без
заказов, только из расходов — 0. Витрина сейчас равна источнику: сумма заказов
в KPI 26 729 / 680 616 255,07, расходящихся ключей 0. Пересчёт перепишет все
31 670 (он полный, upsert), значения изменятся там, где изменятся заказы: на
плато — порядка четверти ячеек (164 из 649 на пробах). **Новые ключи**: на
плато 0 на всех четырёх пробах; на 03-15…03-25 — много (03-15: 117 новых SKU,
03-17: 99), порядка тысячи — оценка по двум датам. **Ключей без источника не будет**: сейчас их 0,
а восстановление только upsert-ит и ничего не удаляет — в отличие от Ozon-22.
SKU «в базе есть, в `flag=1` нет» на шести датах — 0. `reports_daily_sku_kpi.py`
пересчитывает обе площадки — час согласовать с Ozon-потоком.

### 4. Ручной отчёт — WB

Файлы одной книги в двух состояниях (июль: 45 листов, сентябрь: 51).

**Месячные листы WB**: видимые `WB - февраль`, `март`, `апрель`, `май`, `июнь`,
`июль`, `август (2)`, `сентябрь`; скрытый `WB - май (2)`. **Прочие WB**:
`WB Дискаунтер` (видимый), скрытые `Выкупы - WB Топаз` (декабрь 2025, мёртвый),
`WB - прогноз`, `Прогноз WB - февраль`, `(2)`, `(3)`. **Общие с WB-колонками**:
`Заказы`, `Заказы Standard`, `сверка`, скрытые `Обе площадки*`.

**`WB - сентябрь`** (151 × 25, рабочие колонки A–S, T81 — дневные накладные
74 002), четыре блока:

1. **«Выкупы WB», строки 4–38, итог 39** — это и есть заготовка под генератор.
   Заполнено по 09-15.
   `A` дата (`=A4+1`) · `B` Оборот с НДС — **число** · `C` Комиссия с НДС —
   **число** · `D =IFERROR(C/B,)` · `E` Выручка `=(B-C)/1.22-[1]свод!AB{r+1}` —
   минус «НДС за возмещение» из внешней книги `V:\…\Сентябрь\Аналитика выкупов
   Wildberries Сентябрь.xlsx` · `F` Себестоимость — **число** · `G =E-F` ·
   `H =G/E` · `I` Логистика — **число** без НДС (16 135,7295… = x/1,22) ·
   `J =I/E` · `K` Реклама — число, в сентябре везде 0 (в июле
   `='Заказы Standard'!N{r-1}`) · `L =K/B` · `M` Эквайринг — **число** без НДС ·
   `N =M/E` · `O` Прочее — **число** без НДС · `P =G-I-K-M-O` · `Q =P/E` ·
   `R =P-$T$81` · `S =R/E`. Итог — `SUM` по колонкам; строки 40–41 — недельные
   суммы и свод с `Ozon - сентябрь` и `ЯМ- сентябрь`.
2. «Заказы с конвертацией в выкупы», 46–73 — мёртвый февральский блок
   (`="01.02.2026"`, `=Заказы!#REF!`).
3. «План/факт по выкупам», 81–110: план — числа, факт — ссылки на блок 1
   (`C=E{r-77}`, `F=G{r-77}`, `I=K{r-77}`, `L=I+M+O` блока 1).
4. «Заказы в выкупы», 117–150: `E =Заказы!K{r-114}*0.55`, `G =Заказы!L*0.55`,
   `I =E*0.8%`, `M =E*4.4%`, `O =E*0.1%`, `K` = реклама блока 1, дальше как в
   блоке 1. Выкуп 55 % и три ставки зашиты константами; на 147 — `=#REF!+1`.

`WB - июль` — те же колонки; в июльской книге заполнено по 07-09, хвост 35–38
прогнозный (`D = 0.33`, `C = B*D`); в сентябрьской книге июль закрыт целиком.

**`Заказы`**, J–P «созданные заказы Wildberries»: `K` Выручка
`='Заказы Standard'!K + 'WB Дискаунтер'!B`, `L` Маржа — так же, `N`, `P` — из
`Заказы Standard`; `M =L/K`; итог `O39 =N/(K/0.64*0.6)`. **`Заказы Standard`**
J–P: `K`, `L`, `P` (соинвест) — числа, выручка **без НДС** (863 109,41 × 1,22 =
1 052 993,48); `P10 =1-[1]свод!B78/[1]свод!C78`. **`WB Дискаунтер`**: `B`
Выручка, `C` Маржа — числа, `D =C/B`. У владельца WB — два потока, Standard и
Дискаунтер, в заказах они складываются.

**`сверка`**, блок «1–10 сентября» (70–87), колонка C «WB из ЛК»: Оборот
`=SUM('WB - сентябрь'!B4:B13)`, Выручка `=(C73-C77)/1.22`, Фин. рез `=SUM(P4:P13)`,
Комиссия `=SUM(C4:C13)`, Эквайринг `=SUM(M4:M13)`; Ебитда, возвраты, хранение,
штрафы, прочее — числа руками. Рядом G «WB Пульт» и отклонение в млн.

**Сверено по значениям** (колонка B против `marketplace_buyouts`, WB,
`sum(buyouts_amount_seller)` по дате выкупа): **сентябрь — 15 дат из 15 рубль в
рубль** (09-01 1 255 921 … 09-15 540 749; копеек на этих датах нет). Якорь
генератора есть. **Июль** (31 день из сентябрьской книги): у владельца
147 533 073,37, в базе 145 357 194,39, **−2 175 878,98 (−1,47 %)**; 6 дат равны, на
23 база ниже, на 2 выше (+847 и +246). Направление то же, что у дефекта
`wb_sales_loader` без окна записи; считать это его доказанным размером нельзя —
лист владельца тоже срез на свой день (07-21: лист 2 927 937, `flag=1` 09-03 —
2 941 348, база 2 920 115). Заказы: 09-01 у владельца (863 109,41 + 112 516,20) ×
1,22 = 1 190 263 против 1 170 007 в базе, −1,7 % — не разбирал, определения
могут не совпадать.

Комиссии, логистики, эквайринга и прочего по WB в базе нет: в
`marketplace_expenses` строк WB ноль (292 571 строка, все Ozon). Генератор сегодня
закроет колонку B; C, I, M, O — финансовый отчёт WB, которого мы не собираем.
F (себестоимость) у Ozon-генератора считается из `article_unit_costs` (снимок 1С)
× позиции выкупов — путь для WB тот же, покрытие артикулов WB не проверял.

### 5. Обращения к WB API — 18, все перечислены

`statistics-api`, пауза 65 с, повторов нет, **429 — 0**. 16:03–16:20 UTC, вне
ночного окна.

| # | UTC | запрос | ответ |
|---:|---|---|---|
| 1 | 16:03:15 | orders `flag=0`, `dateFrom=2026-08-22` | 200, 1 812 строк |
| 2–3 | 16:04–16:05 | orders `flag=1`: 09-05, 08-24 | 200: 58, 66 |
| 4 | 16:06:32 | orders `flag=1` 03-01 | 200, `[]` |
| 5–8 | 16:07–16:10 | orders `flag=1`: 07-21, 06-05, 05-22, 04-02 | 200: 280, 271, 366, 291 |
| 9 | 16:12:00 | `GET supplier/stocks` | 404 deprecated |
| 10 | 16:12:00 | `POST stocks-report/wb-warehouses` (seller-analytics-api) | 200, 1 000 строк |
| 11–15 | 16:13–16:17 | поиск горизонта, `flag=1`: 03-17, 03-09, 03-13, 03-15, 03-14 | 200: 206, 0, 0, 218, 0 |
| 16–18 | 16:18–16:20 | сырьё, `flag=1`: 03-16, 03-18, 03-19 | 200: 243, 189, 192 |

Ориентир 8–10 превышен на 8, сознательно: 5 — найти горизонт (без него строка
плана «глубже 186 дней» была бы неверной), 3 — положить на диск дни, которые при
скользящем горизонте выпадают первыми. В `logs/wb_flag1_raw/` лежит сырьё
03-15…03-19 — ближайшие пять суток ничего не теряется безвозвратно. Между № 15
и № 16 прошло 51 с вместо 65 (два скрипта подряд) — отказа не было.

### Не сошлось — отдельной строкой

* **глубина `flag=1`**: 03-01 отдавала 362 строки, теперь пусто; горизонт 190
  дней, 16 дат очереди потеряны, срок для 03-26 — 2026-10-03 (гипотеза
  «скользящий»; проверка — одно обращение завтра: 03-15 должна стать пустой);
* **ожидание в рублях**: +360 млн (коридор 265–466) → +218 млн (152–351) для
  плато; в runbook рублёвую долю не мерили, взяли штучную;
* **объём очереди**: 158 дат / 24 919 / 617,4 млн → 176 / 26 729 / 680,6 млн; те же
  158 дат потеряли 325 заказов / 6,62 млн ₽;
* **07-21 в базе**: 160 → 154;
* **`KNOWN_GOOD_DEPTH_DAYS = 186`** и шапка скрипта/runbook (158 дат, 2 ч 51 мин,
  360 млн) устарели; сегодняшний `--plan` зовёт «глубже проверенного» 20 дат,
  а за горизонтом на деле 16. Не правил: числа поедут снова, а план по правилу
  строит та сессия, что пойдёт писать;
* **тестов 687, а не ~680**;
* **`regionName`** в ответе остатков — в документах 09-03 не упомянут;
* **деньги в загрузчике — float**: `aggregate_orders` суммирует float, в базе уже
  лежит `5258633.509999999995` (08-05). Восстановление перепишет 160 дат тем же
  путём; у WB копейки бывают. Правка в `loaders/wb_orders_loader.py` — поведение
  ночного шага, без слова не трогал;
* **одно имя — два смысла**: у Ozon с 09-17/18 `orders_*` — подтверждённые,
  отмены в `cancelled_orders_*`; у WB `orders_*` — созданные вместе с отменёнными
  (51 % на пробах), `cancelled_orders_*` пусты (0 строк). Любая строка «заказы
  обеих площадок» складывает разное.

### Вопросы к советнику и владельцу

1. **Снять сырьё `flag=1` по всем 160 датам сейчас, а писать потом?** 2 ч 53 мин,
   только чтение, от старых к свежим, `scripts/wb_flag1_raw_capture.py` готов.
   Тогда мерж, снимок и слово владельца перестают бежать наперегонки с
   горизонтом, а план перед `--apply` строится по сырью на диске без обращений —
   как у Ozon в двадцать третьей. Скрипту восстановления понадобится режим «из
   файлов» — небольшая правка в моих файлах. Без этого — порядок из runbook надо
   успеть до 2026-10-02.
2. **Привести WB к правилу Ozon до восстановления?** `isCancel` есть в каждой
   строке `flag=1`. Если да — делать до записи, чтобы не переписывать 160 дат
   дважды; это правка загрузчика и витринного смысла, решение не моё. Что именно
   WB называет `isCancel` (отмена до выдачи или и отказ на ПВЗ), по спеке не
   проверял.
3. **03-15…03-25 восстанавливать?** Это +1,9 тыс. заказов в дни, где выкупов WB в
   базе нет (4 штуки до 03-26): появятся даты с заказами и нулём выкупов.
   Сырьё сниму в любом случае, если п. 1 — да.
