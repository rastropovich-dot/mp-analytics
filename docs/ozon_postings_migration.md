# Миграция загрузчиков заказов на курсорные методы

`/v2/posting/fbo/list` и `/v3/posting/fbs/list` помечены `deprecated: true`
с датой отключения **31.08.2026**. Дата прошла, оба метода на 2026-09-13 ещё
отвечают — но на это полагаться нельзя: `/v3/finance/transaction/list`
переносили дважды и отключили ровно в объявленный срок.

Замены: `/v3/posting/fbo/list` и `/v4/posting/fbs/list`.

## Три отличия контракта

```
                      старое                  новое
постраничность        offset                  cursor
верхний уровень FBO   result: []              postings: [] + cursor + has_next
верхний уровень FBS   result.postings: []     postings: [] + cursor + has_next
размер страницы       1000                    100        ← в именах полей не видно
```

Третье отличие нашлось только на живом вызове: параметр `limit` есть в обеих
версиях, но у новых стоит `maximum: 100`
(`posting.v3.PostingFboListRequest.limit`, `posting.v4.PostingFbsListRequest.limit`).
**Страниц станет вдесятеро больше**, и это надо закладывать вместе с частотным
лимитом.

## Сверка, 2026-09-13 (этап 1, db_writes = 0)

Сравнивались МНОЖЕСТВА `posting_number` в обе стороны, а не количество: при
потерянной странице и лишних дублях количество совпало бы.

Код: `scripts/posting_api_parity.py`.

### FBO — чисто

```
дата          старый  новый  уник ст  уник нов  стр ст  стр нов
2026-08-06       485    485      485       485       1        5   ✅
2026-09-05       148    148      148       148       1        2   ✅
2026-09-08       151    151      151       151       1        2   ✅
2026-09-10       168    168      168       168       1        2   ✅
```

2026-08-06 взята как самая крупная дата августа: курсор перелистнул пять
страниц и собрал ровно то же множество, что старый метод за одно обращение.
Это и была главная проверка перехода со смещения на курсор.

### FBS — множества сошлись, поля отличаются

```
дата          старый  новый  уник ст  уник нов  стр ст  стр нов
2026-08-06       275    275      275       275       1        3
2026-09-05       218    218      218       218       1        3
2026-09-08       244    244      244       244       1        3
2026-09-10       212    212      212       212       1        3
```

**В `/v4/posting/fbs/list` нет поля `created_at`** — подтверждено живым ответом
и спекой (`posting.v4.PostingFbsListResponse.Postings`, 40+ полей, `created_at`
среди них нет).

Наш загрузчик FBS его **не читает**: дата берётся из `in_process_at` или
`shipment_date` (`loaders/ozon_fbs_orders_loader.py:101`), оба поля в новой
версии есть. Для FBS это не поломка, а неточность проверки — список полей был
общий для обеих схем, по образцу FBO, где `created_at` читается.

Поймать стоило: если бы `created_at` читался, переключение молча испортило бы
дату заказа.

### Цена сменила форму — четвёртое отличие контракта

Сравнение ЗНАЧЕНИЙ (а не только наличия полей) нашло то, чего не видно ни в
именах, ни в множествах `posting_number`:

```
старый   products.price = '14560.00'                  строка
новый    products.price = {'amount': '14560',          объект money.postingMoney
                           'currency': 'RUB'}
```

981 отличие на четырёх датах FBO, и все до единого — этой формы. Расхождений в
`status`, `in_process_at`, `shipment_date`, `sku`, `offer_id`, `quantity` нет
ни одного; случаев, когда товар есть в одном ответе и отсутствует в другом,
тоже нет.

По спеке это **единственное значимое поле, сменившее форму**:

```
FBO v2 -> v3
  products.price          string -> money.postingMoney {amount, currency}
  products.digital_codes  —      -> array   (не читаем)
```

Остальные 40+ полей сохранили тип.

**Чем это опасно.** Загрузчик читает цену как
`float(product.get("price", 0) or 0)` (`ozon_fbo_orders_loader.py:132`).
`float({'amount': ...})` — это `TypeError`, то есть загрузчик упадёт на первом
же товаре. Громкая поломка здесь — лучший исход: конструкция вида
`float(x) if x else 0` внутри `try` обнулила бы цену молча, и выручка по всем
заказам стала бы нулевой.

**Чего сверка не поймала бы без сравнения значений.** Множества совпадали, поля
присутствовали, количество сходилось — отчёт сказал бы «чисто», а переключение
сломало бы сбор заказов целиком.

### Поля, которые читают загрузчики

```
FBO   posting_number, status, created_at, in_process_at, products
      products: sku, offer_id, name, quantity, price
FBS   posting_number, status, in_process_at, shipment_date, products
      products: sku, product_id, offer_id, name, quantity, price
```

Все присутствуют в новых версиях, кроме `created_at` у FBS, который FBS и не
читает.

## Частотный лимит

За один сеанс сверки дважды получен `429 code 8` «request rate limit per
second» — на вызовах подряд без пауз, ретраев `http_retry` (потолок 10 с) не
хватило. Помогла выдержка **60 секунд**, ровно как описывает поддержка Ozon про
антиспам. Первое живое подтверждение этого правила на Seller API — раньше оно
было записано только для Performance.

Из этого следует требование к переключению: **пауза между страницами в самом
загрузчике**, выдержка 60 с и не более 3 попыток на 429, предел страниц с
падением вместо частичного результата.

## Контрольные замеры `get_fbo_postings` на `/v3` (этап 2, db_writes = 0)

Замер без окна, даты и условий сравнивать не с чем: 2026-09-14 сессия
отказалась сопоставлять 8 775 с 6 324, и правильно — окна оказались разной
длины. Каждый новый замер — строкой сюда, с окном.

```
дата замера  окно                                          дней  отправл.  обращ.  429  время   с/обращ.
2026-09-13   не записано (со слов советника: 38 дней)        38     8 775      88    0  156 с    1,77
2026-09-14   2026-08-15T10:47:49Z … 2026-09-14T10:47:49Z     30     6 324      64    0  131 с    2,05
2026-09-14   то же окно, повтор                              30     6 324      64    0  130 с    2,03
```

Условия 2026-09-14: локально, `days_back=30`, пауза 1,5 с перед каждой
страницей (`OZON_POSTING_PAGE_PAUSE_SECONDS` не задан), страница 100. Все 64
ответа — 200. На страницу 98,8 отправления. Цены: 6 529, все объекты
`{amount, currency}`, строк нет.

Замер 2026-09-13 в репозиторий не попал — числа известны только из inbox
от 2026-09-13 и уточнения советника 09-14 про 38 дней; окно, время суток и
пауза неизвестны. 8 775 / 88 = 99,7 отправления на страницу, что сходится с
размером страницы 100; 1,77 с на обращение против 2,05 — либо меньшая
пауза, либо более быстрые ответы, установить нельзя.

Сверка 2026-09-14 с `/v2` на том же 30-дневном окне: 6 324 за 7 обращений,
множества `posting_number` совпали в обе стороны, строки загрузчика
3 428 = 3 428, итог `orders_amount_buyer` 69 482 228,00 в обоих,
расхождений 0.

## Инцидент: FBO не пишется с 2026-09-11

Логи Render: с 09-11 шаг FBO получает `429 rate_limit_per_second` (09-11 и
09-12 — на первой же странице, 09-13 — на второй, 09-14 — на четвёртой).
Старый загрузчик обрывался на первом 429 и писал накопленное с кодом 0;
при сортировке ASC доезжали только старые даты. В БД 09-10 — 7 строк FBO,
09-11 … 09-13 — 0. FBS, идущий перед ним, отказов не получал.

Чат разработчиков (`knowledge/telegram/`): с 2026-09-11 несколько
продавцов сообщают о `rate limit per second` на методах заказов при
единичных запросах; 08-31 текст отказа называет `max rate per sec.: 2`.
Начало совпадает. Ответа Ozon в чате до 09-13 нет.

Новый код (пауза 1,5 с, на 429 — 60 с × 3, затем исключение) в ночь
2026-09-15 идёт первым разом. Шаг с 2026-09-14 нефатальный
(`NON_FATAL_STEPS`, решение владельца во второй задаче inbox от 09-14):
частичной записи больше нет, цена отказа — одна ночь, которую 30-дневное
окно доберёт следующей; отказ приходит в Telegram как «Шаг не выполнен,
прогон продолжен» с именем шага, а утренний алерт ставит блокер
`ozon_fbo_orders_missing`, если за вчера нет строк FBO.
