# Лист «WB - месяц»: карта колонок ручного листа на WB API

> Карта 2026-09-22 (задача WB-4, §2). Только чтение: лист владельца
> `data/manual_report_september_20260922_v2.xlsx` («WB - сентябрь», 1…21
> сентября), `spec/wb/13-finances.yaml`, `08-promotion.yaml`, `11-analytics.yaml`
> и семь обращений к API с текущим токеном (`logs/wb_api_probe_20260922/`,
> сырьё не в git). Кода нет. Каждое «=» ниже — проверено по значениям на 7 днях
> 1…7 сентября, если не сказано иначе.

## 0. Что в листе у владельца (как есть)

Блок «Выкупы WB», строки 4–34, колонки A–S. Формулы владельца:

```
D  Комиссия %   = C / B                              ← 0,42 на 14 днях из 21, 0,4163…0,4198 на остальных
E  Выручка      = (B − C) / 1,22 − [свод]!AB{r+1}     ← вычет «НДС за возмещение», 1…21.09 = 88 946,47
G  Маржа        = E − F
P  Фин. рез.    = G − I − K − M − O
R  Ebitda       = P − $T$81                           ← T81 = 74 002,00 в день
```

Числа (B, C, F, I, K, M, O) — вбиты (формулы в ячейках проверены 2026-09-23:
C4 = 527486.82 — число, не формула); I, M, O — с хвостом `/1,22`, то есть
пересчитаны из сумм с НДС. **«C — константа 42 % от B» — опровергнуто 2026-09-23
(WB-5 §3):** C = Σ `retailPriceWithDisc` × `commissionPercent` / 100 по строкам
Продажа (+) и Возврат (−) — кВВ строки, 42 у большинства, 38 / 39,5 / 43 у части;
21 из 21 до копейки. На 09-05 (три строки 43 %, одна 39,5 %) это 447 569,17
против 447 777,54 у константы. Итоги 1…21: оборот 16 667 220,00, комиссия
6 994 883,75, выручка 7 839 198,00, СС 5 943 927,07, логистика 207 256,16,
реклама 0, эквайринг 332 324,01, прочее 65 971,76, фин. рез. 1 289 718,99,
Ebitda −264 323,01 — как в задаче, сверено с файлом.

## 1. Источник — детализация отчёта реализации

`POST finance-api /api/finance/v1/sales-reports/detailed` (`13-finances.yaml`):
тело `{dateFrom, dateTo, limit ≤ 100 000, rrdId}` — пагинация по `rrdId` (последний
из ответа → в следующий запрос, до `204`), `period: weekly|daily` (по умолчанию
weekly — отчёты за неделю понедельник…воскресенье, `reportId`, `dateFrom`,
`dateTo`, `createDate`). **Лимит 1 запрос в минуту.** Данные доступны с
**2024-01-29** — проверено: запрос от 2024-01-29 с `limit 1000` вернул строки
отчёта `reportId 251047423` за 2024-01-29…02-04, `min rrDate = 2024-01-29`,
`orderDt` с 2023-12-28. Неделя 1…7 сентября — **5 979 строк, 12,1 МБ, 200**, два
отчёта (835101460: 09-01…09-06, 841839787: 09-07…09-13).

Зерно — строка операции (`rrdId`): `nmId`, `vendorCode` (наш артикул, в
нижнем регистре: `f000283615`), `techSize`, `srid`, `shkId`, `sellerOperName`
(«Обоснование для оплаты»), `docTypeName` («Продажа» / «Возврат» / пусто),
`bonusTypeName`, три даты и ~90 полей сумм. Операции за неделю: Возмещение за
выдачу и возврат товаров на ПВЗ 2 595, Возмещение издержек по перемещению и
операционной обработке товара 1 878, Доставка 1 114, Продажа 277, Штраф 102,
Хранение 7, Возврат 4, Компенсация скидки по программе лояльности 2.

**Дата у владельца — `saleDt` в московском времени, у всех строк** (2026-09-23,
WB-5 §3; прежнее «= `rrDate`» опровергнуто). Доказательство: оборот по `rrDate`
расходился 09-09 / 09-10 на 96 641 (четыре продажи `saleDt` 09-09 16:50–18:44
UTC с `rrDate` 09-10 — у него в 09-09), по `saleDt` МСК — 21 из 21; логистика
по `rrDate` расходилась на −1 523…+1 892 в день, по `saleDt` МСК — 21 из 21 до
копейки; эквайринг, комиссия, прочее — тоже 21 из 21 только так. Прежний довод
про штраф 2 560 (`saleDt` 09-01 / `rrDate` 09-02) не противоречит: его `saleDt`
09-01 по UTC — это 09-02 по МСК, как и `rrDate`. `saleDt` заполнен у всех 15
операций отчёта; по МСК он равен `rrDate` у всех строк, кроме 65 из 32 009
(02-01…09-22): 60 на 1…3 дня, 5 на 16…115 (возвраты старых продаж и поздно
проведённые продажи — на лист своего месяца они не попадут; генератор их
считает и печатает).

## 2. Колонка → поля отчёта (по дню продажи `saleDt` МСК, суммы по дню)

| колонка листа | как считается из отчёта | проверка 1…7 сентября |
|---|---|---|
| **B Оборот (с НДС)** | Σ `retailPriceWithDisc` × (+1 Продажа / −1 Возврат) по `sellerOperName ∈ {Продажа, Возврат}` | **7 из 7 дней до рубля** (1 255 921, 1 621 368, …). Это же = `priceWithDisc` из `supplier/sales` = `marketplace_buyouts.buyouts_amount_seller`: 1…21.09 — 16 667 220 в базе и в листе |
| **C Комиссия (с НДС)** | **Σ `retailPriceWithDisc` × `commissionPercent` / 100, Продажа +, Возврат −** (кВВ строки; 1…21.09 — 21 из 21 до копейки, найдено 2026-09-23). **Решение советника 2026-09-23: колонка «Комиссия» нашего листа — она же (как у владельца); `B − Σ forPay±` — справочная колонка «удержано из выплаты всего», не комиссия** (в ней ещё согласованная скидка и доплаты). Прежнее «константа 0,42 × B» — опровергнуто: 42 у большинства строк, 38 / 39,5 / 43 у части. Факт по отчёту: `B − Σ forPay±` («К перечислению продавцу»): неделя 3 632 155,57 = **44,42 %** от B (по дням 44,25…44,61 %); владелец 42 % = 3 434 177,46, разница −197 978 за неделю. Разложение факта: «согласованная скидка» `retailPriceWithDisc − retailAmount` = 43,37 % + (`retailAmount − forPay`) ≈ 1,05 % | тождества нет по построению; факт измерен |
| **E Выручка** | `(B − C)/1,22 − НДС_возмещения`, где **`НДС_возмещения = (Σ ppvzReward + Σ rebillLogisticCost) × 22/122`** — `ppvzReward` «Возмещение за выдачу и возврат товаров на ПВЗ», `rebillLogisticCost` «Возмещение издержек по перемещению и операционной обработке товара» | **7 из 7 до копейки** (5 261,53; 6 127,07; 3 851,56; 4 385,70; 4 717,26; 4 998,65; 5 029,47) — это и есть колонка AB «свода» |
| **F Себестоимость** | не из API — 1С; см. §4 | — |
| **I Логистика (без НДС)** | `Σ deliveryService` («Услуги по доставке товара покупателю», строки «Доставка»; `bonusTypeName`: К клиенту при продаже / К клиенту при отмене / От клиента при отмене / Возврат … к продавцу / От клиента при возврате) **/ 1,22** | **21 из 21 до копейки по `saleDt` в московском времени** (2026-09-23; 1…21.09 — 207 256,15). Прежнее «близко, не тождество» и гипотезы про день доставки — опровергнуты: то были суммы по `rrDate` (−1 523…+1 892 в день) и по `saleDt` в UTC (−1 250…+844) |
| **K Реклама** | `GET advert-api /adv/v1/upd?from&to` — фактические списания по кампаниям (`updSum`, `advertId`, `paymentType`), интервал ≤ 31 дня; `GET /adv/v3/fullstats?ids&beginDate&endDate` — `sum` по кампаниям (≤ 50 id, ≤ 31 дня, 3 запроса/мин) | сентябрь 1…21: `upd` → `[]` (списаний нет), `fullstats` 09-21 по 50 кампаниям: 4 с активностью, `sum` 14,40 / 0,90 / 0,06 / 0 ₽. **Владелец 0 — сходится.** Кампаний у аккаунта: 171 завершённых (7), 377 на паузе (11), активных (9) — 0 |
| **M Эквайринг (без НДС)** | `Σ acquiringFee` («Компенсация платёжных услуг / комиссия за интеграцию платёжных сервисов») **/ 1,22** | **возврат со знаком минус** (2026-09-23): Σ `acquiringFee` × (+1 Продажа / −1 Возврат) / 1,22 — 20 из 21 до копейки за 1…21.09 (09-03: 20 818,53; 09-07: 25 596,13; 09-14: 14 248,48 — прежнее «не объяснено» снято). **09-17 владелец возвраты не вычел** (441,25 + 936,24 с НДС; его 7 740,70 против 4 985,72 с НДС) — его сторона, в приёмке известное расхождение. `acquiring/detailed` за ту же неделю → **204 (нет данных)**, эквайринг живёт только внутри отчёта реализации |
| **O Прочее (без НДС)** | `Σ paidStorage / 1,22 + Σ penalty + Σ deduction / 1,22` — «Хранение» с НДС, **штрафы без деления на 1,22** (строки Штраф, `bonusTypeName` «Платное хранение возвратов на ПВЗ более 3 дней»), «Удержание» (`deduction`) с НДС | **21 из 21 до копейки** (2026-09-23). Прежнее «09-02 +563,20 не объяснён» снято: это штраф 2 560 без НДС (2 560 − 2 560/1,22 = 461,64 + 101,56 хранения — старая формула делила штраф на 1,22); 09-18 — `deduction` 9 990 / 1,22 = 8 188,52 |
| **P, R** | формулы листа; `T81` 74 002/день — константа владельца | — |

Что в отчёте есть ещё и в лист не входит: `retailAmount` — что заплатил
покупатель (неделя 4 630 500 нетто; = `finishedPrice` из `supplier/sales` =
`buyouts_amount_buyer`), `vw` / `vwNds` — вознаграждение WB и его НДС
(отрицательные в строках продаж: −177 859 / −39 129 за неделю), `ppvzSalesCommission`,
`kvw` / `kvwBase` / `supRatingUp` — проценты кВВ, `cashbackDiscount` — компенсация
скидки по лояльности (доход, 1 275 за неделю), `deduction`, `paidAcceptance`.

## 3. Что уже есть у нас, чего нет

| у нас | что даёт для листа | чего не хватает |
|---|---|---|
| `loaders/wb_sales_loader.py` → `marketplace_buyouts` (`supplier/sales flag=0`, окно 30 дней) | **B Оборот** = `buyouts_amount_seller` по `buyout_date` (= `rrDate` продаж): 1…21.09 — 16 667 220 = лист; `buyouts_amount_buyer` = `retailAmount` | размера (`techSize`) в таблице нет — для СС по вариантам нужен; `commission_amount`, `vat_amount`, `revenue_after_commission_vat` — нули |
| `loaders/wb_orders_loader.py` → `marketplace_orders` | заказы; к листу выкупов не относятся | — |
| `marketplace_expenses` по WB | **0 строк** (все 292 571 — Ozon) | вся правая часть листа: комиссия факт, логистика, эквайринг, хранение, штрафы, возмещения |
| `article_unit_costs` (снимок 1С 2026-05-20) | СС, см. §4 | стыковка по варианту |
| `stock_daily` WB | нет с 07-14, новый эндпоинт в ветке | к листу не нужно |

Отчёта реализации у нас нет ни в каком виде — ни таблицы, ни загрузчика.

## 4. Себестоимость: артикулы WB в снимке 1С

`supplierArticle` WB = базовый артикул (`F000287741`), размер — отдельно в
`techSize` (`'0'`, `'17'`, `'17,5'`, `'45'` …). В снимке `offer_id` — вариант
`артикул-размер` (`F000287741-17,5`, запятая как в WB) или без размера для
безразмерных; у одного артикула 8–20 вариантов, и **себестоимость по размерам
разная** (`F000287741`: 14 765,41 … 19 903,50; `T000066064`: 34 343,77 …
59 717,50), `unit_cost_uniform` — одна на артикул (17 414,44 и т. д.).

Покрытие выкупов WB за 1…21 сентября (486 строк, 257 артикулов, 586 позиций,
оборот 16 667 220):

| стыковка | артикулов | оборот | доля |
|---|---:|---:|---:|
| точное совпадение `lower(article)` = `offer_id_norm` (только безразмерные) | 119 из 257 | 4 117 385 | 24,7 % |
| базовый артикул есть в снимке (любой вариант) | **225 из 257** | **15 765 032** | **94,6 %** |
| базового артикула в снимке нет | 32 | 902 188 | 5,4 % |

Значит СС считается по варианту `vendorCode + techSize` (оба есть в каждой строке
отчёта реализации и в `supplier/sales`), с откатом на `unit_cost_uniform` там, где
варианта нет; из `marketplace_buyouts` этого не собрать — размера нет.
Себестоимость владельца 1…21 — 5 943 927,07 (35,7 % оборота); по нашему снимку и
позициям без размера не сравнить, сравнение — после загрузчика. Снимок 05-20;
владелец считает по своим ценам 1С («на 16 % выше нашего снимка» у Ozon,
`cost_of_goods.md`).

## 5. Доступ токена — проверено 2026-09-22 20:13–20:16 UTC

| эндпоинт | запрос | ответ |
|---|---|---|
| `POST finance-api …/sales-reports/detailed` | 09-01…09-07 | **200**, 5 979 строк, 12,1 МБ |
| то же | 2024-01-29…2026-03-31, `limit 1000` | **200**, 1 000 строк, отчёт с 2024-01-29 |
| `POST finance-api …/acquiring/detailed` | 09-01…09-07 | **204** — нет данных |
| `GET advert-api /adv/v1/promotion/count` | — | **200**, 548 кампаний (171 завершённых, 377 на паузе) |
| `GET advert-api /adv/v1/upd` | 09-01…09-21 | **200**, `[]` |
| `GET advert-api /adv/v3/fullstats` | 50 id, 09-21 | **200**, 4 кампании с активностью, копейки |
| `POST seller-analytics-api …/order-feed` | 09-15 | **200**, 49 заказов |

Категории `finance`, `advert`, `contentanalytics` у текущего токена **есть**; 401/403
не встретилось. Новых прав от владельца не нужно. 429 — 0.

## 6. Лента заказов и `isCancel` (задача §3)

`POST /api/analytics/v1/order-feed` (`11-analytics.yaml`): период ≤ 31 дня **по
времени текущего статуса** (`updatedAt`), 1 запрос/мин, пагинация `offset` +
`snapshotTime`. Поля: `srid`, `nmId`, `chrtId`, `createdAt`, `updatedAt`, `status`
∈ {created, buyout, cancel, return, returnDefective}, **`cancelType` ∈ {`app` —
отказ до получения, `receipt` — отказ при получении, `expire` — истёк срок
получения, `other` — техническая отмена}**, `sellerPrice`, `isMp`, `isB2b`.

**Лента различает.** 09-15 (по времени статуса): 49 заказов — cancel 21, buyout
18, created 10; из 21 отмен **`receipt` 15, `app` 4, `expire` 2**. То есть
`isCancel` в `supplier/orders` покрывает и отказ до получения, и отказ при
получении, и истёкший срок — гипотеза 09-21 подтверждена данными ленты (71 %
отмен дня — отказ при получении). На `cancelled_orders_*` это ложится так:
правило записи не меняется (все четыре типа — отмена); чтобы разложить отмены по
типу, нужен join `srid` → `cancelType` из ленты и три-четыре новых счётчика (или
одна колонка `cancel_type` в логе, как `ozon_posting_status_log`) — отдельная
задача, глубина ленты 31 день по времени статуса.

## 6а. Как отчёт живёт во времени — проверено 2026-09-23 (WB-5)

| вопрос | ответ | чем проверено |
|---|---|---|
| меняются ли строки закрытой недели задним числом | за сутки — нет: 09-01…09-07 сняты 09-22 20:13 и 09-23 09:35 UTC — **5 979 = 5 979 rrdId, поле в поле** | один замер; гарантии нет, поэтому окно ночи 21 день и чистка застрявших после полного сбора |
| `period=daily` против `weekly` у закрытой недели | **те же rrdId и те же значения** (5 979 из 5 979; отчёты 6338020260901…07 против 835101460), суммы по дням равны | 09-23 09:39 UTC |
| незакрытая неделя | `weekly` → **204**; `daily` → отчёты по дням: день D появляется **D+1** (`reportId 6338020260922`, `createDate 2026-09-23`), 09-21 — 225 строк, 09-22 — 207 | 09-23 09:36–09:37 UTC |
| оборот дня из daily = лист | 09-21: 236 850 = B владельца | — |

Отсюда правило загрузчика (`loaders/wb_sales_report_loader.py`): всегда `period=daily`,
upsert по `rrd_id`, окно 21 день — одна страница на 100 000 строк = **1 обращение в
ночь**; строки окна, которых в свежем полном ответе нет, снимает `loaders/stale_keys.py`
(порог, список до удаления, только при полном сборе). Шаг «WB: отчёт реализации» — после
продаж WB, нефатальный (читателей у таблицы пока нет). Таблица —
`sql/20260923_create_wb_sales_report_rows.sql` (по слову владельца). История с
2026-02-01 — `scripts/wb_sales_report_backfill.py`: `--fetch` в файлы кусками по 14 дней
(`data/wb_sales_report_raw/`, вне git), `--plan` по месяцам против `marketplace_buyouts`,
`--apply` — по слову.

Лист — `scripts/report_wb_month.py` (формулы §2, приёмка `--check` по листу владельца;
строки — из таблицы или из файлов сырья `--rows-from-files`).

## 7. План пятой задачи — предложение

1. **Загрузчик отчёта реализации** `loaders/wb_sales_report_loader.py`: недели по
   `rrDate`, пагинация `rrdId`, 65 с между запросами, сырьё в
   `data/wb_sales_report_raw/<reportId>.json`; таблица **`wb_sales_report_rows`**
   с зерном `rrdId` (миграция — владелец): ключевые ~30 полей (`rrDate`, `saleDt`,
   `orderDt`, `nmId`, `vendorCode`, `techSize`, `srid`, `sellerOperName`,
   `docTypeName`, `bonusTypeName`, `quantity`, `retailPriceWithDisc`,
   `retailAmount`, `forPay`, `ppvzReward`, `rebillLogisticCost`, `deliveryService`,
   `acquiringFee`, `paidStorage`, `penalty`, `additionalPayment`, `deduction`,
   `paidAcceptance`, `vw`, `vwNds`, `cashback*`). Объём: ~6 тыс. строк/неделю, с
   2026-03-26 — ~150 тыс.; upsert по `rrdId` идемпотентен. Ночной шаг —
   нефатальный, окно 3 недели назад (отчёт за неделю появляется в понедельник
   после неё: `createDate` 09-07 для 09-01…09-06) — 3–4 обращения за ночь.
2. **Витрина/лист** `scripts/report_wb_month.py` по образцу `report_ozon_month.py`:
   колонки §2 из `wb_sales_report_rows` по `rrDate`; комиссия — **две строки**:
   «как у владельца» 42 % (для `--check`) и факт `B − forPay`; СС — по
   `vendorCode + techSize` из `article_unit_costs` с откатом на `unit_cost_uniform`;
   `--check` против листа владельца: B, AB, M, O — до копейки, I — с показом
   разницы, C — обе версии.
3. **Открытое, к владельцу до кода:** комиссию в нашем листе брать фактом
   (44,4 %) или его константой (42 %); правило логистики (наше `deliveryService`
   расходится с его числами до 1,5 тыс/день); чем объясняются 09-03 / 09-07
   эквайринга и +563,20 «прочего» 09-02; нужен ли новый снимок 1С для WB.
4. **Прав токена — не нужно**; таблица — миграция по слову.
